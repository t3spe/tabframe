# Session agent-a4add48b86344201e

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 82 · user messages: 1 · assistant messages: 2 · tool calls: 33 · tool results: 33
- from 2026-09-04T18:30:41.871Z to 2026-09-04T18:41:32.169Z
- scrubbed: nothing

---
## User · 2026-09-04T18:30:41.871Z

You are an expert technical writer reviewing the documentation of the repository at /home/mircea/homework/tabframe (a fault-tolerant distributed computer whose cores are browser tabs and AWS Lambda MicroVMs, programmed in WebAssembly; a take-home project whose README, design record, runbook, walkthrough, plan, rationale, and per-work-package notes will be read by reviewers who have never seen it).

Rules: read-only — do not modify any file. Never run any AWS CLI or SDK command, never use the AWS profile `tabframe`, never run `mise run deploy`, `up`, `down`, `rotate`, `verify`, `demo`, `logs`, or `health`. You may run `bun test <path>` and read anything, including code, to check that the documents tell the truth. Never include AWS account ids, MicroVM endpoint hostnames, tokens, or email addresses in your report.

This is review loop 3 of 3. Loops 1 and 2 are merged into the tree you see. Start by reading docs/implementation/wp-8.1-review-loop-1.md and docs/implementation/wp-8.2-review-loop-2.md (check their counts against their own tables and their claims against the code), then README.md, docs/design.md (especially the status line, §16 glossary, and the §17 drift log), docs/runbook.md, docs/walkthrough.md, docs/plan.md, docs/rationale.md, docs/implementation/README.md, packages/sdk-as/README.md (the in-page guide is cut from it at "## Compiling" by packages/web/src/editor-core.ts), and packages/web/public/*.html copy. Check whether loop 2's accepted documentation rows actually hold (mark a broken one "loop-2 regression"), and look for what both loops missed.

Look for: statements that contradict the code or each other (constants, counts, names, file paths, section references), stale "where we are" lines, broken relative links and anchors, the vocabulary settled in loop 2 (a core is any worker, a cloud core the MicroVM kind, a node the ledger's word for a joined core) applied inconsistently, sentences a first-time reader cannot follow, missing steps in the runbook or walkthrough, and anything that would make a careful reviewer distrust the rest.

Return at most twelve findings, most severe first, under a "## Findings" heading, each as ONE numbered line in this exact shape:
`[H|M|L] document — location (file, section or line) — what is wrong (quote it) — why it matters — the fix (the corrected wording where possible)`
Every finding must quote text you actually read. Then a "## Summary" paragraph: what to fix first and why. Also list, in one sentence, which loop-2 documentation claims you verified as real and complete.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T18:30:47.022Z

I'll start with the two prior review-loop documents, then work through the rest.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.1-review-loop-1.md
```

**Result of `Read`:**

```
1	# WP8.1 — Review loop 1
2	
3	**Branch** `wp/8.1-review-loop-1` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop one.
7	
8	## How the loop ran
9	
10	Five independent reviewers (fresh agents, read-only, no AWS access) each returned at most twelve
11	findings with severity, location, cause, and fix. The 52 findings were de-duplicated into the
12	table below (52 rows); each was accepted, deferred to a later loop, or rejected with the reason (the
13	critique): 46 accepted in full or in part, 6 deferred to loops 2–3, and one (11) rejected as a change but kept as a bound. The accepted ones landed on this branch with tests; CI gates the merge; the next loop
14	reviews the tree with these changes in.
15	
16	## The findings and what was done
17	
18	*From*: dist = distributed-systems developer, sec = security engineer, fs = full-stack developer,
19	tw = technical writer, devops = DevOps engineer. *Sev*: H/M/L as the reviewer rated it.
20	
21	| # | From | Sev | Finding | Decision |
22	|---|---|---|---|---|
23	| 1 | dist | H | Stage-advancing store effects (inherited root, stage spec, folded manifest) were fire-and-forget: a handover mid-flight or one S3 error wedged the execution for ever. | **Accepted.** `resumePending()` re-derives the pending effect from the ledger on adopt and again every `STORE_RETRY_MS` (`waitingSince` on the execution); a pruned origin now falls back to the bundle with the `expired-root` warning instead of a silent start. |
24	| 2 | dist | H | `encode()` over 64 KB threw through `dispatch`; params and the queue were unbounded. | **Accepted.** `PARAMS_MAX_BYTES` (4 KB canonical) and `QUEUE_CAP` (32) in `enqueue`; a send that cannot be encoded closes that connection instead of throwing. |
25	| 3 | dist | H | A node reported store or network failures as program faults, failing everybody's execution. | **Accepted.** `hostFailure()` covers fetch, upload, presign timeouts and network errors → `RELEASED`; the store client retries three times with backoff. |
26	| 4 | sec | H | The control plane fetched blobs named by untrusted parties without a size check. | **Accepted.** `StoreDriver.get(hash, maxBytes)` (HeadObject first on S3, `BlobTooLarge` otherwise); caps at every fetch: stage spec 1 MB, inherited root 4 MB, bundle manifest 1 MB, program manifest 64 KB, module 8 MB. |
27	| 5 | sec | H | Presign sizes were unbounded and uncounted. | **Accepted.** `size ≤ maxOutputBytes` and at most 64 items per presign in the schemas; a 1 GiB presigned-byte budget per connection in the core. |
28	| 6 | sec | H | A module could declare a second memory without a maximum and grow past the cap. | **Accepted.** `countMemories()`; more than one memory is refused. |
29	| 7 | fs | H | A node's presign never timed out and an S3 HeadObject error failed the whole presign: a zombie core. | **Accepted.** `SocketPresigner` times out after 30 s (the task is released); `S3Store.presign` treats a HeadObject error as "not there" and signs. |
30	| 8 | tw, devops | H | The hourly rule rotated a *suspended* control plane, so an idle night booted a generation an hour and the README's stays-up paragraph was false. | **Accepted.** A scheduled invocation skips a `SUSPENDED` control plane (`skipped-suspended`); the README says what happens now. |
31	| 9 | devops | H | `deploy` ran build and test in parallel from the working tree with no clean-tree or green-main guard and no commit stamp. | **Accepted.** `deploy` is sequential: build → test → `deploy-guard.ts` (clean tree, HEAD on origin/main; `TABFRAME_DEPLOY_UNGATED=1` to skip) → stacks → up. `stage-image.ts` writes `build.json`; `/health` shows `build` and `imageVersion`. |
32	| 10 | devops | H | The rotate function returned failures as successes; nothing alarmed. | **Accepted.** The Lambda entry throws on `failed`; CloudWatch alarms on rotate/session errors and rotate throttles go to an SNS topic mailed to the budget address when configured; the hourly rule's target retries twice within thirty minutes. |
33	| 11 | sec | M | Destructive controls are unauthenticated; kill-half in a loop terminates and relaunches cloud cores. | **Rejected as a change, accepted as a bound.** A public machine anyone may drive is the design (D4, §6.7); a machine-wide cooldown of two seconds per destructive control (`CONTROL_COOLDOWN_MS`) removes the cost loop. |
34	| 12 | fs | M | The editor tab re-sent `pause` on every live socket, including after its own launch. | **Accepted.** `holdWanted` is cleared by launch and close. |
35	| 13 | fs | M | A failing session fetch during a silent resubscribe kept a green pill; held clicks were dropped silently. | **Accepted.** The page goes back to "connecting"; dropped clicks are said in the status line (`onDropped`). |
36	| 14 | fs | M | An outdated page reloaded itself in a loop. | **Accepted.** One reload per protocol version (`sessionStorage`), then a hard-refresh hint. |
37	| 15 | fs | M | The ledger tab serialised every row per frame; the blob cache and the demo store grew without bound. | **Accepted** (ledger signature; a 64 MB LRU `BlobCache`). The demo store's growth is deferred to loop 2. |
38	| 16 | fs | M | A dead compiler worker left the editor unusable. | **Accepted.** Pending compiles are rejected, a two-minute compile timeout, the next compile gets a fresh worker; failed drops say so. |
39	| 17 | dist | M | Between handover and promote a dead rotation left the machine dark for an hour; a predecessor kept acting after handing over. | **Accepted.** `HANDOVER_LEASE_MS` (90 s) returns a control plane to `active` when no drain follows; ticks run no fleet, loop, or assignment while `handing-over` or `drained`. |
40	| 18 | dist | M | Slow `RunMicrovm` calls were asked twice; surplus cores were never trimmed. | **Accepted.** `meta.coreLaunches` counts launches until acknowledged (or a minute passes); surplus unlinked cores are terminated. |
… (55 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.2-review-loop-2.md
```

**Result of `Read`:**

```
1	# WP8.2 — Review loop 2
2	
3	**Branch** `wp/8.2-review-loop-2` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop two, on the tree with loop one merged.
7	
8	## How the loop ran
9	
10	Five fresh reviewers (independent agents, read-only, no AWS access), each given the loop-one note
11	first so they would check what it claimed, each returned at most twelve findings with severity,
12	location, cause, and fix. Their 57 findings de-duplicated into the table below; each was accepted,
13	deferred with a reason, or rejected with the reason (the critique). Of the 52 rows, 51 are accepted in full or in part (9 of them with a part deferred to loop 3, and one with a part rejected), 1 deferred whole, none rejected whole. The accepted ones
14	landed on this branch with tests; CI gates the merge; loop three reviews the tree with these
15	changes in and reads this note first.
16	
17	Three of loop one's claims did not hold and are marked *loop-1 regression*: the presign cap made
18	many-file tasks loop across the cluster; the IAM scoping was never applied; the build stamp never
19	reached the image. Two of loop one's documentation repairs did damage (the drift log, a typo). All
20	five are fixed here.
21	
22	## The findings and what was done
23	
24	*From*: dist = distributed-systems developer, sec = security engineer, fs = full-stack developer,
25	tw = technical writer, devops = DevOps engineer. *Sev*: H/M/L as the reviewer rated it. A finding
26	two reviewers made independently is one row naming both.
27	
28	| # | From | Sev | Finding | Decision |
29	|---|---|---|---|---|
30	| 1 | dist, devops | H | A rotation that dies between the pointer flip and the drain leaves two active generations (the successor active at `/run`, the predecessor back to active after the 90 s lease); each terminates the other's cores every two minutes. The step timeouts exceeded the function's five minutes. | **Accepted.** `promote()` records `retiring` in the pointer and `retire()` clears it; a leftover is finished before `repair()`. The rotate function runs ten minutes and waits 120 s for RUNNING. When a lease runs out the control plane reads the pointer (`ControlPlaneDeps.pointer`, SSM in the image): named elsewhere at a higher generation, it drains and terminates itself (`rotation.test.ts`, two cases). A standby boot phase until `/adopt` is *deferred (loop 3)*: the adopt-once guard and phase-gated `latest` writes cover the successor side today. |
31	| 2 | dist | H | A result for an attempt the node never held settled an open task unless it carried an error; any visitor with the token could paint the frame or hijack the plan. | **Accepted.** An open task settles only from a known attempt, any outcome; unknown attempts may verify or contest settled tasks only (`results.ts`; `scheduler.test.ts` rewritten and extended). |
32	| 3 | dist | H | `defaultParams` was unbounded and every program's rode page 0; one dropped manifest could make every subscribe close with "frame too large". Programs were uncapped. | **Accepted.** `defaultParams` ≤ `PARAMS_MAX_BYTES` (4 KB) in the schema; `PROGRAMS_CAP` = 64 in `addProgram`; page 0 over budget strips `defaultParams` from the program views instead of closing; the page split is memoised per (generation, seq). |
33	| 4 | dist | M | A store fetch that threw was dispatched as `bytes: null`, which the ledger read as "missing" and failed the execution; loop one's retry never ran. | **Accepted.** `blobFetched` carries `error`; `onStoreError` counts it and leaves `waitingSince` so the `STORE_RETRY_MS` re-derivation retries; the execution fails only after `STORE_ERRORS_MAX` (6). |
34	| 5 | dist, sec | M/H | A core was linked or created from a self-declared `hostId`, and every snapshot published the ids; an impostor could get a real core terminated, starve the fleet, and blind the reaper. | **Accepted.** A random per-core token in the run payload (`cores.ts`), `hello.coreToken`, a hello links only a matching unlinked record and never creates one; `nodeView.hostId` is `"fleet"` for cores; `gone()` survives a bad id. |
35	| 6 | dist | M | `PRESIGN_BYTES_PER_CONN` was a lifetime cap: an honest Mandelbrot node hit 1 GiB within an hour and was closed as rate-limited. *Loop-1 regression.* | **Accepted.** A refilling bucket, `PRESIGN_BYTES_PER_MIN` = 64 MiB per connection. |
36	| 7 | dist | M | `latest.json.gz` was written while handing over and after drain, so a heal could boot from a drained predecessor; the adopt repeat guard compared `seq`. | **Accepted in part.** `snapshotNow` writes only while active (handover excepted); `/adopt` is accepted once per process (`adoptedOnce`). Keying snapshots so the fleet picks the highest generation is *deferred (loop 3)*: with the phase gate, `latest` is always the active lineage. |
37	| 8 | dist, devops | M | The client token `g<gen>-<hour>` replayed onto a terminated VM after a failed attempt in the same hour; nothing could launch until the hour turned. | **Accepted.** A run that resolves to a TERMINATED/TERMINATING VM is a replay: retried with `-r<n>`, three tries; the fake models the replay (`terminateNextRuns`); tests for the retry and for a leftover `retiring`. |
38	| 9 | dist | L | Inheritance resolves by execution id against 32 kept executions; a persist program's lineage is evicted within minutes of the loop. | **Deferred (loop 3).** A bundle → last done root map that survives pruning is a ledger change with its own snapshot field; the pruned-origin warning of loop 1 makes the loss visible meanwhile. |
39	| 10 | dist | L | Clients could take all sixteen endpoint connections; every `/handover` and `/drain` then got 429 and rotations degraded to snapshot handovers. | **Accepted.** `CLIENT_CONNECTION_CAP` = 14: the fifteenth client upgrade is refused with 503; §9.7 says so. |
40	| 11 | dist | L | `beginHandover` used `Date.now()` beside an injected clock, and a handover from a drained control plane resurrected it. | **Accepted.** `beginHandover(ledger, clock.now())`; `/handover` on a drained control plane is 409. |
… (68 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
ls -la && echo "---docs---" && ls -la docs docs/implementation && echo "---packages---" && ls packages && echo "---web public---" && ls -la packages/web/public && echo "---wc---" && wc -l README.md docs/*.md docs/implementation/*.md packages/sdk-as/README.md packages/web/public/*.html
```

**Result of `Bash`:**

```
total 232
drwxrwxr-x 14 mircea mircea  4096 Sep  4 11:23 .
drwxrwxr-x  5 mircea mircea  4096 Sep  2 07:02 ..
-rw-rw-r--  1 mircea mircea   787 Sep  2 23:35 biome.json
-rw-rw-r--  1 mircea mircea   910 Sep  1 22:51 bunfig.toml
-rw-r--r--  1 mircea mircea 46043 Sep  3 00:13 bun.lock
-rw-rw-r--  1 mircea mircea   543 Sep  1 21:41 cdk.json
drwxrwxr-x 62 mircea mircea 12288 Sep  4 10:54 cdk.out
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:10 .claude
drwxrwxr-x  2 mircea mircea 24576 Sep  4 11:23 coverage
drwxrwxr-x  4 mircea mircea  4096 Sep  4 08:34 docs
drwxrwxr-x  2 mircea mircea  4096 Sep  4 08:34 e2e
-rw-------  1 mircea mircea   189 Sep  2 15:05 .env.local
drwxrwxr-x  8 mircea mircea  4096 Sep  4 11:28 .git
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:27 .github
-rw-rw-r--  1 mircea mircea   364 Sep  2 15:19 .gitignore
-rw-rw-r--  1 mircea mircea 34523 Sep  1 21:08 LICENSE
-rw-rw-r--  1 mircea mircea  7774 Sep  4 11:01 mise.toml
drwxr-xr-x  7 mircea mircea  4096 Sep  1 22:51 node_modules
-rw-rw-r--  1 mircea mircea   436 Sep  1 22:51 package.json
drwxrwxr-x 13 mircea mircea  4096 Sep  1 22:52 packages
-rw-rw-r--  1 mircea mircea  1946 Sep  4 08:34 playwright.config.ts
drwxrwxr-x  5 mircea mircea  4096 Sep  2 23:56 programs
-rw-rw-r--  1 mircea mircea 16186 Sep  4 11:23 README.md
drwxrwxr-x  3 mircea mircea  4096 Sep  2 13:37 scripts
drwxrwxr-x  4 mircea mircea  4096 Sep  4 11:27 test-results
-rw-rw-r--  1 mircea mircea   480 Sep  1 21:08 tsconfig.base.json
-rw-rw-r--  1 mircea mircea   294 Sep  3 20:28 tsconfig.json
---docs---
docs:
total 240
drwxrwxr-x  4 mircea mircea  4096 Sep  4 08:34 .
drwxrwxr-x 14 mircea mircea  4096 Sep  4 11:23 ..
-rw-rw-r--  1 mircea mircea 99386 Sep  4 11:15 design.md
-rw-rw-r--  1 mircea mircea  7616 Sep  3 00:14 feasibility-transformer.md
drwxrwxr-x  3 mircea mircea  4096 Sep  4 11:23 implementation
-rw-rw-r--  1 mircea mircea  3733 Sep  2 07:03 m0-verification.md
-rw-rw-r--  1 mircea mircea  4382 Sep  2 00:38 m1-verification.md
-rw-rw-r--  1 mircea mircea  3585 Sep  2 05:39 m2-verification.md
-rw-rw-r--  1 mircea mircea  5042 Sep  4 10:55 m3-verification.md
… (168 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/README.md
```

**Result of `Bash`:**

```
     1	# Tabframe
     2	
     3	A fault-tolerant distributed computer whose cores are browser tabs and Firecracker microVMs,
     4	programmed with WebAssembly. Every worker is a core — a thread in someone's tab, or one of the two
     5	MicroVMs in AWS, the *cloud cores*. Open the page and your tab is a core. Close it and the machine keeps
     6	computing, correctly. The control plane that schedules the work is itself replaced every hour, with
     7	the work in flight.
     8	
     9	Deployed: **https://d2w9z8juw4oo76.cloudfront.net** — the page lends one core when it opens, and
    10	shows the machine rendering a Mandelbrot frame with whoever else is there.
    11	
    12	**Try it, in five clicks.** **spawn N** (N is one fewer than your machine's cores so the tab keeps a thread; the counters follow), kill half (tiles are taken back and finish
    13	elsewhere), redundancy on (the verified counter moves), editor ↗ (change `CYCLE`, compile, launch:
    14	your program goes ahead of the loop), the ledger tab (hashes, not bytes). `?observe` lends no cores,
    15	`?demo=1` runs a scripted cluster inside the page, and a rotation banner every hour is expected.
    16	
    17	## What it is
    18	
    19	A program is one WebAssembly module with two entry points and a manifest. `plan` turns parameters
    20	and the previous stage's outputs into the next stage's tasks; `run` turns one task's input into
    21	bytes. Both execute on cores — the control plane runs no program code, not even the planner. A
    22	program's only view of the world is a per-execution filesystem of content-addressed blobs: it has
    23	no clock, no randomness, no network, and no failure type anywhere in its API.
    24	
    25	Three programs ship with the machine and go through the same path as anything you write in the
    26	in-page editor: a distributed **Mandelbrot** render (640 tiles of 64×64 per frame, presets that
    27	advance while anyone watches); a three-stage **word count** over *Moby-Dick* (map by byte range,
    28	reduce by partition, merge to a top-25); and **tiny GPT**, an 822 k-parameter character-level
    29	transformer trained on that same corpus, whose whole forward pass runs in WebAssembly on the cores —
    30	one continuation per task, four milliseconds a token, the same bytes on every core
    31	(`docs/feasibility-transformer.md`). You can also compile your own: the editor holds every
    32	program's source and the AssemblyScript compiler runs in a browser worker.
    33	
    34	## Why it looks the way it does
    35	
    36	Three constraints carry the whole fault-tolerance story; everything else follows from them.
    37	
    38	1. **Tasks are idempotent and delivered at least once.** A task may run on two cores, be killed
    39	   halfway, or be handed to a third after its holder vanishes. The program never knows.
    40	2. **Results are single-assignment, memoized by hash.** The first accepted result for a task is
… (178 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/runbook.md
```

**Result of `Bash`:**

```
     1	# Runbook
     2	
     3	How to operate the deployed Tabframe machine. Every command below runs from the repo root with
     4	`mise`, which sets the AWS profile (`tabframe`, the dedicated account) and the region; nothing here
     5	works with any other profile, by design (`mise run whoami` is the guard every task depends on).
     6	
     7	## What is running
     8	
     9	| Thing | Where | Notes |
    10	|---|---|---|
    11	| Control plane | one Lambda MicroVM, 1 GB, generation-stamped | pointed at by the SSM parameter `/tabframe/pointer`; rotates hourly |
    12	| Cloud cores | up to two 0.5 GB MicroVMs while the machine is awake | launched by the control plane; asleep ten minutes after the last observer leaves |
    13	| Session function | Lambda function URL (public) | vends the endpoint and a shared token; heals when nothing runs |
    14	| Rotate function | Lambda, hourly EventBridge rule | launches the successor, hands over, flips the pointer, drains, terminates |
    15	| Store | S3 behind CloudFront (`/blob/*`) | content-addressed, one-year lifecycle |
    16	| Snapshots | S3, `g<generation>/<time>.json.gz` and `latest.json.gz` | the `g*/` history expires after a day; `latest.json.gz` is kept |
    17	| Alarms | SNS topic `tabframe-alarms` | rotate and session errors, rotate throttles, and the canary's page and session checks (WP8.1, WP8.2); mailed to the budget address when configured. The subscription must be confirmed from that mailbox once: `aws sns list-subscriptions-by-topic` shows `PendingConfirmation` until then |
    18	| Canary | Lambda, five-minute EventBridge rule | fetches the page's `config.json` and the session URL, never the MicroVM endpoint; metrics under `Tabframe/Canary` |
    19	| Page | S3 behind the same CloudFront distribution | `https://d2w9z8juw4oo76.cloudfront.net` |
    20	
    21	## Everyday
    22	
    23	| Task | Command | What it does |
    24	|---|---|---|
    25	| Is the machine up? | `mise run health` (`-- --cores` asks each cloud core too) | `/health` and `/diag` of the active control plane through the proxy on the private port, masked. Counts, phase, fleet, snapshotter status, a store round trip, memory. |
    26	| Bring it up | `mise run up` | Sets the pointer to *on*, enables the hourly rule, invokes rotate once. Idempotent: a running control plane is rotated, not duplicated. |
    27	| Take it down | `mise run down` | Disables the rule, terminates every MicroVM from our image, writes *off*. The page shows an off screen; the session function heals nothing. **Destructive: every MicroVM is terminated and the page goes dark until `up`.** |
    28	| Deploy | `mise run deploy` | Refuses unless the tree is clean, HEAD is on `origin/main`, and that commit's CI is green (`TABFRAME_DEPLOY_UNGATED=1` skips the check for an emergency deploy from a branch); builds programs, page, and image; runs lint and every test; fails on any IAM or security-group broadening in `cdk diff --security-only` (`TABFRAME_DEPLOY_IAM=1` acknowledges one); `cdk deploy --all`; then `up`, which rotates the running control plane onto the new image. A deploy is a rotation. |
    29	| Rotate by hand | `mise run rotate` | One rotation now (same code the hourly rule runs). |
    30	| Watch logs | `mise run logs`, `mise run logs:fleet` | The MicroVM log group (one stream per VM) and the two functions' groups. See *Observability* for what actually lands. |
    31	| Verify | `mise run verify:m1` / `verify:m2` / `verify:m3` | The milestone runbooks against the live machine: a frame through a kill-half, the editor and word count, a rotation under load. Each prints one line per check and exits non-zero on a failure. |
    32	| Simulate | `mise run sim -- --seed N` | The churn simulation, locally; `--long` for the nightly shape. |
    33	
    34	## Deploy, step by step
    35	
    36	1. `main` is green in CI (`mise exec -- gh run list --branch main --limit 1`). This is a rule, not a
    37	   preference (plan §0).
    38	2. `mise run deploy`. The image build takes three to five minutes; the first build after a long
    39	   gap has taken fifty. If CloudFormation reports the image "did not stabilize", check the build
    40	   history — the platform has retried and succeeded on its own once while CloudFormation gave up;
… (79 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/walkthrough.md
```

**Result of `Bash`:**

```
     1	# The walkthrough: every screen, every state, everything clickable
     2	
     3	A visitor should be able to land anywhere, in any state, and answer three questions without help:
     4	what is the machine doing, what can I do here, and what just happened because of what I did. This
     5	document is the inventory that makes that testable — for each screen, every state it can be in, what
     6	the page must say, what can be clicked and what happens, and what must not be on the screen. It is
     7	the page's contract; `e2e/walkthrough.e2e.ts` drives the machine through the states below and
     8	asserts the sentence, the header's slot, and the exact set of enabled controls, with a screenshot
     9	per state in the test's output. Written for WP7.7 (2026-09-04), from Mircea's third review.
    10	
    11	## The rules the inventory is checked against
    12	
    13	- **R1 · One sentence of state, always.** The status line says what the machine is doing and why,
    14	  in a visitor's words (`machineSentence` in `packages/web/src/state.ts`): "rendering mandelbrot
    15	  e41 · render · 9 nodes", "running your wordcount e42 · map · 4 nodes · the loop waits behind it",
    16	  "stopped by you · nothing runs until Start · a launch of yours still runs at once", "your
    17	  wordcount e42 is done · the result stays · the loop waits for Start or ten quiet minutes",
    18	  "paused · the editor tab is open · in-flight tasks finish, nothing new starts · closing it or
    19	  launching resumes", "idle · the loop starts a frame when someone watches". The demo and an
    20	  observer say what they are first. While the page is not connected the connection banner speaks.
    21	- **R2 · Every control says what it will do, then what it did.** A label and a tooltip before the
    22	  click; after it, an activity line and, for the page that clicked, the same line in the status
    23	  line for five seconds. A click that could not be sent says so instead of being swallowed.
    24	- **R3 · Disabled with a reason, not hidden.** A control that cannot apply now stays where it is,
    25	  greyed, with the reason after a dash in its tooltip ("— not connected yet", "— nothing is
    26	  running", "— an observer lends no cores; open the plain address to lend some"). The only slot that
    27	  swaps its label is Stop / Start / Resume, and it always means "what you can do to the machine
    28	  right now"; the one other label that changes is a program's "launch…", which reads "cancel" while
    29	  its form is open.
    30	- **R4 · Same kind, same look.** Controls that end things (Stop, kill half, kill execution, close
    31	  all mine) share the red outline and sit together; controls that make things share the plain look.
    32	  Links that leave the page carry ↗, and nothing else opens a tab.
    33	- **R5 · A click never moves the page.** Results land in boxes that were already there; lists
    34	  change a row's class instead of being rebuilt; the only scrolling is the visitor's.
    35	- **R6 · One name per thing.** program · execution (e42) · frame (one execution of the loop's
    36	  program) · stage · task · tile · core (any worker: a browser tab's thread or a MicroVM) · cloud
    37	  core (the MicroVM kind) · node (the ledger's word for a core that has joined) · host (a browser) ·
    38	  tab. The pills, the activity log, the tooltips and the docs use these words (aligned in WP8.2).
    39	- **R7 · Every screen says where you are and how to get back.** Panel tabs and the editor say they
    40	  were opened from the dashboard and link back; the demo says it is a demo in the status line;
… (61 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/rationale.md
```

**Result of `Bash`:**

```
     1	# Tabframe — design rationale
     2	
     3	The short written rationale the assignment asks for, answering its five questions in order. The
     4	design record (`design.md`) is the long version; its drift log is where most of what follows was
     5	learned.
     6	
     7	## Why this theme, why this approach
     8	
     9	I wanted a distributed computer with **no servers doing the compute**: a machine assembled live
    10	from browser tabs that people open and close at will, that stays *correct* through that churn,
    11	and that is programmed like one elastic machine rather than like a fleet of workers. The
    12	assignment offered a coordinator-and-workers demo; I chose to push on the word *computer*. A
    13	program is a WebAssembly module with `plan` and `run`, both of which execute on the cores; the
    14	control plane never runs program code, not even the planner. Mandelbrot is the money shot because
    15	a tile landing is legible; word count is there because a machine that can only draw fractals is a
    16	screensaver. Both ship through the same path as anything a reviewer writes in the in-page editor.
    17	
    18	Three constraints carry the whole fault-tolerance story, and I took them literally rather than as
    19	guidance: tasks are idempotent and delivered at least once; results are single-assignment and
    20	memoized by hash; the control plane holds hashes and never bytes. Everything else is consequence.
    21	A dead core is a non-event. Two cores computing the same tile is a verification, not a waste. A
    22	control plane that dies is replaced from a five-second-old snapshot and nobody's work is wrong.
    23	
    24	The hosting choice was deliberate too. The control plane runs in an AWS Lambda MicroVM — the
    25	stateful Firecracker product, not a Lambda function — which I wanted to learn, and which forced a
    26	useful discipline: the control plane is rotated every hour with work in flight, so "even the
    27	control plane is churn" is not a slogan but a scheduled event, and a deploy is just a rotation.
    28	
    29	## What is non-obvious
    30	
    31	Most of it was learned by running the thing on AWS, not by designing it; the drift log at the end
    32	of the design record is the honest history.
    33	
    34	- **Failure needs no error path.** Because a task may run anywhere, twice, or halfway, the
    35	  program's API has no failure type at all, and the control plane's whole response to a vanished
    36	  core is to release its attempts to the front of the queue. A node that gives up on a task at its
    37	  own deadline reports *released*, not *error*: the machine distinguishes a slow core from a broken
    38	  program, and only the second fails an execution.
    39	- **The checksum pin was silently off.** The store's guarantee — the bucket refuses bytes that do
    40	  not hash to their key — rests on the SHA-256 being part of the presigned PUT's signature. The AWS
… (102 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/implementation/README.md
```

**Result of `Bash`:**

```
     1	# Implementation log
     2	
     3	One document per work package, written on the WP's branch and merged with it. Each covers what was
     4	delivered, how it works, why it is built that way, the evidence, any design drift, and open items.
     5	Screenshots and other evidence live under `assets/wp-<m>.<n>/`.
     6	
     7	| WP | Document | Merged | Summary |
     8	|---|---|---|---|
     9	| WP0.1 | [wp-0.1-repo-scaffold.md](wp-0.1-repo-scaffold.md) | 2026-09-01 | Workspace, tooling, guard task, docs, license |
    10	| WP0.2 | [wp-0.2-protocol.md](wp-0.2-protocol.md) | 2026-09-01 | Envelope, M0 messages, close codes, limits, canonical codec |
    11	| WP0.3 | [wp-0.3-core.md](wp-0.3-core.md) | 2026-09-01 | Ledger, apply(event) → effects, liveness sweep, refusals, interfaces |
    12	| WP0.4 | [wp-0.4-control-plane.md](wp-0.4-control-plane.md) | 2026-09-01 | Process: two listeners, sockets → core, hooks, local store, session emulation |
    13	| WP0.12 | [wp-0.12-ci.md](wp-0.12-ci.md) | 2026-09-01 | GitHub Actions: lint, unit, browser tests on every push; nightly simulation; no AWS in CI |
    14	| WP0.5 | [wp-0.5-node.md](wp-0.5-node.md) | 2026-09-01 | Orchestrator: session, hello, heartbeat, reconnect with backoff and rotation delay; web and Node platforms |
    15	| WP0.6 | [wp-0.6-web.md](wp-0.6-web.md) | 2026-09-01 | Host page: session states, observer client, node table, local nodes, controls; Playwright acceptance |
    16	| WP0.7 | [wp-0.7-dev.md](wp-0.7-dev.md) | 2026-09-01 | `mise run dev`: control plane, two local cores, web watch, one-command teardown |
    17	| WP0.9 | [wp-0.9-fleet-skeleton.md](wp-0.9-fleet-skeleton.md) | 2026-09-01 | Session and rotate v0 functions, pointer, operator scripts, fakes and SDK-mock tests |
    18	| WP0.8 | [wp-0.8-infra-skeleton.md](wp-0.8-infra-skeleton.md) | 2026-09-01 | CDK stacks Core, Image, Fleet; image staging dir with placeholder process; base-image resolver; synth tests |
    19	| WP0.10 | [wp-0.10-first-hello.md](wp-0.10-first-hello.md) | 2026-09-02 | First deploy: four stacks, control plane in a MicroVM, page on CloudFront, browser nodes through the proxy |
    20	| WP1.1 | [wp-1.1-protocol-v1.md](wp-1.1-protocol-v1.md) | 2026-09-02 | Full wire vocabulary: assign/result/presign, controls, events, views, manifests; ABI byte formats |
    21	| WP1.2 | [wp-1.2-core-v1.md](wp-1.2-core-v1.md) | 2026-09-02 | Scheduler: tiers, deadlines, speculation, verification, execution lifecycle, controls, snapshots, invariants |
    22	| WP1.3 | [wp-1.3-store.md](wp-1.3-store.md) | 2026-09-02 | Store package: local and S3 drivers with pinned checksums, the shared client; control plane executes store effects |
    23	| WP1.4 | [wp-1.4-sandbox.md](wp-1.4-sandbox.md) | 2026-09-02 | WebAssembly sandbox: validation, the five tf imports, filesystem glue, deadline kill, Node and web adapters |
    24	| WP1.5 | [wp-1.5-sdk-mandelbrot.md](wp-1.5-sdk-mandelbrot.md) | 2026-09-02 | AssemblyScript SDK mirroring the ABI, Mandelbrot program with presets and goldens, build and host scripts |
    25	| WP1.6 | [wp-1.6-node-v1.md](wp-1.6-node-v1.md) | 2026-09-02 | Node orchestrator: task loop, module and manifest caches, sandbox with grace deadline, uploads via presign, cancel and commands, `released` results |
    26	| WP1.7 | [wp-1.7-control-plane-v1.md](wp-1.7-control-plane-v1.md) | 2026-09-02 | Control plane: seeding of shipped programs, gzipped S3 snapshots with a latest pointer, adopt-from-snapshot in `/run`, the M1 pipeline end to end |
    27	| WP1.8 | [wp-1.8-dashboard-v1.md](wp-1.8-dashboard-v1.md) | 2026-09-02 | Dashboard: verified tiles on the canvas with state overlay, task grid, counters, throughput, controls over the socket, notices, demo mode |
    28	| WP1.9 | [wp-1.9-churn-sim.md](wp-1.9-churn-sim.md) | 2026-09-03 | Churn simulation: virtual nodes running real WASM on a virtual timeline, seeded chaos, invariants after every event, goldens per frame; four control-plane fixes it found |
    29	| WP1.10 | [wp-1.10-deploy-m1.md](wp-1.10-deploy-m1.md) | 2026-09-02 | M1 on AWS: signed-checksum presigns, default-loop backoff, execution pruning, the M1 runbook — 640 of 640 golden tiles from browser tabs through a kill-half |
    30	| WP2.1 | [wp-2.1-filesystem.md](wp-2.1-filesystem.md) | 2026-09-02 | Execution filesystem: bundle files as the first root, persist inheritance, expired-root warning, per-execution size cap |
    31	| WP2.2 | [wp-2.2-wordcount.md](wp-2.2-wordcount.md) | 2026-09-02 | Word count: three-stage map/reduce/merge over Moby-Dick with range ownership by word start, FNV-1a partitions, the `bars` payload, a staged host and goldens |
    32	| WP2.3 | [wp-2.3-launch-path.md](wp-2.3-launch-path.md) | 2026-09-02 | Launch path: uploaded bundles resolved and validated by the process, per-execution task budget, per-observer launch rate |
    33	| WP3.1 | [wp-3.1-cp-lifecycle.md](wp-3.1-cp-lifecycle.md) | 2026-09-02 | Rotation, control-plane side: phases, handover, generation-guarded adopt, jittered drain, fleet-secret gate |
    34	| WP3.2 | [wp-3.2-fleet-v1.md](wp-3.2-fleet-v1.md) | 2026-09-02 | Rotation, fleet side: the five steps, the private-port client, failure paths, and repair of an interrupted rotation |
    35	| WP2.4 | [wp-2.4-editor.md](wp-2.4-editor.md) | 2026-09-02 | In-page editor: asc in a lazily loaded worker (byte-identical to the build), prefilled Mandelbrot, diagnostics with lines, params, compile → bundle → launch over the observer socket, the drop-a-`.wasm` door |
    36	| WP3.3 | [wp-3.3-cloud-cores.md](wp-3.3-cloud-cores.md) | 2026-09-02 | Cloud cores: the fleet and sleep policies in the pure core, core records in the ledger, the image running the same orchestrator a tab runs |
    37	| WP2.5 | [wp-2.5-dashboard-v2.md](wp-2.5-dashboard-v2.md) | 2026-09-02 | Dashboard v2: programs panel with launch forms, queue with drop, stage strip, bars and text views, files panel by root with previews, task detail with attempts and log, failure and warning surfacing |
    38	| WP3.4 | [wp-3.4-handover-tests.md](wp-3.4-handover-tests.md) | 2026-09-02 | A real rotation on a laptop: the local driver behind `dev:rotate`, and a two-process handover taken mid-render as a test |
    39	| WP3.5 | [wp-3.5-deploy-m3.md](wp-3.5-deploy-m3.md) | 2026-09-02 | M3 on AWS: rotation under load measured at 8.4 s of churn, the bundled sandbox worker, retries for throttled fleet calls, the endpoint's socket ceiling |
    40	| WP2.6 | [wp-2.6-playwright-v1.md](wp-2.6-playwright-v1.md) | 2026-09-02 | Browser suites: the money shot (ten nodes, kill half, 640 golden tiles) and the sandbox's deadline kill and trap paths |
… (26 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/packages/sdk-as/README.md; echo "=====EDITOR.HTML====="; cat -n /home/mircea/homework/tabframe/packages/web/public/editor.html; echo "=====GUIDE.HTML====="; cat -n /home/mircea/homework/tabframe/packages/web/public/guide.html; echo "=====INDEX.HTML====="; cat -n /home/mircea/homework/tabframe/packages/web/public/index.html
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

````
     1	# @tabframe/sdk-as
     2	
     3	The AssemblyScript SDK for Tabframe programs, and the tooling around programs: the build script,
     4	the goldens script, and a minimal Node host.
     5	
     6	A program is one WebAssembly module with two entry points and a manifest (design §5). It sees the
     7	world only through a per-execution filesystem, has no clock, no randomness, and no network, and
     8	its API has no failure type: tasks complete, or the execution fails with a program fault.
     9	
    10	## Writing a program
    11	
    12	```ts
    13	import { readRunInput, readPlanInput, stage, done, emit, ByteWriter, ByteReader, Params, fs, log }
    14	  from "@tabframe/sdk-as/assembly/index";
    15	export { alloc } from "@tabframe/sdk-as/assembly/index";
    16	
    17	export function plan(ptr: usize, len: i32): usize {
    18	  const input = readPlanInput(ptr, len);          // stage, params, hints
    19	  if (input.stage == 0) {
    20	    const s = stage("map").canvas(2048, 1280);
    21	    for (let i = 0; i < 640; i++) s.taskAt(inputBytes(i), x(i), y(i), 64, 64);   // or s.task(bytes)
    22	    return emit(s.toBytes());
    23	  }
    24	  const next = new Params();                      // optional follow-up params
    25	  next.setI32("preset", input.params.getI32("preset", 0) + 1);
    26	  return emit(done(next));                        // or done(null)
    27	}
    28	
    29	export function run(ptr: usize, len: i32): usize {
    30	  const t = readRunInput(ptr, len);               // stage, taskIndex, taskCount, input bytes
    31	  const r = new ByteReader(t.input);
    32	  // ... compute ...
    33	  return emit(outputBytes);                       // a Uint8Array
    34	}
    35	```
    36	
    37	- **Params** are a flat table of key → JSON text. `getString`, `getI32`, `getF64`, `getBool` take a
    38	  fallback; `raw(key)` gives the JSON text (arrays and objects come through untouched). Setters
    39	  write JSON. Keys are sorted when written, so equal tables are equal bytes.
    40	- **Stage** builds what `plan` returns: a name, an optional canvas (for the `tiles` view), and tasks
… (288 more lines)
````

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/plan.md
```

**Result of `Read`:**

```
1	# Tabframe — Design Record
2	
3	**Status:** written 2026-09-01 before the build; the sections that name a work package were amended in place (§6.7, §8.3 among them) and everything else is read together with the drift log, §17 (last entry 2026-09-04), whose entries are in merge order.
4	This document is the source of truth for the build. It supersedes the earlier handoff and seed
5	documents wherever they differ.
6	
7	**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
8	microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
9	and which stays correct while cores — and the control plane itself — come and go.
10	
11	---
12	
13	## Contents
14	
15	1. Decisions register
16	2. The system in one page
17	3. Runtime picture
18	4. Nodes
19	5. Programs
20	6. The control plane
21	7. Data paths and the store
22	8. The wire
23	9. Hosting and fleet
24	10. Security and credentials
25	11. Tooling and repo
26	12. Tests and dev loop
27	13. Deploy, operations, and the reviewer's view
28	14. Milestones
29	15. Rationale hooks
30	16. Glossary
31	17. Drift log
32	
33	---
34	
35	## 1. Decisions register
36	
37	| # | Decision | Notes |
38	|---|----------|-------|
39	| D1 | **Scope:** everything in the original handoff, plus straggler speculation, duplicate-result verification with a built-in redundancy toggle, a program ABI, and MapReduce word count. Time is not a design constraint. | The handoff's 8-hour line did not drive any choice here; §15 says how the rationale owns that. |
40	| D2 | **General-purpose programs, minimal but complete.** A program is one WASM module (`plan` + `run`) plus a manifest, content-addressed, sandboxed, uploadable, with in-browser AssemblyScript compilation and an execution queue. | Mandelbrot and word count are real programs shipped through the same path; the node bundle contains no application code. |
… (551 more lines)
```

*Attachment: (file)*

**Result of `Read`:**

````
1	# Tabframe — Execution Plan
2	
3	**Status:** written 2026-09-01 from [`design.md`](design.md). The design record is the contract; this
4	plan is the order of work. When the two disagree, fix the design record first, then the plan.
5	
6	**Where we are (2026-09-04, M8 in progress):** M0–M7 done and deployed (generation 105 at M7's close, three green demo passes). M8 is the three review loops Mircea asked for on 2026-09-04: five reviewers (full-stack, distributed systems, security, technical writing, DevOps) read the tree, their findings are aggregated and critiqued, and the accepted ones land with tests. Loop 1 (WP8.1, 52 findings) is merged; loop 2 (WP8.2) reviews the tree after it and is this branch; loop 3 (WP8.3) follows, then one deploy with three demo passes. Left for Mircea: the README and rationale read-through, developer hours, the narrated video, the Cost Explorer check, the flip. Post-flip candidates: an EC2 host for the control plane (WP4.6), SIMD for the transformer, sampling with a seed.
7	
8	**Shape of the plan:** eight milestones, M0–M7, each ending in a deployable checkpoint, then M8's review loops; M5 is the packaging gate and stays open until the flip. Each milestone
9	is a set of work packages (WP). A WP is done when its code, its tests, its WP document under `docs/implementation/`, and its doc touch
10	are merged together and its acceptance line holds. Checkboxes are updated in the same commit that
11	completes the WP.
12	
13	---
14	
15	## 0. Ground rules for the build
16	
17	- **Every session starts the same way:** read `design.md` and this plan, run `mise run whoami`. Nothing touches AWS before the guard passes.
18	- **AWS identity:** profile `tabframe` only, set by mise and passed explicitly on ad-hoc calls. No fallback to any other profile — stop and ask.
19	- **Independence first.** The build is optimized for uninterrupted work: I stop and ask only when I genuinely cannot proceed. Everything already agreed in the design record and this plan proceeds without asking — `cdk bootstrap`, the first deploy with the budget, enabling the hourly schedule at M3 included. Non-blocking questions go into the WP report's Open line and work continues.
20	- **Hard stops** (the only ones): destructive or irreversible actions — deleting stacks, buckets, or data, rewriting pushed history, `mise run down`; spending outside the agreed design or budget; contradicting a decision Mircea explicitly owns; and gates only he can pass, such as flipping the repo public.
21	- **Transcript hygiene:** commands whose output can contain an account id, token, presigned URL, or address write raw output to the scratchpad; only a masked summary reaches the conversation.
22	- **Git flow:** a branch per WP, `wp/<m>.<n>-<slug>`, pushed as work progresses. Never commit directly to `main`. When lint and tests are green, I merge the branch into `main` myself with a `--no-ff` merge commit, push, and delete the branch. Commit messages name the WP (`M1/WP1.4 sandbox: deadline kill`). Goldens change in the same commit as the kernel that changed them. History on `main` is never rewritten.
23	- **WP document:** every WP lands `docs/implementation/wp-<m>.<n>-<slug>.md` on its branch, and no WP is done without it. Sections: **What** was delivered, **How** it works (key modules, interfaces, the shape of the code), **Why** it is built that way (alternatives considered, links to the design sections it implements), **Evidence** (tests, commands, screenshots under `docs/implementation/assets/`), **Drift** recorded in the design record, **Open** items. The chat report is a condensation of this file.
24	- **Time log:** `docs/timelog.md` gets one line per session with two columns — **developer time** (Mircea's hours) and **total time** (wall-clock of the session, agent included) — starting with the design day (2026-09-01). The rationale states the developer total honestly and shows the total alongside it.
25	- **Design drift:** any deviation discovered while building is recorded in `design.md` as a dated entry before the code lands, and work continues. Only a drift that contradicts an explicit decision waits for Mircea.
26	- **CI green before the next phase — hard rule (added 2026-09-02 after a day of red builds nobody looked at).** After every merge to `main` the CI run for that commit is checked (`gh run watch` or `gh run list`) and must pass before the next work package starts; a red `main` is the first thing fixed, and no milestone is tagged or reported as done while CI is red. Local green is not a substitute: CI runs on a fresh checkout with no built artifacts and no AWS, which is exactly what caught the simulation's dependency on `programs/*/dist`.
27	- **Review mode:** merge-when-green. Each WP merges into `main` when lint and tests pass and is reviewed after the fact, on GitHub or in the WP document. Independent WPs may run in parallel on their own branches; each still merges separately with its own report (Built / Verified / Drift / Open / Next).
28	
29	---
30	
31	## 1. Dependency map
32	
33	```
34	 protocol ──► core ──► control-plane (local mode) ──► dev topology
35	     │          │              │
36	     │          │              └──► store (local driver) ──► S3 driver (deploy)
37	     │          │
38	     ▼          ▼
39	   node ◄── sandbox ◄── sdk-as ◄── programs (mandelbrot, wordcount)
40	     │
… (205 more lines)
````

> **Thinking.** 

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Read`:**

```
592	
593	Lambda builds the image on ARM64 itself; Docker is optional locally. Bundling to one file keeps the snapshot small, which is what resume latency is made of.
594	
595	### 11.4 CDK
596	
597	Three stacks in dependency order — **Core** (buckets for artifacts, blobs, snapshots, and web, with a one-year lifecycle on blobs; one CloudFront distribution with two origins and an error-caching TTL of zero on the blob behavior; blob-bucket CORS allowing PUT with the checksum header from the page's origin; the SSM pointer with its off state; the budget), **Image** (`CfnMicrovmImage` with the base image version looked up by a script, hooks on port 8081, an environment holding only bucket names and the pointer name, and the build, control-plane, and core roles), **Fleet** (session with a public function URL, CORS for GET from the page's origin, and a modest reserved concurrency; rotate on an hourly EventBridge rule created disabled until M3, reserved concurrency 1; their roles). Outputs feed the web config (the session URL only) at deploy. Image publishing goes through CloudFormation so the operator needs no extra rights.
598	
599	### 11.5 Repo policy
600	
601	Created by Mircea at `github.com/t3spe/tabframe`; **private until the end, then public.** License **AGPL-3.0**. Commits under the existing GitHub noreply identity. Contents: code plus `docs/` (this record, the plan, the time log, architecture, rationale, and transcripts — export mechanism to be decided later). The seed and handoff documents stay out. Biome for lint and format. **Git flow:** a branch per work package, merged into `main` with a `--no-ff` merge commit once lint and tests are green, then deleted; no direct commits to `main`; history never rewritten; **and CI on `main` must be green before the next work package starts** — local green is not a substitute, because CI runs on a fresh checkout with nothing built. **Every work package ships a document** at `docs/implementation/wp-<m>.<n>-<slug>.md` — what was done, how, why, evidence, drift, open items — so the implementation history is readable without the commits.
602	
603	---
604	
605	## 12. Tests and dev loop
606	
607	**`mise run dev`** brings up the machine on a laptop: the control plane under `node --watch` in local mode (in-memory ledger, local store route with hash-verifying PUT, self-presign, emulated session endpoint, hooks as routes; public and private ports default to 4080 and 4081 locally because 8080 is taken on the development machine, while the image uses 8080 and 8081), **two local cores** as Node processes on the node platform entry, the web bundles in Bun watch mode, and seeding. **`mise run dev:rotate`** starts a second control plane and drives the real rotate code with a local driver — handover on a laptop before it touches AWS.
608	
609	| Layer | Proves | Runner |
610	|---|---|---|
611	| Protocol | schemas round-trip; invalid, oversized, and foreign-generation messages are rejected | bun test |
612	| Core, pure | fill priority, liveness, deadlines, health, both verification policies, fold and conflict detection, queue ordering, snapshot round-trip, adopt marks nodes gone, fleet policy, sleep policy | bun test |
613	| **Churn simulation** | correctness under arbitrary churn: seeded generator, virtual clock, fake store, real WASM programs under Node; invariants asserted after every event; final hashes equal goldens from single-node runs; lying-node and redundancy-on scenarios; replay by seed | bun test, seed matrix |
614	| Sandbox | forbidden imports and missing exports rejected, memory cap, deadline kill of an infinite loop, write caps, unresolvable paths, traps captured, no state leak, determinism | bun test (Node adapter), Playwright (web adapter) |
615	| Programs | compile; tile hashes match goldens; word count matches a JavaScript reference; plan outputs validate | bun test |
616	| Control-plane process | real sockets against a spawned process: hello → result, presign → PUT → hash check, role from payload, snapshot on suspend, handover between two processes, drain code | bun test |
617	| Fleet | rotate and session against a fake MicroVM client, including old-unreachable and crash-mid-rotation | bun test |
618	| **Browser end to end** | the money shot: spawn 10, kill half, canvas hash equals golden; editor compile and launch; local rotation mid-render | Playwright |
619	| Infra | stack synthesizes; `cdk diff` before every deploy | bun test |
620	
621	Plumbing: the core takes an injected clock and random source; `mise run goldens` regenerates goldens from single-node runs and any kernel change updates them in the same commit; sandbox fixtures are tiny AssemblyScript sources compiled at test time by the pinned compiler. Coverage: `bun test --coverage` with an 85 % line threshold in `bunfig.toml` for protocol, core, sandbox, control-plane, and fleet, report-only for web; property-based tests with fast-check on ledger transitions; the simulation's long mode runs nightly. A minimal GitHub Actions workflow on the private repo runs install, lint, and both suites; **no AWS credentials in CI**; deploys are manual; `mise run verify` is the M0 runbook, not a test.
622	
623	---
624	
625	## 13. Deploy, operations, and the reviewer's view
626	
627	**M0 = skeleton + verification.** Buckets, CloudFront, fleet functions, pointer, and an image whose control plane only does hello and heartbeat. Order: `mise run whoami`, `cdk bootstrap` (with confirmation), deploy, publish image, `rotate`, hit the session URL, open a socket, then `mise run verify` retires §9.6 and records results and any design adjustments in `docs/m0-verification.md`.
628	
629	**Deploy = rotation.** build → test → `cdk deploy` (stack, image version, web assets) → wait for the version to go active → `rotate`. Cores move to the new version over the next cycle. Hashed asset names with a short TTL on the index; a protocol bump reloads stale pages once. **Rollback** = rotate to the previous immutable image version and deactivate the bad one.
630	
631	**Observability:** MicroVM logs in CloudWatch, fleet logs, `/health` and `/diag` on the control plane, and the dashboard's generation, uptime, and next-rotation countdown.
… (367 more lines)
```

**Tool `Bash`:**

```
echo "=====mise.toml=====" && cat -n mise.toml && echo "=====Dockerfile=====" && find . -name Dockerfile -not -path "*/node_modules/*" -not -path "./cdk.out/*" | head && cat -n packages/control-plane/Dockerfile 2>/dev/null
```

**Result of `Bash`:**

```
Exit code 1
=====mise.toml=====
     1	# Tabframe — tooling, environment, and tasks. Everything installs through mise.
     2	[tools]
     3	# Pinned (WP8.1): the deploy path runs under an admin identity, so the CLIs it runs are exact.
     4	node = "22.23.2"
     5	bun = "1.3.13"
     6	aws-cli = "2.36.37"
     7	gh = "2.97.0"
     8	"npm:aws-cdk" = "2.1139.0"
     9	
    10	# The AWS identity and the operator's private file go only to the tasks that talk to AWS (WP8.2):
    11	# `test`, `sim`, `build`, and `dev` run without them.
    12	[env]
    13	AWS_REGION = "us-west-2"
    14	
    15	[tasks.install]
    16	description = "Install workspace dependencies and the Playwright browser"
    17	run = ["bun install", "bunx playwright install chromium"]
    18	
    19	[tasks.whoami]
    20	env = { AWS_PROFILE = "tabframe", _.file = ".env.local" }
    21	description = "Assert the AWS identity is the Tabframe account; every AWS task depends on it"
    22	run = "node packages/infra/scripts/whoami.ts"
    23	
    24	[tasks.lint]
    25	description = "Biome lint and format check, then a TypeScript type check"
    26	run = [
    27	  "bunx biome check .",
    28	  "bunx tsc -p tsconfig.json --noEmit",
    29	  "bunx tsc -p packages/web/tsconfig.json --noEmit",
    30	  "bunx tsc -p packages/infra/tsconfig.json --noEmit",
    31	]
    32	
    33	[tasks.test]
    34	description = "Unit and integration tests, then browser tests"
    35	depends = ["lint"]
    36	run = ["bun test", "bunx playwright test --pass-with-no-tests"]
    37	
    38	[tasks.dev]
… (151 more lines)
```

**Tool `Bash`:**

```
echo "=====m3-verification=====" && cat -n docs/m3-verification.md && echo "=====timelog=====" && cat -n docs/timelog.md && echo "=====submission-checklist=====" && cat -n docs/submission-checklist.md && echo "=====transcripts=====" && ls docs/transcripts | head -20 && echo "=====e2e=====" && ls e2e && echo "=====playwright config=====" && cat playwright.config.ts
```

**Result of `Bash`:**

```
=====m3-verification=====
     1	# M3 verification
     2	
     3	`mise run verify:m3` against the deployed machine in the Tabframe account (us-west-2) on
     4	2026-09-02. It opens browser tabs on the real page, waits until tiles are landing, invokes the
     5	rotate function, and measures what a rotation costs: how long the render is interrupted, whether
     6	the ledger is handed over, and what the session function did while every client reconnected.
     7	
     8	| Check | Result | Pass |
     9	|---|---|---|
    10	| Session before | generation 11 | yes |
    11	| Render in flight | 10 tiles landed before the rotation | yes |
    12	| Rotation | rotated to generation 12, handed over, 10 clients drained | yes |
    13	| Drain | 10 clients let go by the control plane with the rotating code | yes |
    14	| Session after | generation 12 | yes |
    15	| **Churn** | **8.4 s from the drain to the first tile of the new generation**; 25.8 s from the rotate invocation, which includes booting the successor | yes |
    16	| Nodes after | 6 nodes on the new control plane: the tabs came back on their own | yes |
    17	| Session function | peak 3 concurrent executions, 0 throttles, 17 invocations a minute | yes |
    18	
    19	Nine checks, none failed. Four rotations were run in all; the churn was 8.4 to 8.5 seconds every
    20	time.
    21	
    22	## What it means for the design
    23	
    24	- **A rotation costs about eight seconds of render, once an hour.** The clients are told to come
    25	  back with a jittered delay, they fetch a fresh session, and tiles resume. The frame in flight
    26	  continues on the new generation rather than restarting: the ledger came across, so the tasks
    27	  already done stay done.
    28	- **The session function is nowhere near its limits.** Peak three concurrent executions against an
    29	  account limit of ten, no throttles, seventeen invocations in the busiest minute. The jitter
    30	  window plus the shared cached token do what they were designed to do.
    31	- **The handover is an optimization, not a dependency.** In two of the four rotations the fleet
    32	  could not reach the outgoing control plane (see below) and the successor adopted the S3 snapshot
    33	  instead. Both rotations completed correctly with the same churn; the only cost was the work of
    34	  the last few seconds being recomputed, which idempotent tasks make safe.
    35	
    36	## The endpoint's concurrency ceiling — resolved
    37	
    38	Measured while preparing this run, then isolated afterwards (WP4.5,
    39	`packages/infra/scripts/socket-ceiling.ts`):
… (163 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
(bun test 2>&1 | tail -8) 
```

**Result of `Bash`:**

```
Command running in background with ID: bdjy5290m. Output is being written to: /tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tasks/bdjy5290m.output. You will be notified when it completes. To check interim output, use Read on that file path.
```

**Tool `Bash`:**

```
echo "=====Dockerfile=====" && cat -n packages/infra/image/Dockerfile && echo "=====stage-image=====" && sed -n 1,80p packages/infra/scripts/stage-image.ts && echo "=====e2e test counts=====" && for f in e2e/*.e2e.ts; do printf "%s %s\n" "$(grep -cE '^\s*test\(' "$f")" "$f"; done && echo total: $(cat e2e/*.e2e.ts | grep -cE '^\s*test\(') && grep -nE 'test\.(skip|only|describe)' e2e/*.e2e.ts | head -20
```

**Result of `Bash`:**

```
=====Dockerfile=====
     1	# Tabframe MicroVM image (design §11.3): one image, two roles, chosen by the run-hook payload.
     2	# The bundle main.js is produced by `mise run build:image`; this directory ships a placeholder
     3	# until then so the image builds and answers its hooks.
     4	# Pinned by digest (WP8.2): two builds of one commit get the same base. Refresh the digest with
     5	# `docker buildx imagetools inspect public.ecr.aws/lambda/microvms:al2023-minimal`.
     6	FROM public.ecr.aws/lambda/microvms:al2023-minimal@sha256:05cb9b38d841e7ff1b693dc9e894909612f340bf99ec97d426e8000a5bbe96c3
     7	RUN dnf install -y nodejs22 && dnf clean all && node --version
     8	WORKDIR /app
     9	COPY package.json /app/package.json
    10	COPY main.js /app/main.js
    11	COPY node-worker.js /app/node-worker.js
    12	COPY programs/ /app/programs/
    13	# The build stamp (WP8.1, shipped since WP8.2): /health names the commit the image was staged from.
    14	COPY build.json /app/build.json
    15	EXPOSE 8080 8081
    16	CMD ["node", "/app/main.js"]
=====stage-image=====
// Assemble the MicroVM image staging directory from the real control-plane bundle:
//   packages/infra/image-dist/ = Dockerfile + package.json (ESM) + main.js + programs/
// `cdk deploy` reads it through TABFRAME_IMAGE_DIR (the committed packages/infra/image/ holds a
// placeholder so the image builds before the control plane exists).
import { execSync } from "node:child_process";
import { copyFileSync, existsSync, mkdirSync, readdirSync, rmSync, writeFileSync } from "node:fs";
import path from "node:path";

const root = path.resolve(import.meta.dirname, "../../..");
const src = path.join(root, "packages/infra/image");
const bundle = path.join(root, "packages/control-plane/dist/main.js");
const programs = path.join(root, "programs");
const out = path.join(root, "packages/infra/image-dist");

if (!existsSync(bundle)) {
  console.error(`missing ${path.relative(root, bundle)}; run the bundle step first`);
  process.exit(1);
}
rmSync(out, { recursive: true, force: true });
mkdirSync(path.join(out, "programs"), { recursive: true });
copyFileSync(path.join(src, "Dockerfile"), path.join(out, "Dockerfile"));
writeFileSync(path.join(out, "package.json"), '{ "type": "module" }\n');
… (63 more lines)
```

**Tool `Bash`:**

```
echo "=====runbook deploy:stacks=====" && grep -n "deploy:stacks\|cdk deploy\|cdk bootstrap" README.md docs/runbook.md docs/design.md docs/plan.md && echo "=====stagingDirOrRefuse=====" && grep -rn "stagingDirOrRefuse\|TABFRAME_IMAGE_PLACEHOLDER\|image-dist" packages/infra/src packages/infra/bin packages/infra/*.ts 2>/dev/null | head -20 && echo "=====verify scripts AWS use=====" && grep -ln "fromIni\|LambdaClient\|S3Client\|SSMClient\|CloudWatch\|@aws-sdk" packages/infra/scripts/*.ts packages/fleet/scripts/*.ts | sort && echo "---verify-m3 sdk---" && grep -n "@aws-sdk\|AWS_PROFILE\|fromIni\|invoke\|rotate" packages/infra/scripts/verify-m3.ts | head -20 && echo "---verify-m1 sdk---" && grep -n "@aws-sdk\|AWS_PROFILE\|fromIni\|S3\|snapshot" packages/infra/scripts/verify-m1.ts | head -20 && echo "---verify-m2 sdk---" && grep -n "@aws-sdk\|AWS_PROFILE\|fromIni" packages/infra/scripts/verify-m2.ts | head
```

**Result of `Bash`:**

```
=====runbook deploy:stacks=====
README.md:165:cdk bootstrap                   # once
README.md:166:mise run deploy                 # build → test → cdk deploy (four stacks) → up: a rotation onto the new image
docs/plan.md:19:- **Independence first.** The build is optimized for uninterrupted work: I stop and ask only when I genuinely cannot proceed. Everything already agreed in the design record and this plan proceeds without asking — `cdk bootstrap`, the first deploy with the budget, enabling the hourly schedule at M3 included. Non-blocking questions go into the WP report's Open line and work continues.
docs/plan.md:71:  *Gates:* `cdk bootstrap` confirmation; first deploy confirmation (budget).
docs/plan.md:72:  *Acceptance:* `cdk synth` test passes; `cdk deploy --all` succeeds; the image version reaches ACTIVE (build logs tailed).
docs/design.md:54:| D16 | **AWS identity:** a dedicated account; profile **`tabframe`** (an IAM user with AdministratorAccess) is the operator identity for bootstrap and `cdk deploy` only. Every application component runs under a least-privilege role created by CDK. No IAM users or static keys for the application. | Verified 2026-09-01: profile works, region us-west-2, account empty, MicroVMs API reachable, memory quota 8 GB, RunMicrovm rate 1/s. |
docs/design.md:519:- **Operator identity:** profile `tabframe`, an IAM user with AdministratorAccess, used only for `cdk bootstrap` and `cdk deploy`. Every AWS call from the laptop uses this profile; nothing in AWS runs as it. `AWS_PROFILE` is set by mise, `--profile` is passed explicitly on ad-hoc calls, and a `whoami` guard task asserts the account id (kept in the gitignored `.env.local`) before any AWS-touching task runs. There is no fallback to any other profile.
docs/design.md:627:**M0 = skeleton + verification.** Buckets, CloudFront, fleet functions, pointer, and an image whose control plane only does hello and heartbeat. Order: `mise run whoami`, `cdk bootstrap` (with confirmation), deploy, publish image, `rotate`, hit the session URL, open a socket, then `mise run verify` retires §9.6 and records results and any design adjustments in `docs/m0-verification.md`.
docs/design.md:629:**Deploy = rotation.** build → test → `cdk deploy` (stack, image version, web assets) → wait for the version to go active → `rotate`. Cores move to the new version over the next cycle. Hashed asset names with a short TTL on the index; a protocol bump reloads stale pages once. **Rollback** = rotate to the previous immutable image version and deactivate the bad one.
docs/runbook.md:28:| Deploy | `mise run deploy` | Refuses unless the tree is clean, HEAD is on `origin/main`, and that commit's CI is green (`TABFRAME_DEPLOY_UNGATED=1` skips the check for an emergency deploy from a branch); builds programs, page, and image; runs lint and every test; fails on any IAM or security-group broadening in `cdk diff --security-only` (`TABFRAME_DEPLOY_IAM=1` acknowledges one); `cdk deploy --all`; then `up`, which rotates the running control plane onto the new image. A deploy is a rotation. |
docs/runbook.md:41:   a plain re-run of `cdk deploy` then completes.
docs/runbook.md:109:| `did not stabilize` on the image update | CloudFormation gave up before the platform's retry succeeded | re-run `cdk deploy`; check `latestActiveImageVersion` |
=====stagingDirOrRefuse=====
packages/infra/bin/tabframe.ts:24:function stagingDirOrRefuse(): string {
packages/infra/bin/tabframe.ts:25:  const dir = process.env.TABFRAME_IMAGE_DIR ?? resolve(repoRoot, "packages/infra/image-dist");
packages/infra/bin/tabframe.ts:27:  if (process.env.TABFRAME_IMAGE_PLACEHOLDER === "1")
packages/infra/bin/tabframe.ts:30:    `${dir} holds no staged image (main.js and build.json): run \`mise run build\` first, or set TABFRAME_IMAGE_PLACEHOLDER=1 to deploy the placeholder on purpose`,
packages/infra/bin/tabframe.ts:45:  stagingDir: stagingDirOrRefuse(),
=====verify scripts AWS use=====
packages/fleet/scripts/_deps.ts
packages/fleet/scripts/rollback.ts
packages/infra/scripts/demo.ts
packages/infra/scripts/health.ts
packages/infra/scripts/rotation-probe-busy.ts
packages/infra/scripts/rotation-probe.ts
packages/infra/scripts/socket-ceiling.ts
packages/infra/scripts/verify-m0.ts
packages/infra/scripts/verify-m1.ts
packages/infra/scripts/verify-m2.ts
packages/infra/scripts/verify-m3.ts
packages/infra/scripts/whoami.ts
---verify-m3 sdk---
6:import { CloudFormationClient, DescribeStacksCommand } from "@aws-sdk/client-cloudformation";
7:import { CloudWatchClient, GetMetricStatisticsCommand } from "@aws-sdk/client-cloudwatch";
8:import { InvokeCommand, LambdaClient } from "@aws-sdk/client-lambda";
43:const rotateFunction = fleet.RotateFunctionName ?? "";
46:if (!sessionUrl || !rotateFunction) throw new Error("stack outputs missing; deploy first");
165:const rotatedAt = Date.now();
168:    FunctionName: rotateFunction,
… (26 more lines)
```

**Tool `Bash`:**

```
echo "=====core constants=====" && grep -rnE "export const (CLIENT_CONNECTION_CAP|PARAMS_MAX_BYTES|QUEUE_CAP|PROGRAMS_CAP|STORE_RETRY_MS|STORE_ERRORS_MAX|RELEASES_PER_TASK_CAP|PRESIGN_BYTES_PER_MIN|PRESIGN_BYTES_PER_CONN|CONTROL_COOLDOWN_MS|HANDOVER_LEASE_MS|OFF_POLL_MS|MISSING_RETRY_MS|ACTIVITY_CAP|YIELD_IDLE_MS|KEEP_ENDED_TASKS|SOLICITED_RATE|SNAPSHOT_INTERVAL_MS|SLEEP_AFTER_MS|NODE_CAP|OBSERVER_CAP|MAX_NODES|MAX_OBSERVERS|GONE_MS|HEARTBEAT_MS|DEADLINE_FLOOR_MS|KEEP_ENDED_EXECUTIONS|CORE_MAX_AGE_MS|CORE_UNLINKED_MS|HOLD_MS|RESULTS_PER_TASK|RECORDS_PER_TASK)\b" packages --include=*.ts | grep -v node_modules | grep -v "\.test\." | head -60
```

**Result of `Bash`:**

```
=====core constants=====
packages/core/src/fleet.ts:15:export const CORE_MAX_AGE_MS = 3.5 * 60 * 60 * 1_000;
packages/core/src/executions.ts:55:export const PROGRAMS_CAP = 64;
packages/core/src/executions.ts:522:export const PARAMS_MAX_BYTES = 4096;
packages/core/src/executions.ts:523:export const QUEUE_CAP = 32;
packages/core/src/executions.ts:525:export const STORE_RETRY_MS = 10_000;
packages/core/src/executions.ts:536:export const STORE_ERRORS_MAX = 6;
packages/core/src/executions.ts:600:export const KEEP_ENDED_EXECUTIONS = 32;
packages/core/src/executions.ts:741:export const YIELD_IDLE_MS = 10 * 60 * 1000;
packages/core/src/executions.ts:847:export const KEEP_ENDED_TASKS = 2;
packages/core/src/results.ts:187:export const RELEASES_PER_TASK_CAP = 6;
packages/core/src/apply.ts:150:export const HANDOVER_LEASE_MS = 90_000;
packages/core/src/apply.ts:270:export const SOLICITED_RATE = 256;
packages/core/src/apply.ts:272:export const PRESIGN_BYTES_PER_MIN = 64 * 1024 * 1024;
packages/core/src/apply.ts:274:export const CONTROL_COOLDOWN_MS = 2_000;
packages/node/src/orchestrator.ts:94:export const OFF_POLL_MS = 15_000;
packages/web/src/observer.ts:51:export const OFF_POLL_MS = 15_000;
packages/web/src/panels.ts:97:export const MISSING_RETRY_MS = 10_000;
packages/web/src/state.ts:23:export const ACTIVITY_CAP = 400; // (WP8.2) the activity tab promises the last few hundred lines
```

**Tool `Bash`:**

```
echo "=====protocol limits=====" && grep -rnE "^\s*(export const|\s+)[A-Za-z_]+\s*[:=]\s*[0-9_]+" packages/protocol/src/limits.ts 2>/dev/null | head -60; ls packages/protocol/src; echo "=====limits file grep=====" && grep -rn "maxPresignItems\|maxOutputBytes\|maxInlineInput\|maxTasks\|maxSpecBytes\|maxModuleBytes\|maxWrites\|maxFrame\|MAX_FRAME\|maxMessageBytes\|nodeCap\|observerCap\|fsBytesCap\|taskCap\|launchesPerMinute\|maxLogBytes\|computeBudget" packages/protocol/src/*.ts | grep -v test | head -40
```

**Result of `Bash`:**

```
=====protocol limits=====
packages/protocol/src/limits.ts:6:  maxMessageBytes: 64 * 1024,
packages/protocol/src/limits.ts:8:  maxInlineInputBytes: 16 * 1024,
packages/protocol/src/limits.ts:10:  snapshotPageTasks: 256,
packages/protocol/src/limits.ts:12:  nodeCap: 256,
packages/protocol/src/limits.ts:13:  observerCap: 64,
packages/protocol/src/limits.ts:15:  heartbeatMs: 1_000,
packages/protocol/src/limits.ts:16:  goneAfterMs: 4_000,
packages/protocol/src/limits.ts:17:  observerPingMs: 2_000,
packages/protocol/src/limits.ts:18:  maxInFlight: 2,
packages/protocol/src/limits.ts:23:  nodeMessagesPerSecond: 1_000,
packages/protocol/src/limits.ts:24:  observerMessagesPerSecond: 5,
packages/protocol/src/limits.ts:26:  maxWriteFiles: 256,
packages/protocol/src/limits.ts:27:  maxWriteBytes: 16 * 1024 * 1024,
packages/protocol/src/limits.ts:28:  maxOutputBytes: 16 * 1024 * 1024,
packages/protocol/src/limits.ts:29:  maxLogBytes: 64 * 1024,
packages/protocol/src/limits.ts:30:  maxInlineLogBytes: 4 * 1024,
packages/protocol/src/limits.ts:32:  maxPresignItems: 24,
packages/protocol/src/limits.ts:33:  maxModuleBytes: 8 * 1024 * 1024,
packages/protocol/src/limits.ts:35:  reconnectMinMs: 500,
packages/protocol/src/limits.ts:36:  reconnectMaxMs: 30_000,
abi.test.ts
abi.ts
canonical.test.ts
canonical.ts
codec.test.ts
codec.ts
codes.ts
fs.ts
headers.ts
index.ts
limits.ts
messages.test.ts
node.ts
observer.ts
shared.ts
task.ts
=====limits file grep=====
packages/protocol/src/abi.ts:57:  maxTasks: 4096,
packages/protocol/src/abi.ts:59:  maxInlineInput: 16 * 1024,
… (24 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
echo "=====CLIENT_CONNECTION_CAP=====" && grep -rn "CLIENT_CONNECTION_CAP" packages --include=*.ts | grep -v node_modules | grep -v test | head -5 && echo "=====sleep constants=====" && grep -rnE "(SLEEP|OBSERVER|INTERACTION|IDLE|SUSPEND)[A-Z_]*_MS\s*=" packages/core/src/*.ts | head -10 && echo "=====sim drill=====" && grep -n "drill\|--long\|--seed" packages/core/sim/run.ts | head -8 && echo "=====CYCLE in mandelbrot=====" && grep -n "CYCLE" programs/mandelbrot/assembly/index.ts | head -3 && echo "=====wordcount top-K=====" && grep -rn "25\|TOP_K\|topK" programs/wordcount/assembly/index.ts | head -5 && echo "=====attribution/license/transcripts=====" && ls programs/wordcount/in/ATTRIBUTION.txt LICENSE docs/transcripts/index.md && echo "=====guideMarkdown cut=====" && grep -n "Compiling\|guideMarkdown\|README" packages/web/src/editor-core.ts | head -10
```

**Result of `Bash`:**

```
=====CLIENT_CONNECTION_CAP=====
packages/control-plane/src/server.ts:53:const CLIENT_CONNECTION_CAP = 14;
packages/control-plane/src/server.ts:468:    if (conns.size >= CLIENT_CONNECTION_CAP) {
=====sleep constants=====
packages/core/src/fleet.ts:23:export const SLEEP_AFTER_NO_OBSERVER_MS = 10 * 60 * 1_000;
packages/core/src/fleet.ts:25:export const SLEEP_AFTER_NO_INTERACTION_MS = 60 * 60 * 1_000;
packages/core/src/executions.ts:741:export const YIELD_IDLE_MS = 10 * 60 * 1000;
packages/core/src/apply.ts:58:const OBSERVER_SILENCE_MS = 5 * LIMITS.observerPingMs;
=====sim drill=====
3://   node packages/core/sim/run.ts [--seed N | --seeds A..B] [--long] [--tiles N] [--frames N]
4://                                 [--liar | --honest] [--drill] [--verbose] [--keep-going]
6:// Default: seeds 1..3 of the normal scenario over whole frames. `--long` runs the long scenario
19:const long = has("--long");
20:const seeds = parseSeeds(flag("--seeds") ?? flag("--seed"), long);
26:const drill = has("--drill");
75:    drill,
94:    `\nreplay: node packages/core/sim/run.ts --seed ${failed.seed}${long ? " --long" : ""}${tiles !== null ? ` --tiles ${tiles}` : ""}${liar === true ? " --liar" : liar === false ? " --honest" : ""} --verbose`,
=====CYCLE in mandelbrot=====
24:const CYCLE: f64 = 48.0; // iterations per palette cycle
172:  let t = mu / CYCLE;
=====wordcount top-K=====
programs/wordcount/assembly/index.ts:70:  const k = clamp(input.params.getI32("k", 25), 1, MAX_K);
=====attribution/license/transcripts=====
docs/transcripts/index.md
LICENSE
programs/wordcount/in/ATTRIBUTION.txt
=====guideMarkdown cut=====
300:/** The guide's Markdown (the SDK's README), so a test can check it is the file on disk. */
306:export function guideMarkdown(): string {
307:  // The README's "Compiling" and "Tooling" sections are about the repository, not the page (WP8.1):
309:  const cut = GUIDE_MARKDOWN.indexOf("\n## Compiling");
315: * and bold. Everything is text nodes — nothing from the README is interpreted as HTML.
362:      const level = Math.min(6, (heading[1]?.length ?? 1) + 1); // the README's h1 is the page's h2
```

**Tool `Bash`:**

```
echo "=====fleet rotate: grace, young rotation, pending/retiring=====" && grep -rnE "5_000|5000|DRAIN_GRACE|grace|YOUNG|young|300_000|minutes|retiring|pending" packages/fleet/src/rotate.ts | head -40 && echo "=====fleet files=====" && ls packages/fleet/src
```

**Result of `Bash`:**

```
=====fleet rotate: grace, young rotation, pending/retiring=====
packages/fleet/src/rotate.ts:10://   5. CP(g) /drain, then terminate after a grace period
packages/fleet/src/rotate.ts:75:export const DRAIN_GRACE_MS = 5_000;
packages/fleet/src/rotate.ts:167:    retiring: ControlPlaneTarget | null = null,
packages/fleet/src/rotate.ts:177:      pending: null,
packages/fleet/src/rotate.ts:180:      retiring: retiring ? { microvmId: retiring.microvmId, endpoint: retiring.endpoint } : null,
packages/fleet/src/rotate.ts:198:    const stale = p.pending;
packages/fleet/src/rotate.ts:202:      log.warn("rotate: forgetting a pending control plane that is gone", {
packages/fleet/src/rotate.ts:206:      await pointer.write({ ...p, pending: null });
packages/fleet/src/rotate.ts:212:      await pointer.write({ ...p, pending: null });
packages/fleet/src/rotate.ts:215:    // WP6.7: a pending successor never adopted the current ledger — it booted from a snapshot of
packages/fleet/src/rotate.ts:223:      log.warn("rotate: a pending successor with a stale ledger; terminating it, rotating afresh", {
packages/fleet/src/rotate.ts:227:      await terminateQuietly(stale.microvmId, "stale pending successor");
packages/fleet/src/rotate.ts:228:      await pointer.write({ ...p, pending: null });
packages/fleet/src/rotate.ts:264:    await sleep.sleep(DRAIN_GRACE_MS);
packages/fleet/src/rotate.ts:276:    // leave a pending successor behind. A scheduled invocation within five minutes of the last
packages/fleet/src/rotate.ts:284:      log.info("rotate: a rotation ran minutes ago; the scheduled one waits for the next hour", {
packages/fleet/src/rotate.ts:292:    if (p.retiring && p.retiring.microvmId !== p.microvmId) {
packages/fleet/src/rotate.ts:294:        microvmId: p.retiring.microvmId,
packages/fleet/src/rotate.ts:297:        { microvmId: p.retiring.microvmId, endpoint: p.retiring.endpoint ?? "" },
packages/fleet/src/rotate.ts:301:      await pointer.write({ ...(await pointer.read()), retiring: null });
packages/fleet/src/rotate.ts:305:    const current = p.pending ? await pointer.read() : p;
packages/fleet/src/rotate.ts:364:    // From here a crash is recoverable: the pending record names the successor.
packages/fleet/src/rotate.ts:367:      pending: { microvmId: next.microvmId, endpoint: next.endpoint, generation },
packages/fleet/src/rotate.ts:388:    await pointer.write({ ...(await pointer.read()), retiring: null });
=====fleet files=====
aws.ts
config.ts
cp-client.ts
lambda
microvm-client.ts
ops.ts
pointer.ts
rotate.ts
session.ts
testing
types.ts
```

**Tool `Bash`:**

```
echo "=====infra names=====" && grep -rnE "tabframe-alarms|tabframe-monthly|tabframe-canary|reservedConcurrentExecutions|logRetention|RetentionDays|expiration|Duration\.(days|hours|minutes|seconds)\(|prefix:|maxDurationSeconds|maxIdleDurationSeconds|rate\(|cron\(" packages/infra/lib/*.ts packages/infra/src/*.ts 2>/dev/null | grep -v test | head -50; ls packages/infra
```

**Result of `Bash`:**

```
=====infra names=====
packages/infra/lib/names.ts:6:  alarmTopic: "tabframe-alarms",
packages/infra/lib/names.ts:7:  canaryFunction: "tabframe-canary",
packages/infra/lib/fleet-stack.ts:61:    const logRetention = cdk.aws_logs.RetentionDays.TWO_WEEKS;
packages/infra/lib/fleet-stack.ts:63:      logRetention,
packages/infra/lib/fleet-stack.ts:70:      timeout: cdk.Duration.seconds(10),
packages/infra/lib/fleet-stack.ts:99:      logRetention,
packages/infra/lib/fleet-stack.ts:108:      timeout: cdk.Duration.minutes(10),
packages/infra/lib/fleet-stack.ts:160:      schedule: cdk.aws_events.Schedule.rate(cdk.Duration.hours(1)),
packages/infra/lib/fleet-stack.ts:169:      maxEventAge: cdk.Duration.minutes(30),
packages/infra/lib/fleet-stack.ts:191:      this.rotate.metricErrors({ period: cdk.Duration.minutes(5) }),
packages/infra/lib/fleet-stack.ts:196:      this.session.metricErrors({ period: cdk.Duration.minutes(5) }),
packages/infra/lib/fleet-stack.ts:201:      this.rotate.metricThrottles({ period: cdk.Duration.minutes(5) }),
packages/infra/lib/fleet-stack.ts:210:      logRetention,
packages/infra/lib/fleet-stack.ts:217:      timeout: cdk.Duration.seconds(10),
packages/infra/lib/fleet-stack.ts:228:      schedule: cdk.aws_events.Schedule.rate(cdk.Duration.minutes(5)),
packages/infra/lib/fleet-stack.ts:236:        period: cdk.Duration.minutes(5),
packages/infra/lib/image-stack.ts:44:      retention: cdk.aws_logs.RetentionDays.ONE_WEEK,
packages/infra/lib/core-stack.ts:44:      lifecycleRules: [{ id: "expire-one-year", expiration: cdk.Duration.days(365) }],
packages/infra/lib/core-stack.ts:64:      lifecycleRules: [{ id: "expire-one-day", prefix: "g", expiration: cdk.Duration.days(1) }],
packages/infra/lib/core-stack.ts:83:          accessControlMaxAge: cdk.Duration.days(365),
packages/infra/lib/core-stack.ts:129:        { httpStatus: 403, ttl: cdk.Duration.seconds(0) },
packages/infra/lib/core-stack.ts:130:        { httpStatus: 404, ttl: cdk.Duration.seconds(0) },
packages/infra/lib/core-stack.ts:152:          budgetName: "tabframe-monthly",
bin
image
image-dist
lib
node_modules
package.json
scripts
test
tsconfig.json
```

**Tool `Bash`:**

```
echo "=====web strings=====" && grep -rn "Reloaded once already\|1_500\|1500\|Tabframe · \|resume updates\|pause updates\|the pause ended\|launched ·\|spawnCount\|hardwareConcurrency\|is out of date" packages/web/src/*.ts | grep -v test | head -30
```

**Result of `Bash`:**

```
=====web strings=====
packages/web/src/banners.ts:34:        title: "This page is out of date.",
packages/web/src/editor-page.ts:48:      ? "launched · the pause ended and the machine runs your program · keep editing, or close this tab"
packages/web/src/host.ts:99:  document.title = `Tabframe · ${panelMode}`;
packages/web/src/host.ts:122:const cores = navigator.hardwareConcurrency || 1;
packages/web/src/host.ts:164:  spawnCount: $<HTMLSpanElement>("#spawnCount"),
packages/web/src/host.ts:184:els.spawnCount.textContent = String(spawnDefault);
packages/web/src/host.ts:316:const DEMO_STORE_CAP = 1500;
packages/web/src/host.ts:466:      if (!reloaded) setTimeout(() => location.reload(), 1_500);
packages/web/src/host.ts:469:          "Reloaded once already: hard-refresh this page (Shift+reload) to fetch the current bundle.";
packages/web/src/host.ts:652:    freezeButton.textContent = `resume updates (${panelFrozenMissed} held)`;
packages/web/src/host.ts:1049:  freezeButton.textContent = panelFrozen ? "resume updates" : "pause updates";
packages/web/src/state.ts:17:export const FLASH_MS = 1_500;
```

**Tool `Bash`:**

```
echo "=====vocabulary sweep=====" && grep -rnoiE "browser nodes?|tab nodes?|local nodes?|local cores?|cores are browser tabs|tab workers?|every visitor is a core|your tab is a core" README.md docs/design.md docs/runbook.md docs/walkthrough.md docs/rationale.md docs/plan.md packages/web/public/*.html | sort | uniq -c | sort -rn | head -40
```

**Result of `Bash`:**

```
=====vocabulary sweep=====
      1 README.md:5:your tab is a core
      1 README.md:3:cores are browser tabs
      1 README.md:145:local cores
      1 README.md:105:tab nodes
      1 packages/web/public/index.html:122:every visitor is a core
      1 docs/plan.md:68:local cores
      1 docs/plan.md:54:local cores
      1 docs/plan.md:121:local cores
      1 docs/plan.md:102:local cores
      1 docs/design.md:7:cores are browser tabs
      1 docs/design.md:674:tab worker
      1 docs/design.md:650:local cores
      1 docs/design.md:650:browser node
      1 docs/design.md:607:local cores
      1 docs/design.md:174:browser node
      1 docs/design.md:142:Browser nodes
```

**Tool `Bash`:**

```
echo "=====machineSentence strings=====" && grep -n "idle · the loop starts\|stopped by you\|paused · the editor tab\|the loop waits behind it\|the result stays\|observing · this tab\|demo · a scripted" packages/web/src/state.ts | head -10 && echo "=====health build fields=====" && grep -rn "imageVersion\|build:" packages/control-plane/src/health.ts packages/control-plane/src/*.ts 2>/dev/null | grep -v test | head -8 && echo "=====implementation docs vs index=====" && for f in docs/implementation/wp-*.md; do b=$(basename $f); grep -q "$b" docs/implementation/README.md || echo "MISSING ROW: $b"; done; echo done
```

**Result of `Bash`:**

```
=====machineSentence strings=====
653:          ? "the loop yielded to you: the result stays until Start or ten quiet minutes"
1112:      "paused · the editor tab is open · in-flight tasks finish, nothing new starts · closing it or launching resumes";
1116:      ? `running your ${exec.programName} ${exec.executionId} · ${where} · ${nodes} · the loop waits behind it`
1119:    core = "stopped by you · nothing runs until Start · a launch of yours still runs at once";
1124:      ? `your ${exec.programName} ${exec.executionId} ${ended} · the result stays · the loop waits for Start or ten quiet minutes`
1134:  else core = "idle · the loop starts a frame when someone watches";
1136:    ? "demo · a scripted cluster inside this page, nothing is sent anywhere · "
1138:      ? "observing · this tab lends no cores · "
=====health build fields=====
packages/control-plane/src/config.ts:35:  imageVersion: string | null;
packages/control-plane/src/config.ts:67:    imageVersion: env.TABFRAME_IMAGE_VERSION ?? null,
packages/control-plane/src/cores.ts:15:  imageVersion: string | null;
packages/control-plane/src/cores.ts:58:        imageVersion: config.imageVersion,
packages/control-plane/src/static.ts:22:<h1>Tabframe control plane</h1><p>The web bundle is not built. Run <code>mise run build:web</code> or <code>mise run dev</code>.</p>
packages/control-plane/src/server.ts:182:        imageVersion: config.imageVersion,
packages/control-plane/src/server.ts:740:        build: process.env.TABFRAME_BUILD ?? null,
packages/control-plane/src/server.ts:741:        imageVersion: config.imageVersion ?? null,
=====implementation docs vs index=====
done
```

> **Thinking.** 

**Tool `Bash`:**

```
echo "=====server cap=====" && sed -n 50,56p packages/control-plane/src/server.ts && sed -n 460,480p packages/control-plane/src/server.ts
```

**Result of `Bash`:**

```
=====server cap=====
const FLEET_ROUTES = new Set(["/handover", "/adopt", "/drain", "/snapshot", "/diag"]);

/** Client sockets the control plane accepts: the endpoint's sixteen minus two for the fleet (WP8.2). */
const CLIENT_CONNECTION_CAP = 14;
let loggedForwarding = false;

function parseNext(body: Uint8Array | null): number | null {
    if (!connRole || role !== "control-plane" || ledger?.meta.phase !== "active") {
      socket.write("HTTP/1.1 503 Service Unavailable\r\nConnection: close\r\n\r\n");
      socket.destroy();
      return;
    }
    // The endpoint holds sixteen connections (design §9.7); two stay free for the fleet's own
    // calls, so a full house of tabs cannot block a handover or a drain (WP8.2). The client's
    // address is logged once, to learn whether the proxy forwards it (a per-address quota needs it).
    if (conns.size >= CLIENT_CONNECTION_CAP) {
      socket.write(
        "HTTP/1.1 503 Service Unavailable\r\nRetry-After: 10\r\nConnection: close\r\n\r\n",
      );
      socket.destroy();
      return;
    }
    if (!loggedForwarding) {
      loggedForwarding = true;
      log("upgrade-headers", {
        forwardedFor: req.headers["x-forwarded-for"] ?? null,
        realIp: req.headers["x-real-ip"] ?? null,
      });
```

**Tool `Bash`:**

```
echo "=====rotate repair path=====" && sed -n 190,240p packages/fleet/src/rotate.ts && echo "..." && sed -n 270,312p packages/fleet/src/rotate.ts
```

**Result of `Bash`:**

```
=====rotate repair path=====
    }
  }

  /**
   * A previous run died between launching a successor and flipping the pointer. If that successor
   * is still serving, finish the job: promote it and retire the old one. If it is gone, forget it.
   */
  async function repair(p: Pointer, secret: string): Promise<RotateResult | null> {
    const stale = p.pending;
    if (!stale) return null;
    const vm = await microvms.get(stale.microvmId);
    if (!vm || !SERVING_STATES.has(vm.state)) {
      log.warn("rotate: forgetting a pending control plane that is gone", {
        microvmId: stale.microvmId,
        state: vm?.state ?? "not-found",
      });
      await pointer.write({ ...p, pending: null });
      return null;
    }
    if (stale.generation <= p.generation) {
      // The pointer already moved past it: this one lost the race.
      await terminateQuietly(stale.microvmId, "superseded");
      await pointer.write({ ...p, pending: null });
      return null;
    }
    // WP6.7: a pending successor never adopted the current ledger — it booted from a snapshot of
    // its own and has been asleep with it since. While the current control plane still serves,
    // promoting the stale one would hand the dashboards an old, sleeping machine (seen as an
    // "asleep" banner at the start of a rotation, and a successor whose process was half an hour
    // old). Terminate it and rotate afresh from the live ledger instead; only a machine with
    // nothing else serving is worth finishing with.
    const running = p.microvmId ? await microvms.get(p.microvmId) : null;
    if (running && SERVING_STATES.has(running.state)) {
      log.warn("rotate: a pending successor with a stale ledger; terminating it, rotating afresh", {
        microvmId: stale.microvmId,
        generation: stale.generation,
      });
      await terminateQuietly(stale.microvmId, "stale pending successor");
      await pointer.write({ ...p, pending: null });
… (56 more lines)
```

**Tool `Bash`:**

```
echo "=====TABFRAME_BUILD / build.json=====" && grep -rn "TABFRAME_BUILD\|build\.json" packages/control-plane/src packages/infra/lib packages/fleet/src packages/infra/scripts/health.ts 2>/dev/null | grep -v "\.test\." | head -20 && echo "=====health.ts prints=====" && grep -n "build\|imageVersion" packages/infra/scripts/health.ts | head -10
```

**Result of `Bash`:**

```
=====TABFRAME_BUILD / build.json=====
packages/control-plane/src/main.ts:27:  if (!process.env.TABFRAME_BUILD) {
packages/control-plane/src/main.ts:28:    const stamp = JSON.parse(readFileSync(new URL("./build.json", import.meta.url), "utf8")) as {
packages/control-plane/src/main.ts:31:    if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha;
packages/control-plane/src/server.ts:740:        build: process.env.TABFRAME_BUILD ?? null,
=====health.ts prints=====
```

**Tool `Bash`:**

```
echo "=====spawn default=====" && sed -n 120,130p packages/web/src/host.ts && grep -n "spawnDefault" packages/web/src/host.ts | head -5 && echo "=====guide markdown source=====" && grep -rn "GUIDE_MARKDOWN" packages/web/src/*.ts packages/web/scripts/*.ts 2>/dev/null | grep -v "editor-core.ts:3[0-9][0-9]" | head -6 && ls packages/web/src/generated 2>/dev/null; grep -rln "guideMarkdown\|GUIDE_MARKDOWN" packages/web/src/*.test.ts e2e/*.ts 2>/dev/null | head
```

**Result of `Bash`:**

````
=====spawn default=====
}
const hostId = crypto.randomUUID().slice(0, 8);
const cores = navigator.hardwareConcurrency || 1;
const locals = new Map<number, LocalNode>();
let localCounter = 0;
let sessionUrl = "";
let latest: ClusterState = emptyState();
let machine: MachineState = "connecting";
let demoTime = Date.now();
const clockNow = (): number => (demoMode ? demoTime : Date.now());

183:const spawnDefault = Math.max(1, cores - 1);
184:els.spawnCount.textContent = String(spawnDefault);
186:els.spawnHint.dataset.default = String(spawnDefault);
187:els.spawnHint.textContent = `This browser reports ${cores} CPU ${cores === 1 ? "thread" : "threads"}; spawn ${spawnDefault} keeps one for the page. Nodes in this tab share those cores, so spawning more than ${spawnDefault} only slices them thinner — another tab on another device adds real ones.`;
188:els.spawnN.title = `Spawn ${spawnDefault} nodes: one per CPU thread this browser reports, minus one for the page`;
=====guide markdown source=====
packages/web/src/editor-core.ts:16:  GUIDE_MARKDOWN,
packages/web/scripts/gen-sources.ts:58:    `export const GUIDE_MARKDOWN = ${JSON.stringify(readme)};`,
packages/web/src/program-sources.generated.ts:29:export const GUIDE_MARKDOWN = "# @tabframe/sdk-as\n\nThe AssemblyScript SDK for Tabframe programs, and the tooling around programs: the build script,\nthe goldens script, and a minimal Node host.\n\nA program is one WebAssembly module with two entry points and a manifest (design §5). It sees the\nworld only through a per-execution filesystem, has no clock, no randomness, and no network, and\nits API has no failure type: tasks complete, or the execution fails with a program fault.\n\n## Writing a program\n\n```ts\nimport { readRunInput, readPlanInput, stage, done, emit, ByteWriter, ByteReader, Params, fs, log }\n  from \"@tabframe/sdk-as/assembly/index\";\nexport { alloc } from \"@tabframe/sdk-as/assembly/index\";\n\nexport function plan(ptr: usize, len: i32): usize {\n  const input = readPlanInput(ptr, len);          // stage, params, hints\n  if (input.stage == 0) {\n    const s = stage(\"map\").canvas(2048, 1280);\n    for (let i = 0; i < 640; i++) s.taskAt(inputBytes(i), x(i), y(i), 64, 64);   // or s.task(bytes)\n    return emit(s.toBytes());\n  }\n  const next = new Params();                      // optional follow-up params\n  next.setI32(\"preset\", input.params.getI32(\"preset\", 0) + 1);\n  return emit(done(next));                        // or done(null)\n}\n\nexport function run(ptr: usize, len: i32): usize {\n  const t = readRunInput(ptr, len);               // stage, taskIndex, taskCount, input bytes\n  const r = new ByteReader(t.input);\n  // ... compute ...\n  return emit(outputBytes);                       // a Uint8Array\n}\n```\n\n- **Params** are a flat table of key → JSON text. `getString`, `getI32`, `getF64`, `getBool` take a\n  fallback; `raw(key)` gives the JSON text (arrays and objects come through untouched). Setters\n  write JSON. Keys are sorted when written, so equal tables are equal bytes.\n- **Stage** builds what `plan` returns: a name, an optional canvas (for the `tiles` view), and tasks\n  with inline input (≤ 16 KB each, ≤ 4096 tasks, ≤ 1 MB spec) with or without a placement rect.\n- **fs** is the execution's filesystem: `fs.stat(path)` (size or −1), `fs.read(path)` and\n  `fs.readRange(path, offset, len)` (null when absent), `fs.write(path, bytes)` (replaces the whole\n  file; visible to the next stage once the result is accepted), `fs.list(prefix)`. Results land at\n  `/out/<stage>/<task>` automatically; bundle inputs are under `/in/`. Reads cost a round trip\n  each — read whole files or large ranges.\n- **log(text)** appends to the task's log, shown in the dashboard's task detail and capped.\n- **ByteWriter / ByteReader** are for your own compact task inputs (u8/u32/i32/f32/f64, blobs,\n  strings), little-endian.\n\n- **bars()** builds the payload a `bars` program's final task returns:\n  `bars().bar(\"the\", 14529).bar(\"of\", 6620).toBytes()`. Values must be finite (NaN payload bits\n  differ between engines); the builder aborts otherwise. The dashboard decodes it with\n  `decodeBars` from `@tabframe/protocol`.\n\nMulti-stage programs read the previous stage's results as files: `fs.list(\"/out/0/\")` names every\ntask output of stage 0, `fs.readRange` reads a slice of one. See `programs/wordcount` for a\nthree-stage map/reduce/merge that does exactly this.\n\n**Allowed imports:** `tf.stat`, `tf.read`, `tf.write`, `tf.list`, `tf.log`, and `env.abort`.\nAnything else — `Date.now`, `Math.random` (which needs `env.seed`), `console`, WASI — is rejected\nat upload. AssemblyScript's `Math` compiles to WebAssembly and is deterministic across browsers and\nmachines; never write NaN into an output, because NaN payload bits are not.\n\n## Compiling\n\nPrograms depend on `@tabframe/sdk-as` (workspace) and import it by the subpath above; asc resolves\nit through the program's `node_modules`:\n\n```\nasc programs/<name>/assembly/index.ts --outFile programs/<name>/dist/program.wasm \\\n    --path programs/<name>/node_modules --baseDir . -O3 --runtime stub --noAssert --maximumMemory 256\n```\n\n`mise run build:programs` does this for every `programs/*/assembly/index.ts`. The flags matter:\n\n- `--runtime stub` (or `minimal`): a bump allocator with no collector. Every task runs in a fresh\n  instance, so nothing leaks between tasks and nothing needs freeing.\n- `--maximumMemory <pages>`: the sandbox requires a declared memory maximum (64 KiB pages; 256 is\n  16 MiB, the reference value) and rejects modules without one or above its cap.\n- `-O3 --noAssert`: speed; assertions are development aids, not runtime checks.\n\n\n## Byte formats\n\nThe SDK mirrors `packages/protocol/src/abi.ts` byte for byte; that file is the contract. Magics\n`TFRN` (run input), `TFPL` (plan input), `TFSS` (stage spec), `TFBR` (bars payload); little-endian;\n`str` = u32 length + UTF-8; `table` = u32 count + (str key, str JSON value) with keys sorted. Entry\npoints return a pointer to an 8-byte `{outPtr: u32, outLen: u32}` pair, which `emit` produces.\n\n## Tooling\n\n- `scripts/build-programs.ts` — `mise run build:programs`.\n- `scripts/goldens.ts` — `mise run goldens` writes `programs/<name>/goldens.json` from a single-node\n  run (params from the manifest): per-tile hashes for `tiles` programs, every stage's hashes plus\n  the decoded final payload for staged ones (`--program wordcount`); `--all --sample 16` times every\n  Mandelbrot preset; `--preset N` times one.\n- `scripts/host.ts` — instantiate a module under Node with in-memory files, and `runStaged` to run\n  a whole execution stage by stage the way the control plane would; used by the goldens script,\n  the tests, and (from WP1.9) the churn simulation.\n- `scripts/corpus.ts` — `mise run corpus` fetches and normalizes the word-count corpus into\n  `programs/wordcount/in/corpus.txt` (committed; the script is for reproducibility).\n";
packages/web/src/editor-core.test.ts
````

**Tool `Bash`:**

```
echo "=====git=====" && git branch --show-current && git log --oneline -6 && git status --short | head -5 && echo "=====idle policy=====" && grep -rn "idle\|900\|15 \* 60\|maxDuration\|8 \* 60\|4 \* 60" packages/fleet/src/microvm-client.ts packages/fleet/src/ops.ts packages/fleet/src/rotate.ts packages/control-plane/src/cores.ts 2>/dev/null | grep -iv "test" | head -12 && echo "=====rationale programs=====" && grep -n -i "gpt\|transformer\|third program\|three programs" docs/rationale.md | head
```

**Result of `Bash`:**

```
=====git=====
main
f48320d Merge WP8.2: review loop 2 — 57 findings from five fresh personas, 51 rows landed
8487ab6 WP8.2: review loop 2 — 57 findings from five fresh personas, 51 rows landed
b12f8e6 Merge WP8.1: review loop 1 — 40 findings from five personas landed
20dd4c8 WP8.1: review loop 1 — 40 findings from five personas landed
8bfc911 Merge M7 close: plan, time log, rationale total, transcripts; the end-to-end test waits for the seeded program by name
183e8cb CI: the end-to-end test waits for programAdded by name (the program field is the bundle hash)
=====idle policy=====
packages/control-plane/src/cores.ts:11:export const CORE_MAX_DURATION_SECONDS = 4 * 60 * 60;
packages/control-plane/src/cores.ts:64:        idlePolicy: null,
packages/fleet/src/rotate.ts:133:        idlePolicy: CONTROL_PLANE_IDLE_POLICY,
packages/fleet/src/rotate.ts:310:    // instead of booting a fresh generation every hour of an idle night; the first visitor wakes
packages/fleet/src/microvm-client.ts:87:        idlePolicy: params.idlePolicy ?? undefined,
=====rationale programs=====
```

**Tool `Bash`:**

```
sleep 60; cat /tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tasks/bdjy5290m.output 2>/dev/null | tail -8
```

**Result of `Bash`:**

```
---------------------------------------------|---------|---------|-------------------

 543 pass
 0 fail
 55082 expect() calls
Ran 543 tests across 67 files. [143.30s]

[exited with code 0]
```

> **Thinking.** 

**Tool `Bash`:**

```
echo "=====main.ts build stamp=====" && sed -n 22,36p packages/control-plane/src/main.ts && echo "=====health payload=====" && sed -n 730,746p packages/control-plane/src/server.ts && echo "=====idle policy const=====" && grep -n "CONTROL_PLANE_IDLE_POLICY\s*=" -A4 packages/fleet/src/rotate.ts | head -8 && echo "=====RECENT_ROTATION_MS=====" && grep -rn "RECENT_ROTATION_MS\s*=" packages/fleet/src/*.ts && echo "=====coverage threshold / fast-check=====" && grep -n "coverage" bunfig.toml && grep -rln "fast-check" packages/*/package.json package.json packages/core/src/*.test.ts 2>/dev/null | head -3
```

**Result of `Bash`:**

```
=====main.ts build stamp=====
  await cp.close();
  process.exit(0);
}
// The build stamp the image was staged with (WP8.1), for /health.
try {
  if (!process.env.TABFRAME_BUILD) {
    const stamp = JSON.parse(readFileSync(new URL("./build.json", import.meta.url), "utf8")) as {
      sha?: string;
    };
    if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha;
  }
} catch {
  // no stamp beside the entry: a local run
}
process.on("SIGTERM", () => void shutdown("SIGTERM"));
=====health payload=====
      // Everything an operator needs at a glance and nothing a browser could not learn from the
      // dashboard: counts, phases, the fleet, and whether the snapshotter is keeping up.
      const nodes = [...(ledger?.nodes.values() ?? [])];
      return sendJson(res, 200, {
        ok: true,
        role,
        phase: role === "control-plane" ? (ledger?.meta.phase ?? "active") : "neutral",
        mode: config.mode,
        generation,
        protocol: PROTOCOL_VERSION,
        build: process.env.TABFRAME_BUILD ?? null,
        imageVersion: config.imageVersion ?? null,
        awake: ledger?.meta.awake ?? null,
        sleepReason: ledger?.meta.sleepReason ?? null,
        nodes: nodes.length,
        nodesByKind: {
          tab: nodes.filter((n) => n.kind === "tab").length,
=====idle policy const=====
=====RECENT_ROTATION_MS=====
packages/fleet/src/rotate.ts:80:export const RECENT_ROTATION_MS = 5 * 60 * 1000;
=====coverage threshold / fast-check=====
3:coverage = true
4:coverageSkipTestFiles = true
… (7 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
echo "=====web: 503 / full machine handling=====" && grep -rn "503\|Retry-After\|full\b\|is full\|too many\|refused the connection" packages/web/src/observer.ts packages/web/src/banners.ts packages/web/src/host.ts packages/node/src/orchestrator.ts 2>/dev/null | grep -v test | head -12; echo "=====walkthrough mentions=====" && grep -n "503\|full\|fifteenth\|sixteen" docs/walkthrough.md docs/runbook.md | head
```

**Result of `Bash`:**

```
=====web: 503 / full machine handling=====
packages/web/src/banners.ts:17:/** The full-page banner while there is no live observer socket. */
packages/web/src/observer.ts:308:      // refresh so a room full of observers does not hit the session function at once.
packages/web/src/host.ts:90:/** One big panel, full-width, in its own tab (WP6.3): ledger, files, or activity. */
packages/web/src/host.ts:98:  document.getElementById(PANELS[panelMode])?.classList.add("panel-full");
packages/web/src/host.ts:740:  const full = throughputSeries(state, now);
packages/web/src/host.ts:741:  const series = full.slice(0, -1); // the current second is still filling; it would always dip
=====walkthrough mentions=====
```

> **Thinking.** 

## Assistant · 2026-09-04T18:41:32.169Z

## Findings

1. `[H]` docs/runbook.md, docs/design.md, mise.toml — runbook intro (lines 3–5), design §11.2, mise.toml `verify:m1` / `verify:m2` / `verify:m3` / `logs:fleet` — the runbook says "Every command below runs from the repo root with `mise`, which sets the AWS profile (`tabframe`, the dedicated account) and the region; nothing here works with any other profile, by design", and §11.2 says the profile now rides "only on the tasks that talk to AWS (WP8.2)"; but the four tasks above carry `depends = ["whoami"]` and no `env = { AWS_PROFILE = "tabframe", _.file = ".env.local" }`, while `verify-m1.ts` uses `S3Client`/`CloudFormationClient`, `verify-m3.ts` invokes the rotate Lambda and reads CloudWatch, and `logs:fleet` runs `aws logs tail` — so after `whoami` passes under the Tabframe profile, the task itself runs under the shell's default credentials (or fails with none), which is the one fallback the project forbids (loop 2 row 38's "Per-task `env` on the ten AWS tasks" is incomplete) — fix: add the `env` line to those four tasks; until then the runbook sentence is false.

2. `[M]` README.md — "Limits, stated plainly", lines 126–127 — "a consensus control plane is the / an extension, not a feature" is still there although loop 2 row 42 ("'is the an extension' in the README's limits paragraph") is marked **Accepted** — *loop-2 regression*; a reviewer told it was fixed and finding it unfixed doubts every other "Accepted" — fix: "a consensus control plane is an extension, not a feature".

3. `[M]` README.md line 120, docs/design.md §9.7 line 499, docs/rationale.md line 103, docs/m3-verification.md line 63, docs/plan.md risk register line 192 — the loop-2 settled phrase "about seven browser tabs that each lend a node (fifteen that only watch)" contradicts loop 2's own connection cap: `server.ts` has `const CLIENT_CONNECTION_CAP = 14` and §9.7 says "the control plane refuses the fifteenth client upgrade with 503", so a fifteenth watching tab is refused — seven lending tabs (14 sockets) is right, fifteen watchers is not; the plan's risk register still says "one control plane serves ~15 tabs" — fix: "fourteen that only watch" in all four places and the risk register.

4. `[M]` docs/runbook.md "Deploy, step by step" step 2 and the `did not stabilize` incident row; README.md line 165 — "a plain re-run of `cdk deploy` then completes" / "re-run `cdk deploy`" / "cdk bootstrap # once": since WP8.2 moved `AWS_PROFILE` and `.env.local` off the global `[env]`, a bare `cdk` command runs with whatever credentials the shell has, and `mise run deploy:stacks` ("for a re-run after a stack that did not stabilise") exists for exactly this re-run; loop 2 row 33 says "the runbook points at it" but the runbook never mentions `deploy:stacks` — *loop-2 regression* — fix: "re-run `mise run deploy:stacks`" in both places, and `AWS_PROFILE=tabframe cdk bootstrap` (or a `bootstrap` task) in the README.

5. `[M]` docs/design.md §11.3 — the Dockerfile block and the layout — the block reads "FROM public.ecr.aws/lambda/microvms:al2023-minimal … COPY packages/control-plane/dist/main.js /app/main.js … COPY programs/\*/dist/ /app/programs/ … EXPOSE 8080", while the real `packages/infra/image/Dockerfile` pins `@sha256:…`, copies `package.json`, `main.js`, `node-worker.js`, `programs/` and `build.json`, and exposes `8080 8081`; the layout lists the Dockerfile under `control-plane/` and `programs/` without `tinygpt` — loop 2 (row 52) replaced the drifted `mise.toml` copy with a pointer "because the copy drifted" but left this copy, and the runbook's `node-worker.js` incident row cannot be reconciled with it — fix: replace the block with "The image is `packages/infra/image/Dockerfile`, staged by `stage-image.ts` (WP8.2): a digest-pinned base, `main.js`, `node-worker.js`, the programs, `build.json`", and add `tinygpt/` to the layout.

6. `[M]` docs/plan.md "Where we are" line 6 and WP8.1 line 240; docs/timelog.md Build row — "loop 2 (WP8.2) reviews the tree after it and is this branch; loop 3 (WP8.3) follows" is stale on `main` (git: "Merge WP8.2: review loop 2"; the plan's own WP8.2 box is `[x]`); WP8.1 says "11 deferred to loops 2–3 with reasons" while the note and the index say "6 deferred" — loop 2 row 45 claimed the counts were "carried into the plan and the index", so *loop-2 regression*; the timelog's build row still reads "M0–M7: every work package" while M8 is in progress — fix: "Loops 1 and 2 (WP8.1, WP8.2) are merged; loop 3 (WP8.3) is this branch", "6 deferred whole and 7 in part", "M0–M8".

7. `[M]` docs/walkthrough.md Screen A; docs/runbook.md incidents — no state for a refused connection: `server.ts` answers the fifteenth client "HTTP/1.1 503 Service Unavailable / Retry-After: 10" (comment: "two stay free for the fleet's own calls"), `packages/web` has no handling for it, so an eighth lending tab sits on "Connecting…" with no reason — against R1 ("One sentence of state, always") and R8 ("Nothing happens silently") — and the runbook's row still says "A page cannot connect; sockets answer 429 | the endpoint holds **16 concurrent connections per MicroVM**" — fix: a "Machine full" row (what the banner should say, "close a tab or open `?observe`", retry in 10 s) and "sockets answer 429, or 503 from the control plane itself since WP8.2's 14-client cap" in the runbook.

8. `[M]` docs/runbook.md "Observability", `/health` bullet — "since WP8.1 the `build` stamp (commit, branch, and whether the deploy was ungated)": `main.ts` reads only `stamp.sha` ("if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha") and `server.ts` serves `build: process.env.TABFRAME_BUILD ?? null`, so `/health.build` is the short commit (with `-dirty`); `branch` and `ungated` stay in `/app/build.json` and are never served — the first `mise run health` will show the runbook overstating — fix: either expose the whole stamp or write "the `build` stamp (the short commit, `-dirty` if the tree was) and the `imageVersion`; branch and `ungated` are in the image's `build.json`".

9. `[M]` docs/implementation/wp-8.2-review-loop-2.md "How the loop ran"; docs/implementation/wp-8.1-review-loop-1.md — "Three of loop one's claims did not hold and are marked *loop-1 regression*" and "All five are fixed here", but seven rows carry the marker (6, 13, 15, 23, 24, 31, 34), so nine items; the loop-1 note says "The 52 findings were de-duplicated into the table below (52 rows)" although rows 8 ("tw, devops") and 25 ("sec, devops") each merge two reviewers, and "46 accepted in full or in part, 6 deferred to loops 2–3, and one (11) rejected" reads as 53 (repeated in the index) — these are the documents loop 3 is told to read first — fix: "Seven of loop one's claims did not hold … nine items are fixed here"; "54 findings … 52 rows; 45 accepted, 6 deferred, one (11) rejected as a change but kept as a bound".

10. `[L]` docs/runbook.md "Rotation, and when it goes wrong" — "A run that dies half-way leaves a `pending` record in the pointer; the next run finishes it (the successor is promoted) or forgets it (the successor is gone)" omits the path the runbook's own incident row describes and the code takes: `rotate.ts` terminates a pending successor "with a stale ledger … rotating afresh" whenever a control plane still serves (WP6.7), promotes only when nothing serves, and first finishes "the retire a previous run left behind" via the `retiring` record (WP8.2) — fix: "the next run finishes it only if nothing else serves, terminates it and rotates afresh if the old control plane still does, or forgets it if it is gone; a `retiring` record from a run that died after the flip is finished first".

11. `[L]` docs/design.md §2 and §4; README.md line 105 — vocabulary settled in loop 2 (§16: "**Host** — a browser tab … **Core** — any worker … a tab worker or a MicroVM … **Node** — the ledger's word for a core that has joined") is contradicted by §2 "**Cores** are browser tabs and MicroVMs" and §4 "Browser nodes run the orchestrator in a Web Worker … Cloud cores run the same orchestrator under Node", and the README's "6 tab nodes, 3 killed" — the two sentences a first-time reader meets before the glossary teach the old meaning — fix: "Cores are workers: threads in browser tabs, and MicroVMs", "Browser cores run the orchestrator in a Web Worker … cloud cores …", "6 nodes from tabs".

12. `[L]` docs/rationale.md "Why this theme" — "word count is there because a machine that can only draw fractals is a screensaver. Both ship through the same path as anything a reviewer writes" names two programs while the README says "Three programs ship with the machine" and the design's WP6.5 entry adds `tinygpt`; the assignment's rationale never mentions the transformer at all — fix: "…and tiny GPT, an 822 k-parameter transformer whose forward pass runs on the cores, is there to show the machine is general. All three ship through the same path…".

## Summary

Fix finding 1 first: it is the only item that touches the project's one hard rule (no fallback to another AWS identity), it is a one-line `env` addition on four tasks, and until it lands the runbook's opening sentence and design §11.2 are untrue. Then the two loop-2 regressions a reviewer can verify in seconds (the README typo, 2; the plan's stale "where we are" and "11 deferred", 6) and the two places where loop 2 contradicted itself (the "fifteen that only watch" phrase against the 14-client cap, 3; the `deploy:stacks` pointer that never reached the runbook, 4) — those are what make a careful reader distrust the "Accepted" column. The design's stale Dockerfile (5), the missing "machine full" state (7), and the `/health` overstatement (8) are the remaining truth gaps; 9–12 are consistency polish. I verified as real and complete loop 2's rows 41 (§17 rebuilt, the Freeze sentence back in WP1.9, merge-order note), 43 (R6 and §16 aligned), 46 (loop-1 rows 44–52 and its deferred list), 47 (allowed imports and the NaN caveat above the "## Compiling" cut, `program-sources.generated.ts` regenerated), 48 (the runbook's deploy row names the guard, the escape hatch and the diff gate; `/health` lists `build` and `imageVersion`), 49 (the reloaded-once walkthrough row, matching `host.ts`'s 1.5 s reload and hint text), 50 (snapshot lifecycle row, alarm-test chore closed, topic in the table), 51 ("spawn N" with what N is — `Math.max(1, cores - 1)` — and one three-program paragraph), 52's "M0 to M8", §11.2 pointer and the "§6.7 below" removal, plus row 10's §9.7 sentence, row 35's rollback paragraph and row 37's subscription note; and I confirmed the README's counts (543 unit tests pass across 67 files; 31 Playwright `test(` calls) and every named constant (4 KB params, queue 32, 64 programs, 10 s store retry, 6 store errors, 6 releases, 64 MiB/min presign budget, 24 presign items, 2 s cooldown, 90 s lease, 15 s off poll, 10 s missing retry, activity cap 400, 5 s drain grace, 5-minute skip, `tabframe-alarms`, `tabframe-monthly`, `tabframe-canary`, one-year blob and one-day `g` lifecycles, 14/7-day log retention).
