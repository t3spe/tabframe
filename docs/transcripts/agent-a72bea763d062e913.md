# Session agent-a72bea763d062e913

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 120 · user messages: 1 · assistant messages: 6 · tool calls: 48 · tool results: 48
- from 2026-09-04T15:33:35.489Z to 2026-09-04T15:47:50.985Z
- scrubbed: microvm-endpoint ×5, lambda-url ×1, account-id ×2, masked-account-tail ×1, email ×7

---
## User · 2026-09-04T15:33:35.489Z

You are an expert security engineer reviewing the Tabframe repository at /home/mircea/homework/tabframe (a public web dashboard where any visitor's browser tab runs WebAssembly programs other people uploaded, sandboxed; a control plane on AWS Lambda MicroVMs; an S3/CloudFront content-addressed store with presigned uploads; TypeScript on Bun; AWS CDK). This is the SECOND review loop. Read docs/implementation/wp-8.1-review-loop-1.md first: it lists the first loop's findings and what was done — including the security items (size-capped blob fetches, presign size/count bounds and the per-connection byte budget, one memory per module, the solicited rate bucket, the CloudFront response headers and CSP, RunMicrovm scoped to the image, pinned tools, fleet routes closed in image mode, the control cooldown). Verify those fixes are real and complete (a wrong CSP that breaks the page is a finding too), do not repeat what is fixed, and go deeper: what is still reachable from the public surface, what the deferred items (per-IP connection quota, snapshot memoisation, the core-identity token, the fleet secret in the core payload, base image pinning) look like concretely, and anything the first loop missed. Read docs/design.md (sandbox, store, limits, fleet, hosting) and README.md, then packages/sandbox, packages/control-plane/src, packages/core/src/apply.ts, packages/store, packages/web, packages/infra/lib, packages/fleet/src, mise.toml, .github/workflows.

Rules: do NOT modify any file, do NOT run git commands that write, do NOT run any AWS, aws, cdk, deploy, up, down, or rotate commands, and never use the `tabframe` AWS profile. Reading, grep, and running `bun test <path>` locally are fine. Do not print secrets or account identifiers; describe where they are.

Output (at most 1500 words): a section "## Findings" with at most 12 entries, most severe first, each on the form:
`[H|M|L] <area> — <file:line> — <problem> — <why it matters> — <concrete fix>`
Mark an entry "(loop 1 regression)" if the first loop's fixes introduced or left it. Only real risks with a plausible attack or failure path. Then "## Summary": three sentences on the security posture now and the top three things to fix.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T15:33:39.941Z

I'll start with the loop-1 record, then the design doc and README, before going into the code.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.1-review-loop-1.md
```

**Result of `Read`:**

```
1	# WP8.1 — Review loop 1
2	
3	**Branch** `wp/8.1-review-loop-1` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop one.
7	
8	## How the loop ran
9	
10	Five independent reviewers (fresh agents, read-only, no AWS access) each returned at most twelve
11	findings with severity, location, cause, and fix. The 52 findings were de-duplicated into the
12	table below; each was accepted, deferred to a later loop, or rejected with the reason (the
13	critique). The accepted ones landed on this branch with tests; CI gates the merge; the next loop
14	reviews the tree with these changes in.
15	
16	## The findings and what was done
17	
18	| # | From | Sev | Finding | Decision |
19	|---|---|---|---|---|
20	| 1 | dist | H | Stage-advancing store effects (inherited root, stage spec, folded manifest) were fire-and-forget: a handover mid-flight or one S3 error wedged the execution for ever. | **Accepted.** `resumePending()` re-derives the pending effect from the ledger on adopt and again every `STORE_RETRY_MS` (`waitingSince` on the execution); a pruned origin now falls back to the bundle with the `expired-root` warning instead of a silent start. |
21	| 2 | dist | H | `encode()` over 64 KB threw through `dispatch`; params and the queue were unbounded. | **Accepted.** `PARAMS_MAX_BYTES` (4 KB canonical) and `QUEUE_CAP` (32) in `enqueue`; a send that cannot be encoded closes that connection instead of throwing. |
22	| 3 | dist | H | A node reported store or network failures as program faults, failing everybody's execution. | **Accepted.** `hostFailure()` covers fetch, upload, presign timeouts and network errors → `RELEASED`; the store client retries three times with backoff. |
23	| 4 | sec | H | The control plane fetched blobs named by untrusted parties without a size check. | **Accepted.** `StoreDriver.get(hash, maxBytes)` (HeadObject first on S3, `BlobTooLarge` otherwise); caps at every fetch: stage spec 1 MB, inherited root 4 MB, bundle manifest 1 MB, program manifest 64 KB, module 8 MB. |
24	| 5 | sec | H | Presign sizes were unbounded and uncounted. | **Accepted.** `size ≤ maxOutputBytes` and at most 64 items per presign in the schemas; a 1 GiB presigned-byte budget per connection in the core. |
25	| 6 | sec | H | A module could declare a second memory without a maximum and grow past the cap. | **Accepted.** `countMemories()`; more than one memory is refused. |
26	| 7 | fs | H | A node's presign never timed out and an S3 HeadObject error failed the whole presign: a zombie core. | **Accepted.** `SocketPresigner` times out after 30 s (the task is released); `S3Store.presign` treats a HeadObject error as "not there" and signs. |
27	| 8 | tw, devops | H | The hourly rule rotated a *suspended* control plane, so an idle night booted a generation an hour and the README's stays-up paragraph was false. | **Accepted.** A scheduled invocation skips a `SUSPENDED` control plane (`skipped-suspended`); the README says what happens now. |
28	| 9 | devops | H | `deploy` ran build and test in parallel from the working tree with no clean-tree or green-main guard and no commit stamp. | **Accepted.** `deploy` is sequential: build → test → `deploy-guard.ts` (clean tree, HEAD on origin/main; `TABFRAME_DEPLOY_UNGATED=1` to skip) → stacks → up. `stage-image.ts` writes `build.json`; `/health` shows `build` and `imageVersion`. |
29	| 10 | devops | H | The rotate function returned failures as successes; nothing alarmed. | **Accepted.** The Lambda entry throws on `failed`; CloudWatch alarms on rotate/session errors and rotate throttles go to an SNS topic mailed to the budget address when configured; the hourly rule's target retries twice within thirty minutes. |
30	| 11 | sec | M | Destructive controls are unauthenticated; kill-half in a loop terminates and relaunches cloud cores. | **Rejected as a change, accepted as a bound.** A public machine anyone may drive is the design (D4, §6.7); a machine-wide cooldown of three seconds per destructive control (`CONTROL_COOLDOWN_MS`) removes the cost loop. |
31	| 12 | fs | M | The editor tab re-sent `pause` on every live socket, including after its own launch. | **Accepted.** `holdWanted` is cleared by launch and close. |
32	| 13 | fs | M | A failing session fetch during a silent resubscribe kept a green pill; held clicks were dropped silently. | **Accepted.** The page goes back to "connecting"; dropped clicks are said in the status line (`onDropped`). |
33	| 14 | fs | M | An outdated page reloaded itself in a loop. | **Accepted.** One reload per protocol version (`sessionStorage`), then a hard-refresh hint. |
34	| 15 | fs | M | The ledger tab serialised every row per frame; the blob cache and the demo store grew without bound. | **Accepted** (ledger signature; a 64 MB LRU `BlobCache`). The demo store's growth is deferred to loop 2. |
35	| 16 | fs | M | A dead compiler worker left the editor unusable. | **Accepted.** Pending compiles are rejected, a two-minute compile timeout, the next compile gets a fresh worker; failed drops say so. |
36	| 17 | dist | M | Between handover and promote a dead rotation left the machine dark for an hour; a predecessor kept acting after handing over. | **Accepted.** `HANDOVER_LEASE_MS` (90 s) returns a control plane to `active` when no drain follows; ticks run no fleet, loop, or assignment while `handing-over` or `drained`. |
37	| 18 | dist | M | Slow `RunMicrovm` calls were asked twice; surplus cores were never trimmed. | **Accepted.** `meta.coreLaunches` counts launches until acknowledged (or a minute passes); surplus unlinked cores are terminated. |
38	| 19 | dist | M | Any node's report could settle or fail any open task; `computeMs` was unbounded; results per task grew without bound. | **Accepted in part.** A report for an attempt the node never held may still verify or contest a settled task (D7, the tests lean on it) but can no longer *fail* a task; `computeMs` is bounded; sixteen records per task. |
39	| 20 | sec | M | Blobs are served on the page's origin with no `nosniff`, no CSP. | **Accepted.** CloudFront response-header policies: nosniff, DENY framing, HSTS, a CSP for the page; `Content-Disposition: attachment` and a sandbox CSP for `blob/*`. |
40	| 21 | sec | M | Results and presigns were exempt from the rate bucket. | **Accepted.** A second bucket (`SOLICITED_RATE` 256/s) for them. |
… (35 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T15:33:46.940Z

Next I need the design record, the README, the repo layout, mise.toml, and the workflows — none depend on each other, so I'll fetch them together.

**Tool `Bash`:**

```
ls -la && echo "=== packages ===" && ls packages && echo "=== tree (depth 3, no node_modules) ===" && find . -maxdepth 4 -type f \( -name "*.ts" -o -name "*.toml" -o -name "*.yml" -o -name "*.yaml" -o -name "*.json" -o -name "*.html" -o -name "*.js" -o -name "*.md" -o -name "*.sh" -o -name "Dockerfile" -o -name "*.wat" \) -not -path "*/node_modules/*" -not -path "./.git/*" | sort
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (36.1KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b44jrfu3f.txt

Preview (first 2KB):
total 228
drwxrwxr-x 14 mircea mircea  4096 Sep  4 08:23 .
drwxrwxr-x  5 mircea mircea  4096 Sep  2 07:02 ..
-rw-rw-r--  1 mircea mircea   787 Sep  2 23:35 biome.json
-rw-rw-r--  1 mircea mircea   910 Sep  1 22:51 bunfig.toml
-rw-r--r--  1 mircea mircea 46043 Sep  3 00:13 bun.lock
-rw-rw-r--  1 mircea mircea   543 Sep  1 21:41 cdk.json
drwxrwxr-x 58 mircea mircea 12288 Sep  4 08:10 cdk.out
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:10 .claude
drwxrwxr-x  2 mircea mircea 20480 Sep  4 08:22 coverage
drwxrwxr-x  4 mircea mircea  4096 Sep  3 22:12 docs
drwxrwxr-x  2 mircea mircea  4096 Sep  4 08:08 e2e
-rw-------  1 mircea mircea   189 Sep  2 15:05 .env.local
drwxrwxr-x  8 mircea mircea  4096 Sep  4 08:27 .git
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:27 .github
-rw-rw-r--  1 mircea mircea   364 Sep  2 15:19 .gitignore
-rw-rw-r--  1 mircea mircea 34523 Sep  1 21:08 LICENSE
-rw-rw-r--  1 mircea mircea  5998 Sep  4 08:08 mise.toml
drwxr-xr-x  7 mircea mircea  4096 Sep  1 22:51 node_modules
-rw-rw-r--  1 mircea mircea   436 Sep  1 22:51 package.json
drwxrwxr-x 13 mircea mircea  4096 Sep  1 22:52 packages
-rw-rw-r--  1 mircea mircea  1946 Sep  4 07:54 playwright.config.ts
drwxrwxr-x  5 mircea mircea  4096 Sep  2 23:56 programs
-rw-rw-r--  1 mircea mircea 16114 Sep  4 08:19 README.md
drwxrwxr-x  3 mircea mircea  4096 Sep  2 13:37 scripts
drwxrwxr-x  4 mircea mircea  4096 Sep  4 08:26 test-results
-rw-rw-r--  1 mircea mircea   480 Sep  1 21:08 tsconfig.base.json
-rw-rw-r--  1 mircea mircea   294 Sep  3 20:28 tsconfig.json
=== packages ===
control-plane
core
dev
fleet
infra
node
protocol
… (13 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T15:33:55.274Z

That listing got swamped by `cdk.out`. I'll re-list the source tree without build artifacts and, in the same pass, read the README, mise.toml, and the workflows since none depend on each other.

**Tool `Bash`:**

```
find packages docs scripts programs e2e .github -type f -not -path "*/node_modules/*" -not -path "*/dist/*" -not -path "*/build/*" | sort | grep -v -E "\.(wasm|png|jpg|snap)$"
```

**Result of `Bash`:**

```
docs/design.md
docs/feasibility-transformer.md
docs/implementation/README.md
docs/implementation/wp-0.10-first-hello.md
docs/implementation/wp-0.12-ci.md
docs/implementation/wp-0.1-repo-scaffold.md
docs/implementation/wp-0.2-protocol.md
docs/implementation/wp-0.3-core.md
docs/implementation/wp-0.4-control-plane.md
docs/implementation/wp-0.5-node.md
docs/implementation/wp-0.6-web.md
docs/implementation/wp-0.7-dev.md
docs/implementation/wp-0.8-infra-skeleton.md
docs/implementation/wp-0.9-fleet-skeleton.md
docs/implementation/wp-1.10-deploy-m1.md
docs/implementation/wp-1.1-protocol-v1.md
docs/implementation/wp-1.2-core-v1.md
docs/implementation/wp-1.3-store.md
docs/implementation/wp-1.4-sandbox.md
docs/implementation/wp-1.5-sdk-mandelbrot.md
docs/implementation/wp-1.6-node-v1.md
docs/implementation/wp-1.7-control-plane-v1.md
docs/implementation/wp-1.8-dashboard-v1.md
docs/implementation/wp-1.9-churn-sim.md
docs/implementation/wp-2.1-filesystem.md
docs/implementation/wp-2.2-wordcount.md
docs/implementation/wp-2.3-launch-path.md
docs/implementation/wp-2.4-editor.md
docs/implementation/wp-2.5-dashboard-v2.md
docs/implementation/wp-2.6-playwright-v1.md
docs/implementation/wp-2.7-deploy-m2.md
docs/implementation/wp-3.1-cp-lifecycle.md
docs/implementation/wp-3.2-fleet-v1.md
docs/implementation/wp-3.3-cloud-cores.md
docs/implementation/wp-3.4-handover-tests.md
docs/implementation/wp-3.5-deploy-m3.md
docs/implementation/wp-4.1-dashboard-polish.md
docs/implementation/wp-4.2-operations.md
docs/implementation/wp-4.3-pacing.md
docs/implementation/wp-4.4-unattended-demo.md
… (333 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T15:34:04.910Z

Now the documents and config. README, mise.toml, and the CI workflow in one call; the design record (likely long) via Read — they're independent.

**Tool `Bash`:**

```
echo "===== README.md ====="; cat /home/mircea/homework/tabframe/README.md; echo; echo "===== mise.toml ====="; cat /home/mircea/homework/tabframe/mise.toml; echo; echo "===== .github/workflows/ci.yml ====="; cat /home/mircea/homework/tabframe/.github/workflows/ci.yml; echo; echo "===== .gitignore ====="; cat /home/mircea/homework/tabframe/.gitignore; echo; echo "===== bunfig.toml ====="; cat /home/mircea/homework/tabframe/bunfig.toml; echo; echo "===== cdk.json ====="; cat /home/mircea/homework/tabframe/cdk.json; echo; echo "===== package.json ====="; cat /home/mircea/homework/tabframe/package.json
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

```
===== README.md =====
# Tabframe

A fault-tolerant distributed computer whose cores are browser tabs and Firecracker microVMs,
programmed with WebAssembly. Every worker is a core — a thread in someone's tab, or one of the two
MicroVMs in AWS, the *cloud cores*. Open the page and your tab is a core. Close it and the machine keeps
computing, correctly. The control plane that schedules the work is itself replaced every hour, with
the work in flight.

Deployed: **https://d2w9z8juw4oo76.cloudfront.net** — the page lends one core when it opens, and
shows the machine rendering a Mandelbrot frame with whoever else is there.

**Try it, in five clicks.** Spawn 3 (the counters follow), kill half (tiles are taken back and finish
elsewhere), redundancy on (the verified counter moves), editor ↗ (change `CYCLE`, compile, launch:
your program goes ahead of the loop), the ledger tab (hashes, not bytes). `?observe` lends no cores,
`?demo=1` runs a scripted cluster inside the page, and a rotation banner every hour is expected.

## What it is

A program is one WebAssembly module with two entry points and a manifest. `plan` turns parameters
and the previous stage's outputs into the next stage's tasks; `run` turns one task's input into
bytes. Both execute on cores — the control plane runs no program code, not even the planner. A
program's only view of the world is a per-execution filesystem of content-addressed blobs: it has
no clock, no randomness, no network, and no failure type anywhere in its API.

Three programs ship with the machine and go through the same path as anything you write in the
in-page editor: a distributed **Mandelbrot** render (640 tiles of 64×64 per frame, presets that
advance while anyone watches) and a three-stage **word count** over *Moby-Dick* (map by byte range,
reduce by partition, merge to a top-25). You can also compile your own: the editor holds the
Mandelbrot source and the AssemblyScript compiler runs in a browser worker.

Since M6 a third program ships with the image: **tiny GPT**, an 822 k-parameter character-level
transformer trained on that same corpus, whose whole forward pass runs in WebAssembly on the cores —
one continuation per task, four milliseconds a token, the same bytes on every core
(`docs/feasibility-transformer.md`).

## Why it looks the way it does

Three constraints carry the whole fault-tolerance story; everything else follows from them.

… (553 more lines)
```

**Result of `Read`:**

```
1	# Tabframe — Design Record
2	
3	**Status:** written 2026-09-01 before the build; the sections that name a work package were amended in place (§6.7, §8.3 among them) and everything else is read together with the drift log, §17 (last entry 2026-09-04), whose entries are in merge order.
4	This document is the source of truth for the build. It supersedes the earlier handoff and seed
5	documents wherever they differ.
6	
7	**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
8	microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
9	and which stays correct while cores — and the control plane itself — come and go.
10	
11	---
12	
13	## Contents
14	
15	1. Decisions register
16	2. The system in one page
17	3. Runtime picture
18	4. Nodes
19	5. Programs
20	6. The control plane
21	7. Data paths and the store
22	8. The wire
23	9. Hosting and fleet
24	10. Security and credentials
25	11. Tooling and repo
26	12. Tests and dev loop
27	13. Deploy, operations, and the reviewer's view
28	14. Milestones
29	15. Rationale hooks
30	16. Glossary
31	17. Drift log
32	
33	---
34	
35	## 1. Decisions register
36	
37	| # | Decision | Notes |
38	|---|----------|-------|
39	| D1 | **Scope:** everything in the original handoff, plus straggler speculation, duplicate-result verification with a built-in redundancy toggle, a program ABI, and MapReduce word count. Time is not a design constraint. | The handoff's 8-hour line did not drive any choice here; §15 says how the rationale owns that. |
40	| D2 | **General-purpose programs, minimal but complete.** A program is one WASM module (`plan` + `run`) plus a manifest, content-addressed, sandboxed, uploadable, with in-browser AssemblyScript compilation and an execution queue. | Mandelbrot and word count are real programs shipped through the same path; the node bundle contains no application code. |
… (540 more lines)
```

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T15:34:22.386Z

I still need the rest of the design record (§11.3 onward, including the drift log), and I want file sizes to plan the code reads. Both are independent.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Read`:**

````
581	
582	AssemblyScript is a workspace devDependency, not a mise tool, so the build-time and in-browser compilers are the same pinned version.
583	
584	### 11.3 Layout and image
585	
586	```
587	tabframe/
588	  mise.toml  package.json  tsconfig.base.json  biome.json  .env.local (gitignored)
589	  packages/
590	    protocol/        zod schemas + types for every message, close codes, generation, limits; canonical codecs
591	    core/            pure control-plane logic and the churn simulation harness; Store/Transport/Clock interfaces
592	    control-plane/   the process: http + ws, lifecycle hooks, handover/adopt/drain, presign, S3 snapshotter,
593	                     core fleet manager, seeding, local store route; Dockerfile
594	    sandbox/         WASM glue: ABI, the five imports, FS manifest resolution, caps;
595	                     adapters: web worker (sync XHR) and node worker_thread (Atomics.wait)
596	    node/            the orchestrator; entry points platform/web.ts and platform/node.ts
597	    web/             host page, dashboard, controls, editor, programs panel, queue, banners
598	    sdk-as/          AssemblyScript SDK, program build and goldens scripts
599	    fleet/           the two Lambdas: session and rotate; shared MicroVM client and SSM pointer; rotate/down scripts
600	    infra/           CDK app, image publish, whoami and verify scripts
601	    dev/             the one-command local topology
602	  programs/
603	    mandelbrot/      assembly/index.ts, manifest.json, goldens.json
604	    wordcount/       assembly/index.ts, manifest.json, in/corpus.txt, goldens.json
605	  docs/              this record, architecture notes, rationale, transcripts
606	```
607	
608	```dockerfile
609	FROM public.ecr.aws/lambda/microvms:al2023-minimal
610	RUN dnf install -y nodejs22 && dnf clean all
611	COPY packages/control-plane/dist/main.js /app/main.js
612	COPY programs/*/dist/ /app/programs/
613	EXPOSE 8080
614	CMD ["node", "/app/main.js"]
615	```
616	
617	Lambda builds the image on ARM64 itself; Docker is optional locally. Bundling to one file keeps the snapshot small, which is what resume latency is made of.
618	
619	### 11.4 CDK
620	
… (348 more lines)
````

**Tool `Bash`:**

```
wc -l packages/protocol/src/*.ts packages/sandbox/src/*.ts packages/sandbox/src/adapters/*.ts packages/control-plane/src/*.ts packages/core/src/*.ts packages/store/src/*.ts packages/web/src/*.ts packages/web/public/*.html packages/web/scripts/*.ts packages/infra/lib/*.ts packages/infra/bin/*.ts packages/infra/image/* packages/infra/scripts/*.ts packages/fleet/src/*.ts packages/fleet/src/lambda/*.ts packages/fleet/scripts/*.ts packages/node/src/*.ts packages/node/src/platform/*.ts packages/dev/src/*.ts 2>/dev/null | grep -v -E "\.test\.ts" | sort -k2
```

**Result of `Bash`:**

```
     89 packages/control-plane/src/bundles.ts
     77 packages/control-plane/src/config.ts
     62 packages/control-plane/src/core-node.ts
     75 packages/control-plane/src/cores.ts
     35 packages/control-plane/src/fixtures.ts
    105 packages/control-plane/src/hooks.ts
      9 packages/control-plane/src/log.ts
     37 packages/control-plane/src/main.ts
    124 packages/control-plane/src/seed.ts
    819 packages/control-plane/src/server.ts
     80 packages/control-plane/src/snapshotter.ts
     98 packages/control-plane/src/static.ts
    782 packages/core/src/apply.ts
     36 packages/core/src/bytes.ts
     73 packages/core/src/events.ts
    849 packages/core/src/executions.ts
    131 packages/core/src/fleet.ts
     61 packages/core/src/handover.ts
    192 packages/core/src/harness.ts
     79 packages/core/src/index.ts
     42 packages/core/src/interfaces.ts
    111 packages/core/src/invariants.ts
    431 packages/core/src/ledger.ts
     23 packages/core/src/observers.ts
    341 packages/core/src/results.ts
    288 packages/core/src/scheduler.ts
    116 packages/core/src/snapshot.ts
    244 packages/dev/src/rotate.ts
    331 packages/dev/src/transcripts.ts
    127 packages/dev/src/up.ts
     38 packages/fleet/scripts/_deps.ts
      6 packages/fleet/scripts/down.ts
      8 packages/fleet/scripts/rotate.ts
      6 packages/fleet/scripts/up.ts
    112 packages/fleet/src/aws.ts
     97 packages/fleet/src/config.ts
    150 packages/fleet/src/cp-client.ts
     53 packages/fleet/src/lambda/rotate.ts
     17 packages/fleet/src/lambda/session.ts
    146 packages/fleet/src/microvm-client.ts
… (90 more lines)
```

> **Thinking.** 

> **Thinking.** I've finished the design record. Now I'll pull in the backend surface in one batch—infra/CSP, control-plane process, core apply logic, protocol schemas, sandbox, store, fleet, node, plus static HTML and build script—then follow up with the dashboard TypeScript files once I know what to focus on.

**Tool `Bash`:**

```
for f in packages/infra/lib/core-stack.ts packages/infra/lib/web-stack.ts packages/infra/lib/names.ts packages/infra/bin/tabframe.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/infra/lib/image-stack.ts packages/infra/lib/fleet-stack.ts packages/infra/lib/base-image.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/infra/lib/core-stack.ts =====
     1	// Core stack (design §11.4): buckets, CloudFront, the pointer parameter, the fleet secret, the budget.
     2	import * as cdk from "aws-cdk-lib";
     3	import type { Construct } from "constructs";
     4	import { NAMES } from "./names.ts";
     5	
     6	export interface CoreStackProps extends cdk.StackProps {
     7	  /** Notification address for the budget. When absent the budget is skipped with a synth warning. */
     8	  budgetEmail?: string;
     9	}
    10	
    11	export class CoreStack extends cdk.Stack {
    12	  readonly artifactsBucket: cdk.aws_s3.Bucket;
    13	  readonly blobBucket: cdk.aws_s3.Bucket;
    14	  readonly snapshotBucket: cdk.aws_s3.Bucket;
    15	  readonly webBucket: cdk.aws_s3.Bucket;
    16	  readonly distribution: cdk.aws_cloudfront.Distribution;
    17	  readonly pointer: cdk.aws_ssm.StringParameter;
    18	  readonly fleetSecret: cdk.aws_secretsmanager.Secret;
    19	  /** `https://<distribution domain>` — the page origin and the store base. */
    20	  readonly webOrigin: string;
    21	
    22	  constructor(scope: Construct, id: string, props: CoreStackProps) {
    23	    super(scope, id, props);
    24	
    25	    const s3 = cdk.aws_s3;
    26	    const privateBucket = (name: string, extra: cdk.aws_s3.BucketProps = {}) =>
    27	      new s3.Bucket(this, name, {
    28	        blockPublicAccess: s3.BlockPublicAccess.BLOCK_ALL,
    29	        encryption: s3.BucketEncryption.S3_MANAGED,
    30	        enforceSSL: true,
    31	        removalPolicy: cdk.RemovalPolicy.DESTROY,
    32	        autoDeleteObjects: true,
    33	        ...extra,
    34	      });
    35	
    36	    this.artifactsBucket = privateBucket("Artifacts");
    37	
    38	    // The blob and snapshot buckets outlive the stack (WP8.1): a logical-id change or a property
    39	    // that forces replacement must not empty a year of content-addressed blobs the ledger names.
… (346 more lines)
```

**Tool `Bash`:**

```
for f in packages/infra/image/Dockerfile packages/infra/image/main.js packages/infra/image/package.json packages/infra/scripts/stage-image.ts packages/infra/scripts/base-image-version.ts packages/infra/scripts/deploy-guard.ts packages/infra/scripts/whoami.ts packages/infra/scripts/mask.ts packages/infra/scripts/health.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/server.ts
```

**Result of `Bash`:**

```
===== packages/infra/lib/image-stack.ts =====
     1	// Image stack (design §9.3, §11.3, §11.4): the MicroVM image, its build role, and the two execution
     2	// roles. Nothing fleet-related is baked into the image, which keeps Core → Image → Fleet acyclic.
     3	import * as cdk from "aws-cdk-lib";
     4	import type { Construct } from "constructs";
     5	import type { CoreStack } from "./core-stack.ts";
     6	import {
     7	  anyImageArn,
     8	  baseImageArn,
     9	  imageArn,
    10	  managedConnectorArn,
    11	  NAMES,
    12	  PORTS,
    13	  parameterArn,
    14	} from "./names.ts";
    15	
    16	export interface ImageStackProps extends cdk.StackProps {
    17	  core: CoreStack;
    18	  /** Version of the managed al2023-1 base image; resolved by scripts/base-image-version.ts. */
    19	  baseImageVersion: string;
    20	  /** Directory zipped as the code artifact: Dockerfile, main.js, programs/. */
    21	  stagingDir: string;
    22	}
    23	
    24	function microvmServicePrincipal(): cdk.aws_iam.IPrincipal {
    25	  // Both roles trust lambda.amazonaws.com for sts:AssumeRole and sts:TagSession (MicroVMs security guide).
    26	  return new cdk.aws_iam.ServicePrincipal("lambda.amazonaws.com").withSessionTags();
    27	}
    28	
    29	export class ImageStack extends cdk.Stack {
    30	  readonly image: cdk.aws_lambda.CfnMicrovmImage;
    31	  readonly imageArn: string;
    32	  readonly buildRole: cdk.aws_iam.Role;
    33	  readonly controlPlaneRole: cdk.aws_iam.Role;
    34	  readonly coreRole: cdk.aws_iam.Role;
    35	  readonly logGroup: cdk.aws_logs.LogGroup;
    36	
    37	  constructor(scope: Construct, id: string, props: ImageStackProps) {
    38	    super(scope, id, props);
    39	    const { core } = props;
… (400 more lines)
```

**Tool `Bash`:**

```
for f in packages/control-plane/src/main.ts packages/control-plane/src/config.ts packages/control-plane/src/hooks.ts packages/control-plane/src/core-node.ts packages/control-plane/src/cores.ts packages/control-plane/src/bundles.ts packages/control-plane/src/seed.ts packages/control-plane/src/snapshotter.ts packages/control-plane/src/static.ts packages/control-plane/src/log.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/infra/image/Dockerfile =====
     1	# Tabframe MicroVM image (design §11.3): one image, two roles, chosen by the run-hook payload.
     2	# The bundle main.js is produced by `mise run build:image`; this directory ships a placeholder
     3	# until then so the image builds and answers its hooks.
     4	FROM public.ecr.aws/lambda/microvms:al2023-minimal
     5	RUN dnf install -y nodejs22 && dnf clean all
     6	WORKDIR /app
     7	COPY package.json /app/package.json
     8	COPY main.js /app/main.js
     9	COPY node-worker.js /app/node-worker.js
    10	COPY programs/ /app/programs/
    11	EXPOSE 8080 8081
    12	CMD ["node", "/app/main.js"]
===== packages/infra/image/main.js =====
     1	// Placeholder control plane for the image skeleton (WP0.8): answers the lifecycle hooks on the
     2	// private port so the image build completes, and a health route on both ports. Replaced by the
     3	// real bundle from packages/control-plane in WP0.4/WP0.10.
     4	const http = require("node:http");
     5	
     6	const PUBLIC_PORT = Number(process.env.TABFRAME_PUBLIC_PORT || 8080);
     7	const PRIVATE_PORT = Number(process.env.TABFRAME_PRIVATE_PORT || 8081);
     8	const HOOK_BASE = "/aws/lambda-microvms/runtime/v1/";
     9	
    10	let role = "neutral";
    11	let generation = 0;
    12	
    13	function log(message, fields) {
    14	  console.log(JSON.stringify({ level: "info", message, role, ...fields }));
    15	}
    16	
    17	function readBody(req) {
    18	  return new Promise((resolve) => {
    19	    let data = "";
    20	    req.on("data", (c) => {
    21	      data += c;
    22	    });
    23	    req.on("end", () => resolve(data));
    24	  });
    25	}
    26	
… (303 more lines)
```

**Result of `Read`:**

```
1	import { promises as dns } from "node:dns";
2	import { createServer, type IncomingMessage, type Server, type ServerResponse } from "node:http";
3	import type { Socket } from "node:net";
4	import {
5	  adoptLedger,
6	  apply,
7	  beginHandover,
8	  type Clock,
9	  createLedger,
10	  DEFAULT_TASK_LIMITS,
11	  deserializeLedger,
12	  drain,
13	  type Effect,
14	  type Event,
15	  type Ledger,
16	  systemClock,
17	} from "@tabframe/core";
18	import { CLOSE, encode, LIMITS, PROTOCOL_VERSION } from "@tabframe/protocol";
19	import {
20	  HASH_RE,
21	  LocalStore,
22	  MemorySnapshots,
23	  parseRange,
24	  S3Snapshots,
25	  S3Store,
26	  type SnapshotStore,
27	  type StoreDriver,
28	} from "@tabframe/store";
29	import { type WebSocket, WebSocketServer } from "ws";
30	import { resolveBundle } from "./bundles.ts";
31	import type { Config, Role } from "./config.ts";
32	import { type CoreFleet, createCoreFleet } from "./cores.ts";
33	import { HOOK_PREFIX, type HookHost, handleHook, type RunPayload } from "./hooks.ts";
34	import { log } from "./log.ts";
35	import { type DiscoveredProgram, discoverPrograms, seedPrograms } from "./seed.ts";
36	import { type SnapshotStatus, Snapshotter } from "./snapshotter.ts";
37	import { readBody, send, sendJson, serveStatic } from "./static.ts";
38	
39	/** An unshipped program nobody has run in the ledger's memory is retired at seeding after this long. */
40	const STALE_DROP_MS = 60 * 60 * 1000;
… (780 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/core/src/apply.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/core/src/executions.ts
```

**Tool `Bash`:**

```
for f in packages/core/src/ledger.ts packages/core/src/results.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (34.1KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bntg0ztjz.txt

Preview (first 2KB):
===== packages/control-plane/src/main.ts =====
     1	import { readFileSync } from "node:fs";
     2	import { configFromEnv } from "./config.ts";
     3	import { log } from "./log.ts";
     4	import { createControlPlane } from "./server.ts";
     5	
     6	const config = configFromEnv();
     7	const cp = await createControlPlane(config);
     8	log("listening", {
     9	  mode: config.mode,
    10	  role: cp.role,
    11	  generation: cp.generation,
    12	  publicPort: cp.publicAddress.port,
    13	  privatePort: cp.privateAddress.port,
    14	  host: cp.publicAddress.host,
    15	});
    16	
    17	let closing = false;
    18	async function shutdown(signal: string): Promise<void> {
    19	  if (closing) return;
    20	  closing = true;
    21	  log("shutdown", { signal });
    22	  await cp.close();
    23	  process.exit(0);
    24	}
    25	// The build stamp the image was staged with (WP8.1), for /health.
    26	try {
    27	  if (!process.env.TABFRAME_BUILD) {
    28	    const stamp = JSON.parse(readFileSync(new URL("./build.json", import.meta.url), "utf8")) as {
    29	      sha?: string;
    30	    };
    31	    if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha;
    32	  }
    33	} catch {
    34	  // no stamp beside the entry: a local run
    35	}
… (12 more lines)
```

**Result of `Read`:**

```
1	import {
2	  AbiError,
3	  byteLength,
4	  canonicalStringify,
5	  decodeStageSpec,
6	  encodePlanInput,
7	  type FsManifest,
8	  fsManifest,
9	  PROTOCOL_VERSION,
10	  type ProgramManifest,
11	} from "@tabframe/protocol";
12	import type { Effect } from "./events.ts";
13	import {
14	  type ExecutionRecord,
15	  emptyCounters,
16	  executionView,
17	  type Ledger,
18	  type NodeRecord,
19	  queueEntry,
20	  type TaskRecord,
21	  taskView,
22	} from "./ledger.ts";
23	import { broadcast } from "./observers.ts";
24	import { cancelOthers, fill } from "./scheduler.ts";
25	
26	const encoder = new TextEncoder();
27	const decoder = new TextDecoder();
28	
29	export function addProgram(
30	  ledger: Ledger,
31	  bundle: string,
32	  module: string,
33	  manifest: ProgramManifest,
34	  files: FsManifest["files"],
35	  now: number,
36	): Effect[] {
37	  ledger.programs.set(bundle, { bundle, module, manifest, files, addedAt: now });
38	  return broadcast(ledger, { t: "programAdded", program: bundle, name: manifest.name });
39	}
40	
… (810 more lines)
```

**Result of `Read`:**

```
1	import {
2	  byteLength,
3	  CLOSE,
4	  type Control,
5	  type ControlPlaneToObserver,
6	  canonicalStringify,
7	  decode,
8	  type Heartbeat,
9	  type Hello,
10	  LIMITS,
11	  type MachineView,
12	  nodeToControlPlane,
13	  observerToControlPlane,
14	  PROTOCOL_VERSION,
15	  type ProgramView,
16	  type Snapshot,
17	  type TaskView,
18	} from "@tabframe/protocol";
19	import type { Effect, Event } from "./events.ts";
20	import {
21	  addProgram,
22	  afterTaskSettled,
23	  cancelExecution,
24	  commandHalf,
25	  enqueue,
26	  ensureDefaultLoop,
27	  executionTasks,
28	  maybeStart,
29	  onInheritRoot,
30	  onManifestStored,
31	  onStageSpec,
32	  pruneExecutions,
33	  resumeAll,
34	  resumePending,
35	  retireProgram,
36	} from "./executions.ts";
37	import { coreGone, coreLaunched, fleetTick, microvmIdOfHost } from "./fleet.ts";
38	import {
39	  type ConnRole,
40	  type ConnState,
… (743 more lines)
```

**Tool `Bash`:**

```
for f in packages/core/src/scheduler.ts packages/core/src/fleet.ts packages/core/src/handover.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (33KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b41mevafk.txt

Preview (first 2KB):
===== packages/core/src/ledger.ts =====
     1	import type {
     2	  Counters,
     3	  ExecutionView,
     4	  FsManifest,
     5	  Health,
     6	  NodeKind,
     7	  NodeView,
     8	  Place,
     9	  ProgramManifest,
    10	  QueueEntry,
    11	  TaskLimits,
    12	  TaskView,
    13	} from "@tabframe/protocol";
    14	
    15	/** One live node. Everything the control plane knows about a core (design §6.2). */
    16	export interface NodeRecord {
    17	  nodeId: string;
    18	  connId: string;
    19	  hostId: string;
    20	  kind: NodeKind;
    21	  cores: number;
    22	  sandboxVersion: string;
    23	  joinedAt: number;
    24	  lastSeen: number;
    25	  visible: boolean;
    26	  health: Health;
    27	  tasksDone: number;
    28	  lastTaskMs: number | null;
    29	  /** Exponentially weighted compute time, the basis of fast/slow. */
    30	  ewmaMs: number | null;
    31	  /** Task ids with an open attempt on this node. */
    32	  inFlight: string[];
    33	  /** The last demo command applied, so resumeAll knows whom to resume. */
    34	  commanded: "throttle" | "freeze" | null;
    35	}
… (23 more lines)
```

**Tool `Bash`:**

```
for f in packages/core/src/snapshot.ts packages/core/src/interfaces.ts packages/core/src/events.ts packages/core/src/observers.ts packages/core/src/bytes.ts packages/core/src/index.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/core/src/scheduler.ts =====
     1	import { type Assign, type Health, LIMITS, PROTOCOL_VERSION } from "@tabframe/protocol";
     2	import { toBase64 } from "./bytes.ts";
     3	import type { Effect } from "./events.ts";
     4	import type { ExecutionRecord, Ledger, NodeRecord, TaskRecord } from "./ledger.ts";
     5	import { broadcast } from "./observers.ts";
     6	
     7	/** Median of the execution's recent compute samples times the factor, floored (design §6.4). */
     8	export function deadlineMs(ledger: Ledger, exec: ExecutionRecord): number {
     9	  const samples = exec.computeSamples;
    10	  if (samples.length === 0) return ledger.config.deadlineFloorMs;
    11	  const sorted = [...samples].sort((a, b) => a - b);
    12	  const median = sorted[Math.floor(sorted.length / 2)] ?? 0;
    13	  return Math.max(ledger.config.deadlineFloorMs, Math.round(median * ledger.config.deadlineFactor));
    14	}
    15	
    16	export function runningAttempts(task: TaskRecord): number {
    17	  return task.attempts.filter((a) => a.outcome === "running").length;
    18	}
    19	
    20	function holds(task: TaskRecord, nodeId: string): boolean {
    21	  return task.attempts.some((a) => a.outcome === "running" && a.nodeId === nodeId);
    22	}
    23	
    24	/** Results reported in the current contested round that agree with each other, at most. */
    25	function currentRoundResults(task: TaskRecord): number {
    26	  return task.results.filter((r) => r.round === task.contestedRounds).length;
    27	}
    28	
    29	/** How many more attempts this task wants before it can settle. */
    30	export function wanted(task: TaskRecord): number {
    31	  if (task.status === "done" || task.status === "failed") return 0;
    32	  return Math.max(0, task.requiredAgreement - currentRoundResults(task) - runningAttempts(task));
    33	}
    34	
    35	/** The node already reported this round: a second attempt there could only agree with itself. */
    36	function answered(task: TaskRecord, nodeId: string): boolean {
    37	  return task.results.some((r) => r.round === task.contestedRounds && r.nodeId === nodeId);
    38	}
    39	
… (443 more lines)
```

**Tool `Bash`:**

```
for f in packages/protocol/src/limits.ts packages/protocol/src/shared.ts packages/protocol/src/task.ts packages/protocol/src/fs.ts packages/protocol/src/codec.ts packages/protocol/src/canonical.ts packages/protocol/src/codes.ts packages/protocol/src/index.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/core/src/snapshot.ts =====
     1	import { fromBase64, toBase64 } from "./bytes.ts";
     2	import type { Effect } from "./events.ts";
     3	import { resumePending } from "./executions.ts";
     4	import type {
     5	  CoreRecord,
     6	  ExecutionRecord,
     7	  Ledger,
     8	  NodeRecord,
     9	  ProgramRecord,
    10	  TaskRecord,
    11	} from "./ledger.ts";
    12	import { releaseNode } from "./scheduler.ts";
    13	
    14	/**
    15	 * The ledger as JSON for the S3 snapshot and the handover (design §9.4). Connections and observers
    16	 * are not persisted: they belong to the process that held them. Nodes are, so an adopting
    17	 * control plane knows what to release.
    18	 */
    19	export interface SerializedLedger {
    20	  version: 1;
    21	  meta: Ledger["meta"];
    22	  config: Ledger["config"];
    23	  nodes: NodeRecord[];
    24	  programs: ProgramRecord[];
    25	  /** Cloud cores travel with the ledger, so a successor inherits them (design §6.8). */
    26	  cores?: CoreRecord[];
    27	  executions: ExecutionRecord[];
    28	  queue: string[];
    29	  running: string | null;
    30	  tasks: Array<Omit<TaskRecord, "input"> & { input: string }>;
    31	}
    32	
    33	export function serializeLedger(ledger: Ledger): string {
    34	  const s: SerializedLedger = {
    35	    version: 1,
    36	    meta: ledger.meta,
    37	    config: ledger.config,
    38	    nodes: [...ledger.nodes.values()],
    39	    programs: [...ledger.programs.values()],
… (335 more lines)
```

**Tool `Bash`:**

```
for f in packages/protocol/src/node.ts packages/protocol/src/observer.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/protocol/src/abi.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/protocol/src/limits.ts =====
     1	/** Wire-level limits (design §8.4). Both ends enforce them. */
     2	export const PROTOCOL_VERSION = 1;
     3	
     4	export const LIMITS = {
     5	  /** Every socket message stays under this many UTF-8 bytes. */
     6	  maxMessageBytes: 64 * 1024,
     7	  /** A task's inline input, base64, before it must become a blob instead. */
     8	  maxInlineInputBytes: 16 * 1024,
     9	  /** Tasks per snapshot page. */
    10	  snapshotPageTasks: 256,
    11	  /** Cluster caps (decision: 256 nodes, 64 observers, no per-IP limit). */
    12	  nodeCap: 256,
    13	  observerCap: 64,
    14	  /** Liveness. */
    15	  heartbeatMs: 1_000,
    16	  goneAfterMs: 4_000,
    17	  observerPingMs: 2_000,
    18	  maxInFlight: 2,
    19	  /**
    20	   * Rate limits, messages per second per connection. A node sends a presign and a result per
    21	   * task, and a tile takes a few milliseconds, so an honest node reaches hundreds per second.
    22	   */
    23	  nodeMessagesPerSecond: 1_000,
    24	  observerMessagesPerSecond: 5,
    25	  /** Results and uploads (design §5.5, §8.4). */
    26	  maxWriteFiles: 256,
    27	  maxWriteBytes: 16 * 1024 * 1024,
    28	  maxOutputBytes: 16 * 1024 * 1024,
    29	  maxLogBytes: 64 * 1024,
    30	  maxInlineLogBytes: 4 * 1024,
    31	  maxPresignItems: 300,
    32	  maxModuleBytes: 8 * 1024 * 1024,
    33	  /** Reconnect backoff window. */
    34	  reconnectMinMs: 500,
    35	  reconnectMaxMs: 30_000,
    36	} as const;
===== packages/protocol/src/shared.ts =====
     1	import { z } from "zod";
     2	import { PROTOCOL_VERSION } from "./limits.ts";
… (315 more lines)
```

**Tool `Bash`:**

```
for f in packages/sandbox/src/validate.ts packages/sandbox/src/host.ts packages/sandbox/src/run.ts packages/sandbox/src/fs.ts packages/sandbox/src/types.ts packages/sandbox/src/index.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/protocol/src/node.ts =====
     1	import { z } from "zod";
     2	import { fsPath } from "./fs.ts";
     3	import { LIMITS } from "./limits.ts";
     4	import { envelope, hash, hostId, millis, nodeId, nodeKind } from "./shared.ts";
     5	import { taskKind, taskLimits } from "./task.ts";
     6	
     7	/** Node → control plane. */
     8	export const hello = z.object({
     9	  t: z.literal("hello"),
    10	  ...envelope,
    11	  hostId,
    12	  kind: nodeKind,
    13	  cores: z.number().int().min(1).max(1024),
    14	  sandboxVersion: z.string().min(1).max(32),
    15	});
    16	
    17	export const heartbeat = z.object({
    18	  t: z.literal("heartbeat"),
    19	  ...envelope,
    20	  visible: z.boolean(),
    21	  queue: z.number().int().min(0).max(LIMITS.maxInFlight),
    22	  lastTaskMs: millis.nullable(),
    23	  tasksDone: z.number().int().nonnegative(),
    24	});
    25	
    26	const writeEntry = z.object({ path: fsPath, hash, size: z.number().int().nonnegative() });
    27	
    28	/**
    29	 * The error string a node sends when it gave up on a task at its own deadline (design §4.2): not a
    30	 * program fault, so the control plane releases the attempt instead of failing the task.
    31	 */
    32	export const RELEASED = "released";
    33	
    34	/** A result names hashes the store vouches for; the error form carries a message instead (design §8.2). */
    35	export const result = z
    36	  .object({
    37	    t: z.literal("result"),
    38	    ...envelope,
    39	    taskId: z.string().min(1).max(64),
… (468 more lines)
```

**Tool `Bash`:**

```
for f in packages/sandbox/src/adapters/bridge.ts packages/sandbox/src/adapters/node-host.ts packages/sandbox/src/adapters/node-worker.ts packages/sandbox/src/adapters/web-host.ts packages/sandbox/src/adapters/web-worker.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/protocol/src/abi.ts =====
     1	/**
     2	 * The program ABI's byte formats (design §5.3): what `run` and `plan` receive and what `plan`
     3	 * returns. Binary, little-endian, deterministic, trivial to produce from AssemblyScript and to
     4	 * parse on the control plane. The SDK mirrors these layouts exactly.
     5	 *
     6	 *   run input   : "TFRN" u32 version=1 | u32 stage | u32 taskIndex | u32 taskCount | u32 len | input[len]
     7	 *   plan input  : "TFPL" u32 version=1 | u32 stage | table params | table hints
     8	 *   plan output : "TFSS" u32 version=1 | u8 kind (0 = stage, 1 = done)
     9	 *                 stage: str name | u8 hasCanvas [u32 w u32 h] | u32 n | n × (u32 len input[len] | u8 hasPlace [i32 x y w h])
    10	 *                 done : u8 hasNext [table next]
    11	 *   bars        : "TFBR" u32 version=1 | u32 count | count × (str label | f64 value)
    12	 *                 — what a `bars` program's final task returns (design §5.1); the dashboard draws it
    13	 *   table       : u32 count | count × (str key | str value)   — values are JSON text
    14	 *   str         : u32 len | utf8[len]
    15	 */
    16	
    17	export const ABI_VERSION = 1;
    18	const MAGIC_RUN = 0x4e524654; // "TFRN" little-endian
    19	const MAGIC_PLAN = 0x4c504654; // "TFPL"
    20	const MAGIC_SPEC = 0x53534654; // "TFSS"
    21	const MAGIC_BARS = 0x52424654; // "TFBR"
    22	
    23	/** Params and hints travel as a flat table of key → JSON text, so a program needs no JSON parser. */
    24	export type ParamTable = Record<string, unknown>;
    25	
    26	export interface RunInput {
    27	  stage: number;
    28	  taskIndex: number;
    29	  taskCount: number;
    30	  input: Uint8Array;
    31	}
    32	
    33	export interface PlanInput {
    34	  stage: number;
    35	  params: ParamTable;
    36	  hints: ParamTable;
    37	}
    38	
    39	export interface Place {
… (309 more lines)
```

**Tool `Bash`:**

```
for f in packages/store/src/client.ts packages/store/src/driver.ts packages/store/src/hash.ts packages/store/src/local.ts packages/store/src/s3.ts packages/store/src/snapshots.ts packages/store/src/index.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/sandbox/src/validate.ts =====
     1	import { LIMITS } from "@tabframe/protocol";
     2	
     3	/** The only imports a program may declare (design §5.3, §5.5). */
     4	export const ALLOWED_IMPORTS: Readonly<Record<string, ReadonlySet<string>>> = {
     5	  tf: new Set(["stat", "read", "write", "list", "log"]),
     6	  env: new Set(["abort"]),
     7	};
     8	
     9	export const REQUIRED_EXPORTS = ["memory", "alloc", "run", "plan"] as const;
    10	
    11	export interface MemoryLimits {
    12	  min: number;
    13	  max: number | null;
    14	  shared: boolean;
    15	  memory64: boolean;
    16	}
    17	
    18	export type Validation =
    19	  | { ok: true; memory: MemoryLimits; module: WebAssembly.Module }
    20	  | { ok: false; reason: string };
    21	
    22	/**
    23	 * Validate program bytes before anything is instantiated: size, well-formedness, a declared memory
    24	 * maximum under the cap, the import allowlist, and the required exports.
    25	 */
    26	export function validateModuleBytes(
    27	  bytes: Uint8Array,
    28	  limits: { memoryPagesMax: number },
    29	): Validation {
    30	  if (bytes.length > LIMITS.maxModuleBytes) {
    31	    return { ok: false, reason: `module is ${bytes.length} bytes, cap ${LIMITS.maxModuleBytes}` };
    32	  }
    33	  if (!WebAssembly.validate(asSource(bytes))) {
    34	    return { ok: false, reason: "not a valid WebAssembly module" };
    35	  }
    36	  // One memory only (WP8.1): with multi-memory a module could declare a small first memory and
    37	  // grow an unbounded second one past the cap the design promises.
    38	  const memories = countMemories(bytes);
    39	  if (memories > 1)
… (597 more lines)
```

**Tool `Bash`:**

```
for f in packages/fleet/src/aws.ts packages/fleet/src/config.ts packages/fleet/src/cp-client.ts packages/fleet/src/lambda/rotate.ts packages/fleet/src/lambda/session.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/sandbox/src/adapters/bridge.ts =====
     1	import type { BlobReader } from "../types.ts";
     2	
     3	/**
     4	 * The Atomics bridge (design §4.2, node adapter): a sandbox thread blocks on a SharedArrayBuffer
     5	 * while the orchestrator thread fetches bytes asynchronously and drops them into the buffer.
     6	 *
     7	 *   header (Int32Array over the first 16 bytes): [0] status, [1] bytes delivered
     8	 *   data region: the rest of the buffer; reads longer than the region are chunked
     9	 */
    10	export const BRIDGE_HEADER_BYTES = 16;
    11	export const DEFAULT_REGION_BYTES = 4 * 1024 * 1024;
    12	export const BRIDGE = { waiting: 0, ready: 1, notFound: 2 } as const;
    13	
    14	export interface BlobRequest {
    15	  type: "blob";
    16	  hash: string;
    17	  offset: number;
    18	  len: number;
    19	}
    20	
    21	export function isBlobRequest(msg: unknown): msg is BlobRequest {
    22	  return typeof msg === "object" && msg !== null && (msg as { type?: unknown }).type === "blob";
    23	}
    24	
    25	export function createBridgeBuffer(regionBytes = DEFAULT_REGION_BYTES): SharedArrayBuffer {
    26	  return new SharedArrayBuffer(BRIDGE_HEADER_BYTES + regionBytes);
    27	}
    28	
    29	/** Worker side: post a request, block until the host answers, collect chunks. */
    30	export class BridgeBlobReader implements BlobReader {
    31	  private readonly header: Int32Array;
    32	  private readonly data: Uint8Array;
    33	  private readonly post: (req: BlobRequest) => void;
    34	
    35	  constructor(sab: SharedArrayBuffer, post: (req: BlobRequest) => void) {
    36	    this.header = new Int32Array(sab, 0, BRIDGE_HEADER_BYTES / 4);
    37	    this.data = new Uint8Array(sab, BRIDGE_HEADER_BYTES);
    38	    this.post = post;
    39	  }
… (223 more lines)
```

**Tool `Bash`:**

```
for f in packages/fleet/src/microvm-client.ts packages/fleet/src/ops.ts packages/fleet/src/pointer.ts packages/fleet/src/types.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/store/src/client.ts =====
     1	import type { PresignedUpload, PresignItem } from "./driver.ts";
     2	import { sha256Hex } from "./hash.ts";
     3	
     4	/** Presign is a socket message (D18); the node and the page provide the round trip. */
     5	export interface PresignRequester {
     6	  presign(items: PresignItem[]): Promise<PresignedUpload[]>;
     7	}
     8	
     9	export type FetchLike = (url: string, init?: RequestInit) => Promise<Response>;
    10	
    11	/**
    12	 * The one client flow everywhere (design §7.1): hash locally, ask for presigned URLs over the
    13	 * socket, PUT straight to the store with the pinned headers, and read blobs by hash from the CDN.
    14	 */
    15	export class StoreClient {
    16	  private readonly base: string;
    17	  private readonly requester: PresignRequester;
    18	  private readonly fetchImpl: FetchLike;
    19	
    20	  constructor(
    21	    base: string,
    22	    requester: PresignRequester,
    23	    fetchImpl: FetchLike = (u, i) => fetch(u, i),
    24	  ) {
    25	    this.base = base.replace(/\/$/, "");
    26	    this.requester = requester;
    27	    this.fetchImpl = fetchImpl;
    28	  }
    29	
    30	  urlFor(hash: string): string {
    31	    return `${this.base}/${hash}`;
    32	  }
    33	
    34	  async put(bytes: Uint8Array): Promise<{ hash: string; size: number }> {
    35	    const [r] = await this.putMany([bytes]);
    36	    if (!r) throw new Error("unreachable");
    37	    return r;
    38	  }
    39	
… (444 more lines)
```

**Tool `Bash`:**

```
for f in packages/fleet/src/rotate.ts packages/fleet/src/session.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/node/src/orchestrator.ts packages/node/src/session.ts packages/node/src/backoff.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/fleet/src/aws.ts =====
     1	// Real adapters for the pointer store, Lambda invocation, EventBridge rules, and the fleet secret.
     2	// Field mapping only; tested with aws-sdk-client-mock.
     3	import {
     4	  DisableRuleCommand,
     5	  EnableRuleCommand,
     6	  EventBridgeClient,
     7	} from "@aws-sdk/client-eventbridge";
     8	import { InvokeCommand, LambdaClient } from "@aws-sdk/client-lambda";
     9	import { GetSecretValueCommand, SecretsManagerClient } from "@aws-sdk/client-secrets-manager";
    10	import { GetParameterCommand, PutParameterCommand, SSMClient } from "@aws-sdk/client-ssm";
    11	import {
    12	  EMPTY_POINTER,
    13	  type Pointer,
    14	  type PointerStore,
    15	  parsePointer,
    16	  serializePointer,
    17	} from "./pointer.ts";
    18	import type { Invoker, RuleControl, SecretReader } from "./types.ts";
    19	
    20	export class SsmPointerStore implements PointerStore {
    21	  private readonly ssm: SSMClient;
    22	  private readonly name: string;
    23	
    24	  constructor(name: string, ssm: SSMClient = new SSMClient({})) {
    25	    this.name = name;
    26	    this.ssm = ssm;
    27	  }
    28	
    29	  async read(): Promise<Pointer> {
    30	    try {
    31	      const out = await this.ssm.send(new GetParameterCommand({ Name: this.name }));
    32	      return parsePointer(out.Parameter?.Value);
    33	    } catch (error) {
    34	      if ((error as { name?: unknown }).name === "ParameterNotFound") return { ...EMPTY_POINTER };
    35	      throw error;
    36	    }
    37	  }
    38	
    39	  async write(pointer: Pointer): Promise<void> {
… (394 more lines)
```

**Tool `Bash`:**

```
for f in packages/node/src/tasks.ts packages/node/src/platform/node.ts packages/node/src/platform/web.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/fleet/src/microvm-client.ts =====
     1	// Thin adapter from the MicrovmClient interface to @aws-sdk/client-lambda-microvms.
     2	// No logic lives here beyond field mapping; it is tested with aws-sdk-client-mock.
     3	import {
     4	  CreateMicrovmAuthTokenCommand,
     5	  GetMicrovmCommand,
     6	  type GetMicrovmCommandOutput,
     7	  LambdaMicrovmsClient,
     8	  ListMicrovmsCommand,
     9	  type MicrovmItem,
    10	  type PortSpecification,
    11	  ResumeMicrovmCommand,
    12	  RunMicrovmCommand,
    13	  SuspendMicrovmCommand,
    14	  TerminateMicrovmCommand,
    15	} from "@aws-sdk/client-lambda-microvms";
    16	import {
    17	  isNotFound,
    18	  type MicrovmClient,
    19	  type MicrovmInfo,
    20	  type MicrovmState,
    21	  normalizeEndpoint,
    22	  type PortSpec,
    23	  type RunMicrovmParams,
    24	} from "./types.ts";
    25	
    26	const KNOWN_STATES: ReadonlySet<string> = new Set([
    27	  "PENDING",
    28	  "RUNNING",
    29	  "SUSPENDED",
    30	  "SUSPENDING",
    31	  "TERMINATED",
    32	  "TERMINATING",
    33	]);
    34	
    35	function toState(state: string | undefined): MicrovmState {
    36	  // An unknown state is treated as pending: not serving, not gone, poll again.
    37	  return state !== undefined && KNOWN_STATES.has(state) ? (state as MicrovmState) : "PENDING";
    38	}
    39	
… (386 more lines)
```

**Result of `Bash`:**

```
===== packages/fleet/src/rotate.ts =====
     1	// The rotate function (design §9.4): the hourly handover, which is also the deploy path and the
     2	// heal path. One run does at most one rotation and leaves the fleet in a state the next run can
     3	// finish or roll back — an EventBridge schedule with no reserved concurrency can overlap runs, and
     4	// a Lambda can die at any line.
     5	//
     6	//   1. launch CP(g+1), naming the latest snapshot so it boots adopted even if step 2 fails
     7	//   2. CP(g) /handover  → its ledger
     8	//   3. CP(g+1) /adopt   ← that ledger
     9	//   4. flip the pointer
    10	//   5. CP(g) /drain, then terminate after a grace period
    11	
    12	import {
    13	  CONTROL_PLANE_IDLE_POLICY,
    14	  CONTROL_PLANE_MAX_DURATION_SECONDS,
    15	  egressConnectorArn,
    16	  ingressConnectorArn,
    17	  type RotateConfig,
    18	} from "./config.ts";
    19	import type { ControlPlaneClient, ControlPlaneTarget } from "./cp-client.ts";
    20	import type { Pointer, PointerStore } from "./pointer.ts";
    21	import {
    22	  type Clock,
    23	  isThrottling,
    24	  type Logger,
    25	  type MicrovmClient,
    26	  type MicrovmInfo,
    27	  SERVING_STATES,
    28	  type SecretReader,
    29	  type Sleeper,
    30	} from "./types.ts";
    31	
    32	export interface RotateDeps {
    33	  pointer: PointerStore;
    34	  microvms: MicrovmClient;
    35	  secrets: SecretReader;
    36	  clock: Clock;
    37	  sleep: Sleeper;
    38	  log: Logger;
    39	  config: RotateConfig;
… (465 more lines)
```

**Tool `Bash`:**

```
for f in packages/web/public/index.html packages/web/public/editor.html packages/web/public/guide.html packages/web/scripts/build.ts packages/web/scripts/gen-sources.ts; do echo "===== $f ====="; cat -n "$f"; done; echo "===== grep: risky DOM/JS sinks in web/src ====="; grep -n -E "innerHTML|outerHTML|insertAdjacentHTML|document\.write|eval\(|new Function|srcdoc|javascript:" packages/web/src/*.ts | grep -v "\.test\.ts" | head -80; echo "===== grep: Worker/blob URL creation ====="; grep -n -E "new Worker|URL\.createObjectURL|new Blob\(|importScripts|import\(" packages/web/src/*.ts packages/sandbox/src/adapters/*.ts packages/node/src/platform/*.ts | grep -v "\.test\.ts" | head -60
```

**Result of `Bash`:**

```
===== packages/node/src/orchestrator.ts =====
     1	import {
     2	  type Assign,
     3	  CLOSE,
     4	  type Command,
     5	  type ControlPlaneToNode,
     6	  controlPlaneToNode,
     7	  decode,
     8	  encode,
     9	  type Heartbeat,
    10	  type Hello,
    11	  LIMITS,
    12	  type NodeKind,
    13	  PROTOCOL_VERSION,
    14	  type RotatingReason,
    15	} from "@tabframe/protocol";
    16	import { StoreClient } from "@tabframe/store";
    17	import { Backoff } from "./backoff.ts";
    18	import { type FetchLike, fetchSession, type Session, socketProtocols } from "./session.ts";
    19	import { type SandboxRunner, SocketPresigner, TaskRunner } from "./tasks.ts";
    20	
    21	/** The WHATWG surface the orchestrator needs; browser and Node WebSockets both provide it. */
    22	export interface SocketLike {
    23	  readonly readyState: number;
    24	  send(data: string): void;
    25	  close(code?: number, reason?: string): void;
    26	  onopen: ((ev: unknown) => void) | null;
    27	  onmessage: ((ev: { data: unknown }) => void) | null;
    28	  onclose: ((ev: { code: number; reason: string }) => void) | null;
    29	  onerror: ((ev: unknown) => void) | null;
    30	}
    31	
    32	export interface Timers {
    33	  setTimeout(fn: () => void, ms: number): unknown;
    34	  clearTimeout(handle: unknown): void;
    35	}
    36	
    37	export type NodeState =
    38	  | "connecting"
    39	  | "idle"
… (497 more lines)
```

**Result of `Bash`:**

```
===== packages/node/src/tasks.ts =====
     1	import {
     2	  type Assign,
     3	  encodeRunInput,
     4	  type FsManifest,
     5	  fsManifest,
     6	  LIMITS,
     7	  PROTOCOL_VERSION,
     8	  RELEASED,
     9	  type Result,
    10	} from "@tabframe/protocol";
    11	import type { HostRequest, TaskResult } from "@tabframe/sandbox";
    12	import type { PresignRequester, StoreClient } from "@tabframe/store";
    13	
    14	/** The sandbox as the orchestrator sees it (design §4.2): one call, one deadline, a kill switch. */
    15	export interface SandboxRunner {
    16	  run(module: WebAssembly.Module, request: HostRequest, deadlineMs: number): Promise<TaskResult>;
    17	  dispose(): void;
    18	}
    19	
    20	export interface TaskRunnerDeps {
    21	  store: StoreClient;
    22	  createSandbox(): SandboxRunner;
    23	  compile(bytes: Uint8Array): Promise<WebAssembly.Module>;
    24	  now(): number;
    25	  log?: ((event: string, fields?: Record<string, unknown>) => void) | undefined;
    26	}
    27	
    28	const EMPTY_MANIFEST: FsManifest = { version: 1, files: {} };
    29	const MAX_CACHED_MODULES = 8;
    30	const MAX_CACHED_MANIFESTS = 32;
    31	
    32	export type Outcome =
    33	  | { kind: "result"; msg: Omit<Result, "v" | "gen"> }
    34	  | { kind: "dropped"; reason: string };
    35	
    36	/**
    37	 * Runs one task end to end: module and manifest by hash (cached), ABI framing, the sandbox with
    38	 * the deadline plus a grace second, then output, writes, and log uploaded through the store so
    39	 * the result carries only hashes the store vouches for (design §4.1, §7.3).
… (401 more lines)
```

**Result of `Bash`:**

```
===== packages/web/public/index.html =====
     1	<!doctype html>
     2	<html lang="en">
     3	<head>
     4	<meta charset="utf-8">
     5	<meta name="viewport" content="width=device-width, initial-scale=1">
     6	<title>Tabframe</title>
     7	<link rel="stylesheet" href="/styles.css">
     8	</head>
     9	<body>
    10	<header>
    11	  <h1>TABFRAME</h1>
    12	  <span id="machine" class="pill wait">connecting</span>
    13	  <span id="gen" class="pill">gen —</span>
    14	  <span id="nextRotation" class="pill" title="The control plane hands over to a fresh MicroVM every hour; the render continues across it" hidden>rotation in — min</span>
    15	  <span id="counts" class="pill">0 nodes · 0 hosts</span>
    16	  <span id="seq" class="pill">seq 0</span>
    17	  <span id="exec" class="pill">idle</span>
    18	  <span id="loop" class="pill loop" data-loop="running" title="The automatic loop renders frame after frame while someone watches">loop · running</span>
    19	  <span id="rate" class="pill">0.0 tasks/s</span>
    20	  <span class="spacer"></span>
    21	  <button type="button" id="stop" class="danger" title="Ends what is running and holds the automatic loop until Start; a launch of yours still runs at once">stop</button>
    22	  <button type="button" id="start" title="Lets the automatic loop run again" hidden>start</button>
    23	  <button type="button" id="resume" title="Resumes the machine an editor tab holds paused" hidden>resume</button>
    24	  <button type="button" id="freezePanel" class="panel-only" hidden title="Stop this tab from updating so you can read it; updates queue and apply on resume">pause updates</button>
    25	  <a id="backToDashboard" class="panel-only" href="/" hidden title="Back to the dashboard this tab was opened from">← dashboard</a>
    26	  <button type="button" id="openEditor" title="Open the editor in its own tab; the machine pauses while it is open">editor ↗</button>
    27	</header>
    28	<div class="status-line"><div id="machineBanner" class="machine-banner" role="status" hidden></div><div id="notice" class="notice" aria-live="polite" hidden></div></div>
    29	<main>
    30	  <section class="hero">
    31	    <div id="banner" class="banner" data-state="connecting"><strong id="bannerTitle">Connecting…</strong><span id="bannerBody"></span><span id="bannerHint" class="muted small"></span></div>
    32	    <div id="stage" hidden>
    33	      <div class="execrow">
    34	        <span id="execName">No execution</span>
    35	        <span id="execDetail" class="muted"></span>
    36	        <div class="bar"><div id="progressFill"></div></div>
    37	        <span id="progressText">0/0</span>
    38	        <span class="throughput"><canvas id="throughputChart" width="240" height="36" aria-label="tasks done per second over the last minute"></canvas><span id="throughputFigure">0.0 tasks/s · 0 nodes</span></span>
    39	      </div>
… (416 more lines)
```

> **Thinking.** 

> **Thinking.** I'll re-read the spilled control-plane process file and ledger.ts/results.ts in smaller chunks, alongside checking the dashboard TypeScript, transcript scrubber, and running targeted greps on the loop-1 RunMicrovm fix, compiler worker eval usage vs CSP, and client address exposure—all independently.

**Tool `Bash`:**

```
for f in packages/control-plane/src/main.ts packages/control-plane/src/config.ts packages/control-plane/src/hooks.ts packages/control-plane/src/core-node.ts packages/control-plane/src/cores.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/control-plane/src/bundles.ts packages/control-plane/src/seed.ts packages/control-plane/src/snapshotter.ts packages/control-plane/src/static.ts packages/control-plane/src/log.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
cat -n packages/core/src/ledger.ts
```

**Tool `Bash`:**

```
cat -n packages/core/src/results.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/host.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/state.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/panels.ts
```

**Result of `Bash`:**

```
===== packages/control-plane/src/main.ts =====
     1	import { readFileSync } from "node:fs";
     2	import { configFromEnv } from "./config.ts";
     3	import { log } from "./log.ts";
     4	import { createControlPlane } from "./server.ts";
     5	
     6	const config = configFromEnv();
     7	const cp = await createControlPlane(config);
     8	log("listening", {
     9	  mode: config.mode,
    10	  role: cp.role,
    11	  generation: cp.generation,
    12	  publicPort: cp.publicAddress.port,
    13	  privatePort: cp.privateAddress.port,
    14	  host: cp.publicAddress.host,
    15	});
    16	
    17	let closing = false;
    18	async function shutdown(signal: string): Promise<void> {
    19	  if (closing) return;
    20	  closing = true;
    21	  log("shutdown", { signal });
    22	  await cp.close();
    23	  process.exit(0);
    24	}
    25	// The build stamp the image was staged with (WP8.1), for /health.
    26	try {
    27	  if (!process.env.TABFRAME_BUILD) {
    28	    const stamp = JSON.parse(readFileSync(new URL("./build.json", import.meta.url), "utf8")) as {
    29	      sha?: string;
    30	    };
    31	    if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha;
    32	  }
    33	} catch {
    34	  // no stamp beside the entry: a local run
    35	}
    36	process.on("SIGTERM", () => void shutdown("SIGTERM"));
    37	process.on("SIGINT", () => void shutdown("SIGINT"));
===== packages/control-plane/src/config.ts =====
     1	/** Process configuration from the environment. Nothing here is secret except the fleet secret. */
… (321 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor.ts
```

**Tool `Bash`:**

```
cat -n packages/web/src/editor-core.ts
```

**Result of `Bash`:**

```
===== packages/control-plane/src/bundles.ts =====
     1	// Resolving an uploaded bundle (design §5.2, §5.5): the ledger only knows hashes, so before an
     2	// upload can be launched the process fetches its manifest and module, checks them the way seeding
     3	// checks the shipped programs, and reports a program the core can launch — or a refusal a person
     4	// can act on.
     5	import {
     6	  BUNDLE_PATHS,
     7	  type FsManifest,
     8	  fsManifest,
     9	  LIMITS,
    10	  type ProgramManifest,
    11	  programManifest,
    12	} from "@tabframe/protocol";
    13	import { validateModuleBytes } from "@tabframe/sandbox";
    14	import type { StoreDriver } from "@tabframe/store";
    15	
    16	export type BundleResolution =
    17	  | {
    18	      ok: true;
    19	      bundle: string;
    20	      module: string;
    21	      manifest: ProgramManifest;
    22	      files: FsManifest["files"];
    23	    }
    24	  | { ok: false; reason: string };
    25	
    26	/** Total bytes a bundle's own files may occupy, so an upload cannot fill the store on its own. */
    27	export const MAX_BUNDLE_BYTES = 64 * 1024 * 1024;
    28	
    29	/** What a launch may make the control plane read before it says no (WP8.1). */
    30	const BUNDLE_MANIFEST_CAP = 1024 * 1024;
    31	const PROGRAM_MANIFEST_CAP = 64 * 1024;
    32	
    33	export async function resolveBundle(
    34	  store: StoreDriver,
    35	  bundle: string,
    36	  memoryPagesMax: number,
    37	): Promise<BundleResolution> {
    38	  const manifestBytes = await store.get(bundle, BUNDLE_MANIFEST_CAP);
    39	  if (!manifestBytes) return { ok: false, reason: "the bundle manifest is not in the store" };
… (365 more lines)
```

**Result of `Bash`:**

```
     1	// The editor's pure parts (design §5.6): the virtual filesystem a compile sees, what a compile
     2	// reports, the manifest and params the reviewer edits, the bundle a launch uploads, and the
     3	// checks on a dropped module. No DOM, no sockets, no compiler: the UI (editor.ts) and the worker
     4	// (compiler-worker.ts) are thin around this, and the tests run it under Bun.
     5	import {
     6	  BUNDLE_PATHS,
     7	  canonicalStringify,
     8	  type FsManifest,
     9	  LIMITS,
    10	  type ProgramManifest,
    11	  programManifest,
    12	} from "@tabframe/protocol";
    13	import { validateModuleBytes } from "@tabframe/sandbox/validate";
    14	import { sha256Hex } from "@tabframe/store/hash";
    15	import {
    16	  GUIDE_MARKDOWN,
    17	  HELLO_MANIFEST,
    18	  HELLO_SOURCE,
    19	  MANDELBROT_MANIFEST,
    20	  MANDELBROT_SOURCE,
    21	  SDK_FILES,
    22	  WORDCOUNT_MANIFEST,
    23	  WORDCOUNT_SOURCE,
    24	} from "./program-sources.generated.ts";
    25	
    26	/** The compiler flags every program uses; must equal the SDK build's (a test pins it). */
    27	export const ASC_FLAGS: readonly string[] = [
    28	  "-O3",
    29	  "--runtime",
    30	  "stub",
    31	  "--noAssert",
    32	  "--maximumMemory",
    33	  "256",
    34	];
    35	/** The sandbox's reference memory maximum, in 64 KiB pages. */
    36	export const MEMORY_PAGES_MAX = 256;
    37	
    38	/** Where the edited program lives in the virtual filesystem, and where the SDK lives. */
    39	export const ENTRY = "program/assembly/index.ts";
    40	export const SDK_ROOT = "node_modules/@tabframe/sdk-as";
… (362 more lines)
```

**Result of `Bash`:**

```
     1	import { canonicalStringify, PROTOCOL_VERSION, RELEASED, type Result } from "@tabframe/protocol";
     2	import type { Effect } from "./events.ts";
     3	import type {
     4	  ExecutionRecord,
     5	  Ledger,
     6	  NodeRecord,
     7	  ResultRecord,
     8	  TaskRecord,
     9	  WriteRecord,
    10	} from "./ledger.ts";
    11	import { broadcast } from "./observers.ts";
    12	import { cancelOthers, runningAttempts } from "./scheduler.ts";
    13	
    14	/** Equal identities mean identical output bytes and identical written files (design §5.4). */
    15	export function resultIdentity(output: string, writes: WriteRecord[]): string {
    16	  const sorted = [...writes].sort((a, b) => (a.path < b.path ? -1 : a.path > b.path ? 1 : 0));
    17	  return canonicalStringify({ o: output, w: sorted.map((w) => [w.path, w.hash, w.size]) });
    18	}
    19	
    20	export type Settlement =
    21	  | { kind: "none" }
    22	  | { kind: "done"; task: TaskRecord; result: ResultRecord }
    23	  | { kind: "failed"; task: TaskRecord; reason: string }
    24	  | { kind: "contested"; task: TaskRecord };
    25	
    26	/**
    27	 * A result arrived (design §6.5, D7). Updates node statistics, records the result, and settles the
    28	 * task when the required agreement is reached; disagreement discards the round and recomputes,
    29	 * and a third disagreement is resolved by majority. Returns the settlement so the caller can
    30	 * advance the stage.
    31	 */
    32	export function onResult(
    33	  ledger: Ledger,
    34	  node: NodeRecord,
    35	  msg: Result,
    36	  now: number,
    37	): { effects: Effect[]; settlement: Settlement } {
    38	  const effects: Effect[] = [];
    39	  const task = ledger.tasks.get(msg.taskId);
    40	  // The result names its attempt. Only that attempt closes: a stale report for an attempt that was
… (301 more lines)
```

**Result of `Bash`:**

```
     1	import type {
     2	  Counters,
     3	  ExecutionView,
     4	  FsManifest,
     5	  Health,
     6	  NodeKind,
     7	  NodeView,
     8	  Place,
     9	  ProgramManifest,
    10	  QueueEntry,
    11	  TaskLimits,
    12	  TaskView,
    13	} from "@tabframe/protocol";
    14	
    15	/** One live node. Everything the control plane knows about a core (design §6.2). */
    16	export interface NodeRecord {
    17	  nodeId: string;
    18	  connId: string;
    19	  hostId: string;
    20	  kind: NodeKind;
    21	  cores: number;
    22	  sandboxVersion: string;
    23	  joinedAt: number;
    24	  lastSeen: number;
    25	  visible: boolean;
    26	  health: Health;
    27	  tasksDone: number;
    28	  lastTaskMs: number | null;
    29	  /** Exponentially weighted compute time, the basis of fast/slow. */
    30	  ewmaMs: number | null;
    31	  /** Task ids with an open attempt on this node. */
    32	  inFlight: string[];
    33	  /** The last demo command applied, so resumeAll knows whom to resume. */
    34	  commanded: "throttle" | "freeze" | null;
    35	}
    36	
    37	/** One dashboard connection. */
    38	export interface ObserverRecord {
    39	  connId: string;
    40	  subscribedAt: number;
… (391 more lines)
```

**Result of `Read`:**

```
1	// The host page (design §3): one observer socket for the dashboard, zero or more node workers.
2	// The canvas is the hero: finished tiles land on it as they are verified, and the scheduler's
3	// state is drawn over the rectangles that are still open. Around it, the panels of dashboard v2:
4	// programs, queue, the stage strip, the result of a bars or text program, files, task detail, the
5	// ledger; and the polish of WP4.1: flashes with a log of what just moved, a legend, the rotation
6	// and sleep banners, a throughput chart, and a spawn hint sized to this browser's cores.
7	
8	import type { HostToWorker, WorkerToHost } from "@tabframe/node/platform/web";
9	import type { NodeView, PlaceView } from "@tabframe/protocol";
10	import { PROTOCOL_VERSION } from "@tabframe/protocol";
11	import { connectionCopy, fmtCountdown, machineCopy, ROTATING_DETAIL } from "./banners.ts";
12	import { DEMO_CYCLE, type DemoHandle, type DemoProgram, startDemo } from "./demo.ts";
13	import { type ControlRequest, type MachineState, ObserverClient } from "./observer.ts";
14	import { gridIndexAt, gridLayout, mountPanels, type Panels } from "./panels.ts";
15	import {
16	  applyMessage,
17	  type ClusterState,
18	  emptyState,
19	  headerSlot,
20	  hostCount,
21	  inFlightByNode,
22	  isFlashing,
23	  isRunning,
24	  loopLabel,
25	  loopState,
26	  machineBanner,
27	  machineSentence,
28	  type Pulse,
29	  planTask,
30	  progress,
31	  stageTasks,
32	  stopTitle,
33	  TASK_COLOR_LABELS,
34	  type TaskColor,
35	  taskColor,
36	  throughput,
37	  throughputSeries,
38	  withRedundancy,
39	} from "./state.ts";
40	import {
… (1084 more lines)
```

**Result of `Read`:**

```
1	import type {
2	  ControlPlaneToObserver,
3	  Counters,
4	  ExecutionView,
5	  MachineView,
6	  NodeView,
7	  ProgramView,
8	  QueueEntry,
9	  Snapshot,
10	  TaskLog,
11	  TaskView,
12	} from "@tabframe/protocol";
13	
14	/** Throughput is computed on the dashboard from `taskDone` arrival times over this window (design §6.7). */
15	export const THROUGHPUT_WINDOW_MS = 5_000;
16	/** How long a reassignment, a retraction, or a control's victims stay highlighted. */
17	export const FLASH_MS = 1_500;
18	/** The throughput chart looks back this far, one bucket per second (design §6.7). */
19	export const CHART_WINDOW_MS = 60_000;
20	/** Flash-worthy events kept for the pulse list beside the grid. */
21	export const PULSE_CAP = 12;
22	/** Notable events kept for the activity list. */
23	export const ACTIVITY_CAP = 60;
24	/** Attempt records kept per task; a task that churns more than this keeps the latest. */
25	export const HISTORY_CAP = 16;
26	
27	/** What became of one attempt at a task, as the event stream told it (design §6.4, §6.5). */
28	export type AttemptOutcome =
29	  | "running"
30	  | "done"
31	  | "verified"
32	  | "released"
33	  | "mismatch"
34	  | "retracted"
35	  | "cancelled"
36	  | "failed";
37	
38	export interface AttemptRecord {
39	  attempt: number;
40	  nodeId: string;
… (1257 more lines)
```

**Result of `Read`:**

```
1	// Dashboard v2 panels (design §5.1, §6.7, §8.3): programs, queue, the stage strip, the result of
2	// a `bars` or `text` program, the files behind an execution's root, a task's detail, the ledger
3	// of settled tasks (hashes and where their bytes live), and failure surfacing. The state comes from `state.ts`; bytes come from the store by hash; controls go back
4	// through the observer socket. No framework: each panel is a render function over the state that
5	// rebuilds its DOM only when what it shows has changed, so buttons stay put under a finger.
6	import type { ControlRequest } from "./observer.ts";
7	import {
8	  barRows,
9	  type FileEntry,
10	  finalOutput,
11	  fmtBytes,
12	  fmtValue,
13	  groupFiles,
14	  listFiles,
15	  type Preview,
16	  parseManifest,
17	  previewOf,
18	  readBars,
19	  TEXT_PREVIEW_BYTES,
20	} from "./result.ts";
21	import {
22	  type AttemptRecord,
23	  type ClusterState,
24	  type ExecutionState,
25	  ledgerRows,
26	  type ProgramInfo,
27	  programList,
28	  stageStrip,
29	  type TaskState,
30	} from "./state.ts";
31	import type { BlobSource } from "./tiles.ts";
32	
33	export interface PanelDeps {
34	  /** The store as the page reaches it right now (the demo's in-memory store, or the CDN). */
35	  blobs(): BlobSource;
36	  /** Where the store's blobs are addressed from, `<base>/<hash>`; null for the demo's in-page store. */
37	  storeBase(): string | null;
38	  /** Issue a control; false when the machine is not there to take it. */
39	  send(control: ControlRequest): boolean;
40	  /** Ask the page to render again once a fetch has landed or a panel's own state moved. */
… (1083 more lines)
```

**Result of `Read`:**

```
1	// The in-page editor (design §5.6): the Mandelbrot source prefilled, the compiler in a lazily
2	// loaded worker, diagnostics with line numbers, a manifest and params form, compile → bundle →
3	// launch through the observer socket, and the drop-a-`.wasm` door. Loaded by host.ts on demand,
4	// so the dashboard never pays for it.
5	import { BUNDLE_PATHS, fsManifest, programManifest } from "@tabframe/protocol";
6	import { type PresignRequester, StoreClient } from "@tabframe/store/client";
7	import { sha256Hex } from "@tabframe/store/hash";
8	import type { WorkerReply, WorkerRequest } from "./compiler-worker.ts";
9	import {
10	  ASC_FLAGS,
11	  assembleSources,
12	  buildBundle,
13	  buildManifest,
14	  type CompileResult,
15	  type Diagnostic,
16	  examples,
17	  fmtBytes,
18	  formatDiagnostic,
19	  type InputRef,
20	  inspectModule,
21	  looksLikeWasm,
22	  MANDELBROT_SOURCE,
23	  MAX_MODULE_BYTES,
24	  type ModuleInfo,
25	  parseParams,
26	  shippedManifest,
27	} from "./editor-core.ts";
28	import { type ClusterState, type ProgramInfo, programList } from "./state.ts";
29	
30	/** What the page gives the editor: the socket's upload and launch paths, and the cluster feed. */
31	export interface EditorHost {
32	  connected(): boolean;
33	  storeBase(): string | null;
34	  presign: PresignRequester["presign"];
35	  launch(bundle: string, params: Record<string, unknown>): boolean;
36	  subscribe(listener: (state: ClusterState) => void): () => void;
37	  /** Where the compiler worker script lives; defaults to `compiler-worker.js` next to this module. */
38	  workerUrl?: string;
39	  /** A demo page: the compile is real, launching needs the live machine (WP7.7). */
40	  demo?: boolean;
… (628 more lines)
```

**Tool `Bash`:**

```
for f in packages/web/src/editor-page.ts packages/web/src/compiler.ts packages/web/src/compiler-worker.ts packages/web/src/guide-page.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/web/src/observer.ts packages/web/src/banners.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
for f in packages/web/src/result.ts packages/web/src/tiles.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== packages/web/src/editor-page.ts =====
     1	// The editor in its own tab (WP6.4): its own observer socket, the same editor as before, and a
     2	// pause on the machine for as long as this tab lives — launching or closing resumes it, and so
     3	// does the control plane by itself when this socket goes away.
     4	import { mountEditor } from "./editor.ts";
     5	import { type MachineState, ObserverClient } from "./observer.ts";
     6	import type { ClusterState } from "./state.ts";
     7	
     8	const $ = <T extends Element>(sel: string): T => {
     9	  const el = document.querySelector<T>(sel);
    10	  if (!el) throw new Error(`missing ${sel}`);
    11	  return el;
    12	};
    13	const machineEl = $<HTMLSpanElement>("#machine");
    14	const pauseEl = $<HTMLSpanElement>("#pauseState");
    15	const listeners = new Set<(state: ClusterState) => void>();
    16	let storeBase: string | null = null;
    17	let client: ObserverClient | null = null;
    18	let paused = false;
    19	/** Whether this tab still wants the machine held (WP8.1): not after its launch, not after close. */
    20	let holdWanted = true;
    21	
    22	function setMachine(state: MachineState, detail?: string): void {
    23	  machineEl.textContent = detail ? `${state} · ${detail}` : state;
    24	  machineEl.className = `pill ${state === "live" ? "live" : state === "off" || state === "outdated" ? "off" : "wait"}`;
    25	  if (state === "live") {
    26	    // Every live socket asks again — a reconnect (a rotation, a gap) is a new holder — but only
    27	    // while this tab still wants the hold (WP8.1): after its launch a resubscribe used to pause
    28	    // the machine again under the person's own running program.
    29	    if (!holdWanted) return;
    30	    client?.send({ t: "pause" });
    31	    paused = true;
    32	    pauseEl.textContent =
    33	      "the machine is paused while this tab is open — in-flight tasks finish, nothing new starts; launch or close to resume";
    34	  } else if (state === "off" || state === "outdated") {
    35	    paused = false;
    36	    pauseEl.textContent = "no machine to pause";
    37	  }
    38	}
    39	
… (242 more lines)
```

**Tool `Bash`:**

```
cat -n packages/dev/src/transcripts.ts; echo "===== scripts/transcripts/export.ts ====="; cat -n scripts/transcripts/export.ts
```

**Result of `Bash`:**

```
===== packages/web/src/observer.ts =====
     1	import { Backoff } from "@tabframe/node/backoff";
     2	import { fetchSession, type Session, socketProtocols } from "@tabframe/node/session";
     3	import {
     4	  CLOSE,
     5	  type Control,
     6	  type ControlPlaneToObserver,
     7	  controlPlaneToObserver,
     8	  decode,
     9	  encode,
    10	  LIMITS,
    11	  type ObserverToControlPlane,
    12	  PROTOCOL_VERSION,
    13	} from "@tabframe/protocol";
    14	import type { PresignedUpload, PresignItem } from "@tabframe/store";
    15	import { applyMessage, type ClusterState, emptyState, withRedundancy } from "./state.ts";
    16	
    17	export type MachineState = "connecting" | "starting" | "off" | "live" | "outdated";
    18	
    19	/** A control as the page issues it; the client stamps the version and generation. */
    20	export type ControlRequest = Control extends infer C
    21	  ? C extends { v: number; gen: number }
    22	    ? Omit<C, "v" | "gen">
    23	    : never
    24	  : never;
    25	
    26	export interface ObserverHandlers {
    27	  /** Controls held across a reconnect that were too old to send (WP8.1). */
    28	  onDropped?: (count: number) => void;
    29	  onState(machine: MachineState, detail?: string): void;
    30	  onCluster(state: ClusterState): void;
    31	  onSession(session: Session & { kind: "on" }): void;
    32	}
    33	
    34	/** Anything the page sends besides subscribe and ping, before the version and generation are stamped. */
    35	type Outgoing = ControlRequest | { t: "presign"; items: PresignItem[] };
    36	
    37	/** How long a presign may stay unanswered before the upload gives up. */
    38	export const PRESIGN_TIMEOUT_MS = 30_000;
    39	
… (412 more lines)
```

**Result of `Bash`:**

```
===== packages/web/src/result.ts =====
     1	// What an execution produced, read from the store by hash (design §5.1, §5.4): the filesystem
     2	// manifest behind `execution.root`, the final output of a `bars` or `text` program, and previews
     3	// of the files a person clicks on. Pure functions over bytes; the page fetches.
     4	import {
     5	  AbiError,
     6	  BARS_LIMITS,
     7	  type Bar,
     8	  BUNDLE_PATHS,
     9	  decodeBars,
    10	  type FsManifest,
    11	  fsManifest,
    12	} from "@tabframe/protocol";
    13	
    14	export interface FileEntry {
    15	  path: string;
    16	  hash: string;
    17	  size: number;
    18	}
    19	
    20	/** Files grouped for the panel: the bundle's own, then each stage's outputs, then everything else. */
    21	export interface FileGroup {
    22	  /** `/in/`, `/out/<stage>/`, `/` for the bundle's two fixed files, or the first path segment. */
    23	  label: string;
    24	  files: FileEntry[];
    25	  bytes: number;
    26	}
    27	
    28	export function parseManifest(bytes: Uint8Array): FsManifest {
    29	  return fsManifest.parse(JSON.parse(new TextDecoder().decode(bytes)));
    30	}
    31	
    32	/** Entries sorted by path, with `/out/<stage>/<index>` sorted numerically inside a stage. */
    33	export function listFiles(manifest: FsManifest): FileEntry[] {
    34	  return Object.entries(manifest.files)
    35	    .map(([path, f]) => ({ path, hash: f.hash, size: f.size }))
    36	    .sort((a, b) => comparePaths(a.path, b.path));
    37	}
    38	
    39	export function groupFiles(files: readonly FileEntry[]): FileGroup[] {
… (360 more lines)
```

**Result of `Bash`:**

```
     1	// Transcript export (plan WP5.4): a Claude Code session transcript — JSONL, one event per line —
     2	// rendered as readable Markdown with the secrets scrubbed. The build's transcripts are submitted
     3	// with the code; every one of them contains an account id, MicroVM endpoints, and proxy tokens in
     4	// tool output, so nothing leaves this module unscrubbed. Tool results are truncated so the export
     5	// reads as a conversation rather than a log dump; user and assistant text are kept in full.
     6	
     7	/** A scrub pattern: what it matches, what replaces it, and the name the summary counts under. */
     8	export interface ScrubPattern {
     9	  name: string;
    10	  re: RegExp;
    11	  replacement: string;
    12	}
    13	
    14	/**
    15	 * Applied in this order: a presigned URL carries a token and often an id, so it goes first and
    16	 * whole; tokens before ids; account ids before emails (an email never contains twelve digits, but
    17	 * a replacement must not create a new match). Every pattern is global.
    18	 */
    19	export const PATTERNS: readonly ScrubPattern[] = [
    20	  {
    21	    name: "presigned-url",
    22	    re: /https?:\/\/[^\s"'<>)\]]*X-Amz-Signature[^\s"'<>)\]]*/g,
    23	    replacement: "<presigned-url>",
    24	  },
    25	  // JWE/JWS compact serialization: base64url parts joined by dots, an empty part allowed (`..`).
    26	  {
    27	    name: "token",
    28	    re: /\beyJ[A-Za-z0-9_-]{8,}(?:\.{1,2}[A-Za-z0-9_-]+)*/g,
    29	    replacement: "<token>",
    30	  },
    31	  {
    32	    name: "microvm-endpoint",
    33	    // A hostname cut short by a terminal's column limit is still a hostname: the suffix is optional.
    34	    re: /\b[a-z0-9-]+\.lambda-microvm(?:\.[a-z0-9-]+)*(?:\.on\.aws)?\b/g,
    35	    replacement: "<microvm-endpoint>",
    36	  },
    37	  {
    38	    name: "lambda-url",
    39	    re: /\b[a-z0-9-]+\.lambda-url\.[a-z0-9-]+\.on\.aws\b/g,
    40	    replacement: "<lambda-url>",
… (381 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; echo "===== synth test: RunMicrovm / image scoping assertions ====="; grep -n -i -E "RunMicrovm|anyImage|microvm-image|Resource" packages/infra/test/synth.test.ts | head -40; echo; echo "===== dist compiler worker / binaryen: eval usage (CSP) ====="; for f in packages/web/dist/compiler-worker.js packages/web/dist/binaryen.js packages/web/dist/host.js packages/web/dist/node.js packages/web/dist/sandbox.js packages/web/dist/editor-page.js; do if [ -f "$f" ]; then printf "%s: " "$f"; grep -o -E "\beval\(|new Function\(|Function\(\"|importScripts\(|document\.write|unsafe-eval" "$f" | sort | uniq -c | tr '\n' ' '; echo; else echo "$f: (not built)"; fi; done; echo; echo "===== inline handlers / third-party https in public+src ====="; grep -n -E "on[a-z]+=\"|javascript:|https://" packages/web/public/*.html packages/web/public/styles.css packages/web/src/*.ts | grep -v "\.test\.ts" | grep -v -E "^\s*//" | head -30; echo; echo "===== href/location/open sinks in web/src ====="; grep -n -E "\.href\s*=|location\.(href|assign|replace)|window\.open\(|\.src\s*=|setAttribute\(\"(href|src)\"|target=\"_blank\"|\.download\s*=" packages/web/src/*.ts | grep -v "\.test\.ts" | head -40
```

**Result of `Bash`:**

```
===== synth test: RunMicrovm / image scoping assertions =====
56:    core.resourceCountIs("AWS::S3::Bucket", 4);
57:    core.hasResourceProperties("AWS::S3::Bucket", {
67:    core.hasResourceProperties("AWS::S3::Bucket", {
70:    core.allResourcesProperties("AWS::S3::Bucket", {
79:    core.resourceCountIs("AWS::CloudFront::Distribution", 1);
80:    core.hasResourceProperties("AWS::CloudFront::Distribution", {
93:    core.hasResourceProperties("AWS::SSM::Parameter", {
98:    core.resourceCountIs("AWS::SecretsManager::Secret", 1);
102:    core.resourceCountIs("AWS::Budgets::Budget", 0);
113:    withBudget.hasResourceProperties("AWS::Budgets::Budget", {
141:    image.resourceCountIs("AWS::Lambda::MicrovmImage", 1);
142:    image.hasResourceProperties("AWS::Lambda::MicrovmImage", {
144:      BaseImageArn: "arn:aws:lambda:us-west-2:aws:microvm-image:al2023-1",
147:      Resources: [{ MinimumMemoryInMiB: 1024 }],
160:    const [props] = Object.values(image.findResources("AWS::Lambda::MicrovmImage")).map(
170:    image.resourceCountIs("AWS::IAM::Role", 3);
171:    image.allResourcesProperties("AWS::IAM::Role", {
184:    const policies = Object.values(image.findResources("AWS::IAM::Policy")).map((p) =>
190:    expect(coreRolePolicy[0]).not.toContain("lambda:RunMicrovm");
199:    fleet.resourceCountIs("AWS::Lambda::Function", 3);
200:    fleet.resourceCountIs("Custom::LogRetention", 2);
201:    fleet.hasResourceProperties("AWS::Lambda::Function", {
206:    fleet.hasResourceProperties("AWS::Lambda::Function", {
219:    fleet.hasResourceProperties("AWS::Lambda::Url", { AuthType: "NONE" });
220:    const urls = Object.values(fleet.findResources("AWS::Lambda::Url"));
224:    const fns = Object.values(fleet.findResources("AWS::Lambda::Function"));
230:    fleet.hasResourceProperties("AWS::Events::Rule", {
238:    const policies = Object.values(fleet.findResources("AWS::IAM::Policy")).map((p) =>
244:    expect(sessionPolicy).not.toContain("lambda:RunMicrovm");
246:    expect(rotatePolicy).toContain("lambda:RunMicrovm");
274:    web.hasResourceProperties("Custom::CDKBucketDeployment", {
278:    const deployments = Object.values(web.findResources("Custom::CDKBucketDeployment"));
287:    const images = Object.values(image.findResources("AWS::Lambda::MicrovmImage"));

===== dist compiler worker / binaryen: eval usage (CSP) =====
packages/web/dist/compiler-worker.js:       2 document.write       1 Function(" 
packages/web/dist/binaryen.js: 
packages/web/dist/host.js: 
packages/web/dist/node.js: 
… (16 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; echo "===== endpoint format / forwarded headers in docs ====="; grep -rn -i -E "x-forwarded|forwarded-for|sourceIp|source ip|client address|\.on\.aws|microvms\.[a-z0-9-]+\.amazonaws|endpoint looks|endpoint host" docs/*.md docs/implementation/*.md packages --include=*.md --include=*.ts 2>/dev/null | grep -v node_modules | grep -v transcripts | head -30; echo; echo "===== Content-Security-Policy mentions outside core-stack ====="; grep -rn "Content-Security-Policy\|contentSecurityPolicy\|csp" --include=*.ts --include=*.md -i packages e2e docs 2>/dev/null | grep -v node_modules | grep -v "docs/transcripts" | grep -v core-stack | head -20; echo; echo "===== transcripts: secret-shaped strings (counts only) ====="; for pat in '\b[0-9]{12}\b' 'AKIA[0-9A-Z]{16}' 'X-Amz-Signature=' 'X-Amz-Credential=' 'lambda-microvms\.authentication\.' 'eyJ[A-Za-z0-9_-]{20,}' 'x-tabframe-fleet-secret: [A-Za-z0-9]{20,}' 'arn:aws:[a-z-]+:[a-z0-9-]*:[0-9]{12}' '@[a-z0-9-]+\.(com|org|net)\b'; do printf "%-45s " "$pat"; grep -r -E -o "$pat" docs/transcripts/ 2>/dev/null | wc -l; done; echo; echo "===== repo (non-transcript) for account-id / email shapes ====="; grep -rn -E '\b[0-9]{12}\b' --include=*.ts --include=*.json --include=*.toml --include=*.md --include=*.yml . 2>/dev/null | grep -v node_modules | grep -v cdk.out | grep -v "docs/transcripts" | grep -v bun.lock | grep -v -E "coverage|test-results" | head -10; grep -rn -E '[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[a-z]{2,}' --include=*.ts --include=*.json --include=*.toml --include=*.md --include=*.yml . 2>/dev/null | grep -v node_modules | grep -v cdk.out | grep -v "docs/transcripts" | grep -v bun.lock | grep -v -E "coverage|test-results|noreply|example\.|@aws-sdk|@tabframe|@types|@biomejs|@playwright" | head -10; echo; echo "===== ws / zod versions ====="; grep -n -E '^\s*"(ws|zod|@aws-sdk/client-s3|assemblyscript)@' bun.lock | head; echo; echo "===== demo.ts network/DOM sinks ====="; grep -n -E "fetch\(|WebSocket|innerHTML|\.href|postMessage|localStorage|sessionStorage" packages/web/src/demo.ts | head -20; echo; echo "===== core-reaper / cores.gone semantics test names ====="; grep -n -E "test\(|it\(" packages/control-plane/src/core-reaper.test.ts packages/control-plane/src/cores.test.ts | head -30
```

**Result of `Bash`:**

```
===== endpoint format / forwarded headers in docs =====
docs/implementation/wp-8.1-review-loop-1.md:41:| 22 | sec | M | Sixteen sockets from one client lock everyone out. | **Deferred (loop 2).** Needs the client address at the endpoint; documented in §9.7. |
docs/implementation/wp-5.1-packaging.md:17:  and the corpus attribution. The deployed URL is in it; no account id, endpoint hostname, token,
docs/implementation/wp-5.1-packaging.md:37:  uses), MicroVM endpoint hostnames, Lambda function URL hostnames (added to the asked-for list:
packages/fleet/src/testing/fake.ts:115:      endpoint: `${info.microvmId}.lambda-microvm.us-west-2.on.aws`,
packages/fleet/test/cp-client.test.ts:5:const target = { microvmId: "mvm-1", endpoint: "<microvm-endpoint>" };
packages/fleet/test/pointer.test.ts:35:      endpoint: "mvm-1.on.aws",
packages/fleet/test/pointer.test.ts:39:      pending: { microvmId: "mvm-2", endpoint: "mvm-2.on.aws", generation: 13 },
packages/fleet/test/adapters.test.ts:43:      endpoint: "https://<microvm-endpoint>/",
packages/fleet/test/adapters.test.ts:67:      endpoint: "<microvm-endpoint>",
packages/fleet/test/adapters.test.ts:250:    expect(normalizeEndpoint("https://a.b.on.aws/")).toBe("a.b.on.aws");
packages/fleet/test/adapters.test.ts:251:    expect(normalizeEndpoint("a.b.on.aws")).toBe("a.b.on.aws");
packages/fleet/test/ops.test.ts:63:      endpoint: "cp-1.on.aws",
packages/fleet/test/session.test.ts:77:    microvms.add({ microvmId: "mvm-1", endpoint: "<microvm-endpoint>" });
packages/fleet/test/session.test.ts:82:    expect(a.endpoint).toBe("<microvm-endpoint>");
packages/fleet/test/session.test.ts:150:        pointerStoreWith({ state: "on", microvmId: "mvm-1", endpoint: "from-pointer.on.aws" }),
packages/fleet/test/session.test.ts:154:    expect(res.endpoint).toBe("from-pointer.on.aws");
packages/fleet/test/rotate.test.ts:24:  sessionUrl: "https://<lambda-url>/",
packages/fleet/test/rotate.test.ts:117:    expect(written?.endpoint).toBe(`${result.microvmId}.lambda-microvm.us-west-2.on.aws`);
packages/fleet/test/rotate.test.ts:264:      endpoint: "old.on.aws",
packages/fleet/test/rotate.test.ts:332:      endpoint: "old.on.aws",

===== Content-Security-Policy mentions outside core-stack =====
docs/design.md:964:  buckets are retained; response headers (a CSP, nosniff, downloads for blobs) on the distribution
docs/implementation/wp-8.1-review-loop-1.md:39:| 20 | sec | M | Blobs are served on the page's origin with no `nosniff`, no CSP. | **Accepted.** CloudFront response-header policies: nosniff, DENY framing, HSTS, a CSP for the page; `Content-Disposition: attachment` and a sandbox CSP for `blob/*`. |

===== transcripts: secret-shaped strings (counts only) =====
\b[0-9]{12}\b                                 0
AKIA[0-9A-Z]{16}                              0
X-Amz-Signature=                              3
X-Amz-Credential=                             0
lambda-microvms\.authentication\.             8
eyJ[A-Za-z0-9_-]{20,}                         0
x-tabframe-fleet-secret: [A-Za-z0-9]{20,}     0
arn:aws:[a-z-]+:[a-z0-9-]*:[0-9]{12}          0
@[a-z0-9-]+\.(com|org|net)\b                  0

===== repo (non-transcript) for account-id / email shapes =====
packages/infra/scripts/mask.test.ts:6:    expect(maskAccount("************")).toBe("************");
packages/infra/scripts/mask.test.ts:9:    expect(maskAccount("arn:aws:iam::************:user/tab AIDA6CYK2IGRUYFECWJOT")).toBe(
… (20 more lines)
```

> **Thinking.** 

> **Thinking.** I need to confirm two things before writing up: whether the built compiler worker's `Function("…")` call sits behind a never-taken branch (since CSP would otherwise block it), and the test assertions backing the loop-1 image scoping. I'll also independently verify the masked presigned-URL/subprotocol transcript context, the S3 presigner's signed-header assertions, and the dependency versions.

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; echo "===== compiler-worker.js: context around Function(\" and document.write ====="; grep -o -E ".{160}Function\(\".{100}" packages/web/dist/compiler-worker.js | sed -E 's/[A-Za-z0-9+\/=]{60,}/<b64>/g' | head -3; echo "---"; grep -o -E ".{100}document\.write.{60}" packages/web/dist/compiler-worker.js | head -3; echo; echo "===== dist ages vs src ====="; ls -la --time-style=long-iso packages/web/dist/*.js 2>/dev/null | awk '{print $6, $7, $8}'; stat -c '%y %n' packages/web/src/compiler-worker.ts packages/web/src/compiler.ts | cut -c1-19,36-; echo; echo "===== synth.test.ts 180-200 and 236-252 ====="; sed -n '180,200p;236,252p' packages/infra/test/synth.test.ts; echo; echo "===== store.test.ts: signed header assertions ====="; grep -n -i -E "SignedHeaders|content-length|signableHeaders|checksum" packages/store/src/store.test.ts | head -20
```

**Result of `Bash`:**

```
===== compiler-worker.js: context around Function(" and document.write =====
(p.stack_pointer,this.ptrType),i.i32(0),_.I32,e-r)))}}makeStackCheck(){let e=this.module;return this.hasStackCheckFunction||(this.hasStackCheckFunction=!0,e.addFunction("~stack_check",_.None,_.None,null,e.if(e.binary(17,e.global_get(p.stack_pointer,this.ptrType),e.globa
---

===== dist ages vs src =====
2026-09-02 01:10 packages/web/dist/binaryen.js
2026-09-04 08:23 packages/web/dist/compiler-worker.js
2026-09-02 21:58 packages/web/dist/editor.js
2026-09-04 08:23 packages/web/dist/editor-page.js
2026-09-04 08:23 packages/web/dist/guide-page.js
2026-09-04 08:23 packages/web/dist/host.js
2026-09-04 08:23 packages/web/dist/node.js
2026-09-04 08:23 packages/web/dist/sandbox.js
2026-09-02 01:16:57 packages/web/src/compiler-worker.ts
2026-09-02 01:16:57 packages/web/src/compiler.ts

===== synth.test.ts 180-200 and 236-252 =====
    });
  });

  test("the control-plane role can pass only the core role, and the core role has no MicroVM actions", () => {
    const policies = Object.values(image.findResources("AWS::IAM::Policy")).map((p) =>
      JSON.stringify(p.Properties),
    );
    expect(policies.some((p) => p.includes("iam:PassRole") && p.includes("CoreRole"))).toBe(true);
    const coreRolePolicy = policies.filter((p) => p.includes('"Roles":[{"Ref":"CoreRole'));
    expect(coreRolePolicy.length).toBe(1);
    expect(coreRolePolicy[0]).not.toContain("lambda:RunMicrovm");
  });
});

describe("Fleet stack", () => {
  const { fleet } = synth();

  test("two Node 22 arm64 functions with the design's names and concurrency", () => {
    // Ours, plus the retention provider CDK adds for `logRetention` on existing log groups.
    fleet.resourceCountIs("AWS::Lambda::Function", 3);
    fleet.resourceCountIs("Custom::LogRetention", 2);

  test("session may invoke rotate but may not run MicroVMs", () => {
… (35 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; echo "===== transcripts: X-Amz-Signature contexts (values masked) ====="; grep -r -o -E ".{80}X-Amz-Signature=.{40}" docs/transcripts/ | sed -E 's/[A-Za-z0-9%_-]{16,}/<masked>/g' | head -5; echo; echo "===== transcripts: lambda-microvms.authentication contexts (values masked) ====="; grep -r -o -E ".{40}lambda-microvms\.authentication\..{60}" docs/transcripts/ | sed -E 's/[A-Za-z0-9._-]{16,}/<masked>/g' | sort | uniq -c | head -10; echo; echo "===== transcripts: any long base64url runs that look like tokens (count by file) ====="; grep -r -c -E "[A-Za-z0-9_-]{80,}" docs/transcripts/*.md | grep -v ":0" | head; echo; echo "===== docs (non-transcript): endpoint hostname format mentions ====="; grep -rn -i -E "lambda-microvm|endpoint.*on\.aws|hostname" docs/m0-verification.md docs/m3-verification.md docs/runbook.md 2>/dev/null | head -8; echo; echo "===== dependency versions ====="; grep -n -E '^    "(ws|zod|@aws-sdk/client-s3|@aws-sdk/s3-request-presigner|assemblyscript)": \["' bun.lock | head; grep -n -E '"(ws|zod)@' bun.lock | head -4; echo; echo "===== hello/hostId tests mentioning core impersonation ====="; grep -rn -E "core-microvm|microvmIdOfHost|hostId.*core" packages/core/src/*.test.ts packages/control-plane/src/*.test.ts | head -8
```

**Result of `Bash`:**

```
===== transcripts: X-Amz-Signature contexts (values masked) =====
docs/transcripts/<masked>.md:dacted>: $(cat *.md | grep -ci '<redacted>'); presigned: $(cat *.md | grep -Eo 'X-Amz-Signature=[0-9a-f]{10,}' | wc -l)"; head -30 index

===== transcripts: lambda-microvms.authentication contexts (values masked) =====
      1 docs/transcripts/<masked>:in via subprotocols `lambda-microvms`, `<masked><token>`, `<masked>`; 8h max duration; idle po
      1 docs/transcripts/<masked>:ken as subprotocols `lambda-microvms`, `<masked><token>`, `<masked>`, 8-hour max → hourly r

===== transcripts: any long base64url runs that look like tokens (count by file) =====
docs/transcripts/ab48791174a186c73.md:2
docs/transcripts/af4c7961d642942fe.md:2
docs/transcripts/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a.md:33

===== docs (non-transcript): endpoint hostname format mentions =====

===== dependency versions =====
186:    "@aws-sdk/client-s3": ["@aws-sdk/<email>", "", { "dependencies": { "@aws-sdk/checksums": "^3.1000.29", "@aws-sdk/core": "^3.977.9", "@aws-sdk/credential-provider-node": "^3.972.82", "@aws-sdk/middleware-sdk-s3": "^3.972.75", "@aws-sdk/signature-v4-multi-region": "^3.996.46", "@aws-sdk/types": "^3.974.5", "@smithy/core": "^3.33.3", "@smithy/fetch-http-handler": "^5.7.2", "@smithy/node-http-handler": "^4.11.3", "@smithy/types": "^4.17.2", "tslib": "^2.6.2" } }, "sha512-f20BksgVlXufcf/mijde1LAjwM/iSDdG3mGtN3yRFOUzXtlFEOjxNi1Rf2Kb+rSgoiOSpO0hUOna9Bs+J1gX2A=="],
218:    "@aws-sdk/s3-request-presigner": ["@aws-sdk/<email>", "", { "dependencies": { "@aws-sdk/core": "^3.977.9", "@aws-sdk/signature-v4-multi-region": "^3.996.46", "@aws-sdk/types": "^3.974.5", "@smithy/core": "^3.33.3", "@smithy/types": "^4.17.2", "tslib": "^2.6.2" } }, "sha512-e9R8FQD4QNAM+g2X8HB/wr5q4pgJXjFDMI+FOC759Ez70xG5Zo7lCBU/wVILSFQ2wOrT76F6kVPe3bMERxMf2w=="],
404:    "assemblyscript": ["<email>", "", { "dependencies": { "binaryen": "131.0.0-nightly.20260721", "long": "^5.2.4" }, "bin": { "asc": "bin/asc.js", "asinit": "bin/asinit.js" } }, "sha512-5PM7GZpvcLypHcLmi30aP8mqm5HjU+LL3BdV1xDyA3USGtL2w+iMiUnhtH+vW8+2kOuHlAyPu7M+QgyEw5he1g=="],
486:    "ws": ["<email>", "", { "peerDependencies": { "bufferutil": "^4.0.1", "utf-8-validate": ">=5.0.2" }, "optionalPeers": ["bufferutil", "utf-8-validate"] }, "sha512-201TZ/kPWxoPr/OKWjquZR1SWKXcvxdH+e1xrx89b3YbmzLMFCLfnaG1HFIgWzJOEWZ7MvpK++odZufgYR50Rw=="],
490:    "zod": ["<email>", "", {}, "sha512-sC95tT5iHHH9gtpj6A81kh+NEaRAUFN+qlUPDUbRfOMvNf5QCBqsb3WgvnpVtK5Y+4UfA6KqufotuTvMGiTlsA=="],
486:    "ws": ["<email>", "", { "peerDependencies": { "bufferutil": "^4.0.1", "utf-8-validate": ">=5.0.2" }, "optionalPeers": ["bufferutil", "utf-8-validate"] }, "sha512-201TZ/kPWxoPr/OKWjquZR1SWKXcvxdH+e1xrx89b3YbmzLMFCLfnaG1HFIgWzJOEWZ7MvpK++odZufgYR50Rw=="],
490:    "zod": ["<email>", "", {}, "sha512-sC95tT5iHHH9gtpj6A81kh+NEaRAUFN+qlUPDUbRfOMvNf5QCBqsb3WgvnpVtK5Y+4UfA6KqufotuTvMGiTlsA=="],

===== hello/hostId tests mentioning core impersonation =====
packages/core/src/fleet.test.ts:10:  microvmIdOfHost,
packages/core/src/fleet.test.ts:42:    h.hello("c1", "core-microvm-a", "core");
packages/core/src/fleet.test.ts:56:    h.hello("c1", "core-microvm-a", "core");
packages/core/src/fleet.test.ts:85:    h.hello("c2", "core-microvm-b", "core");
packages/core/src/fleet.test.ts:109:    h.hello("c1", "core-microvm-a", "core");
packages/core/src/fleet.test.ts:133:    h.hello("c1", "core-microvm-a", "core");
packages/core/src/fleet.test.ts:148:    h.hello("c1", "core-microvm-a", "core");
packages/core/src/fleet.test.ts:160:    g.hello("c2", "core-microvm-b", "core");
```

> **Thinking.** 

> **Thinking.** The `Function("` match is just `addFunction("...")` from the AssemblyScript compiler, not `eval`, so the CSP is safe—the other transcript items are just a grep command and scrubbed placeholders. Before writing up results, I'll classify the 33 long base64url strings in the transcript and run the loop-1 tests to confirm the fixes work.

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; echo "===== long base64url runs in transcripts, classified (prefix + length only) ====="; grep -r -h -o -E "[A-Za-z0-9_-]{80,}" docs/transcripts/*.md | awk '{ printf "%s… len=%d\n", substr($0,1,7), length($0) }' | sort | uniq -c | sort -rn | head -12
```

**Tool `Bash`:**

```
timeout 170 bun test packages/protocol/src/messages.test.ts packages/sandbox/test/validate.test.ts packages/core/src/review-loop-1.test.ts packages/store/src/store.test.ts packages/core/src/apply.test.ts 2>&1 | grep -E "^(\(pass\)|\(fail\)| [0-9]+ pass| [0-9]+ fail|Ran |error:|.*coverage.*threshold)" | sed -E 's/\[[0-9.]+m?s\]//' | tail -60
```

**Result of `Bash`:**

```
===== long base64url runs in transcripts, classified (prefix + length only) =====
      6 -------… len=85
      6 -------… len=80
      5 i-made-… len=85
      4 run-iso… len=81
      4 announc… len=107
      3 -------… len=81
      3 bundlin… len=87
      2 -------… len=83
      2 -------… len=82
      1 websock… len=88
      1 -------… len=91
      1 -------… len=90
```

**Result of `Bash`:**

```
 50 pass
 0 fail
Ran 50 tests across 5 files.
```

> **Thinking.** 

## Assistant · 2026-09-04T15:47:50.985Z

## Findings

1. `[H] core identity / fleet — packages/core/src/apply.ts:506-513, packages/core/src/executions.ts:775-781, packages/core/src/fleet.ts:79-85, packages/infra/lib/image-stack.ts:81-91 — onHello links or creates a CoreRecord from the client-chosen hostId (core-microvm-<id>, kind core); cloud-core ids are public in every snapshot (nodeView.hostId, ledger.ts:371-383), and if the vended endpoint hostname embeds the MicroVM id as fleet/src/testing/fake.ts:115 models, so is the control plane's own. A kill-half that picks the impostor, or just disconnecting and waiting CORE_LINK_TIMEOUT (2 min), makes the control plane call TerminateMicrovm on that id; fake records also count toward DESIRED_CORES (fleet.ts:90-97) and cost a GetMicrovm each per 30 s — any anonymous visitor can kill the cloud cores (possibly the control plane) repeatedly, starve the fleet, and churn RunMicrovm; this is the deferred core-identity item made concrete — never create a record in onHello, link only an existing unlinked record, add a random per-core token to the core run payload (cores.ts:31-40) and a coreToken field in hello and link only on match, replace hostId in nodeView with an opaque label.`

2. `[H] sandbox / scheduler — packages/core/src/results.ts:57-62 (RELEASED returns before compute accounting at :75, no attempt cap), packages/node/src/tasks.ts:91-94 and :191-198 — a program that spins (deadline) or aborts with text matching the hostFailure regex ("out of memory", "HTTP 500", "network error"…) is reported RELEASED by every node, re-queued at the front, never fails, never charges computeMsUsed; task.attempts grows unbounded (every record rides snapshots and /handover, MAX_LEDGER_BYTES 64 MB) and each cycle broadcasts two events to every observer — one upload (6/min, queue 32) burns every visitor's CPU and both cloud cores indefinitely, the §5.5 compute budget is bypassed, snapshots grow until rotation fails, and only a human pressing skip ends it — charge released attempts their deadline window (deadlineAt − assignedAt) against computeMsUsed, cap attempts per task (e.g. 8) and fail it as a program fault beyond that, and in tasks.ts never classify errors starting with abort:/trap:/link: as host failures.`

3. `[M] presign amplification (loop 1 regression) — packages/core/src/apply.ts:202-208, 224-231; packages/protocol/src/node.ts:63-68; packages/store/src/s3.ts:55-89 — the solicited bucket charges one token per presign message, each message carries 64 items, and each item costs a HeadObject plus getSignedUrl; sizes of 1 byte cost nothing against the byte budget — one node connection can drive ~16 k S3 HEADs and signatures per second, exhausting the 1 GB control plane's CPU (heartbeats stall, nodes declared gone) at roughly $23/hour of S3 requests — charge the bucket per item, allow one outstanding presign per connection (the clients already do), dedupe hashes.`

4. `[M] store write bound (loop 1 regression) — packages/core/src/apply.ts:174-181, 261, 266-273; packages/store/src/s3.ts:63-79 with store.test.ts:117 showing content-length is not a signed header — the 1 GiB budget is per socket (a reconnect refills it) and counts a self-declared size; S3 enforces only the checksum pin, so a presign for "1 byte" accepts a 5 GB object whose key is its own hash — unbounded S3 fill through the public endpoint, kept a year on a RETAIN bucket, against a notification-only budget — add content-length to signableHeaders (S3 then rejects a mismatched length; verify against the real bucket as WP1.10 did), budget per hostId or machine-wide per hour in meta, and tag uploads for a shorter lifecycle than the shipped bundles.`

5. `[M] IAM (loop 1 regression) — packages/infra/lib/fleet-stack.ts:44-52 scopes RunMicrovm to the image only when called with a single-action list, and both call sites (:82, :120-126) pass several, so rotate and session keep anyImageArn; packages/infra/lib/image-stack.ts:81-91 grants the control plane Run/Get/Terminate on any image; packages/infra/test/synth.test.ts:236-247 asserts action names only — loop-1 finding #24 is recorded as fixed but the deployed policy is unchanged, and the control plane (which acts on untrusted input, finding 1) may run or terminate any MicroVM in the account and pass any connector (PassNetworkConnector on *) — a separate statement RunMicrovm → image.imageArn for rotate and the control plane, and a synth assertion on that statement's Resource.`

6. `[M] endpoint quota / per-IP (deferred) — packages/control-plane/src/server.ts:390-412 (onUpgrade sees no client address), packages/core/src/apply.ts:552-563, 592-651 (every subscribe rebuilds and sends the whole snapshot) — one client can hold all 16 endpoint connections (the 256/64 caps never bind), and a connect-subscribe-close loop pulls 100 KB+ per subscribe against the VM's ~2 MB/s egress, starving live observers — cheap anonymous lock-out of the public URL — log req.headers once in onUpgrade to learn whether the proxy forwards x-forwarded-for; if so cap sockets and subscribes per address, if not throttle per requestContext.http.sourceIp in packages/fleet/src/session.ts:106-134 and vend per-client one-minute tokens; memoise pages per meta.seq (saves CPU, not egress).`

7. `[M] control plane compiles untrusted WASM — packages/control-plane/src/bundles.ts:79-82 → packages/sandbox/src/validate.ts:52-57 (new WebAssembly.Module in the control-plane process), reachable by any observer through launch of an unknown bundle at 6/min — up to 8 MB of attacker bytes is compiled on the single event loop; a multi-second compile stalls heartbeat processing past goneAfterMs (4 s), releasing every node's work, and any V8 decoder bug lands in the control plane, against §2's "runs no program code" — use WebAssembly.validate plus hand-parsed import/export sections (the memory section already is), or compile in a worker_thread with resourceLimits and a timeout.`

8. `[L] fleet secret in the core payload (deferred) — packages/control-plane/src/cores.ts:31-40 includes fleetSecret; packages/control-plane/src/server.ts:488 keeps it for any role and :586-620 lets /adopt turn a core into an "active" control plane — a 0.5 GB core executing arbitrary WASM holds the secret gating /handover, /adopt, /drain, /snapshot, /diag for no benefit — drop the field from corePayload, make fleetAuthorized false when role === "core", refuse /adopt unless role is neutral or control-plane.`

9. `[L] image pinning (deferred) — packages/infra/image/Dockerfile:4-5 (mutable al2023-minimal tag, unpinned dnf nodejs22), cdk.json baseImageVersion "1" vs packages/infra/lib/base-image.ts:38-45 picking the newest AVAILABLE — two deploys of one commit yield different runtimes and build.json (stage-image.ts:42-45) names the commit only — a "rollback to the previous image version" does not roll the runtime back and drift is invisible — FROM …@sha256:<digest>, dnf install nodejs22-<version>, record base version and digest in build.json and /health.`

10. `[L] CSP untested — packages/infra/lib/core-stack.ts:86-96 is enforced only by CloudFront; packages/control-plane/src/static.ts:51-56 sends none, so Playwright never runs the page under it, and default-src 'self' https: wss: leaves connect-src open to any host — verified it does not break the page today (no inline scripts, no eval in the built bundles: the compiler worker's Function(" is addFunction(", 'wasm-unsafe-eval' present), but the next regression would ship silently — emit the same header from serveStatic; tighten connect-src to the session URL host, wss://*.lambda-microvm.<region>.on.aws and the blob bucket host.`

11. `[L] CI supply chain — .github/workflows/ci.yml:26,40,47,61,73 pin third-party actions by tag (v7.0.1, v4.3.0, v6.1.0), not commit SHA, on a repository about to go public with pull_request runs — a moved tag executes arbitrary code in CI; contents: read and the no-secrets assertion bound it — pin actions to full SHAs.`

Verified as real and complete: size-capped fetches (store/driver.ts, bundles.ts caps), presign size/count bounds, one memory per module (validate.ts:36-40), the solicited bucket, the cooldown, RETAIN buckets, fleet routes closed before the secret arrives, pinned tools, and the CloudFront headers; `bun test` on the loop-1 suites passes 50/50. Transcripts show no account ids, tokens, or emails by the shapes checked.

## Summary

The first loop hardened the byte paths (fetch caps, checksum-pinned uploads, headers) and the process lifecycle, but the public control surface still trusts what a client says about itself: a hello can name a real MicroVM and get it terminated, a RELEASED result is free and endless, and the write and presign bounds count self-declared numbers per socket. Two claimed fixes are not what the doc says — RunMicrovm is still image-wide for every launcher and the presign bound meters messages rather than S3 calls. Fix first: (1) stop linking or creating cores from hostId and add the per-core token, (2) charge released attempts to the compute budget with an attempt cap and stop treating abort text as host failure, (3) scope RunMicrovm and Terminate properly and meter presigns per item with a signed content-length so the store budget is real.
