# Session agent-a5d61666501946056

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 169 · user messages: 1 · assistant messages: 6 · tool calls: 71 · tool results: 71
- from 2026-09-04T18:30:09.413Z to 2026-09-04T18:44:17.080Z
- scrubbed: nothing

---
## User · 2026-09-04T18:30:09.413Z

You are an expert full-stack software developer reviewing the repository at /home/mircea/homework/tabframe (a fault-tolerant distributed computer whose cores are browser tabs and AWS Lambda MicroVMs, programmed in WebAssembly; TypeScript, Bun, a web page in packages/web, a node runtime in packages/node, a control plane in packages/control-plane, a protocol in packages/protocol, a store client in packages/store).

Rules: read-only — do not modify any file. Never run any AWS CLI or SDK command, never use the AWS profile `tabframe`, never run `mise run deploy`, `up`, `down`, `rotate`, `verify`, `demo`, `logs`, or `health`. You may run `bun test <path>`, `bunx tsc -p <tsconfig> --noEmit`, and `bunx biome check`, and read anything. Never include AWS account ids, MicroVM endpoint hostnames, tokens, or email addresses in your report.

This is review loop 3 of 3. Loops 1 and 2 are merged into the tree you see. Start by reading docs/implementation/wp-8.1-review-loop-1.md and docs/implementation/wp-8.2-review-loop-2.md, then docs/walkthrough.md (the page's contract) and skim docs/design.md. Check whether loop 2's accepted rows in your area actually hold in the code (mark a broken one "loop-2 regression"), and look for what both loops missed.

Your area: packages/web (host.ts, state.ts, panels.ts, editor.ts, editor-page.ts, editor-core.ts, observer.ts, demo.ts, banners.ts, public/*.html, styles), packages/node, packages/store/src/client.ts, packages/protocol, the browser tests in e2e/, and the unit tests. Look for: correctness bugs, race conditions in the page and the node, UI states that contradict docs/walkthrough.md, DOM churn and performance, accessibility, error handling that swallows failures, tests that assert the wrong thing, dead code, and duplicated logic.

Return at most twelve findings, most severe first, under a "## Findings" heading, each as ONE numbered line in this exact shape:
`[H|M|L] area — file:lines — what is wrong — why it matters (the consequence) — the fix`
Every finding must cite code you actually read. Then a "## Summary" paragraph: what to fix first and why. Also list, in one sentence, which loop-2 claims you verified as real and complete.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T18:30:15.235Z

I'll start with the two prior review-loop documents, then the walkthrough and design.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.1-review-loop-1.md
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.2-review-loop-2.md
```

**Result of `Read`:**

```
1	# WP8.1 — Review loop 1
2	
3	**Branch** `wp/8.1-review-loop-1` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop one.
7	
8	## How the loop ran
9	
10	Five independent reviewers (fresh agents, read-only, no AWS access) each returned at most twelve
11	findings with severity, location, cause, and fix. The 52 findings were de-duplicated into the
12	table below (52 rows); each was accepted, deferred to a later loop, or rejected with the reason (the
13	critique): 46 accepted in full or in part, 6 deferred to loops 2–3, and one (11) rejected as a change but kept as a bound. The accepted ones landed on this branch with tests; CI gates the merge; the next loop
14	reviews the tree with these changes in.
15	
16	## The findings and what was done
17	
18	*From*: dist = distributed-systems developer, sec = security engineer, fs = full-stack developer,
19	tw = technical writer, devops = DevOps engineer. *Sev*: H/M/L as the reviewer rated it.
20	
21	| # | From | Sev | Finding | Decision |
22	|---|---|---|---|---|
23	| 1 | dist | H | Stage-advancing store effects (inherited root, stage spec, folded manifest) were fire-and-forget: a handover mid-flight or one S3 error wedged the execution for ever. | **Accepted.** `resumePending()` re-derives the pending effect from the ledger on adopt and again every `STORE_RETRY_MS` (`waitingSince` on the execution); a pruned origin now falls back to the bundle with the `expired-root` warning instead of a silent start. |
24	| 2 | dist | H | `encode()` over 64 KB threw through `dispatch`; params and the queue were unbounded. | **Accepted.** `PARAMS_MAX_BYTES` (4 KB canonical) and `QUEUE_CAP` (32) in `enqueue`; a send that cannot be encoded closes that connection instead of throwing. |
25	| 3 | dist | H | A node reported store or network failures as program faults, failing everybody's execution. | **Accepted.** `hostFailure()` covers fetch, upload, presign timeouts and network errors → `RELEASED`; the store client retries three times with backoff. |
26	| 4 | sec | H | The control plane fetched blobs named by untrusted parties without a size check. | **Accepted.** `StoreDriver.get(hash, maxBytes)` (HeadObject first on S3, `BlobTooLarge` otherwise); caps at every fetch: stage spec 1 MB, inherited root 4 MB, bundle manifest 1 MB, program manifest 64 KB, module 8 MB. |
27	| 5 | sec | H | Presign sizes were unbounded and uncounted. | **Accepted.** `size ≤ maxOutputBytes` and at most 64 items per presign in the schemas; a 1 GiB presigned-byte budget per connection in the core. |
28	| 6 | sec | H | A module could declare a second memory without a maximum and grow past the cap. | **Accepted.** `countMemories()`; more than one memory is refused. |
29	| 7 | fs | H | A node's presign never timed out and an S3 HeadObject error failed the whole presign: a zombie core. | **Accepted.** `SocketPresigner` times out after 30 s (the task is released); `S3Store.presign` treats a HeadObject error as "not there" and signs. |
30	| 8 | tw, devops | H | The hourly rule rotated a *suspended* control plane, so an idle night booted a generation an hour and the README's stays-up paragraph was false. | **Accepted.** A scheduled invocation skips a `SUSPENDED` control plane (`skipped-suspended`); the README says what happens now. |
31	| 9 | devops | H | `deploy` ran build and test in parallel from the working tree with no clean-tree or green-main guard and no commit stamp. | **Accepted.** `deploy` is sequential: build → test → `deploy-guard.ts` (clean tree, HEAD on origin/main; `TABFRAME_DEPLOY_UNGATED=1` to skip) → stacks → up. `stage-image.ts` writes `build.json`; `/health` shows `build` and `imageVersion`. |
32	| 10 | devops | H | The rotate function returned failures as successes; nothing alarmed. | **Accepted.** The Lambda entry throws on `failed`; CloudWatch alarms on rotate/session errors and rotate throttles go to an SNS topic mailed to the budget address when configured; the hourly rule's target retries twice within thirty minutes. |
33	| 11 | sec | M | Destructive controls are unauthenticated; kill-half in a loop terminates and relaunches cloud cores. | **Rejected as a change, accepted as a bound.** A public machine anyone may drive is the design (D4, §6.7); a machine-wide cooldown of two seconds per destructive control (`CONTROL_COOLDOWN_MS`) removes the cost loop. |
34	| 12 | fs | M | The editor tab re-sent `pause` on every live socket, including after its own launch. | **Accepted.** `holdWanted` is cleared by launch and close. |
35	| 13 | fs | M | A failing session fetch during a silent resubscribe kept a green pill; held clicks were dropped silently. | **Accepted.** The page goes back to "connecting"; dropped clicks are said in the status line (`onDropped`). |
36	| 14 | fs | M | An outdated page reloaded itself in a loop. | **Accepted.** One reload per protocol version (`sessionStorage`), then a hard-refresh hint. |
37	| 15 | fs | M | The ledger tab serialised every row per frame; the blob cache and the demo store grew without bound. | **Accepted** (ledger signature; a 64 MB LRU `BlobCache`). The demo store's growth is deferred to loop 2. |
38	| 16 | fs | M | A dead compiler worker left the editor unusable. | **Accepted.** Pending compiles are rejected, a two-minute compile timeout, the next compile gets a fresh worker; failed drops say so. |
39	| 17 | dist | M | Between handover and promote a dead rotation left the machine dark for an hour; a predecessor kept acting after handing over. | **Accepted.** `HANDOVER_LEASE_MS` (90 s) returns a control plane to `active` when no drain follows; ticks run no fleet, loop, or assignment while `handing-over` or `drained`. |
40	| 18 | dist | M | Slow `RunMicrovm` calls were asked twice; surplus cores were never trimmed. | **Accepted.** `meta.coreLaunches` counts launches until acknowledged (or a minute passes); surplus unlinked cores are terminated. |
… (55 more lines)
```

**Result of `Read`:**

```
1	# WP8.2 — Review loop 2
2	
3	**Branch** `wp/8.2-review-loop-2` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop two, on the tree with loop one merged.
7	
8	## How the loop ran
9	
10	Five fresh reviewers (independent agents, read-only, no AWS access), each given the loop-one note
11	first so they would check what it claimed, each returned at most twelve findings with severity,
12	location, cause, and fix. Their 57 findings de-duplicated into the table below; each was accepted,
13	deferred with a reason, or rejected with the reason (the critique). Of the 52 rows, 51 are accepted in full or in part (9 of them with a part deferred to loop 3, and one with a part rejected), 1 deferred whole, none rejected whole. The accepted ones
14	landed on this branch with tests; CI gates the merge; loop three reviews the tree with these
15	changes in and reads this note first.
16	
17	Three of loop one's claims did not hold and are marked *loop-1 regression*: the presign cap made
18	many-file tasks loop across the cluster; the IAM scoping was never applied; the build stamp never
19	reached the image. Two of loop one's documentation repairs did damage (the drift log, a typo). All
20	five are fixed here.
21	
22	## The findings and what was done
23	
24	*From*: dist = distributed-systems developer, sec = security engineer, fs = full-stack developer,
25	tw = technical writer, devops = DevOps engineer. *Sev*: H/M/L as the reviewer rated it. A finding
26	two reviewers made independently is one row naming both.
27	
28	| # | From | Sev | Finding | Decision |
29	|---|---|---|---|---|
30	| 1 | dist, devops | H | A rotation that dies between the pointer flip and the drain leaves two active generations (the successor active at `/run`, the predecessor back to active after the 90 s lease); each terminates the other's cores every two minutes. The step timeouts exceeded the function's five minutes. | **Accepted.** `promote()` records `retiring` in the pointer and `retire()` clears it; a leftover is finished before `repair()`. The rotate function runs ten minutes and waits 120 s for RUNNING. When a lease runs out the control plane reads the pointer (`ControlPlaneDeps.pointer`, SSM in the image): named elsewhere at a higher generation, it drains and terminates itself (`rotation.test.ts`, two cases). A standby boot phase until `/adopt` is *deferred (loop 3)*: the adopt-once guard and phase-gated `latest` writes cover the successor side today. |
31	| 2 | dist | H | A result for an attempt the node never held settled an open task unless it carried an error; any visitor with the token could paint the frame or hijack the plan. | **Accepted.** An open task settles only from a known attempt, any outcome; unknown attempts may verify or contest settled tasks only (`results.ts`; `scheduler.test.ts` rewritten and extended). |
32	| 3 | dist | H | `defaultParams` was unbounded and every program's rode page 0; one dropped manifest could make every subscribe close with "frame too large". Programs were uncapped. | **Accepted.** `defaultParams` ≤ `PARAMS_MAX_BYTES` (4 KB) in the schema; `PROGRAMS_CAP` = 64 in `addProgram`; page 0 over budget strips `defaultParams` from the program views instead of closing; the page split is memoised per (generation, seq). |
33	| 4 | dist | M | A store fetch that threw was dispatched as `bytes: null`, which the ledger read as "missing" and failed the execution; loop one's retry never ran. | **Accepted.** `blobFetched` carries `error`; `onStoreError` counts it and leaves `waitingSince` so the `STORE_RETRY_MS` re-derivation retries; the execution fails only after `STORE_ERRORS_MAX` (6). |
34	| 5 | dist, sec | M/H | A core was linked or created from a self-declared `hostId`, and every snapshot published the ids; an impostor could get a real core terminated, starve the fleet, and blind the reaper. | **Accepted.** A random per-core token in the run payload (`cores.ts`), `hello.coreToken`, a hello links only a matching unlinked record and never creates one; `nodeView.hostId` is `"fleet"` for cores; `gone()` survives a bad id. |
35	| 6 | dist | M | `PRESIGN_BYTES_PER_CONN` was a lifetime cap: an honest Mandelbrot node hit 1 GiB within an hour and was closed as rate-limited. *Loop-1 regression.* | **Accepted.** A refilling bucket, `PRESIGN_BYTES_PER_MIN` = 64 MiB per connection. |
36	| 7 | dist | M | `latest.json.gz` was written while handing over and after drain, so a heal could boot from a drained predecessor; the adopt repeat guard compared `seq`. | **Accepted in part.** `snapshotNow` writes only while active (handover excepted); `/adopt` is accepted once per process (`adoptedOnce`). Keying snapshots so the fleet picks the highest generation is *deferred (loop 3)*: with the phase gate, `latest` is always the active lineage. |
37	| 8 | dist, devops | M | The client token `g<gen>-<hour>` replayed onto a terminated VM after a failed attempt in the same hour; nothing could launch until the hour turned. | **Accepted.** A run that resolves to a TERMINATED/TERMINATING VM is a replay: retried with `-r<n>`, three tries; the fake models the replay (`terminateNextRuns`); tests for the retry and for a leftover `retiring`. |
38	| 9 | dist | L | Inheritance resolves by execution id against 32 kept executions; a persist program's lineage is evicted within minutes of the loop. | **Deferred (loop 3).** A bundle → last done root map that survives pruning is a ledger change with its own snapshot field; the pruned-origin warning of loop 1 makes the loss visible meanwhile. |
39	| 10 | dist | L | Clients could take all sixteen endpoint connections; every `/handover` and `/drain` then got 429 and rotations degraded to snapshot handovers. | **Accepted.** `CLIENT_CONNECTION_CAP` = 14: the fifteenth client upgrade is refused with 503; §9.7 says so. |
40	| 11 | dist | L | `beginHandover` used `Date.now()` beside an injected clock, and a handover from a drained control plane resurrected it. | **Accepted.** `beginHandover(ledger, clock.now())`; `/handover` on a drained control plane is 409. |
… (68 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T18:30:23.312Z

Next I need the page's contract (walkthrough), a skim of the design record, and the layout of the packages in my area. These are independent, so I'll fetch them together.

**Tool `Bash`:**

```
cat /home/mircea/homework/tabframe/docs/walkthrough.md
```

**Result of `Bash`:**

```
# The walkthrough: every screen, every state, everything clickable

A visitor should be able to land anywhere, in any state, and answer three questions without help:
what is the machine doing, what can I do here, and what just happened because of what I did. This
document is the inventory that makes that testable — for each screen, every state it can be in, what
the page must say, what can be clicked and what happens, and what must not be on the screen. It is
the page's contract; `e2e/walkthrough.e2e.ts` drives the machine through the states below and
asserts the sentence, the header's slot, and the exact set of enabled controls, with a screenshot
per state in the test's output. Written for WP7.7 (2026-09-04), from Mircea's third review.

## The rules the inventory is checked against

- **R1 · One sentence of state, always.** The status line says what the machine is doing and why,
  in a visitor's words (`machineSentence` in `packages/web/src/state.ts`): "rendering mandelbrot
  e41 · render · 9 nodes", "running your wordcount e42 · map · 4 nodes · the loop waits behind it",
  "stopped by you · nothing runs until Start · a launch of yours still runs at once", "your
  wordcount e42 is done · the result stays · the loop waits for Start or ten quiet minutes",
  "paused · the editor tab is open · in-flight tasks finish, nothing new starts · closing it or
  launching resumes", "idle · the loop starts a frame when someone watches". The demo and an
  observer say what they are first. While the page is not connected the connection banner speaks.
- **R2 · Every control says what it will do, then what it did.** A label and a tooltip before the
  click; after it, an activity line and, for the page that clicked, the same line in the status
  line for five seconds. A click that could not be sent says so instead of being swallowed.
- **R3 · Disabled with a reason, not hidden.** A control that cannot apply now stays where it is,
  greyed, with the reason after a dash in its tooltip ("— not connected yet", "— nothing is
  running", "— an observer lends no cores; open the plain address to lend some"). The only slot that
  swaps its label is Stop / Start / Resume, and it always means "what you can do to the machine
  right now"; the one other label that changes is a program's "launch…", which reads "cancel" while
  its form is open.
- **R4 · Same kind, same look.** Controls that end things (Stop, kill half, kill execution, close
  all mine) share the red outline and sit together; controls that make things share the plain look.
  Links that leave the page carry ↗, and nothing else opens a tab.
- **R5 · A click never moves the page.** Results land in boxes that were already there; lists
  change a row's class instead of being rebuilt; the only scrolling is the visitor's.
- **R6 · One name per thing.** program · execution (e42) · frame (one execution of the loop's
  program) · stage · task · tile · core (any worker: a browser tab's thread or a MicroVM) · cloud
  core (the MicroVM kind) · node (the ledger's word for a core that has joined) · host (a browser) ·
  tab. The pills, the activity log, the tooltips and the docs use these words (aligned in WP8.2).
- **R7 · Every screen says where you are and how to get back.** Panel tabs and the editor say they
  were opened from the dashboard and link back; the demo says it is a demo in the status line;
… (61 more lines)
```

**Tool `Bash`:**

```
wc -l docs/design.md && grep -n '^## \|^### ' docs/design.md | head -150
```

**Result of `Bash`:**

```
997 docs/design.md
13:## Contents
35:## 1. Decisions register
66:## 2. The system in one page
89:## 3. Runtime picture
140:## 4. Nodes
144:### 4.1 Lifecycle
157:### 4.2 Orchestrator and sandbox
172:### 4.3 Cloud cores
178:## 5. Programs
180:### 5.1 What a program is
191:### 5.2 Executions
197:### 5.3 ABI
221:### 5.4 Filesystem
232:### 5.5 Sandbox guarantees, consent, budgets
240:### 5.6 SDK and demo programs
250:## 6. The control plane
252:### 6.1 Shape
256:### 6.2 Ledger
269:### 6.3 Filling a node
279:### 6.4 Liveness, deadlines, health
287:### 6.5 Results and verification
293:### 6.6 Stage advance
297:### 6.7 Queue, observers, controls
307:### 6.8 Fleet policy and sleep
313:### 6.9 Generation
317:### 6.10 Invariants (each one a test)
328:## 7. Data paths and the store
330:### 7.1 Store API
343:### 7.2 Read path (during `run`)
347:### 7.3 Write path
357:### 7.4 Who touches bytes
361:### 7.5 Late joiners and gaps
367:## 8. The wire
369:### 8.1 Channels and encoding
381:### 8.2 Node socket
395:### 8.3 Observer socket
407:### 8.4 Sizes, limits, codes
425:## 9. Hosting and fleet
427:### 9.1 Platform facts that shape the design
… (21 more lines)
```

**Tool `Bash`:**

```
find packages/web packages/node packages/store packages/protocol e2e -type f -not -path '*/node_modules/*' -not -path '*/dist/*' | sort | xargs wc -l | sort -n | tail -120
```

**Result of `Bash`:**

```
     1 e2e/README.md
     2 packages/web/src/node.ts
     2 packages/web/src/sandbox.ts
     6 packages/store/src/index.ts
     8 packages/web/tsconfig.json
     9 packages/web/src/guide-page.ts
    11 packages/protocol/src/index.ts
    13 packages/protocol/package.json
    13 packages/web/package.json
    19 packages/node/package.json
    20 packages/protocol/src/headers.ts
    22 packages/store/package.json
    23 packages/node/src/backoff.ts
    27 packages/protocol/src/canonical.test.ts
    27 packages/protocol/src/canonical.ts
    27 packages/store/src/hash.ts
    28 packages/protocol/src/codes.ts
    28 packages/web/public/guide.html
    29 packages/web/src/compiler-worker.ts
    29 packages/web/src/program-sources.generated.ts
    34 packages/protocol/src/shared.ts
    35 packages/store/src/driver.ts
    37 packages/protocol/src/limits.ts
    37 packages/web/src/banners.test.ts
    48 packages/protocol/src/fs.ts
    53 e2e/host.e2e.ts
    55 packages/store/src/snapshots.test.ts
    61 packages/store/src/snapshots.ts
    68 packages/node/src/platform/web.ts
    70 packages/node/src/platform/node.ts
    70 packages/web/public/editor.html
    71 packages/node/src/session.ts
    74 packages/protocol/src/codec.ts
    77 packages/web/scripts/gen-sources.ts
    78 packages/web/scripts/screenshot.ts
    79 packages/web/src/compiler.test.ts
    83 packages/web/src/banners.ts
    86 packages/store/src/local.ts
    93 packages/protocol/src/task.ts
    94 packages/node/dist-test/mandelbrot.wasm
… (45 more lines)
```

> **Thinking.** 

> **Thinking.** I've got the contract. Now I'll batch-read the relevant design record sections plus the foundational protocol and store client packages that the page and node depend on.

**Tool `Bash`:**

```
sed -n 66,140p docs/design.md; echo ----- ; sed -n 140,180p docs/design.md; echo ------; sed -n 232,250p docs/design.md; echo -----; sed -n 297,330p docs/design.md; echo ------; sed -n 367,425p docs/design.md; echo -----; sed -n 487,514p docs/design.md; echo -----; sed -n 605,625p docs/design.md
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (29.9KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bsazguw2t.txt

Preview (first 2KB):
## 2. The system in one page

**Cores** are browser tabs and MicroVMs. Each core runs the same orchestrator, which owns a WebSocket to the control plane and a disposable sandbox that executes program code. Cores come and go constantly; the design treats every departure as ordinary.

**Programs** are WebAssembly modules with two entry points. `plan` turns params and the previous stage's outputs into the next stage's tasks. `run` turns one task's input into bytes. Both execute on cores. A program's only view of the world is a per-execution filesystem of content-addressed blobs. It has no clock, no randomness, no network, and no failure type anywhere in its API.

**The control plane** is a pure function over a small ledger: which nodes exist, which execution is running, which task is in which state and who holds it, and the hash of every accepted result. It assigns, detects death, releases, speculates on stragglers, verifies duplicates, folds stage outputs into filesystem roots, and fans events out to observers. It never touches a payload byte. It runs as one Node process in a Lambda MicroVM, snapshots its ledger to S3 every few seconds, and hands over to a fresh MicroVM every hour.

**The store** is S3 behind CloudFront. Every blob's key is its SHA-256. Nodes upload results, written files, logs, and program modules; observers and nodes read them back by hash. The store computes hashes on upload, so a hash reported to the ledger is one the store vouches for.

**Invariants held everywhere:**

- No shared memory across nodes. Coordination is message passing over sockets; data is blobs by hash.
- Tasks are idempotent and at-least-once. Re-execution is always safe.
- Results are single-assignment and memoized by hash. A done task is never recomputed unless a mismatch invalidates it.
- Location transparency. Nodes are addressed by id; the control plane never learns or cares which tab or VM a node lives in.
...
</persisted-output>
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/protocol/src && for f in index.ts limits.ts codes.ts shared.ts headers.ts canonical.ts codec.ts fs.ts task.ts node.ts observer.ts; do echo "=== $f ==="; cat -n $f; done
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/store/src && for f in index.ts client.ts hash.ts driver.ts; do echo "=== $f ==="; cat -n $f; done
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (34.2KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bc78m2gz2.txt

Preview (first 2KB):
=== index.ts ===
     1	export * from "./abi.ts";
     2	export { byteLength, canonicalStringify } from "./canonical.ts";
     3	export { type Decoded, type DecodeOptions, decode, encode } from "./codec.ts";
     4	export { CLOSE, type CloseCode, type RotatingReason } from "./codes.ts";
     5	export * from "./fs.ts";
     6	export { LOCAL_PAGE_CSP, PAGE_CSP } from "./headers.ts";
     7	export { LIMITS, PROTOCOL_VERSION } from "./limits.ts";
     8	export * from "./node.ts";
     9	export * from "./observer.ts";
    10	export * from "./shared.ts";
    11	export * from "./task.ts";
=== limits.ts ===
     1	/** Wire-level limits (design §8.4). Both ends enforce them. */
     2	export const PROTOCOL_VERSION = 1;
     3	
     4	export const LIMITS = {
     5	  /** Every socket message stays under this many UTF-8 bytes. */
     6	  maxMessageBytes: 64 * 1024,
     7	  /** A task's inline input, base64, before it must become a blob instead. */
     8	  maxInlineInputBytes: 16 * 1024,
     9	  /** Tasks per snapshot page. */
    10	  snapshotPageTasks: 256,
    11	  /** Cluster caps (decision: 256 nodes, 64 observers, no per-IP limit). */
    12	  nodeCap: 256,
    13	  observerCap: 64,
    14	  /** Liveness. */
    15	  heartbeatMs: 1_000,
    16	  goneAfterMs: 4_000,
    17	  observerPingMs: 2_000,
    18	  maxInFlight: 2,
    19	  /**
    20	   * Rate limits, messages per second per connection. A node sends a presign and a result per
    21	   * task, and a tile takes a few milliseconds, so an honest node reaches hundreds per second.
    22	   */
    23	  nodeMessagesPerSecond: 1_000,
… (9 more lines)
```

**Result of `Bash`:**

```
=== index.ts ===
     1	export { type FetchLike, type PresignRequester, StoreClient } from "./client.ts";
     2	export type { PresignedUpload, PresignItem, StoreDriver } from "./driver.ts";
     3	export { HASH_RE, hex, hexToBase64, hexToBytes, sha256Hex } from "./hash.ts";
     4	export { LocalStore, parseRange } from "./local.ts";
     5	export { IMMUTABLE, S3Store, signedHeaders } from "./s3.ts";
     6	export { MemorySnapshots, S3Snapshots, type SnapshotStore } from "./snapshots.ts";
=== client.ts ===
     1	import { LIMITS } from "@tabframe/protocol";
     2	import type { PresignedUpload, PresignItem } from "./driver.ts";
     3	import { sha256Hex } from "./hash.ts";
     4	
     5	/** Presign is a socket message (D18); the node and the page provide the round trip. */
     6	export interface PresignRequester {
     7	  presign(items: PresignItem[]): Promise<PresignedUpload[]>;
     8	}
     9	
    10	export type FetchLike = (url: string, init?: RequestInit) => Promise<Response>;
    11	
    12	/**
    13	 * The one client flow everywhere (design §7.1): hash locally, ask for presigned URLs over the
    14	 * socket, PUT straight to the store with the pinned headers, and read blobs by hash from the CDN.
    15	 */
    16	export class StoreClient {
    17	  private readonly base: string;
    18	  private readonly requester: PresignRequester;
    19	  private readonly fetchImpl: FetchLike;
    20	
    21	  constructor(
    22	    base: string,
    23	    requester: PresignRequester,
    24	    fetchImpl: FetchLike = (u, i) => fetch(u, i),
    25	  ) {
    26	    this.base = base.replace(/\/$/, "");
    27	    this.requester = requester;
    28	    this.fetchImpl = fetchImpl;
    29	  }
    30	
    31	  urlFor(hash: string): string {
    32	    return `${this.base}/${hash}`;
… (133 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T18:30:51.060Z

Two of those outputs were saved to files; I'll read both in full now.

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bsazguw2t.txt
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (29.9KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bxhktrlzj.txt

Preview (first 2KB):
## 2. The system in one page

**Cores** are browser tabs and MicroVMs. Each core runs the same orchestrator, which owns a WebSocket to the control plane and a disposable sandbox that executes program code. Cores come and go constantly; the design treats every departure as ordinary.

**Programs** are WebAssembly modules with two entry points. `plan` turns params and the previous stage's outputs into the next stage's tasks. `run` turns one task's input into bytes. Both execute on cores. A program's only view of the world is a per-execution filesystem of content-addressed blobs. It has no clock, no randomness, no network, and no failure type anywhere in its API.

**The control plane** is a pure function over a small ledger: which nodes exist, which execution is running, which task is in which state and who holds it, and the hash of every accepted result. It assigns, detects death, releases, speculates on stragglers, verifies duplicates, folds stage outputs into filesystem roots, and fans events out to observers. It never touches a payload byte. It runs as one Node process in a Lambda MicroVM, snapshots its ledger to S3 every few seconds, and hands over to a fresh MicroVM every hour.

**The store** is S3 behind CloudFront. Every blob's key is its SHA-256. Nodes upload results, written files, logs, and program modules; observers and nodes read them back by hash. The store computes hashes on upload, so a hash reported to the ledger is one the store vouches for.

**Invariants held everywhere:**

- No shared memory across nodes. Coordination is message passing over sockets; data is blobs by hash.
- Tasks are idempotent and at-least-once. Re-execution is always safe.
- Results are single-assignment and memoized by hash. A done task is never recomputed unless a mismatch invalidates it.
- Location transparency. Nodes are addressed by id; the control plane never learns or cares which tab or VM a node lives in.
...
</persisted-output>
```

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bc78m2gz2.txt
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (34.2KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bollpy1d6.txt

Preview (first 2KB):
=== index.ts ===
     1	export * from "./abi.ts";
     2	export { byteLength, canonicalStringify } from "./canonical.ts";
     3	export { type Decoded, type DecodeOptions, decode, encode } from "./codec.ts";
     4	export { CLOSE, type CloseCode, type RotatingReason } from "./codes.ts";
     5	export * from "./fs.ts";
     6	export { LOCAL_PAGE_CSP, PAGE_CSP } from "./headers.ts";
     7	export { LIMITS, PROTOCOL_VERSION } from "./limits.ts";
     8	export * from "./node.ts";
     9	export * from "./observer.ts";
    10	export * from "./shared.ts";
    11	export * from "./task.ts";
=== limits.ts ===
     1	/** Wire-level limits (design §8.4). Both ends enforce them. */
     2	export const PROTOCOL_VERSION = 1;
     3	
     4	export const LIMITS = {
     5	  /** Every socket message stays under this many UTF-8 bytes. */
     6	  maxMessageBytes: 64 * 1024,
     7	  /** A task's inline input, base64, before it must become a blob instead. */
     8	  maxInlineInputBytes: 16 * 1024,
     9	  /** Tasks per snapshot page. */
    10	  snapshotPageTasks: 256,
    11	  /** Cluster caps (decision: 256 nodes, 64 observers, no per-IP limit). */
    12	  nodeCap: 256,
    13	  observerCap: 64,
    14	  /** Liveness. */
    15	  heartbeatMs: 1_000,
    16	  goneAfterMs: 4_000,
    17	  observerPingMs: 2_000,
    18	  maxInFlight: 2,
    19	  /**
    20	   * Rate limits, messages per second per connection. A node sends a presign and a result per
    21	   * task, and a tile takes a few milliseconds, so an honest node reaches hundreds per second.
    22	   */
    23	  nodeMessagesPerSecond: 1_000,
… (9 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T18:31:02.935Z

The Bash tool truncates large outputs, so I'll switch to the Read tool for whole files. First the protocol package and the design sections I skipped, all independent.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/observer.ts
```

**Result of `Read`:**

```
1	import { z } from "zod";
2	import { LIMITS } from "./limits.ts";
3	import { envelope, hash, health, millis, nodeId, nodeView, seq } from "./shared.ts";
4	import { executionView, params, place, queueEntry, taskLog, taskView } from "./task.ts";
5	
6	const executionId = z.string().min(1).max(64);
7	const taskId = z.string().min(1).max(64);
8	
9	/** Observer → control plane. */
10	export const subscribe = z.object({
11	  t: z.literal("subscribe"),
12	  ...envelope,
13	  /** Last sequence number seen, when resubscribing after a gap. */
14	  since: seq.optional(),
15	});
16	
17	export const ping = z.object({ t: z.literal("ping"), ...envelope });
18	
19	/** Bundle uploads presign through the observer socket (design D18). */
20	export const observerPresign = z.object({
21	  t: z.literal("presign"),
22	  ...envelope,
23	  items: z
24	    .array(z.object({ hash, size: z.number().int().positive().max(LIMITS.maxOutputBytes) }))
25	    .max(64)
26	    .min(1)
27	    .max(LIMITS.maxPresignItems),
28	});
29	
30	/** Controls (design §6.7, §8.3). Spawn is not a message. */
31	export const killHalf = z.object({ t: z.literal("killHalf"), ...envelope });
32	export const freezeHalf = z.object({ t: z.literal("freezeHalf"), ...envelope });
33	export const throttleHalf = z.object({ t: z.literal("throttleHalf"), ...envelope });
34	export const resumeAll = z.object({ t: z.literal("resumeAll"), ...envelope });
35	/** Stop: end the running execution, drop the loop's queued continuations, hold the loop (WP6.1). */
36	export const stop = z.object({ t: z.literal("stop"), ...envelope });
37	/** Start: let the loop run again after a stop. */
38	export const start = z.object({ t: z.literal("start"), ...envelope });
39	/**
40	 * Pause (WP6.4): nothing new is assigned or started while the sender's socket lives; in-flight
… (324 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/node.ts
```

**Result of `Read`:**

```
1	import { z } from "zod";
2	import { fsPath } from "./fs.ts";
3	import { LIMITS } from "./limits.ts";
4	import { envelope, hash, hostId, millis, nodeId, nodeKind } from "./shared.ts";
5	import { taskKind, taskLimits } from "./task.ts";
6	
7	/** Node → control plane. */
8	export const hello = z.object({
9	  t: z.literal("hello"),
10	  ...envelope,
11	  hostId,
12	  kind: nodeKind,
13	  cores: z.number().int().min(1).max(1024),
14	  sandboxVersion: z.string().min(1).max(32),
15	  /** A cloud core proves it is the MicroVM it names: the token its run payload carried (WP8.2). */
16	  coreToken: z.string().min(16).max(64).optional(),
17	});
18	
19	export const heartbeat = z.object({
20	  t: z.literal("heartbeat"),
21	  ...envelope,
22	  visible: z.boolean(),
23	  queue: z.number().int().min(0).max(LIMITS.maxInFlight),
24	  lastTaskMs: millis.nullable(),
25	  tasksDone: z.number().int().nonnegative(),
26	});
27	
28	const writeEntry = z.object({ path: fsPath, hash, size: z.number().int().nonnegative() });
29	
30	/**
31	 * The error string a node sends when it gave up on a task at its own deadline (design §4.2): not a
32	 * program fault, so the control plane releases the attempt instead of failing the task.
33	 */
34	export const RELEASED = "released";
35	
36	/** A result names hashes the store vouches for; the error form carries a message instead (design §8.2). */
37	export const result = z
38	  .object({
39	    t: z.literal("result"),
40	    ...envelope,
… (106 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/codec.ts
```

**Result of `Read`:**

```
1	import type { ZodType } from "zod";
2	import { byteLength, canonicalStringify } from "./canonical.ts";
3	import { CLOSE, type CloseCode } from "./codes.ts";
4	import { LIMITS, PROTOCOL_VERSION } from "./limits.ts";
5	
6	export type Decoded<T> = { ok: true; msg: T } | { ok: false; closeCode: CloseCode; reason: string };
7	
8	export interface DecodeOptions {
9	  /** Reject messages stamped with any other generation. */
10	  expectGen?: number;
11	  /** Override the size cap (tests). */
12	  maxBytes?: number;
13	}
14	
15	/** Serialize a message as canonical JSON, asserting the size cap. */
16	export function encode(msg: unknown): string {
17	  const text = canonicalStringify(msg);
18	  const bytes = byteLength(text);
19	  if (bytes > LIMITS.maxMessageBytes) {
20	    throw new Error(`message is ${bytes} bytes, cap is ${LIMITS.maxMessageBytes}`);
21	  }
22	  return text;
23	}
24	
25	/**
26	 * Parse one text frame against a schema. Every failure maps to the close code the receiving
27	 * end should hang up with; nothing is ever half-accepted.
28	 */
29	export function decode<T>(schema: ZodType<T>, raw: unknown, opts: DecodeOptions = {}): Decoded<T> {
30	  if (typeof raw !== "string")
31	    return reject(CLOSE.invalidMessage, "binary frames are not accepted");
32	  const cap = opts.maxBytes ?? LIMITS.maxMessageBytes;
33	  if (byteLength(raw) > cap) return reject(CLOSE.invalidMessage, `message exceeds ${cap} bytes`);
34	
35	  let parsed: unknown;
36	  try {
37	    parsed = JSON.parse(raw);
38	  } catch {
39	    return reject(CLOSE.invalidMessage, "not JSON");
40	  }
… (35 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/limits.ts
```

**Result of `Read`:**

```
1	/** Wire-level limits (design §8.4). Both ends enforce them. */
2	export const PROTOCOL_VERSION = 1;
3	
4	export const LIMITS = {
5	  /** Every socket message stays under this many UTF-8 bytes. */
6	  maxMessageBytes: 64 * 1024,
7	  /** A task's inline input, base64, before it must become a blob instead. */
8	  maxInlineInputBytes: 16 * 1024,
9	  /** Tasks per snapshot page. */
10	  snapshotPageTasks: 256,
11	  /** Cluster caps (decision: 256 nodes, 64 observers, no per-IP limit). */
12	  nodeCap: 256,
13	  observerCap: 64,
14	  /** Liveness. */
15	  heartbeatMs: 1_000,
16	  goneAfterMs: 4_000,
17	  observerPingMs: 2_000,
18	  maxInFlight: 2,
19	  /**
20	   * Rate limits, messages per second per connection. A node sends a presign and a result per
21	   * task, and a tile takes a few milliseconds, so an honest node reaches hundreds per second.
22	   */
23	  nodeMessagesPerSecond: 1_000,
24	  observerMessagesPerSecond: 5,
25	  /** Results and uploads (design §5.5, §8.4). */
26	  maxWriteFiles: 256,
27	  maxWriteBytes: 16 * 1024 * 1024,
28	  maxOutputBytes: 16 * 1024 * 1024,
29	  maxLogBytes: 64 * 1024,
30	  maxInlineLogBytes: 4 * 1024,
31	  /** Items per presign, sized so a `presigned` reply of signed URLs (about 2 KB each) fits one frame (WP8.2). */
32	  maxPresignItems: 24,
33	  maxModuleBytes: 8 * 1024 * 1024,
34	  /** Reconnect backoff window. */
35	  reconnectMinMs: 500,
36	  reconnectMaxMs: 30_000,
37	} as const;
38	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/task.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/shared.ts
```

**Result of `Read`:**

```
1	import { z } from "zod";
2	import { PROTOCOL_VERSION } from "./limits.ts";
3	
4	/** Every message carries its type, the protocol version, and the control-plane generation. */
5	export const envelope = {
6	  v: z.literal(PROTOCOL_VERSION),
7	  gen: z.number().int().nonnegative(),
8	};
9	
10	export const nodeId = z.string().min(1).max(64);
11	export const hostId = z.string().min(1).max(64);
12	export const hash = z.string().regex(/^[0-9a-f]{64}$/, "sha-256 hex");
13	export const seq = z.number().int().nonnegative();
14	export const millis = z.number().int().nonnegative();
15	
16	export const nodeKind = z.enum(["tab", "core"]);
17	export type NodeKind = z.infer<typeof nodeKind>;
18	
19	export const health = z.enum(["fast", "slow", "throttled", "gone"]);
20	export type Health = z.infer<typeof health>;
21	
22	/** What observers see about a node. */
23	export const nodeView = z.object({
24	  nodeId,
25	  hostId,
26	  kind: nodeKind,
27	  health,
28	  visible: z.boolean(),
29	  tasksDone: z.number().int().nonnegative(),
30	  lastTaskMs: millis.nullable(),
31	  inFlight: z.number().int().nonnegative(),
32	  joinedAt: millis,
33	});
34	export type NodeView = z.infer<typeof nodeView>;
35	
```

**Result of `Read`:**

```
1	import { z } from "zod";
2	import { LIMITS } from "./limits.ts";
3	import { hash, millis, nodeId } from "./shared.ts";
4	
5	export const taskKind = z.enum(["run", "plan"]);
6	export const taskStatus = z.enum(["pending", "assigned", "done", "failed"]);
7	export const executionStatus = z.enum(["queued", "running", "done", "failed", "cancelled"]);
8	export const view = z.enum(["tiles", "bars", "text"]);
9	
10	export const place = z.object({
11	  x: z.number().int(),
12	  y: z.number().int(),
13	  w: z.number().int().positive(),
14	  h: z.number().int().positive(),
15	});
16	export type PlaceView = z.infer<typeof place>;
17	
18	export const params = z.record(z.string(), z.unknown());
19	
20	/** A task's log as the node reported it: inline up to the cap, a blob beyond (design §8.2). */
21	export const taskLog = z
22	  .union([z.object({ hash }), z.object({ text: z.string().max(LIMITS.maxInlineLogBytes) })])
23	  .nullable();
24	export type TaskLog = z.infer<typeof taskLog>;
25	
26	/** What observers see about a task (design §6.2, §8.3). */
27	export const taskView = z.object({
28	  taskId: z.string().min(1).max(64),
29	  executionId: z.string().min(1).max(64),
30	  stage: z.number().int().nonnegative(),
31	  index: z.number().int().nonnegative(),
32	  kind: taskKind,
33	  status: taskStatus,
34	  /** Nodes with an open attempt; two means a speculative twin. */
35	  holders: z.array(nodeId).max(2),
36	  attempts: z.number().int().nonnegative(),
37	  output: hash.nullable(),
38	  place: place.nullable(),
39	  contested: z.boolean(),
40	  /** The accepted result's log, once the control plane carries it (dashboard v2). */
… (54 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/codes.ts
```

**Result of `Read`:**

```
1	/** WebSocket close codes in the application range (design §8.4). */
2	export const CLOSE = {
3	  /** The peer sent something that failed schema validation or the size cap. */
4	  invalidMessage: 4000,
5	  /** Protocol version mismatch; the page reloads itself once. */
6	  versionMismatch: 4001,
7	  /** The node cap is reached. */
8	  nodeCap: 4002,
9	  /** The connection exceeded its message rate. */
10	  rateLimited: 4003,
11	  /** The control plane declared this node gone (silence) and is hanging up on the zombie. */
12	  declaredGone: 4004,
13	  /** The control plane is rotating; reconnect through the session function after the jittered delay in the reason. */
14	  rotatingReconnect: 4005,
15	  /** The message carried another control plane's generation. */
16	  generationMismatch: 4006,
17	  /** The observer cap is reached. */
18	  observerCap: 4007,
19	} as const;
20	
21	export type CloseCode = (typeof CLOSE)[keyof typeof CLOSE];
22	
23	/** Reason payload for `rotatingReconnect`, JSON-encoded in the close reason (≤ 123 bytes). */
24	export interface RotatingReason {
25	  gen: number;
26	  next: number;
27	  reconnectAfterMs: number;
28	}
29	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/fs.ts
```

**Result of `Read`:**

```
1	import { z } from "zod";
2	import { hash } from "./shared.ts";
3	
4	/** A path inside an execution's filesystem or a bundle: absolute, normalized, no traversal. */
5	export const fsPath = z
6	  .string()
7	  .min(2)
8	  .max(512)
9	  .regex(/^\/(?:[A-Za-z0-9._-]+\/)*[A-Za-z0-9._-]+$/, "absolute path with simple segments")
10	  .refine((p) => p.split("/").every((seg) => seg !== "." && seg !== ".."), "no dot segments");
11	
12	export const fileEntry = z.object({ hash, size: z.number().int().nonnegative() });
13	
14	/**
15	 * A manifest blob: the whole filesystem (or bundle) as path → blob. Stored content-addressed
16	 * itself, so a filesystem is one root hash (design §5.4).
17	 */
18	export const fsManifest = z.object({
19	  version: z.literal(1),
20	  files: z.record(fsPath, fileEntry),
21	});
22	export type FsManifest = z.infer<typeof fsManifest>;
23	
24	/** What a program declares about itself (design §5.1). */
25	export const programManifest = z.object({
26	  name: z.string().min(1).max(64),
27	  view: z.enum(["tiles", "bars", "text"]),
28	  persist: z.boolean().default(false),
29	  defaultParams: z
30	    .record(z.string(), z.unknown())
31	    .default({})
32	    .refine((p) => JSON.stringify(p).length <= 4096, "defaultParams over 4 KB"),
33	  description: z.string().max(512).optional(),
34	  /**
35	   * The hash of the program's source text in the store (WP7.6): the editor uploads it with the
36	   * module and can reopen the program on any browser; the seeder sets it for shipped programs.
37	   * Absent for a module dropped as a .wasm.
38	   */
39	  source: hash.optional(),
40	});
… (9 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/headers.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/canonical.ts
```

**Result of `Read`:**

```
1	/**
2	 * The page's content security policy (WP8.2), one string for CloudFront and the local server, so
3	 * the browser suites run under the policy the deployed page gets. Scripts come from the page's
4	 * origin and blob: workers; WebAssembly needs 'wasm-unsafe-eval'; styles are the page's plus inline
5	 * attributes; nothing frames the page.
6	 */
7	export const PAGE_CSP = [
8	  "default-src 'self' https: wss: data: blob:",
9	  "script-src 'self' 'wasm-unsafe-eval' blob:",
10	  "style-src 'self' 'unsafe-inline'",
11	  "object-src 'none'",
12	  "base-uri 'self'",
13	  "frame-ancestors 'none'",
14	].join("; ");
15	
16	/** Locally the sockets and the store are plain http and ws on the loopback. */
17	export const LOCAL_PAGE_CSP = PAGE_CSP.replace(
18	  "default-src 'self' https: wss:",
19	  "default-src 'self' https: wss: http: ws:",
20	);
21	
```

**Result of `Read`:**

```
1	/**
2	 * Canonical JSON: object keys sorted recursively, no whitespace, arrays in order.
3	 * Used wherever bytes are hashed or compared, so identical logic yields identical bytes.
4	 */
5	export function canonicalStringify(value: unknown): string {
6	  return JSON.stringify(sortKeys(value));
7	}
8	
9	function sortKeys(value: unknown): unknown {
10	  if (Array.isArray(value)) return value.map(sortKeys);
11	  if (value !== null && typeof value === "object") {
12	    const out: Record<string, unknown> = {};
13	    for (const key of Object.keys(value as Record<string, unknown>).sort()) {
14	      const v = (value as Record<string, unknown>)[key];
15	      if (v !== undefined) out[key] = sortKeys(v);
16	    }
17	    return out;
18	  }
19	  return value;
20	}
21	
22	const encoder = new TextEncoder();
23	
24	/** UTF-8 byte length of a string, identical in browsers and Node. */
25	export function byteLength(text: string): number {
26	  return encoder.encode(text).length;
27	}
28	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Read`:**

````
66	## 2. The system in one page
67	
68	**Cores** are browser tabs and MicroVMs. Each core runs the same orchestrator, which owns a WebSocket to the control plane and a disposable sandbox that executes program code. Cores come and go constantly; the design treats every departure as ordinary.
69	
70	**Programs** are WebAssembly modules with two entry points. `plan` turns params and the previous stage's outputs into the next stage's tasks. `run` turns one task's input into bytes. Both execute on cores. A program's only view of the world is a per-execution filesystem of content-addressed blobs. It has no clock, no randomness, no network, and no failure type anywhere in its API.
71	
72	**The control plane** is a pure function over a small ledger: which nodes exist, which execution is running, which task is in which state and who holds it, and the hash of every accepted result. It assigns, detects death, releases, speculates on stragglers, verifies duplicates, folds stage outputs into filesystem roots, and fans events out to observers. It never touches a payload byte. It runs as one Node process in a Lambda MicroVM, snapshots its ledger to S3 every few seconds, and hands over to a fresh MicroVM every hour.
73	
74	**The store** is S3 behind CloudFront. Every blob's key is its SHA-256. Nodes upload results, written files, logs, and program modules; observers and nodes read them back by hash. The store computes hashes on upload, so a hash reported to the ledger is one the store vouches for.
75	
76	**Invariants held everywhere:**
77	
78	- No shared memory across nodes. Coordination is message passing over sockets; data is blobs by hash.
79	- Tasks are idempotent and at-least-once. Re-execution is always safe.
80	- Results are single-assignment and memoized by hash. A done task is never recomputed unless a mismatch invalidates it.
81	- Location transparency. Nodes are addressed by id; the control plane never learns or cares which tab or VM a node lives in.
82	- Control plane / data plane split. Metadata and hashes in the ledger; bytes in the store.
83	- Program code has no error path. The only recovery logic in the system is the control plane's reassignment.
84	- Determinism. Same input and filesystem, same bytes, on any core.
85	- Public, verifiable compute only. Nothing is hidden from the host running it.
86	
87	---
88	
89	## 3. Runtime picture
90	
91	```
92	 One browser tab = one HOST                                  AWS (us-west-2)
93	 +--------------------------------------------+
94	 | Host page (main thread)                    |   HTTPS     +------------------------------+
95	 |  loads html + bundles                      |<----------- | CloudFront                   |
96	 |  calls session → {endpoint, token, ...}    |             |   /            S3 web assets |
97	 |  dashboard, controls, editor, queue,       |   GET blob  |   /blob/<hash> S3 blob store |
98	 |  programs panel, consent banner            |<----------- +------------------------------+
99	 |  observer socket  <----------------------->|--- WSS ---> +------------------------------+
100	 |  spawns node workers, relays visibility    |             | Control plane MicroVM (1 GB) |
101	 |                                            |             |  ledger · scheduler ·        |
102	 |  +---------------+  +---------------+      |             |  liveness · speculation ·    |
103	 |  | Node          |  | Node          | ...  |             |  verification · fold ·       |
104	 |  |  orchestrator |  |  orchestrator |      |             |  fan-out · presign · hooks   |
105	 |  |  + sandbox    |  |  + sandbox    | <--->|--- WSS ---> |  handover/adopt/drain        |
… (75 more lines)
````

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Read`:**

````
297	### 6.7 Queue, observers, controls
298	
299	One execution runs at a time. Human-launched executions go ahead of automatic continuations, so a submission starts within seconds while the current frame is abandoned.
300	
301	Observers subscribe and receive a paged snapshot, then sequence-numbered events; a gap triggers a resubscribe. Throughput is computed on the dashboard from `taskDone` timestamps.
302	
303	Controls: `killHalf`, `freezeHalf`, `throttleHalf` pick half the live nodes at random across the whole cluster — including cloud cores — and command them, then emit `controlApplied` naming the victims; `resumeAll`; `restart` (abandon the current execution, enqueue a fresh one of the same program at the front); `skip` (end the current execution, start the next); `killExecution {id}`; `launch`; `setRedundancy`; `ping`. Spawn is not a message: only the host page can create a thread in its own tab. Controls are rate limited per observer; the caps — 256 nodes, 64 observers, no per-IP limit — are enforced at hello and subscribe.
304	
305	**Stop, Start, and the loop's place (WP6.1, WP6.4, WP6.8).** `stop` ends the running execution, drops the loop's queued continuations (a person's queued launches stay), and holds the loop — `meta.loopStopped` — until `start`. The loop also **yields to people**: once a person's launch has ended — done, failed, or killed — it launches nothing, neither a new frame nor a queued continuation, until someone presses Start or nobody has interacted with the machine (a control, a launch, a subscribe) for ten minutes; `meta.loopYielded`, carried by snapshots and announced as it changes by the `loopYielded` event, and the machine view says `yielded`. The dashboard shows Stop in the header at all times, swapping it for Start while the machine is stopped or the loop has yielded. `pause`/`resume` are the editor tab's hold (WP6.4, in this section): nothing new is assigned or started while `meta.pausedBy` names a live socket.
306	
307	### 6.8 Fleet policy and sleep
308	
309	The active control plane keeps **two cloud cores** alive while the machine is awake: while any observer is connected or a human-launched execution is running. It launches them a second apart (the account's RunMicrovm rate is one per second) with backoff on throttling, replaces one that dies or nears its four-hour ceiling, and records their MicroVM ids in the ledger so they survive a handover; listing by tag is only a reconciliation fallback.
310	
311	After **ten minutes without an observer**, or **sixty minutes without any human interaction even with a dashboard open** (a tab left open overnight), it terminates the cores and pauses automatic continuation, and the dashboard says so with a click-to-resume. With no traffic left, the control plane's idle policy suspends it after fifteen minutes; the first visitor wakes everything.
312	
313	### 6.9 Generation
314	
315	Every control plane is stamped with a generation. It appears on welcome, snapshot, and every message; a control plane rejects messages from another generation. Exactly one generation is active at any time (§9.4).
316	
317	### 6.10 Invariants (each one a test)
318	
319	- A task is done at most once; the first accepted result is final unless a mismatch invalidates it.
320	- Nothing is ever assigned to a node that is gone.
321	- Every task completes as long as one live node exists.
322	- At most two open attempts per task.
323	- Released work outranks fresh work.
324	- No correctness depends on a message from a dying node — or from a dying control plane.
325	
326	---
327	
328	## 7. Data paths and the store
329	
330	### 7.1 Store API
331	
332	```
333	GET  /blob/<hash>                 via CloudFront: immutable, cache-forever, Range supported
334	presign {hash,size}[]             a socket message (node or observer socket) → presigned PUT URLs
335	                                  for keys equal to the hashes, SHA-256 checksum pinned
336	PUT  <presigned url>              straight to S3; S3 rejects bytes that don't hash to the key
… (90 more lines)
````

> **Thinking.** 

## Assistant · 2026-09-04T18:31:20.307Z

Now the web package sources — the heart of my area. All independent files, so one batch.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/host.ts
```

**Result of `Read`:**

```
1	// The host page (design §3): one observer socket for the dashboard, zero or more node workers.
2	// The canvas is the hero: finished tiles land on it as they are verified, and the scheduler's
3	// state is drawn over the rectangles that are still open. Around it, the panels of dashboard v2:
4	// programs, queue, the stage strip, the result of a bars or text program, files, task detail, the
5	// ledger; and the polish of WP4.1: flashes with a log of what just moved, a legend, the rotation
6	// and sleep banners, a throughput chart, and a spawn hint sized to this browser's cores.
7	
8	import type { HostToWorker, WorkerToHost } from "@tabframe/node/platform/web";
9	import type { NodeView, PlaceView } from "@tabframe/protocol";
10	import { PROTOCOL_VERSION } from "@tabframe/protocol";
11	import { connectionCopy, fmtCountdown, machineCopy, ROTATING_DETAIL } from "./banners.ts";
12	import { DEMO_CYCLE, type DemoHandle, type DemoProgram, startDemo } from "./demo.ts";
13	import { type ControlRequest, type MachineState, ObserverClient } from "./observer.ts";
14	import { gridIndexAt, gridLayout, mountPanels, type Panels } from "./panels.ts";
15	import {
16	  applyMessage,
17	  type ClusterState,
18	  emptyState,
19	  headerSlot,
20	  hostCount,
21	  inFlightByNode,
22	  isFlashing,
23	  isRunning,
24	  loopLabel,
25	  loopState,
26	  machineBanner,
27	  machineSentence,
28	  type Pulse,
29	  planTask,
30	  progress,
31	  stageTasks,
32	  stopTitle,
33	  TASK_COLOR_LABELS,
34	  type TaskColor,
35	  taskColor,
36	  throughput,
37	  throughputSeries,
38	  withRedundancy,
39	} from "./state.ts";
40	import {
… (1121 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/state.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/observer.ts
```

**Result of `Read`:**

```
1	import { Backoff } from "@tabframe/node/backoff";
2	import { fetchSession, type Session, socketProtocols } from "@tabframe/node/session";
3	import {
4	  CLOSE,
5	  type Control,
6	  type ControlPlaneToObserver,
7	  controlPlaneToObserver,
8	  decode,
9	  encode,
10	  LIMITS,
11	  type ObserverToControlPlane,
12	  PROTOCOL_VERSION,
13	} from "@tabframe/protocol";
14	import type { PresignedUpload, PresignItem } from "@tabframe/store";
15	import { applyMessage, type ClusterState, emptyState, withRedundancy } from "./state.ts";
16	
17	export type MachineState = "connecting" | "starting" | "off" | "live" | "outdated";
18	
19	/** A control as the page issues it; the client stamps the version and generation. */
20	export type ControlRequest = Control extends infer C
21	  ? C extends { v: number; gen: number }
22	    ? Omit<C, "v" | "gen">
23	    : never
24	  : never;
25	
26	export interface ObserverHandlers {
27	  /** Controls held across a reconnect that were too old to send (WP8.1). */
28	  onDropped?: (count: number) => void;
29	  onState(machine: MachineState, detail?: string): void;
30	  onCluster(state: ClusterState): void;
31	  onSession(session: Session & { kind: "on" }): void;
32	}
33	
34	/** Anything the page sends besides subscribe and ping, before the version and generation are stamped. */
35	type Outgoing = ControlRequest | { t: "presign"; items: PresignItem[] };
36	
37	/** How long a presign may stay unanswered before the upload gives up. */
38	export const PRESIGN_TIMEOUT_MS = 30_000;
39	
40	/** Spacing between outgoing controls: with a ping every two seconds this stays under the observer rate. */
… (333 more lines)
```

**Result of `Read`:**

```
1	import type {
2	  ControlPlaneToObserver,
3	  Counters,
4	  ExecutionView,
5	  MachineView,
6	  NodeView,
7	  ProgramView,
8	  QueueEntry,
9	  Snapshot,
10	  TaskLog,
11	  TaskView,
12	} from "@tabframe/protocol";
13	
14	/** Throughput is computed on the dashboard from `taskDone` arrival times over this window (design §6.7). */
15	export const THROUGHPUT_WINDOW_MS = 5_000;
16	/** How long a reassignment, a retraction, or a control's victims stay highlighted. */
17	export const FLASH_MS = 1_500;
18	/** The throughput chart looks back this far, one bucket per second (design §6.7). */
19	export const CHART_WINDOW_MS = 60_000;
20	/** Flash-worthy events kept for the pulse list beside the grid. */
21	export const PULSE_CAP = 12;
22	/** Notable events kept for the activity list. */
23	export const ACTIVITY_CAP = 400; // (WP8.2) the activity tab promises the last few hundred lines
24	/** Attempt records kept per task; a task that churns more than this keeps the latest. */
25	export const HISTORY_CAP = 16;
26	
27	/** What became of one attempt at a task, as the event stream told it (design §6.4, §6.5). */
28	export type AttemptOutcome =
29	  | "running"
30	  | "done"
31	  | "verified"
32	  | "released"
33	  | "mismatch"
34	  | "retracted"
35	  | "cancelled"
36	  | "failed";
37	
38	export interface AttemptRecord {
39	  attempt: number;
40	  nodeId: string;
… (1276 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/banners.ts
```

**Result of `Read`:**

```
1	// Words for the states in which the dashboard is not simply watching a live control plane: what is
2	// happening and what the visitor can do about it (design §6.7, §6.8, §9.4). Pure, so the copy is
3	// unit-tested and the page only places it.
4	import type { MachineState } from "./observer.ts";
5	import type { MachineBanner } from "./state.ts";
6	
7	export interface Copy {
8	  title: string;
9	  body: string;
10	  /** What the visitor can do, or that there is nothing to do. */
11	  hint: string;
12	}
13	
14	/** The detail the observer client attaches to `connecting` after a rotation closed its socket. */
15	export const ROTATING_DETAIL = "control plane rotating";
16	
17	/** The full-page banner while there is no live observer socket. */
18	export function connectionCopy(state: Exclude<MachineState, "live">, detail?: string): Copy {
19	  switch (state) {
20	    case "off":
21	      return {
22	        title: "The machine is off.",
23	        body: "An operator turns it on with mise run up. This page asks the session every few seconds and connects on its own once the machine answers.",
24	        hint: "Meanwhile the demo (?demo=1) shows this dashboard driving a scripted cluster.",
25	      };
26	    case "starting":
27	      return {
28	        title: "Waking the machine…",
29	        body: "A fresh MicroVM is booting the control plane from its snapshot, which takes about a minute; the page connects as soon as it answers.",
30	        hint: "Nothing to do but keep this tab open. Tiles start landing once a node joins — this tab lends one unless it is only observing.",
31	      };
32	    case "outdated":
33	      return {
34	        title: "This page is out of date.",
35	        body: detail
36	          ? `The machine said: ${detail}.`
37	          : "The machine speaks a newer protocol than this page was built for.",
38	        hint: "Reloading…",
39	      };
40	    default:
… (44 more lines)
```

> **Thinking.** 

> **Thinking.** Found that ObserverClient never resets backoff, main() lacks a catch around the config fetch, and the nodes table rebuilds four times a second. Next I'll pull the rest of the page (panels, editor, tiles, compiler, result) plus the node helpers (backoff, session, web platform).

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/panels.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor-page.ts
```

**Result of `Read`:**

```
1	// The in-page editor (design §5.6): the Mandelbrot source prefilled, the compiler in a lazily
2	// loaded worker, diagnostics with line numbers, a manifest and params form, compile → bundle →
3	// launch through the observer socket, and the drop-a-`.wasm` door. Loaded by host.ts on demand,
4	// so the dashboard never pays for it.
5	import { BUNDLE_PATHS, fsManifest, programManifest } from "@tabframe/protocol";
6	import { type PresignRequester, StoreClient } from "@tabframe/store/client";
7	import { sha256Hex } from "@tabframe/store/hash";
8	import type { WorkerReply, WorkerRequest } from "./compiler-worker.ts";
9	import {
10	  ASC_FLAGS,
11	  assembleSources,
12	  buildBundle,
13	  buildManifest,
14	  type CompileResult,
15	  type Diagnostic,
16	  examples,
17	  fmtBytes,
18	  formatDiagnostic,
19	  type InputRef,
20	  inspectModule,
21	  looksLikeWasm,
22	  MANDELBROT_SOURCE,
23	  MAX_MODULE_BYTES,
24	  type ModuleInfo,
25	  parseParams,
26	  shippedManifest,
27	} from "./editor-core.ts";
28	import { type ClusterState, type ProgramInfo, programList } from "./state.ts";
29	
30	/** What the page gives the editor: the socket's upload and launch paths, and the cluster feed. */
31	export interface EditorHost {
32	  connected(): boolean;
33	  storeBase(): string | null;
34	  presign: PresignRequester["presign"];
35	  launch(bundle: string, params: Record<string, unknown>): boolean;
36	  subscribe(listener: (state: ClusterState) => void): () => void;
37	  /** Where the compiler worker script lives; defaults to `compiler-worker.js` next to this module. */
38	  workerUrl?: string;
39	  /** A demo page: the compile is real, launching needs the live machine (WP7.7). */
40	  demo?: boolean;
… (658 more lines)
```

**Result of `Read`:**

```
1	// Dashboard v2 panels (design §5.1, §6.7, §8.3): programs, queue, the stage strip, the result of
2	// a `bars` or `text` program, the files behind an execution's root, a task's detail, the ledger
3	// of settled tasks (hashes and where their bytes live), and failure surfacing. The state comes from `state.ts`; bytes come from the store by hash; controls go back
4	// through the observer socket. No framework: each panel is a render function over the state that
5	// rebuilds its DOM only when what it shows has changed, so buttons stay put under a finger.
6	import type { ControlRequest } from "./observer.ts";
7	import {
8	  barRows,
9	  type FileEntry,
10	  finalOutput,
11	  fmtBytes,
12	  fmtValue,
13	  groupFiles,
14	  listFiles,
15	  type Preview,
16	  parseManifest,
17	  previewOf,
18	  readBars,
19	  TEXT_PREVIEW_BYTES,
20	} from "./result.ts";
21	import {
22	  type AttemptRecord,
23	  type ClusterState,
24	  type ExecutionState,
25	  ledgerRows,
26	  type ProgramInfo,
27	  programList,
28	  stageStrip,
29	  type TaskState,
30	} from "./state.ts";
31	import type { BlobSource } from "./tiles.ts";
32	
33	export interface PanelDeps {
34	  /** The store as the page reaches it right now (the demo's in-memory store, or the CDN). */
35	  blobs(): BlobSource;
36	  /** Where the store's blobs are addressed from, `<base>/<hash>`; null for the demo's in-page store. */
37	  storeBase(): string | null;
38	  /** Issue a control; false when the machine is not there to take it. */
39	  send(control: ControlRequest): boolean;
40	  /** Ask the page to render again once a fetch has landed or a panel's own state moved. */
… (1122 more lines)
```

**Result of `Read`:**

```
1	// The editor in its own tab (WP6.4): its own observer socket, the same editor as before, and a
2	// pause on the machine for as long as this tab lives — launching or closing resumes it, and so
3	// does the control plane by itself when this socket goes away.
4	import { mountEditor } from "./editor.ts";
5	import { type MachineState, ObserverClient } from "./observer.ts";
6	import type { ClusterState } from "./state.ts";
7	
8	const $ = <T extends Element>(sel: string): T => {
9	  const el = document.querySelector<T>(sel);
10	  if (!el) throw new Error(`missing ${sel}`);
11	  return el;
12	};
13	const machineEl = $<HTMLSpanElement>("#machine");
14	const pauseEl = $<HTMLSpanElement>("#pauseState");
15	const listeners = new Set<(state: ClusterState) => void>();
16	let storeBase: string | null = null;
17	let client: ObserverClient | null = null;
18	let paused = false;
19	/** Whether this tab still wants the machine held (WP8.1): not after its launch, not after close. */
20	let holdWanted = true;
21	let lastLaunch: "sent" | "held" | "refused" = "sent";
22	
23	function setMachine(state: MachineState, detail?: string): void {
24	  machineEl.textContent = detail ? `${state} · ${detail}` : state;
25	  machineEl.className = `pill ${state === "live" ? "live" : state === "off" || state === "outdated" ? "off" : "wait"}`;
26	  if (state === "live") {
27	    // Every live socket asks again — a reconnect (a rotation, a gap) is a new holder — but only
28	    // while this tab still wants the hold (WP8.1): after its launch a resubscribe used to pause
29	    // the machine again under the person's own running program.
30	    if (!holdWanted) return;
31	    client?.send({ t: "pause" });
32	    paused = true;
33	    pauseEl.textContent =
34	      "the machine is paused while this tab is open — in-flight tasks finish, nothing new starts; launch or close to resume";
35	  } else if (state === "off" || state === "outdated") {
36	    paused = false;
37	    pauseEl.textContent = "no machine to pause";
38	  }
39	}
40	
… (86 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor-core.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/tiles.ts
```

**Result of `Read`:**

```
1	// The editor's pure parts (design §5.6): the virtual filesystem a compile sees, what a compile
2	// reports, the manifest and params the reviewer edits, the bundle a launch uploads, and the
3	// checks on a dropped module. No DOM, no sockets, no compiler: the UI (editor.ts) and the worker
4	// (compiler-worker.ts) are thin around this, and the tests run it under Bun.
5	import {
6	  BUNDLE_PATHS,
7	  canonicalStringify,
8	  type FsManifest,
9	  LIMITS,
10	  type ProgramManifest,
11	  programManifest,
12	} from "@tabframe/protocol";
13	import { validateModuleBytes } from "@tabframe/sandbox/validate";
14	import { sha256Hex } from "@tabframe/store/hash";
15	import {
16	  GUIDE_MARKDOWN,
17	  HELLO_MANIFEST,
18	  HELLO_SOURCE,
19	  MANDELBROT_MANIFEST,
20	  MANDELBROT_SOURCE,
21	  SDK_FILES,
22	  WORDCOUNT_MANIFEST,
23	  WORDCOUNT_SOURCE,
24	} from "./program-sources.generated.ts";
25	
26	/** The compiler flags every program uses; must equal the SDK build's (a test pins it). */
27	export const ASC_FLAGS: readonly string[] = [
28	  "-O3",
29	  "--runtime",
30	  "stub",
31	  "--noAssert",
32	  "--maximumMemory",
33	  "256",
34	];
35	/** The sandbox's reference memory maximum, in 64 KiB pages. */
36	export const MEMORY_PAGES_MAX = 256;
37	
38	/** Where the edited program lives in the virtual filesystem, and where the SDK lives. */
39	export const ENTRY = "program/assembly/index.ts";
40	export const SDK_ROOT = "node_modules/@tabframe/sdk-as";
… (365 more lines)
```

**Result of `Read`:**

```
1	// The tiles view (design §3 step 3, §5.6): finished tiles are fetched by hash from the store,
2	// re-hashed before they are trusted, and painted into an offscreen canvas at the execution's size.
3	import type { PlaceView } from "@tabframe/protocol";
4	import { sha256Hex } from "@tabframe/store/hash";
5	import { type ClusterState, stageTasks, type TaskState } from "./state.ts";
6	
7	export type TileFlag = "bad-hash" | "bad-size" | "missing";
8	
9	/** Where pixels go. The page backs it with canvases; tests record calls. */
10	export interface TilePainter {
11	  /** A new frame: size the surface and clear it. */
12	  reset(w: number, h: number): void;
13	  put(place: PlaceView, rgba: Uint8ClampedArray): void;
14	  /** Outline a rectangle the dashboard refuses to trust. */
15	  flag(place: PlaceView, flag: TileFlag): void;
16	  /** A retracted or withdrawn tile. */
17	  clear(place: PlaceView): void;
18	}
19	
20	export interface BlobSource {
21	  /** Bytes for a hash, or null when the store has no such blob (yet). */
22	  get(hash: string): Promise<Uint8Array | null>;
23	}
24	
25	export interface TileStats {
26	  fetched: number;
27	  painted: number;
28	  flagged: number;
29	  inFlight: number;
30	}
31	
32	interface Painted {
33	  hash: string;
34	  place: PlaceView;
35	  flag: TileFlag | null;
36	}
37	
38	/** Fetches from `<storeBase>/<hash>` with the cache the CDN allows: blobs are immutable. */
39	export function storeSource(storeBase: string, fetchImpl: typeof fetch = fetch): BlobSource {
40	  const base = storeBase.replace(/\/$/, "");
… (163 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/compiler.ts
```

**Result of `Read`:**

```
1	// The AssemblyScript compiler over an in-memory filesystem (design §5.6). Imported only by the
2	// compiler worker and the tests: asc is 1.6 MB before binaryen, and the dashboard must not pay
3	// for it. The same function runs under Node in the tests that check a page compile is
4	// byte-identical to the build's.
5	import type { DiagnosticMessage } from "assemblyscript";
6	import asc from "assemblyscript/asc";
7	// Types only: the worker bundle must not drag the protocol schemas or the sandbox along.
8	import type { CompileResult, Diagnostic, DiagnosticLevel, VirtualFs } from "./editor-core.ts";
9	
10	const OUT = "program.wasm";
11	
12	/** Normalize what asc asks for: strip `./` segments and join with the base directory. */
13	export function virtualPath(filename: string, baseDir: string): string {
14	  const joined =
15	    baseDir && baseDir !== "." && !filename.startsWith("/") ? `${baseDir}/${filename}` : filename;
16	  const parts: string[] = [];
17	  for (const seg of joined.split("/")) {
18	    if (seg === "" || seg === ".") continue;
19	    if (seg === "..") parts.pop();
20	    else parts.push(seg);
21	  }
22	  return parts.join("/");
23	}
24	
25	const LEVELS: Record<number, DiagnosticLevel> = {
26	  0: "pedantic",
27	  1: "info",
28	  2: "warning",
29	  3: "error",
30	};
31	
32	/**
33	 * asc hands the reporter a plain copy: code, category, message, and a range with byte offsets and
34	 * the source's normalized path — no line numbers. Those come from the virtual file itself.
35	 */
36	function toDiagnostic(d: DiagnosticMessage, fs: VirtualFs): Diagnostic {
37	  let file: string | null = null;
38	  let line: number | null = null;
39	  let column: number | null = null;
40	  if (d.range) {
… (86 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/compiler-worker.ts
```

**Result of `Read`:**

```
1	/// <reference lib="webworker" />
2	// The compiler worker (design §5.6): AssemblyScript's asc, bundled for the browser, loaded only
3	// when the editor opens. Binaryen arrives as its own asset next to this script. One compile at a
4	// time; the page waits.
5	import { COMPILER_VERSION, compileInMemory } from "./compiler.ts";
6	import type { CompileResult, VirtualFs } from "./editor-core.ts";
7	
8	export interface CompileRequest {
9	  type: "compile";
10	  id: number;
11	  fs: VirtualFs;
12	  flags: string[];
13	}
14	export type WorkerRequest = CompileRequest;
15	export type WorkerReply =
16	  | { type: "ready"; version: string }
17	  | ({ type: "compiled"; id: number } & CompileResult);
18	
19	const scope = self as unknown as DedicatedWorkerGlobalScope;
20	
21	scope.onmessage = async (ev: MessageEvent<WorkerRequest>) => {
22	  const msg = ev.data;
23	  if (msg?.type !== "compile") return;
24	  const result = await compileInMemory(msg.fs, msg.flags);
25	  const reply: WorkerReply = { type: "compiled", id: msg.id, ...result };
26	  scope.postMessage(reply);
27	};
28	
29	scope.postMessage({ type: "ready", version: COMPILER_VERSION } satisfies WorkerReply);
30	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/result.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/backoff.ts
```

**Result of `Read`:**

```
1	// What an execution produced, read from the store by hash (design §5.1, §5.4): the filesystem
2	// manifest behind `execution.root`, the final output of a `bars` or `text` program, and previews
3	// of the files a person clicks on. Pure functions over bytes; the page fetches.
4	import {
5	  AbiError,
6	  BARS_LIMITS,
7	  type Bar,
8	  BUNDLE_PATHS,
9	  decodeBars,
10	  type FsManifest,
11	  fsManifest,
12	} from "@tabframe/protocol";
13	
14	export interface FileEntry {
15	  path: string;
16	  hash: string;
17	  size: number;
18	}
19	
20	/** Files grouped for the panel: the bundle's own, then each stage's outputs, then everything else. */
21	export interface FileGroup {
22	  /** `/in/`, `/out/<stage>/`, `/` for the bundle's two fixed files, or the first path segment. */
23	  label: string;
24	  files: FileEntry[];
25	  bytes: number;
26	}
27	
28	export function parseManifest(bytes: Uint8Array): FsManifest {
29	  return fsManifest.parse(JSON.parse(new TextDecoder().decode(bytes)));
30	}
31	
32	/** Entries sorted by path, with `/out/<stage>/<index>` sorted numerically inside a stage. */
33	export function listFiles(manifest: FsManifest): FileEntry[] {
34	  return Object.entries(manifest.files)
35	    .map(([path, f]) => ({ path, hash: f.hash, size: f.size }))
36	    .sort((a, b) => comparePaths(a.path, b.path));
37	}
38	
39	export function groupFiles(files: readonly FileEntry[]): FileGroup[] {
40	  const groups = new Map<string, FileGroup>();
… (157 more lines)
```

**Result of `Read`:**

```
1	import { LIMITS } from "@tabframe/protocol";
2	
3	/** Exponential backoff with full jitter between the protocol's reconnect bounds. */
4	export class Backoff {
5	  private attempt = 0;
6	  private readonly rng: () => number;
7	
8	  constructor(rng: () => number = Math.random) {
9	    this.rng = rng;
10	  }
11	
12	  next(): number {
13	    const cap = Math.min(LIMITS.reconnectMaxMs, LIMITS.reconnectMinMs * 2 ** this.attempt);
14	    this.attempt = Math.min(this.attempt + 1, 10);
15	    return Math.floor(
16	      LIMITS.reconnectMinMs + this.rng() * Math.max(0, cap - LIMITS.reconnectMinMs),
17	    );
18	  }
19	
20	  reset(): void {
21	    this.attempt = 0;
22	  }
23	}
24	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/session.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/platform/web.ts
```

**Result of `Read`:**

```
1	/** What the session function (or the local control plane) answers (design §8.1). */
2	export type Session =
3	  | {
4	      kind: "on";
5	      endpoint: string;
6	      token: string;
7	      expiresAt: number;
8	      storeBase: string;
9	      generation: number;
10	    }
11	  | { kind: "off" }
12	  | { kind: "starting"; retryAfterMs: number };
13	
14	export type FetchLike = (
15	  url: string,
16	) => Promise<{ ok: boolean; status: number; json(): Promise<unknown> }>;
17	
18	export async function fetchSession(sessionUrl: string, fetchImpl: FetchLike): Promise<Session> {
19	  const res = await fetchImpl(sessionUrl);
20	  if (!res.ok) throw new Error(`session ${res.status}`);
21	  const body = (await res.json()) as Record<string, unknown>;
22	  if (body.off === true) return { kind: "off" };
23	  if (body.starting === true) {
24	    const retry = typeof body.retryAfterMs === "number" ? body.retryAfterMs : 3_000;
25	    return { kind: "starting", retryAfterMs: retry };
26	  }
27	  const endpoint = body.endpoint;
28	  const token = body.token;
29	  const storeBase = body.storeBase;
30	  const generation = body.generation;
31	  if (
32	    typeof endpoint !== "string" ||
33	    typeof token !== "string" ||
34	    typeof storeBase !== "string" ||
35	    typeof generation !== "number"
36	  ) {
37	    throw new Error("malformed session");
38	  }
39	  const expiresAt =
40	    typeof body.expiresAt === "number"
… (32 more lines)
```

**Result of `Read`:**

```
1	/// <reference lib="webworker" />
2	/**
3	 * Browser platform: this file is the Web Worker a host page spawns per node. The worker owns the
4	 * socket, the heartbeat, and the sandbox worker; the host only sends init and visibility and
5	 * receives status.
6	 */
7	import { createWebSandboxHost } from "@tabframe/sandbox/adapters/web-host";
8	import { Orchestrator, type SocketLike, type Status } from "../orchestrator.ts";
9	
10	interface InitMessage {
11	  type: "init";
12	  sessionUrl: string;
13	  hostId: string;
14	  visible: boolean;
15	  /** The bundled sandbox worker; defaults to `sandbox.js` next to this worker's script. */
16	  sandboxUrl?: string;
17	}
18	interface VisibilityMessage {
19	  type: "visibility";
20	  visible: boolean;
21	}
22	interface StopMessage {
23	  type: "stop";
24	}
25	export type HostToWorker = InitMessage | VisibilityMessage | StopMessage;
26	export type WorkerToHost = { type: "status" } & Status;
27	
28	const scope = self as unknown as DedicatedWorkerGlobalScope;
29	let orchestrator: Orchestrator | null = null;
30	
31	scope.onmessage = (ev: MessageEvent<HostToWorker>) => {
32	  const msg = ev.data;
33	  switch (msg.type) {
34	    case "init": {
35	      if (orchestrator) return;
36	      const sandboxUrl = msg.sandboxUrl ?? new URL("./sandbox.js", scope.location.href).href;
37	      orchestrator = new Orchestrator({
38	        sessionUrl: msg.sessionUrl,
39	        hostId: msg.hostId,
40	        kind: "tab",
… (29 more lines)
```

> **Thinking.** 

> **Thinking.** I've confirmed several issues: BlobCache doesn't retry error entries, the files/ledger panels rebuild on global cache.version causing focus loss, the running pill stays lit past failures via `phase !== "done"`, and the editor matches launches by program name. Next I'll check the demo, HTML shells, node runtime, and control-plane facts on stopped-execution status and observer liveness to verify the state.ts mapping.
> 
> 

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/demo.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/public/index.html
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/public/editor.html
```

**Result of `Read`:**

```
1	<!doctype html>
2	<html lang="en">
3	<head>
4	<meta charset="utf-8">
5	<meta name="viewport" content="width=device-width, initial-scale=1">
6	<title>Tabframe · editor</title>
7	<link rel="stylesheet" href="/styles.css">
8	<style>
9	  /* WP7.5: the editor uses the whole viewport — the source fills the height, the launch column sits
10	     beside it, one action bar under the source; the guide is a page of its own. */
11	  html, body { height: 100%; }
12	  body { box-sizing: border-box; margin: 0; padding: .75rem 1.25rem; display: flex; flex-direction: column; gap: .5rem; overflow: hidden; }
13	  .editor-page-head { display: flex; align-items: center; gap: .8rem; flex-wrap: wrap; }
14	  .editor-page-head h1 { margin: 0; font-size: 1.2rem; }
15	  .editor-page-head .grow { flex: 1; min-width: 12rem; }
16	  .editor-page-head .guide-link { white-space: nowrap; }
17	  #editorIntro { margin: 0; max-width: 80rem; }
18	  .editor { display: flex; flex-direction: column; flex: 1; min-height: 0; }
19	  .editor-head { margin-bottom: .4rem; }
20	  .editor-grid { flex: 1; min-height: 0; display: grid; grid-template-columns: minmax(0, 1fr) 320px; gap: 1rem; }
21	  .editor-main { display: flex; flex-direction: column; min-height: 0; }
22	  #source { flex: 1; min-height: 8rem; height: auto; resize: none; }
23	  .editor-actions { margin: .5rem 0 0; flex-wrap: wrap; height: auto; }
24	  #diagnostics { max-height: 7rem; overflow: auto; margin: .3rem 0 0; }
25	  .editor-side { min-height: 0; overflow: auto; padding-right: .2rem; }
26	  @media (max-width: 1100px) { .editor-grid { grid-template-columns: 1fr; } body { overflow: auto; } }
27	</style>
28	</head>
29	<body>
30	<div class="editor-page-head">
31	  <h1>Tabframe editor</h1>
32	  <span id="machine" class="pill">connecting</span>
33	  <span id="pauseState" class="muted grow">connecting to the machine…</span>
34	  <a class="guide-link" href="/" title="The dashboard this tab was opened from">← dashboard</a>
35	  <a class="guide-link" id="guideLink" href="/guide.html" target="_blank" rel="noreferrer" title="What a program is, what it may do, and the limits on this machine — in its own tab">What is a program? ↗</a>
36	</div>
37	<p id="editorIntro" class="muted small">A program is one WebAssembly module with two entry points, <code>plan</code> and <code>run</code>, and a manifest. Edit the source, compile it in this tab, and launch it on the machine — or drop a <code>.wasm</code> built any other way. While this tab is open the machine is paused; launching or closing resumes it. <span class="muted">Opened from the dashboard.</span></p>
38	  <section id="editor" class="editor">
39	    <div class="editor-head">
40	      <h2>Editor</h2>
… (31 more lines)
```

**Result of `Read`:**

```
1	<!doctype html>
2	<html lang="en">
3	<head>
4	<meta charset="utf-8">
5	<meta name="viewport" content="width=device-width, initial-scale=1">
6	<title>Tabframe</title>
7	<link rel="stylesheet" href="/styles.css">
8	</head>
9	<body>
10	<header>
11	  <h1>TABFRAME</h1>
12	  <span id="machine" class="pill wait">connecting</span>
13	  <span id="gen" class="pill">gen —</span>
14	  <span id="nextRotation" class="pill" title="The control plane hands over to a fresh MicroVM every hour; the render continues across it" hidden>rotation in — min</span>
15	  <span id="counts" class="pill">0 nodes · 0 hosts</span>
16	  <span id="seq" class="pill">seq 0</span>
17	  <span id="exec" class="pill">idle</span>
18	  <span id="loop" class="pill loop" data-loop="running" title="The automatic loop renders frame after frame while someone watches">loop · running</span>
19	  <span id="rate" class="pill">0.0 tasks/s</span>
20	  <span class="spacer"></span>
21	  <button type="button" id="stop" class="danger" title="Ends what is running and holds the automatic loop until Start; a launch of yours still runs at once">stop</button>
22	  <button type="button" id="start" title="Lets the automatic loop run again" hidden>start</button>
23	  <button type="button" id="resume" title="Resumes the machine an editor tab holds paused" hidden>resume</button>
24	  <button type="button" id="freezePanel" class="panel-only" hidden title="Stop this tab from updating so you can read it; updates queue and apply on resume">pause updates</button>
25	  <a id="backToDashboard" class="panel-only" href="/" hidden title="Back to the dashboard this tab was opened from">← dashboard</a>
26	  <button type="button" id="openEditor" title="Open the editor in its own tab; the machine pauses while it is open">editor ↗</button>
27	</header>
28	<div class="status-line"><div id="machineBanner" class="machine-banner" role="status" hidden></div><div id="notice" class="notice" aria-live="polite" hidden></div></div>
29	<main>
30	  <section class="hero">
31	    <div id="banner" class="banner" data-state="connecting"><strong id="bannerTitle">Connecting…</strong><span id="bannerBody"></span><span id="bannerHint" class="muted small"></span></div>
32	    <div id="stage" hidden>
33	      <div class="execrow">
34	        <span id="execName">No execution</span>
35	        <span id="execDetail" class="muted"></span>
36	        <div class="bar"><div id="progressFill"></div></div>
37	        <span id="progressText">0/0</span>
38	        <span class="throughput"><canvas id="throughputChart" width="240" height="36" aria-label="tasks done per second over the last minute"></canvas><span id="throughputFigure">0.0 tasks/s · 0 nodes</span></span>
39	      </div>
40	      <div id="messages" class="messages">
… (86 more lines)
```

**Result of `Read`:**

```
1	// `?demo=1`: a scripted control plane inside the page. It feeds the reducer the same messages the
2	// wire carries (snapshot, executions, stages, task events, controls, a rotation) and serves real
3	// Mandelbrot tiles from an in-memory store under their true hashes, so the dashboard's fetch and
4	// re-hash path runs unchanged. Nothing here touches the network. Used for screenshots and for
5	// looking at the dashboard without a cluster.
6	//
7	// The machine cycles through three programs: a Mandelbrot frame (the story of dashboard v1: a
8	// straggler, a liar, a scrambled tile, kill half, a rotation), a word count (three stages, a
9	// `bars` result, a filesystem to browse, logs on the tasks), and a broken program whose planner
10	// traps (the failure banner), after which the machine goes to sleep and wakes for the next frame
11	// (the sleep banner). `?program=<name>` starts the cycle there; `?hold=1` pauses after the first
12	// execution ends.
13	import type {
14	  ControlPlaneToObserver,
15	  Counters,
16	  ExecutionView,
17	  FsManifest,
18	  NodeView,
19	  PlaceView,
20	  ProgramView,
21	  Snapshot,
22	  TaskView,
23	} from "@tabframe/protocol";
24	import { canonicalStringify, encodeBars, PROTOCOL_VERSION } from "@tabframe/protocol";
25	import { sha256Hex } from "@tabframe/store/hash";
26	import type { ControlRequest } from "./observer.ts";
27	
28	export const DEMO_CANVAS = { w: 2048, h: 1280 } as const;
29	export const DEMO_TILE = 64;
30	const COLS = DEMO_CANVAS.w / DEMO_TILE;
31	const ROWS = DEMO_CANVAS.h / DEMO_TILE;
32	export const DEMO_TASKS = COLS * ROWS;
33	const SLOTS = 2;
34	const PROGRAM = "9c2f0d6b1e4a7c3f5d8b2a6e0c4f1d7b3a9e5c8f2b6d0a4e8c1f3b7d5a9e2c6f";
35	const WORDCOUNT = "4d3b2a1908f7e6d5c4b3a2918070f6e5d4c3b2a1908f7e6d5c4b3a2918070f6e";
36	const BROKEN = "0f1e2d3c4b5a69788796a5b4c3d2e1f00f1e2d3c4b5a69788796a5b4c3d2e1f0";
37	/** The tile (in completion order) served under its true hash with the wrong bytes. */
38	export const DEMO_SCRAMBLED_AT = 150;
39	/** Why the demo machine sleeps after the broken program: the core's own wording (design §6.8). */
40	export const DEMO_SLEEP_REASON = "an hour without anyone touching the dashboard";
… (1080 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/public/guide.html
```

**Result of `Read`:**

```
1	<!doctype html>
2	<html lang="en">
3	<head>
4	<meta charset="utf-8">
5	<meta name="viewport" content="width=device-width, initial-scale=1">
6	<title>Tabframe · what a program is</title>
7	<link rel="stylesheet" href="/styles.css">
8	<style>
9	  body { padding: 1rem 1.25rem 3rem; }
10	  .guide-page-head { display: flex; align-items: baseline; gap: 1rem; flex-wrap: wrap; margin-bottom: .6rem; }
11	  .guide-page-head h1 { margin: 0; font-size: 1.2rem; }
12	  .guide-page-head nav a { margin-right: .8rem; }
13	  .guide { max-width: 68rem; }
14	</style>
15	</head>
16	<body>
17	<div class="guide-page-head">
18	  <h1>What a program is, and what it may do</h1>
19	  <nav class="muted small"><a href="/editor.html" title="The editor: edit, compile, launch">← editor</a><a href="/" title="The dashboard">dashboard</a></nav>
20	</div>
21	<section id="guide" class="guide">
22	  <p class="muted small">Opened from the editor. A program is one WebAssembly module with two entry points, <code>plan</code> and <code>run</code>, and a manifest; it sees the world only through a per-execution filesystem. Below: the SDK's README as the editor ships it, and the limits this machine holds a program to.</p>
23	  <p class="muted small" id="guideLimits"></p>
24	  <div id="guideBody"></div>
25	</section>
26	<script type="module" src="/guide-page.js"></script>
27	</body>
28	</html>
29	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/orchestrator.ts
```

**Result of `Read`:**

```
1	import {
2	  type Assign,
3	  CLOSE,
4	  type Command,
5	  type ControlPlaneToNode,
6	  controlPlaneToNode,
7	  decode,
8	  encode,
9	  type Heartbeat,
10	  type Hello,
11	  LIMITS,
12	  type NodeKind,
13	  PROTOCOL_VERSION,
14	  type RotatingReason,
15	} from "@tabframe/protocol";
16	import { StoreClient } from "@tabframe/store";
17	import { Backoff } from "./backoff.ts";
18	import { type FetchLike, fetchSession, type Session, socketProtocols } from "./session.ts";
19	import { type SandboxRunner, SocketPresigner, TaskRunner } from "./tasks.ts";
20	
21	/** The WHATWG surface the orchestrator needs; browser and Node WebSockets both provide it. */
22	export interface SocketLike {
23	  readonly readyState: number;
24	  send(data: string): void;
25	  close(code?: number, reason?: string): void;
26	  onopen: ((ev: unknown) => void) | null;
27	  onmessage: ((ev: { data: unknown }) => void) | null;
28	  onclose: ((ev: { code: number; reason: string }) => void) | null;
29	  onerror: ((ev: unknown) => void) | null;
30	}
31	
32	export interface Timers {
33	  setTimeout(fn: () => void, ms: number): unknown;
34	  clearTimeout(handle: unknown): void;
35	}
36	
37	export type NodeState =
38	  | "connecting"
39	  | "idle"
40	  | "busy"
… (408 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/tasks.ts
```

**Result of `Read`:**

```
1	import {
2	  type Assign,
3	  encodeRunInput,
4	  type FsManifest,
5	  fsManifest,
6	  LIMITS,
7	  PROTOCOL_VERSION,
8	  RELEASED,
9	  type Result,
10	} from "@tabframe/protocol";
11	import type { HostRequest, TaskResult } from "@tabframe/sandbox";
12	import type { PresignRequester, StoreClient } from "@tabframe/store";
13	
14	/** The sandbox as the orchestrator sees it (design §4.2): one call, one deadline, a kill switch. */
15	export interface SandboxRunner {
16	  run(module: WebAssembly.Module, request: HostRequest, deadlineMs: number): Promise<TaskResult>;
17	  dispose(): void;
18	}
19	
20	export interface TaskRunnerDeps {
21	  store: StoreClient;
22	  createSandbox(): SandboxRunner;
23	  compile(bytes: Uint8Array): Promise<WebAssembly.Module>;
24	  now(): number;
25	  log?: ((event: string, fields?: Record<string, unknown>) => void) | undefined;
26	}
27	
28	const EMPTY_MANIFEST: FsManifest = { version: 1, files: {} };
29	const MAX_CACHED_MODULES = 8;
30	const MAX_CACHED_MANIFESTS = 32;
31	
32	export type Outcome =
33	  | { kind: "result"; msg: Omit<Result, "v" | "gen"> }
34	  | { kind: "dropped"; reason: string };
35	
36	/**
37	 * Runs one task end to end: module and manifest by hash (cached), ABI framing, the sandbox with
38	 * the deadline plus a grace second, then output, writes, and log uploaded through the store so
39	 * the result carries only hashes the store vouches for (design §4.1, §7.3).
40	 */
… (264 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/platform/node.ts
```

**Result of `Read`:**

```
1	/**
2	 * Node platform: one process is one node. Cloud cores run this inside the MicroVM image; the local
3	 * dev topology runs two of them as stand-ins. Status goes to stdout as JSON lines. A close command
4	 * ends the process; whoever supervises it (the fleet policy, the dev runner) brings a fresh one.
5	 */
6	import { hostname } from "node:os";
7	import { createNodeSandboxHost } from "@tabframe/sandbox/adapters/node-host";
8	import { StoreClient } from "@tabframe/store";
9	import { Orchestrator, type SocketLike, type Status } from "../orchestrator.ts";
10	
11	const sessionUrl = process.env.TABFRAME_SESSION_URL;
12	if (!sessionUrl) {
13	  console.error("TABFRAME_SESSION_URL is required");
14	  process.exit(2);
15	}
16	const hostId = process.env.TABFRAME_HOST_ID ?? `core-${hostname()}-${process.pid}`;
17	
18	function emit(event: string, fields: Record<string, unknown> = {}): void {
19	  process.stdout.write(`${JSON.stringify({ at: new Date().toISOString(), event, ...fields })}\n`);
20	}
21	
22	/** The sandbox worker thread reads blobs through the orchestrator's store client. */
23	export function blobReaderFor(storeBase: string) {
24	  const reads = new StoreClient(storeBase, {
25	    presign: () => Promise.reject(new Error("read-only client")),
26	  });
27	  return async (hash: string, offset: number, len: number): Promise<Uint8Array | null> => {
28	    if (offset === 0 && !Number.isFinite(len)) return reads.get(hash);
29	    if (Number.isFinite(len)) return reads.get(hash, { offset, length: len });
30	    const whole = await reads.get(hash);
31	    return whole ? whole.slice(offset) : null;
32	  };
33	}
34	
35	const orchestrator = new Orchestrator({
36	  sessionUrl,
37	  hostId,
38	  kind: "core",
39	  cores: 1,
40	  sandboxVersion: "1",
… (31 more lines)
```

**Tool `Bash`:**

```
grep -rn "stopped by a person" packages/control-plane/src --include=*.ts | grep -v test | head -20; echo ----STATUS----; grep -rn "status: \"cancelled\"\|status = \"cancelled\"\|\"cancelled\"" packages/control-plane/src --include=*.ts | grep -v "\.test\." | head -20; echo ----OBSERVER-LIVENESS----; grep -rn -i "observerPing\|observer.*silent\|lastPing\|pingAt\|observerTimeout\|OBSERVER_.*MS" packages/control-plane/src --include=*.ts | grep -v "\.test\." | head -20; echo ----CAP----; grep -rn "CLIENT_CONNECTION_CAP\|PROGRAMS_CAP\|STORE_RETRY_MS" packages/control-plane/src --include=*.ts | grep -v "\.test\." | head
```

**Result of `Bash`:**

```
----STATUS----
----OBSERVER-LIVENESS----
----CAP----
packages/control-plane/src/server.ts:53:const CLIENT_CONNECTION_CAP = 14;
packages/control-plane/src/server.ts:468:    if (conns.size >= CLIENT_CONNECTION_CAP) {
```

**Tool `Bash`:**

```
cat packages/web/src/guide-page.ts packages/web/src/node.ts packages/web/src/sandbox.ts; echo ----CSS-A11Y----; grep -n "focus\|outline\|prefers-reduced-motion\|prefers-color\|@media\|sr-only\|visually-hidden" packages/web/public/styles.css | head -60; echo ----BUILD----; cat packages/web/scripts/build.ts | head -150
```

**Result of `Bash`:**

```
// The guide in its own page (WP7.5, D4): what a program is and what it may do — the SDK's README
// rendered at build time — and the numbers this machine holds a program to. Linked from the top
// of the editor as "What is a program?".
import { guideMarkdown, limitsSentence, renderGuide } from "./editor-core.ts";

const body = document.querySelector<HTMLDivElement>("#guideBody");
const limits = document.querySelector<HTMLParagraphElement>("#guideLimits");
if (body && body.childElementCount === 0) body.append(renderGuide(guideMarkdown()));
if (limits) limits.textContent = limitsSentence();
// The per-node Web Worker entry: the whole orchestrator lives in @tabframe/node.
import "@tabframe/node/platform/web";
// The sandbox Web Worker entry: the whole worker lives in @tabframe/sandbox.
import "@tabframe/sandbox/adapters/web-worker";
----CSS-A11Y----
101:@media (max-width: 1100px) {
744:@media (max-width: 1100px) {
1352:.visually-hidden,
1353:.editor-side input.visually-hidden {
----BUILD----
// Bundles the browser entry points that exist and copies public/ into dist/. `--watch` rebuilds on
// change. Runs under Bun (the developer toolchain); nothing at runtime depends on it.
//
// Three bundles come out:
//   - the page: host.js, node.js, sandbox.js, editor.js — one file per entry, as before;
//   - compiler-worker.js: AssemblyScript's asc for the browser, minified, with its Node-only
//     imports left as never-taken dynamic imports and binaryen imported from a sibling file;
//   - binaryen.js: binaryen itself, copied as is (13 MB, almost all of it the compiler's own
//     WebAssembly, which no minifier shrinks), so the compiler chunk stays small and the browser
//     caches the big one separately.
import {
  copyFileSync,
  existsSync,
  mkdirSync,
  readdirSync,
  statSync,
  watch,
  writeFileSync,
} from "node:fs";
import path from "node:path";
import { generateSources } from "./gen-sources.ts";
… (125 more lines)
```

> **Thinking.** 

> **Thinking.** No stop reason logic found in the control plane, so it likely lives in packages/core—I'll search there to check the toExecutionState mapping while also pulling the browser suites and page unit tests in parallel.

**Tool `Bash`:**

```
ls packages; echo ----STOP----; grep -rn "stopped by a person" packages --include=*.ts -l | grep -v node_modules | grep -v "\.test\." ; echo; grep -rn -B3 -A3 "stopped by a person" packages/core/src --include=*.ts | grep -v "\.test\." | head -60; echo ----STATUS-ON-STOP----; grep -rn "status: \"cancelled\"\|status = \"cancelled\"\|\"cancelled\"" packages/core/src --include=*.ts | grep -v "\.test\." | head -20
```

**Result of `Bash`:**

```
control-plane
core
dev
fleet
infra
node
protocol
sandbox
sdk-as
store
web
----STOP----
packages/core/src/apply.ts
packages/web/src/demo.ts
packages/web/src/state.ts
packages/protocol/src/task.ts

packages/core/src/apply.ts-465-      // continuations go, a person's own queued launches stay, and the loop waits for Start.
packages/core/src/apply.ts-466-      const effects: Effect[] = [];
packages/core/src/apply.ts-467-      ledger.meta.loopStopped = true;
packages/core/src/apply.ts:468:      if (running) effects.push(...cancelExecution(ledger, running, "stopped by a person", now));
packages/core/src/apply.ts-469-      for (const id of [...ledger.queue]) {
packages/core/src/apply.ts-470-        const queued = ledger.executions.get(id);
packages/core/src/apply.ts-471-        if (queued && !queued.human)
packages/core/src/apply.ts:472:          effects.push(...cancelExecution(ledger, queued, "stopped by a person", now));
packages/core/src/apply.ts-473-      }
packages/core/src/apply.ts-474-      effects.push(...broadcast(ledger, { t: "controlApplied", op: "stop", nodeIds: [] }));
packages/core/src/apply.ts-475-      effects.push(...maybeStart(ledger, now), ...fill(ledger, now));
----STATUS-ON-STOP----
packages/core/src/executions.ts:703:    exec.status = "cancelled";
packages/core/src/executions.ts:713:  exec.status = "cancelled";
packages/core/src/executions.ts:863:    .filter((e) => e.status === "done" || e.status === "failed" || e.status === "cancelled")
packages/core/src/ledger.ts:61:export type AttemptOutcome = "running" | "result" | "released" | "cancelled" | "error";
packages/core/src/ledger.ts:122:export type ExecutionStatus = "queued" | "running" | "done" | "failed" | "cancelled";
packages/core/src/scheduler.ts:255:    a.outcome = "cancelled";
```

**Tool `Bash`:**

```
echo ----OBSERVER-LIVENESS----; grep -rn "observerPingMs\|OBSERVER_SILENCE\|observer.*silence\|lastSeen\|lastPing" packages/core/src packages/control-plane/src --include=*.ts | grep -v "\.test\." | head -20; echo ----CSS-ANIM----; grep -n "animation\|transition\|@keyframes" packages/web/public/styles.css | head -30; echo ----MISE-TEST----; grep -n -A6 "^\[tasks.test\|^\[tasks\.\"test\|^\[tasks.e2e\|^\[tasks.check\|^\[tasks.\"e2e" mise.toml | head -80; echo ----E2E-RUNNER----; ls e2e; cat e2e/README.md; ls playwright.config.* 2>/dev/null; grep -rn "playwright\|puppeteer" package.json packages/web/package.json 2>/dev/null | head
```

**Result of `Bash`:**

```
----OBSERVER-LIVENESS----
packages/core/src/apply.ts:58:const OBSERVER_SILENCE_MS = 5 * LIMITS.observerPingMs;
packages/core/src/apply.ts:228:        node.lastSeen = now;
packages/core/src/apply.ts:238:        node.lastSeen = now;
packages/core/src/apply.ts:523:    lastSeen: now,
packages/core/src/apply.ts:573:  node.lastSeen = now;
packages/core/src/apply.ts:592:  ledger.observers.set(connId, { connId, subscribedAt: now, lastSeen: now, launchedAt: [] });
packages/core/src/apply.ts:708:  observer.lastSeen = now;
packages/core/src/apply.ts:722:    if (now - node.lastSeen > LIMITS.goneAfterMs) {
packages/core/src/apply.ts:733:    if (now - observer.lastSeen > OBSERVER_SILENCE_MS) {
packages/core/src/ledger.ts:24:  lastSeen: number;
packages/core/src/ledger.ts:41:  lastSeen: number;
----CSS-ANIM----
163:  transition: width 0.15s linear;
343:  animation: pulse 1.5s ease-out;
345:@keyframes pulse {
462:  transition: background 0.2s;
----MISE-TEST----
33:[tasks.test]
34-description = "Unit and integration tests, then browser tests"
35-depends = ["lint"]
36-run = ["bun test", "bunx playwright test --pass-with-no-tests"]
37-
38-[tasks.dev]
39-description = "Local topology: control plane, local store, two local cores, web bundles in watch mode"
----E2E-RUNNER----
dashboard.e2e.ts
demo.e2e.ts
editor.e2e.ts
host.e2e.ts
money-shot.e2e.ts
panels.e2e.ts
polish.e2e.ts
README.md
sandbox.e2e.ts
walkthrough.e2e.ts
Browser tests (Playwright). Files are named `*.e2e.ts`; the first ones arrive in WP2.6.
playwright.config.ts
package.json:13:    "@playwright/test": "1.62.1",
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/walkthrough.e2e.ts
```

**Result of `Read`:**

```
1	// The walkthrough (WP7.7): every state a visitor can land in, and for each the sentence of state,
2	// which of Stop / Start / Resume the header offers, and exactly which controls are enabled — with
3	// the reason in the tooltip of every control that is not. Live, against the local control plane
4	// (no programs seeded, so the machine idles until someone launches).
5	import path from "node:path";
6	import { type BrowserContext, expect, type Page, test } from "@playwright/test";
7	
8	const wasmPath = path.resolve("programs/mandelbrot/dist/program.wasm");
9	const CONTROLS = [
10	  "#stop",
11	  "#start",
12	  "#resume",
13	  "#killHalf",
14	  "#freezeHalf",
15	  "#throttleHalf",
16	  "#resumeAll",
17	  "#restart",
18	  "#skip",
19	  "#killExecution",
20	  "#spawn1",
21	  "#spawnN",
22	  "#killMine",
23	  "#redundancy",
24	  "#openEditor",
25	  "#freezePanel",
26	  "#backToDashboard",
27	] as const;
28	type Probe = {
29	  sentence: string;
30	  slot: string;
31	  enabled: Record<string, boolean>;
32	  titles: Record<string, string>;
33	};
34	
35	/** One evaluate: the page's words and its controls at the same instant. */
36	async function probe(page: Page): Promise<Probe> {
37	  return page.evaluate((ids) => {
38	    // Shown as the visitor sees it: the panel-only controls keep a `hidden` attribute the CSS
39	    // overrides on a panel tab, and reserved boxes are `visibility: hidden` rather than gone.
40	    const shown = (el: Element | null): boolean => {
… (184 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/dashboard.e2e.ts
```

**Result of `Read`:**

```
1	import { expect, type Page, test } from "@playwright/test";
2	
3	// WP1.8: the dashboard. The demo mode drives the reducer with a scripted cluster inside the page
4	// (no program runs locally before WP1.6/1.7), so tiles, the grid, counters, and the story beats
5	// are checked there; the controls are checked against the real local control plane.
6	
7	interface Probe {
8	  tasks: number;
9	  placedBeyondFirstPage: boolean;
10	  painted: number;
11	  flags: [string, string][];
12	  inFlight: number;
13	  done: number;
14	  colors: number;
15	  gridHidden: boolean;
16	  gridHeight: number;
17	  paused: boolean;
18	}
19	
20	const probe = (page: Page): Promise<Probe> =>
21	  page.evaluate(() => {
22	    const tf = (
23	      window as unknown as {
24	        tabframe: {
25	          state: {
26	            tasks: Map<string, { index: number; status: string }>;
27	          };
28	          tiles: {
29	            paintedCount: number;
30	            flags: Map<string, string>;
31	            stats: { inFlight: number };
32	          };
33	          demo: { paused: boolean; done: number };
34	        };
35	      }
36	    ).tabframe;
37	    const canvas = document.querySelector<HTMLCanvasElement>("#tiles");
38	    const ctx = canvas?.getContext("2d");
39	    const seen = new Set<string>();
40	    if (canvas && ctx) {
… (148 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/panels.e2e.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/editor.e2e.ts
```

**Result of `Read`:**

```
1	import { createHash } from "node:crypto";
2	import { readFileSync } from "node:fs";
3	import path from "node:path";
4	import { fileURLToPath } from "node:url";
5	import { expect, type Page, test } from "@playwright/test";
6	
7	// WP2.4: the in-page editor against the real local control plane. The compiler loads in a worker
8	// and compiles the prefilled Mandelbrot; the module must hash to what the build produced (the
9	// web server built programs/mandelbrot/dist first). The drop door validates a real module and
10	// refuses junk. A launch uploads the bundle over the observer socket and the control plane
11	// answers — a refusal until WP2.3 teaches the core to take uploaded bundles, a queued execution
12	// after; either way the round trip is what this test pins.
13	
14	const wasmPath = path.resolve(
15	  fileURLToPath(new URL(".", import.meta.url)),
16	  "../programs/mandelbrot/dist/program.wasm",
17	);
18	const sha256 = (bytes: Uint8Array) => createHash("sha256").update(bytes).digest("hex");
19	
20	// The editor is a page of its own since WP6.4: the dashboard's button opens it in a new tab, and
21	// the tab holds the machine paused while it lives. The suites go there directly.
22	async function openEditor(page: Page): Promise<void> {
23	  await page.goto("/editor.html");
24	  await expect(page.locator("#machine")).toHaveText(/live/, { timeout: 30_000 });
25	  await expect(page.locator("#editor")).toBeVisible();
26	  await expect(page.locator("#source")).toHaveValue(/Mandelbrot/);
27	  await expect(page.locator("#editorStatus")).toHaveText(/ready in/, { timeout: 120_000 });
28	}
29	
30	test("the editor opens from the dashboard in its own tab and holds the machine paused while it lives", async ({
31	  page,
32	  context,
33	}) => {
34	  await page.goto("/?observe");
35	  await expect(page.locator("#machine")).toHaveText(/live/, { timeout: 30_000 });
36	  await expect(page.locator("#resume")).toBeHidden();
37	  const opened = context.waitForEvent("page");
38	  await page.click("#openEditor");
39	  const editor = await opened;
40	  await expect(editor).toHaveURL(/\/editor\.html$/);
… (266 more lines)
```

**Result of `Read`:**

```
1	import path from "node:path";
2	import { fileURLToPath } from "node:url";
3	import { expect, type Page, test } from "@playwright/test";
4	
5	// WP2.5: dashboard v2. The demo machine now cycles through three programs, so the panels can be
6	// checked without a cluster: a word count (three stages, a bars result, a filesystem to browse,
7	// logs on the tasks) and a broken program (the failure banner). Against the real local control
8	// plane: an uploaded program appears in the programs panel, launches from it, and the running
9	// execution can be killed from the page.
10	
11	const wasmPath = path.resolve(
12	  fileURLToPath(new URL(".", import.meta.url)),
13	  "../programs/mandelbrot/dist/program.wasm",
14	);
15	
16	const waitHeld = async (page: Page) => {
17	  await page.waitForSelector("body[data-demo-paused]", { timeout: 90_000 });
18	};
19	
20	test("demo: word count runs three stages, draws its bars, and its files can be browsed", async ({
21	  page,
22	}) => {
23	  test.setTimeout(150_000);
24	  await page.goto("/?demo=1&speed=12&program=wordcount&hold=1");
25	  await expect(page.locator("#machine")).toHaveText("live · demo");
26	  // The programs panel lists what the machine can run, with a launch form per program.
27	  await expect(page.locator("#programs .program")).toHaveCount(3);
28	  await expect(page.locator("#programs")).toContainText("mandelbrot");
29	  await expect(page.locator("#programs")).toContainText("wordcount");
30	  await expect(page.locator("#programs")).toContainText("broken");
31	  await expect(page.locator('[data-launch="wordcount"]')).toBeVisible();
32	  // The queue has a person's execution waiting, with a drop button.
33	  await expect(page.locator("#queue")).toContainText("wordcount e39");
34	  await expect(page.locator('#queue [data-drop="e39"]')).toBeVisible();
35	
36	  await waitHeld(page);
37	  await expect(page.locator("#exec")).toHaveText("wordcount · done · 1/1");
38	  // Three stages in the strip, all done, with their tallies.
39	  const stages = page.locator("#strip .stage");
40	  await expect(stages).toHaveCount(3);
… (314 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/demo.e2e.ts
```

**Result of `Read`:**

```
1	import { spawn } from "node:child_process";
2	import { type BrowserContext, expect, type Page, test } from "@playwright/test";
3	
4	// WP4.4: the demo script of design §13, run unattended against the deployed machine in the order
5	// the video follows: one tab plus two cloud cores rendering → two more tabs → spawn ten (bounded by
6	// cores, said on screen) → kill half → freeze half → throttle half → redundancy on → editor: change
7	// the palette, compile in the browser, launch → word count: three stages and a bar chart → a
8	// rotation: banner, reconnect, render continues → ledger and files panels. Skipped locally: it
9	// wants the seeded programs, cloud cores, and a real rotation, none of which the Playwright control
10	// plane has. `mise run demo` points it at the machine; TABFRAME_VIDEO=1 records the browser.
11	const url = process.env.TABFRAME_URL;
12	test.skip(!url, "needs a deployed machine (TABFRAME_URL)");
13	test.use({
14	  video: process.env.TABFRAME_VIDEO ? "on" : "off",
15	  viewport: { width: 1440, height: 1000 },
16	});
17	
18	const counter = (page: Page, name: string): Promise<number> =>
19	  page
20	    .locator(`[data-counter="${name}"] b`)
21	    .textContent()
22	    .then((t) => Number((t ?? "0").replace(/[^\d]/g, "") || "0"));
23	const counts = (page: Page): Promise<{ nodes: number; hosts: number }> =>
24	  page
25	    .locator("#counts")
26	    .textContent()
27	    .then((t) => {
28	      const m = /(\d+) nodes · (\d+) hosts/.exec(t ?? "");
29	      return { nodes: Number(m?.[1] ?? 0), hosts: Number(m?.[2] ?? 0) };
30	    });
31	const generation = (page: Page): Promise<number> =>
32	  page
33	    .locator("#gen")
34	    .textContent()
35	    .then((t) => Number(/gen (\d+)/.exec(t ?? "")?.[1] ?? "0"));
36	/** Regions that must keep their box for the whole run (WP6.2): measured at every beat. */
37	const LAYOUT_SELECTORS = [
38	  "header",
39	  "#machineBanner",
40	  ".hero",
… (481 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/polish.e2e.ts
```

**Result of `Read`:**

```
1	import { expect, type Page, test } from "@playwright/test";
2	
3	// WP4.1: dashboard polish. Everything here runs against the demo mode, paused at a known tile
4	// count or held after an execution ends, so nothing depends on the timing of a live render: the
5	// legend, the flash log, the throughput figure, the spawn hint, the ledger, the rotation banner
6	// with its countdown, and the sleep banner.
7	
8	const waitPaused = async (page: Page) => {
9	  await page.waitForSelector("body[data-demo-paused]", { timeout: 90_000 });
10	  await page.waitForFunction(
11	    () =>
12	      (window as unknown as { tabframe: { tiles: { stats: { inFlight: number } } } }).tabframe.tiles
13	        .stats.inFlight === 0,
14	    null,
15	    { timeout: 30_000 },
16	  );
17	};
18	
19	const pulseKinds = (page: Page): Promise<string[]> =>
20	  page.evaluate(() =>
21	    (
22	      window as unknown as { tabframe: { state: { pulses: { kind: string }[] } } }
23	    ).tabframe.state.pulses.map((p) => p.kind),
24	  );
25	
26	test("demo: legend, flash log, throughput, spawn hint, and the ledger explain the frame", async ({
27	  page,
28	}) => {
29	  test.setTimeout(150_000);
30	  await page.goto("/?demo=1&speed=12&pause=300");
31	  await expect(page.locator("#machine")).toHaveText("live · demo");
32	  // The spawn hint is sized to this browser before anything runs.
33	  const hint = page.locator("#spawnHint");
34	  await expect(hint).toHaveAttribute("data-cores", /^\d+$/);
35	  await expect(hint).toHaveAttribute("data-default", /^\d+$/);
36	  // In the demo the nodes are scripted, and the hint says so (WP8.1); the live page counts cores.
37	  await expect(hint).toContainText(/Demo: the nodes are scripted inside this page/);
38	  await expect(hint).toContainText("open the live address to lend real cores");
39	  await waitPaused(page);
40	
… (183 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/money-shot.e2e.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/host.e2e.ts
```

**Result of `Read`:**

```
1	import { expect, test } from "@playwright/test";
2	
3	// WP0.6 acceptance: two tabs show each other's nodes live.
4	test("two tabs see each other's nodes; observe mode lends none; closing withdraws", async ({
5	  browser,
6	}) => {
7	  const a = await browser.newPage();
8	  const b = await browser.newPage();
9	  await a.goto("/");
10	  await b.goto("/");
11	  await expect(a.locator("#machine")).toHaveText(/live/);
12	  await expect(b.locator("#machine")).toHaveText(/live/);
13	  // Each tab spawned one node on arrival; both dashboards converge on two nodes across two hosts.
14	  await expect(a.locator("#counts")).toHaveText("2 nodes · 2 hosts", { timeout: 15_000 });
15	  await expect(b.locator("#counts")).toHaveText("2 nodes · 2 hosts");
16	  await expect(a.locator("#nodes tbody tr")).toHaveCount(2);
17	  await expect(a.locator("#mine .row")).toHaveCount(1);
18	  await expect(a.locator("#mine .row")).toContainText("idle");
19	
20	  // Spawn one more in tab A; tab B sees three.
21	  await a.click("#spawn1");
22	  await expect(b.locator("#counts")).toHaveText("3 nodes · 2 hosts");
23	  await expect(a.locator("#mine .row")).toHaveCount(2);
24	
25	  // Close all of A's nodes; B sees only its own.
26	  await a.click("#killMine");
27	  await expect(b.locator("#counts")).toHaveText("1 nodes · 1 hosts");
28	  await expect(a.locator("#mine")).toContainText("No nodes in this tab");
29	
30	  // Observe-only tab lends no CPU.
31	  const c = await browser.newPage();
32	  await c.goto("/?observe");
33	  await expect(c.locator("#machine")).toHaveText(/live/);
34	  await expect(c.locator("#mine")).toContainText("Observe-only");
35	  await expect(c.locator("#counts")).toHaveText("1 nodes · 1 hosts");
36	
37	  // Closing tab B removes its node from everyone's view.
38	  await b.close();
39	  await expect(c.locator("#counts")).toHaveText("0 nodes · 0 hosts");
40	  await a.close();
… (14 more lines)
```

**Result of `Read`:**

```
1	// The money shot (plan WP2.6): ten browser nodes render a Mandelbrot frame, half of them are
2	// killed mid-frame, and every tile the page painted hashes to the golden the SDK suite pins.
3	// Nothing here is simulated: the tabs run the real sandbox, upload to the real store, and the
4	// control plane is the real process behind the Playwright web server.
5	import { readFileSync } from "node:fs";
6	import path from "node:path";
7	import { expect, test } from "@playwright/test";
8	
9	const goldens = JSON.parse(
10	  readFileSync(path.resolve(import.meta.dirname, "../programs/mandelbrot/goldens.json"), "utf8"),
11	) as { params: Record<string, unknown>; taskCount: number; hashes: string[] };
12	
13	type Watch = {
14	  done: Record<string, string>;
15	  failed: string[];
16	  finished: string | null;
17	};
18	
19	/**
20	 * Watch the machine from inside the page: a second observer socket collects the events the
21	 * dashboard is reacting to, so the test can check hashes the page never shows.
22	 */
23	function installWatcher(): void {
24	  const w = window as unknown as { __watch: Watch };
25	  w.__watch = { done: {}, failed: [], finished: null };
26	  void (async () => {
27	    const config = (await (await fetch("/config.json")).json()) as { sessionUrl: string };
28	    const session = (await (await fetch(config.sessionUrl)).json()) as {
29	      endpoint?: string;
30	      generation?: number;
31	    };
32	    if (!session.endpoint) return;
33	    const gen = session.generation ?? 1;
34	    const ws = new WebSocket(`${session.endpoint}/observer`);
35	    ws.onopen = () => ws.send(JSON.stringify({ t: "subscribe", v: 1, gen }));
36	    ws.onmessage = (m) => {
37	      const e = JSON.parse(String(m.data)) as {
38	        t: string;
39	        taskId?: string;
40	        output?: string;
… (198 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/e2e/sandbox.e2e.ts
```

**Result of `Read`:**

```
1	// The web sandbox adapter in a real browser (plan WP2.6). The money shot proves the happy path —
2	// tabs compute tiles that hash to goldens — so what is left is the paths a healthy frame never
3	// takes: a program that spins forever must be killed at the deadline without wedging the cluster
4	// or the tab, and a program that traps must fail its execution visibly.
5	import { expect, type Page, test } from "@playwright/test";
6	
7	/** Collect the machine's own events from a second observer socket inside the page. */
8	function watch(): void {
9	  const w = window as unknown as { __seen: string[] };
10	  if (w.__seen) return;
11	  w.__seen = [];
12	  void (async () => {
13	    const config = (await (await fetch("/config.json")).json()) as { sessionUrl: string };
14	    const session = (await (await fetch(config.sessionUrl)).json()) as {
15	      endpoint?: string;
16	      generation?: number;
17	    };
18	    if (!session.endpoint) return;
19	    const gen = session.generation ?? 1;
20	    const ws = new WebSocket(`${session.endpoint}/observer`);
21	    ws.onopen = () => ws.send(JSON.stringify({ t: "subscribe", v: 1, gen }));
22	    ws.onmessage = (m) => {
23	      const e = JSON.parse(String(m.data)) as { t: string; reason?: string };
24	      w.__seen.push(e.reason ? `${e.t}:${e.reason.slice(0, 80)}` : e.t);
25	    };
26	    setInterval(() => {
27	      if (ws.readyState === 1) ws.send(JSON.stringify({ t: "ping", v: 1, gen }));
28	    }, 1_500);
29	  })();
30	}
31	
32	const seenBy = () => (window as unknown as { __seen?: string[] }).__seen ?? [];
33	
34	const SPINNER = `import { emit, readRunInput, stage } from "@tabframe/sdk-as/assembly/index";
35	export { alloc } from "@tabframe/sdk-as/assembly/index";
36	
37	export function plan(ptr: usize, len: i32): usize {
38	  const s = stage("spin");
39	  s.task(new Uint8Array(1));
40	  s.task(new Uint8Array(1));
… (98 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/observer.test.ts
```

**Result of `Read`:**

```
1	import { afterEach, beforeEach, describe, expect, test } from "bun:test";
2	import { PROTOCOL_VERSION } from "@tabframe/protocol";
3	import { CONTROL_HOLD_MS, CONTROL_SPACING_MS, ObserverClient } from "./observer.ts";
4	
5	// WP4.4: a control clicked while the socket is between subscribes (a silent resubscribe after a
6	// gap or a refresh, a rotation) is held for the next live socket instead of being dropped.
7	
8	class FakeSocket {
9	  static OPEN = 1;
10	  static instances: FakeSocket[] = [];
11	  readyState = 0;
12	  sent: string[] = [];
13	  onopen: ((ev: unknown) => void) | null = null;
14	  onmessage: ((ev: { data: string }) => void) | null = null;
15	  onclose: ((ev: { code: number; reason: string }) => void) | null = null;
16	  readonly url: string;
17	  readonly protocols: string[] | undefined;
18	  constructor(url: string, protocols?: string[]) {
19	    this.url = url;
20	    this.protocols = protocols;
21	    FakeSocket.instances.push(this);
22	  }
23	  open(): void {
24	    this.readyState = 1;
25	    this.onopen?.({});
26	  }
27	  deliver(msg: Record<string, unknown>): void {
28	    this.onmessage?.({ data: JSON.stringify(msg) });
29	  }
30	  send(data: string): void {
31	    this.sent.push(data);
32	  }
33	  close(code = 1000, reason = ""): void {
34	    this.readyState = 3;
35	    this.onclose?.({ code, reason });
36	  }
37	  frames(): Array<{ t: string }> {
38	    return this.sent.map((s) => JSON.parse(s) as { t: string });
39	  }
40	}
… (156 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/state.test.ts
```

**Result of `Read`:**

```
1	import { describe, expect, test } from "bun:test";
2	import type { ControlPlaneToObserver, ExecutionView, TaskView } from "@tabframe/protocol";
3	import { PROTOCOL_VERSION } from "@tabframe/protocol";
4	import {
5	  ACTIVITY_CAP,
6	  applyMessage,
7	  CHART_WINDOW_MS,
8	  type ClusterState,
9	  emptyState,
10	  FLASH_MS,
11	  HISTORY_CAP,
12	  headerSlot,
13	  hostCount,
14	  inFlightByNode,
15	  isFlashing,
16	  isRunning,
17	  ledgerRows,
18	  loopLabel,
19	  loopState,
20	  machineBanner,
21	  machineSentence,
22	  PULSE_CAP,
23	  planTask,
24	  programList,
25	  progress,
26	  rotationCountdown,
27	  stageStrip,
28	  stageTasks,
29	  stopTitle,
30	  TASK_COLOR_LABELS,
31	  THROUGHPUT_WINDOW_MS,
32	  taskColor,
33	  throughput,
34	  throughputSeries,
35	  withRedundancy,
36	} from "./state.ts";
37	
38	const env = { v: PROTOCOL_VERSION, gen: 2 } as const;
39	const HASH = "a".repeat(64);
40	const node = (id: string, hostId = "h1") => ({
… (1178 more lines)
```

> **Thinking.** 

> **Thinking.** I've confirmed stop status maps to "cancelled" correctly and observer silence is 10s, and confirmed the e2e suites lack coverage for the Stop tooltip while disconnected (R3 gap remains). Now I'll move through the remaining independent items: unit tests, protocol ABI, Playwright config, CSS rules, and running unit tests/typecheck/lint over my area.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/orchestrator.test.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/tasks.test.ts
```

**Result of `Read`:**

```
1	import { describe, expect, test } from "bun:test";
2	import {
3	  decodeRunInput,
4	  type FsManifest,
5	  LIMITS,
6	  PROTOCOL_VERSION,
7	  RELEASED,
8	} from "@tabframe/protocol";
9	import type { HostRequest, TaskResult } from "@tabframe/sandbox";
10	import { type PresignRequester, StoreClient, sha256Hex } from "@tabframe/store";
11	import {
12	  fromBase64,
13	  hostFailure,
14	  type SandboxRunner,
15	  SocketPresigner,
16	  TaskRunner,
17	} from "./tasks.ts";
18	
19	const BASE = "http://store.test/blob";
20	const PROGRAM = "a".repeat(64);
21	const ROOT = "c".repeat(64);
22	const limits = {
23	  maxOutputBytes: 1 << 20,
24	  maxWriteBytes: 1 << 20,
25	  maxWriteFiles: 16,
26	  maxLogBytes: 1 << 16,
27	  memoryPagesMax: 256,
28	};
29	
30	function world(results: TaskResult[]) {
31	  const blobs = new Map<string, Uint8Array>();
32	  const gets: string[] = [];
33	  const puts: string[] = [];
34	  const fetchImpl = async (url: string, init?: RequestInit): Promise<Response> => {
35	    const hash = url.slice(BASE.length + 1);
36	    if (init?.method === "PUT") {
37	      puts.push(hash);
38	      blobs.set(hash, new Uint8Array(init.body as ArrayBuffer));
39	      return new Response(null, { status: 200 });
40	    }
… (298 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/tiles.test.ts
```

**Result of `Read`:**

```
1	import { describe, expect, test } from "bun:test";
2	import type { PlaceView } from "@tabframe/protocol";
3	import { PROTOCOL_VERSION } from "@tabframe/protocol";
4	import { sha256Hex } from "@tabframe/store/hash";
5	import { applyMessage, type ClusterState, emptyState } from "./state.ts";
6	import {
7	  type BlobSource,
8	  storeSource,
9	  type TileFlag,
10	  type TilePainter,
11	  TileView,
12	} from "./tiles.ts";
13	
14	const env = { v: PROTOCOL_VERSION, gen: 1 } as const;
15	
16	class Recorder implements TilePainter {
17	  calls: string[] = [];
18	  reset(w: number, h: number): void {
19	    this.calls.push(`reset ${w}x${h}`);
20	  }
21	  put(place: PlaceView, rgba: Uint8ClampedArray): void {
22	    this.calls.push(`put ${place.x},${place.y} ${place.w}x${place.h} ${rgba.length}`);
23	  }
24	  flag(place: PlaceView, flag: TileFlag): void {
25	    this.calls.push(`flag ${place.x},${place.y} ${flag}`);
26	  }
27	  clear(place: PlaceView): void {
28	    this.calls.push(`clear ${place.x},${place.y}`);
29	  }
30	}
31	
32	class MemorySource implements BlobSource {
33	  blobs = new Map<string, Uint8Array>();
34	  requests: string[] = [];
35	  async get(hash: string): Promise<Uint8Array | null> {
36	    this.requests.push(hash);
37	    return this.blobs.get(hash) ?? null;
38	  }
39	}
40	
… (262 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor-core.test.ts
```

**Result of `Read`:**

```
1	import { beforeAll, describe, expect, test } from "bun:test";
2	import { readdirSync, readFileSync } from "node:fs";
3	import path from "node:path";
4	import { BUNDLE_PATHS, fsManifest } from "@tabframe/protocol";
5	import { LocalStore } from "@tabframe/store";
6	import { seedPrograms } from "../../control-plane/src/seed.ts";
7	import { compileProgram, ASC_FLAGS as SDK_FLAGS } from "../../sdk-as/scripts/build-programs.ts";
8	import { GENERATED, renderSources } from "../scripts/gen-sources.ts";
9	import {
10	  ASC_FLAGS,
11	  assembleSources,
12	  buildBundle,
13	  buildManifest,
14	  ENTRY,
15	  examples,
16	  fmtBytes,
17	  formatDiagnostic,
18	  guideMarkdown,
19	  inspectModule,
20	  looksLikeWasm,
21	  MANDELBROT_SOURCE,
22	  parseParams,
23	  SDK_ROOT,
24	  shippedManifest,
25	  shortPath,
26	} from "./editor-core.ts";
27	
28	const root = path.resolve(import.meta.dir, "../../..");
29	const out = path.join(root, "packages/web/dist-test/mandelbrot.wasm");
30	let wasm: Uint8Array;
31	
32	beforeAll(async () => {
33	  await compileProgram(path.join(root, "programs/mandelbrot/assembly/index.ts"), out);
34	  wasm = new Uint8Array(readFileSync(out));
35	}, 60_000);
36	
37	describe("embedded sources", () => {
38	  test("the generated module matches the SDK and program files on disk", () => {
39	    expect(readFileSync(GENERATED, "utf8")).toBe(renderSources());
40	  });
… (227 more lines)
```

**Result of `Read`:**

```
1	import { describe, expect, test } from "bun:test";
2	import {
3	  CLOSE,
4	  decodeRunInput,
5	  encodePlanInput,
6	  LIMITS,
7	  PROTOCOL_VERSION,
8	  RELEASED,
9	} from "@tabframe/protocol";
10	import type { HostRequest, TaskResult } from "@tabframe/sandbox";
11	import { sha256Hex } from "@tabframe/store";
12	import { Backoff } from "./backoff.ts";
13	import {
14	  OFF_POLL_MS,
15	  Orchestrator,
16	  parseRotating,
17	  resolveStoreBase,
18	  type SocketLike,
19	  type Status,
20	  THROTTLE_MIN_MS,
21	  type Timers,
22	} from "./orchestrator.ts";
23	import { fetchSession, socketProtocols } from "./session.ts";
24	import type { SandboxRunner } from "./tasks.ts";
25	
26	/** Manually advanced timers. */
27	class FakeTimers implements Timers {
28	  now = 0;
29	  private handles = new Map<number, { at: number; fn: () => void }>();
30	  private next = 1;
31	  setTimeout(fn: () => void, ms: number): unknown {
32	    const id = this.next++;
33	    this.handles.set(id, { at: this.now + ms, fn });
34	    return id;
35	  }
36	  clearTimeout(handle: unknown): void {
37	    if (typeof handle === "number") this.handles.delete(handle);
38	  }
39	  async advance(ms: number): Promise<void> {
40	    const target = this.now + ms;
… (709 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/protocol/src/abi.ts
```

**Result of `Read`:**

```
1	/**
2	 * The program ABI's byte formats (design §5.3): what `run` and `plan` receive and what `plan`
3	 * returns. Binary, little-endian, deterministic, trivial to produce from AssemblyScript and to
4	 * parse on the control plane. The SDK mirrors these layouts exactly.
5	 *
6	 *   run input   : "TFRN" u32 version=1 | u32 stage | u32 taskIndex | u32 taskCount | u32 len | input[len]
7	 *   plan input  : "TFPL" u32 version=1 | u32 stage | table params | table hints
8	 *   plan output : "TFSS" u32 version=1 | u8 kind (0 = stage, 1 = done)
9	 *                 stage: str name | u8 hasCanvas [u32 w u32 h] | u32 n | n × (u32 len input[len] | u8 hasPlace [i32 x y w h])
10	 *                 done : u8 hasNext [table next]
11	 *   bars        : "TFBR" u32 version=1 | u32 count | count × (str label | f64 value)
12	 *                 — what a `bars` program's final task returns (design §5.1); the dashboard draws it
13	 *   table       : u32 count | count × (str key | str value)   — values are JSON text
14	 *   str         : u32 len | utf8[len]
15	 */
16	
17	export const ABI_VERSION = 1;
18	const MAGIC_RUN = 0x4e524654; // "TFRN" little-endian
19	const MAGIC_PLAN = 0x4c504654; // "TFPL"
20	const MAGIC_SPEC = 0x53534654; // "TFSS"
21	const MAGIC_BARS = 0x52424654; // "TFBR"
22	
23	/** Params and hints travel as a flat table of key → JSON text, so a program needs no JSON parser. */
24	export type ParamTable = Record<string, unknown>;
25	
26	export interface RunInput {
27	  stage: number;
28	  taskIndex: number;
29	  taskCount: number;
30	  input: Uint8Array;
31	}
32	
33	export interface PlanInput {
34	  stage: number;
35	  params: ParamTable;
36	  hints: ParamTable;
37	}
38	
39	export interface Place {
40	  x: number;
… (309 more lines)
```

**Tool `Bash`:**

```
cat playwright.config.ts; echo ----CSS----; grep -n -B1 -A4 "panel-only\|\[hidden\]\|^\.notice\|^\.pill\b\|\.pill\.wait\|\.pill\.off\|\.pill\.live\|tr\.hit\|\.selected\|\.danger\|button:disabled\|:disabled" packages/web/public/styles.css | head -120
```

**Result of `Bash`:**

```
import { defineConfig, devices } from "@playwright/test";

// Browser tests live under e2e/ and are named *.e2e.ts, so `bun test` (which picks up *.test.ts)
// and Playwright never see each other's files. The web server is the real control plane in local
// mode serving the built web bundle; TABFRAME_URL points the tests at a deployed machine instead.
const local = !process.env.TABFRAME_URL;
const port = 4090;

export default defineConfig({
  testDir: "./e2e",
  testMatch: /.*\.e2e\.ts$/,
  fullyParallel: false,
  // The suites share one local control plane, and since WP2.3 an upload really launches: files
  // run one at a time, in name order, so a launch in one suite is not a surprise in another.
  workers: 1,
  // One retry on CI's shared runner (WP8.1); none locally, where a flake should be seen.
  retries: process.env.CI ? 1 : 0,
  reporter: [["list"]],
  timeout: 30_000,
  use: {
    baseURL: process.env.TABFRAME_URL ?? `http://127.0.0.1:${port}`,
    trace: "retain-on-failure",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: local
    ? {
        // The programs build first: the editor test compares a page compile to the build's module.
        command:
          "node packages/sdk-as/scripts/build-programs.ts && bun packages/web/scripts/build.ts && node packages/control-plane/src/main.ts",
        url: `http://127.0.0.1:${port}/config.json`,
        reuseExistingServer: false,
        timeout: 60_000,
        env: {
          TABFRAME_MODE: "local",
          TABFRAME_PUBLIC_PORT: String(port),
          TABFRAME_PRIVATE_PORT: String(port + 1),
          TABFRAME_WEB_DIR: "packages/web/dist",
          TABFRAME_TICK_MS: "100",
          // No seeding: these suites want an idle machine. The pipeline in a browser is
          // exercised by the live tests that point at a seeded server.
… (126 more lines)
```

**Tool `Bash`:**

```
for f in packages/web/src/result.test.ts packages/web/src/banners.test.ts packages/web/src/compiler.test.ts packages/node/src/platform/node.test.ts packages/node/src/tasks.integration.test.ts; do echo "=== $f ==="; cat -n "$f"; done 2>&1 | head -520
```

**Tool `Bash`:**

```
timeout 560 bun test packages/web packages/node packages/store packages/protocol 2>&1 | tail -40
```

**Result of `Bash`:**

```
=== packages/web/src/result.test.ts ===
     1	import { describe, expect, test } from "bun:test";
     2	import { encodeBars } from "@tabframe/protocol";
     3	import {
     4	  barRows,
     5	  comparePaths,
     6	  decodeText,
     7	  finalOutput,
     8	  fmtBytes,
     9	  fmtValue,
    10	  groupFiles,
    11	  hexHead,
    12	  listFiles,
    13	  looksLikeText,
    14	  parseManifest,
    15	  previewOf,
    16	  readBars,
    17	} from "./result.ts";
    18	
    19	const H = (c: string) => c.repeat(64);
    20	const manifest = {
    21	  version: 1 as const,
    22	  files: {
    23	    "/out/0/10": { hash: H("a"), size: 10 },
    24	    "/out/0/2": { hash: H("b"), size: 2 },
    25	    "/in/corpus.txt": { hash: H("c"), size: 1_220_332 },
    26	    "/program.wasm": { hash: H("d"), size: 20_655 },
    27	    "/out/1/0": { hash: H("e"), size: 300 },
    28	    "/manifest.json": { hash: H("f"), size: 120 },
    29	    "/state/acc": { hash: H("1"), size: 8 },
    30	  },
    31	};
    32	const bytes = (s: string) => new TextEncoder().encode(s);
    33	
    34	describe("the files panel model", () => {
    35	  test("lists paths in path order with numeric segments sorted as numbers", () => {
    36	    expect(listFiles(manifest).map((f) => f.path)).toEqual([
    37	      "/in/corpus.txt",
    38	      "/manifest.json",
    39	      "/out/0/2",
… (454 more lines)
```

**Tool `Bash`:**

```
ls packages/web/tsconfig.json packages/node/tsconfig.json packages/protocol/tsconfig.json packages/store/tsconfig.json tsconfig.json 2>&1; echo ----TSC-WEB----; timeout 300 bunx tsc -p packages/web/tsconfig.json --noEmit 2>&1 | tail -15; echo ----BIOME----; timeout 300 bunx biome check packages/web packages/node packages/store packages/protocol e2e 2>&1 | tail -15
```

**Result of `Bash`:**

```
 packages/core/src/results.ts              |    0.00 |    4.12 | 15-16,32-180,195-207,212-222,227-237,246,250-252,257-290,295-316,329-361
 packages/core/src/scheduler.ts            |    0.00 |    5.12 | 8-12,16,20,25,30-31,36,41,50-54,59,64-69,73-79,83,88-93,98-120,125-134,139-159,163-214,219-242,247-265,270-286
 packages/core/src/snapshot.ts             |    0.00 |    6.17 | 33-45,49-86,95-119
 packages/node/src/backoff.ts              |  100.00 |  100.00 | 
 packages/node/src/orchestrator.ts         |   90.00 |  100.00 | 
 packages/node/src/session.ts              |  100.00 |   97.87 | 
 packages/node/src/tasks.ts                |  100.00 |  100.00 | 
 packages/protocol/src/abi.ts              |   93.10 |   99.59 | 
 packages/protocol/src/canonical.ts        |  100.00 |  100.00 | 
 packages/protocol/src/codec.ts            |  100.00 |  100.00 | 
 packages/protocol/src/codes.ts            |  100.00 |  100.00 | 
 packages/protocol/src/fs.ts               |  100.00 |  100.00 | 
 packages/protocol/src/headers.ts          |  100.00 |  100.00 | 
 packages/protocol/src/index.ts            |  100.00 |  100.00 | 
 packages/protocol/src/limits.ts           |  100.00 |  100.00 | 
 packages/protocol/src/node.ts             |  100.00 |  100.00 | 
 packages/protocol/src/observer.ts         |  100.00 |  100.00 | 
 packages/protocol/src/shared.ts           |  100.00 |  100.00 | 
 packages/protocol/src/task.ts             |  100.00 |  100.00 | 
 packages/sandbox/src/adapters/bridge.ts   |   16.67 |    2.27 | 10-86,90-98
 packages/sandbox/src/fs.ts                |    0.00 |   20.34 | 25-27,32-35,40-46,51-54,59-69,74-86,90-94
 packages/sandbox/src/host.ts              |   69.23 |   80.77 | 70-71,73-76,99-100,104-106,137-140
 packages/sandbox/src/index.ts             |  100.00 |  100.00 | 
 packages/sandbox/src/run.ts               |    0.00 |    4.04 | 18-105,110,115-120
 packages/sandbox/src/types.ts             |    0.00 |   15.38 | 50-51,55-74
 packages/sandbox/src/validate.ts          |   80.00 |   51.42 | 38,54-57,63-67,72,83-156,161-173,189,216-217,228
 packages/sdk-as/scripts/build-programs.ts |   66.67 |   60.42 | 39-55,60-61
 packages/store/src/client.ts              |   95.00 |  100.00 | 
 packages/store/src/driver.ts              |  100.00 |  100.00 | 
 packages/store/src/hash.ts                |  100.00 |  100.00 | 
 packages/store/src/index.ts               |  100.00 |  100.00 | 
 packages/store/src/local.ts               |   92.31 |   98.11 | 
 packages/store/src/s3.ts                  |   93.33 |  100.00 | 
 packages/store/src/snapshots.ts           |   88.89 |  100.00 | 
-------------------------------------------|---------|---------|-------------------

 180 pass
 0 fail
 1350 expect() calls
Ran 180 tests across 17 files. [12.72s]
```

**Result of `Bash`:**

```
ls: cannot access 'packages/node/tsconfig.json': No such file or directory
ls: cannot access 'packages/protocol/tsconfig.json': No such file or directory
ls: cannot access 'packages/store/tsconfig.json': No such file or directory
packages/web/tsconfig.json
tsconfig.json
----TSC-WEB----
----BIOME----
         │               ^^^^^^^^^^
    1357 │   margin: 0 !important;
    1358 │   padding: 0 !important;
  
  i This style reverses the cascade logic, and precedence is reversed. This could lead to having styles with higher specificity being overridden by styles with lower specificity.
  
  i Unsafe fix: Remove the style.
  
    1356 │ ··height:·1px·!important;
         │              ----------- 

The number of diagnostics exceeds the limit allowed. Use --max-diagnostics to increase it.
Diagnostics not shown: 8.
Checked 79 files in 187ms. No fixes applied.
Found 28 warnings.
```

> **Thinking.** 

> **Thinking.** Tests pass (180 tests, 17 files), tsc is clean, and biome only flags CSS `!important` warnings. Now I'll verify two suspicions empirically with a scratchpad script driving `ObserverClient` against a fake socket—that `resubscribe()` clears the send timer but leaves the outbox intact, and that backoff never resets—while also skimming the remaining protocol/store tests and the core's mismatch path.
> 
> 

**Tool `Write`:**

```
/tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/scratchpad/observer-repro.ts  (149 lines)
```

**Result of `Write`:**

```
File created successfully at: /tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/scratchpad/observer-repro.ts (file state is current in your context — no need to Read it back)
```

**Tool `Bash`:**

```
timeout 120 bun run /tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/scratchpad/observer-repro.ts 2>&1 | tail -30
```

**Tool `Bash`:**

```
grep -n "failed\|mismatch\|contest" packages/core/src/results.ts | head -40; echo ----MESSAGES-TEST----; grep -n "test(\|maxPresignItems\|64" packages/protocol/src/messages.test.ts | head -40; echo ----STORE-TEST----; grep -n "test(\|retries\|429\|content-length" packages/store/src/store.test.ts | head -40
```

**Result of `Bash`:**

```
A: send#1 → sent
A: send#2 → sent
A: s1 frames right after the clicks: [ "subscribe" ]
A: s1 frames: [ "subscribe" ] | s2 frames after live + 3 spacings: [ "subscribe" ]
A: dropped reported: 0
A: s2 frames after a later 'stop' click: [ "subscribe", "resumeAll", "killHalf", "stop" ]
B: reconnect delays after each close (each preceded by a healthy live session): [
  500, 901, 1137, 3561, 723, 6590, 10603, 26373, 8953, 25423
]
```

**Result of `Bash`:**

```
23:  | { kind: "failed"; task: TaskRecord; reason: string }
24:  | { kind: "contested"; task: TaskRecord };
47:  // verifies, or contests; the tests and the design lean on it), but it cannot *fail* a task
50:  // (WP8.2) An unknown attempt may verify or contest a *settled* task (D7's late duplicate); it
71:      if (released >= RELEASES_PER_TASK_CAP && task.status !== "done" && task.status !== "failed") {
73:        task.status = "failed";
75:        exec.counters.failed += 1;
79:        return { effects, settlement: { kind: "failed", task, reason: task.failure } };
110:          round: task.contestedRounds,
121:          round: task.contestedRounds,
125:  // (agreement means two nodes, D7); a node that contradicts itself goes through the mismatch path.
127:    (r) => r.round === task.contestedRounds && r.nodeId === node.nodeId,
134:  // A result for a settled task is a duplicate: verify or contest it.
147:      exec.counters.mismatched += 1;
153:    effects.push(...contest(ledger, exec, task, node.nodeId));
157:  if (task.status === "failed") return { effects, settlement: { kind: "none" } };
160:  const round = task.results.filter((r) => r.round === task.contestedRounds);
163:    effects.push(...contest(ledger, exec, task, node.nodeId));
201:  if (exec?.status !== "running" || task.status === "done" || task.status === "failed")
203:  const round = task.results.filter((r) => r.round === task.contestedRounds);
229:  if (task.results.some((r) => r.round === task.contestedRounds)) return [];
244: * later stages may read, so a late mismatch there is recorded, not acted on.
251:  if (task.status === "failed")
252:    return { kind: "failed", task, reason: task.failure ?? "program fault" };
271:    task.status = "failed";
273:    exec.counters.failed += 1;
295:function contest(
302:  exec.counters.mismatched += 1;
313:  task.contestedRounds += 1;
324: * After the second contested round, the identity reported by the most nodes wins (D7). Votes are
336:  if (task.contestedRounds < 2) return { kind: "contested", task };
355:  if (!winner) return { kind: "contested", task };
356:  if (tied && task.contestedRounds < MAX_CONTESTED_ROUNDS) return { kind: "contested", task };
----MESSAGES-TEST----
10:const H = "a".repeat(64);
20:  test("assign and result round-trip; result needs exactly one of output or error", () => {
31:      count: 640,
80:  test("presign, presigned, cancel, command", () => {
101:  test("paths in writes must be absolute and simple", () => {
119:  test("inline input cap", () => {
… (31 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T18:44:17.080Z

## Findings

1. `[H] web/observer — packages/web/src/observer.ts:195-226, 320-331, 337, 156-163 — a control queued behind the 250 ms spacing timer (\`outbox\`) is orphaned when a gap/refresh \`resubscribe()\` calls \`clearTimers()\` (kills \`sendTimer\`, leaves \`outbox\`) and \`releaseHeld()\` only drains when \`held\` is non-empty; on a real close \`onClose\` does \`this.outbox = []\` with no \`onDropped\` — reproduced with a fake socket: two clicks right after a subscribe never left the old or the new socket, nothing was reported, and a \`stop\` clicked later flushed \`resumeAll\`, \`killHalf\`, \`stop\` in that order — a destructive control fires minutes later on an unrelated click, bypassing the 10 s hold, and lost clicks are silent (R2, R8) — in \`resubscribe\`/\`onClose\` move \`outbox\` controls into \`held\` (timestamped) and call \`drain()\` unconditionally in \`releaseHeld\`; add the case to observer.test.ts.`
2. `[M] web/observer — packages/web/src/observer.ts:344, 241 (no \`backoff.reset()\` anywhere; contrast packages/node/src/orchestrator.ts:219) — \`backoff.next()\` is called on every close, hourly rotations included, and is never reset after a healthy session — reproduced: delays after ten close/live cycles were 500…26 373 ms; a dashboard open all day waits up to 30 s to reconnect after any non-rotation drop or a failed session fetch mid-rotation, while its own node is back in 0.5 s, and the 10 s click hold expires meanwhile (walkthrough "Rotating" promises seconds) — \`reset()\` when the snapshot completes and skip \`next()\` when the rotation reason supplies the delay.`
3. `[M] web/host + editor-page — packages/web/src/host.ts:1107-1111, 1160; packages/web/src/editor-page.ts:74-77, 125 — \`fetch("/config.json")\` (and its JSON parse) has no try/catch or retry and \`void main()\` swallows the rejection — a blip on that one request leaves the dashboard on "Connecting… the page keeps trying on its own" (banners.ts:52) for ever, which is false, and the editor tab stuck on its static "connecting to the machine…" text — wrap in a retry loop with backoff and surface the failure in the connection banner.`
4. `[M] web/panels (a11y, R5) — packages/web/src/panels.ts:627/636, 1005-1017/1032, 886-888, 146-147 — the files list, the whole ledger table and the task detail key their rebuild on the global \`cache.version\`, so the moment the bytes a keyboard user requested land, \`replaceChildren()\` destroys the focused row (undoing loop-2 row 27 for keyboard users) and re-renders hundreds of rows per blob landing; \`version\` is also bumped on a miss (line 146 is outside the else), so loop-2 row 22's "not bumped for a miss" holds only in part (loop-2 regression, bounded by \`missingAt\`); e2e/panels.e2e.ts:290-298's "list was not rebuilt" passes only because \`renderResult\` (panels.ts:566) had already fetched \`/out/2/0\` — key each panel on the cache state of the hashes it actually shows (as \`renderPreview\` does) and restore focus after a rebuild.`
5. `[M] web/panels + tiles — packages/web/src/panels.ts:150-154; packages/web/src/tiles.ts:43-46 (vs packages/store/src/client.ts:87-100) — a thrown fetch (any 5xx, a network blip) is cached as \`"error"\` for the life of the page with no retry, and \`storeSource\` has none of \`StoreClient\`'s three retries — one CDN hiccup shows "the manifest could not be fetched" / "could not be fetched" for the result, files, ledger preview and task log until reload — treat \`"error"\` like a miss (retry after \`MISSING_RETRY_MS\`) and route page fetches through \`StoreClient.get\` or a shared retrying source.`
6. `[M] web/host (walkthrough Connecting row, R3) — packages/web/src/host.ts:472-475 vs 524; 1016-1024 — \`setMachine\` gives every control "— not connected yet", but \`render()\` (triggered by \`setMachine\` itself) overwrites \`#stop.title\` with \`stopTitle(state)\` every frame, so Stop never carries the reason; the click handler re-enables a control after 400 ms from \`machine\` alone, briefly enabling restart/skip while nothing runs — compose \`stopTitle\` through \`reasoned()\` and let \`render()\` own the disabled state.`
7. `[M] web/editor — packages/web/src/editor.ts:394-398, 350-352, 402-414 — a launch is acknowledged by matching \`programName === launchedName\` on the execution or the queue, and \`awaitingAnswer\` is set before \`host.launch()\` succeeds — another visitor's namesake program (the default name is "<name>-edit") or the person's own previous run makes the editor say "running as eNN", call \`onLaunched\` → \`resume\`, and show "launched · the pause ended" before the machine has this launch, the very state loop-2 row 24 set out to prevent — match on \`state.execution.program === bundle\` and, for the queue, on an \`executionQueued\` whose seq follows the launch (or carry the bundle in \`queueEntry\`).`
8. `[L] web/panels (walkthrough Execution failed / Stopped rows) — packages/web/src/panels.ts:234-235, 264 and 526-542 — the program row's "running" pill uses \`phase !== "done"\`, so it stays lit after a failure or a stop until the next execution; a stopped bars/text execution's result box says "stage N … in progress; the result appears when the last stage folds" — a visitor reads a failed or stopped program as running — use \`isRunningPhase()\` (state.ts:1039) in both and give \`phase === "stopped"\` its own line.`
9. `[L] web/host (DOM churn, R5, a11y) — packages/web/src/host.ts:580-601/611-630, 682-684/834-844, 646-653; e2e/polish.e2e.ts:206-207; index.html:47,50 with host.ts:388-414; styles.css:343-345 — the eight counter chips are rebuilt on every animation frame and the nodes table is rebuilt four times a second whether or not anything changed (the e2e already works around detached chips); "resume updates (N held)" counts the state present at freeze time (\`frozenSeenSeq\` starts at −1); the two canvases remain click-only and the pulse animation has no reduced-motion guard — update chip text in place, signature-gate the table, start \`frozenSeenSeq\` at the current seq, add arrow-key selection on the grid and a \`prefers-reduced-motion\` rule.`
10. `[L] web/tiles — packages/web/src/tiles.ts:43 (with 136-139) — \`cache: "force-cache"\` returns whatever the browser stored for the URL without revalidation, a stored 404/403 included, so the 2 s "missing" retry that design §7.1 relies on can be answered from cache for ever for a tile requested a moment before its upload finished — use the default cache mode (the CDN's \`immutable\` header already keeps 200s) or \`cache: "reload"\` on the retry path.`
11. `[L] web/demo (walkthrough Stopped row, Demo row) — packages/web/src/demo.ts:406, 1050-1075, 463-471 — \`startNext\` returns while \`stopped\`, so a launch from the programs panel in the demo is queued for ever instead of running at once as D1 and the walkthrough say, and \`startFrame\` hard-codes \`human: false\`, so a launched mandelbrot reads "rendering mandelbrot" not "running your …" — let a queued human launch bypass the stop guard and carry the launcher's \`human\` flag.`
12. `[L] tests and dead/duplicated code — packages/web/src/state.test.ts:443-462 pins a task counted in both \`pending\` and \`failed\` after a mismatch on a failed task (state.ts:544 bumps pending without \`failed: -1\`) on a path the core never produces (packages/core/src/results.ts:157 ignores results for failed tasks); \`.max(64)\` before \`.max(LIMITS.maxPresignItems)\` is a dead leftover in packages/protocol/src/observer.ts:25-27 and node.ts:67-69; \`TaskRunner.run\`'s \`gen\` is unused (packages/node/src/tasks.ts:62, 154 \`void gen\`); \`fmtBytes\` says KiB in result.ts:187 and KB in editor-core.ts:256, and \`fmtTime\`, \`$\`, \`HASH_RE\`, \`OFF_POLL_MS\`, \`PRESIGN_TIMEOUT_MS\` are each defined twice across web/node/store — a stale test hides a real double count and twin constants drift — fix the counter and the test, drop the dead clauses/parameter, and hoist the helpers into one module.`

## Summary

Fix the observer client first (1, 2): every state the walkthrough promises flows through its reconnect path, and it currently both fires stale destructive controls after a resubscribe and, after a day of hourly rotations, takes up to 30 s to come back — both reproduced with a fake socket, both untested. Then close the two places where a single failed request becomes a permanent lie (3, 5), and re-key the panel rebuilds (4) since they undo loop 2's keyboard work and are the page's largest remaining DOM churn. Items 6–11 are contract polish against docs/walkthrough.md; 12 is hygiene. The unit suite for these packages passes (180 tests, 17 files), `tsc` on packages/web is clean, and biome reports only CSS `!important` warnings. Loop-2 claims I verified as real and complete in the code: rows 3 (`defaultParams` ≤ 4 KB in fs.ts), 12 (`hostFailure` refuses `abort:|trap:|link:`), 13 (`maxPresignItems` = 24 and batched `putMany`), 23 (timeout and death terminate the worker), 24 (`sendStatus`, `onDropped` to `#launchInfo`, "launched" only on the echo — with the name-matching caveat in 7), 25 (`seededFailure`, no `lastFailure` on a stop, and the core does emit `cancelled` + "stopped by a person"), 26 (`BoundedBlobMap` 1500), 27 (Shift+Tab/Escape, `tabindex`/`role` rows), 28 (`OFF_POLL_MS` in both clients), 29–31 (per-seq held count, activity signature and cap 400, verified/mismatched in the ledger signature), plus loop-1 rows 12–14, 16, 39–41; the only claim that does not fully hold is row 22's "version not bumped for a miss" (finding 4).
