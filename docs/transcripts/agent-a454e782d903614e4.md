# Session agent-a454e782d903614e4

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 82 · user messages: 1 · assistant messages: 2 · tool calls: 34 · tool results: 34
- from 2026-09-04T15:33:40.607Z to 2026-09-04T15:43:53.997Z
- scrubbed: email ×2

---
## User · 2026-09-04T15:33:40.607Z

You are an expert DevOps / platform engineer reviewing the Tabframe repository at /home/mircea/homework/tabframe (a control plane on AWS Lambda MicroVMs rotated hourly by a Lambda function on an EventBridge rule, a CloudFront + S3 store, a Docker image built and deployed with AWS CDK, mise as the task runner, Bun, GitHub Actions CI with Playwright). This is the SECOND review loop. Read docs/implementation/wp-8.1-review-loop-1.md first: it lists the first loop's findings and what was done — the sequential guarded deploy (packages/infra/scripts/deploy-guard.ts, the build stamp in stage-image.ts and /health), failed rotations that throw with alarms and rule retries (packages/fleet/src/lambda/rotate.ts, packages/infra/lib/fleet-stack.ts), the scheduled rule skipping a suspended control plane, the stuck-successor cleanup and hourly client token, RETAIN buckets and the snapshot lifecycle prefix, response headers, pinned tools in mise.toml, CI concurrency/retries/cache, the exporter guard. Verify those fixes are real and complete (a CDK change that would fail at deploy, an alarm that can never fire, a guard that can be bypassed by accident), do not repeat what is fixed, and make the deferred items concrete: a real `rollback` task that pins the cores' image version too, pinning the base image by digest and nodejs22 exactly, a `cdk diff` gate, per-task scoping of `.env.local`, a synthetic canary. Read README.md, docs/runbook.md, docs/design.md sections 9 to 12, mise.toml, .github/workflows/*.yml, packages/infra (lib, scripts, image), packages/fleet/src, scripts/.

Rules: do NOT modify any file, do NOT run git commands that write, do NOT run any AWS, aws, cdk, deploy, up, down, or rotate commands, and never use the `tabframe` AWS profile. Reading, grep, and running `bun test <path>` locally are fine. Do not print secrets or account identifiers; describe where they are.

Output (at most 1500 words): a section "## Findings" with at most 12 entries, most severe first, each on the form:
`[H|M|L] <area> — <file:line> — <problem> — <why it matters> — <concrete fix>`
Mark an entry "(loop 1 regression)" if the first loop's fixes introduced or left it. Only real risks or clear improvements. Then "## Summary": three sentences on the operational posture now and the top three things to fix.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T15:33:44.805Z

I'll start with the loop-1 document and the top-level docs, then work through the infra, fleet, and CI code.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.1-review-loop-1.md
```

**Result of `Read`:**

```
1	# WP8.1 — Review loop 1
2	
3	**Branch** `wp/8.1-review-loop-1` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop one.
7	
8	## How the loop ran
9	
10	Five independent reviewers (fresh agents, read-only, no AWS access) each returned at most twelve
11	findings with severity, location, cause, and fix. The 52 findings were de-duplicated into the
12	table below; each was accepted, deferred to a later loop, or rejected with the reason (the
13	critique). The accepted ones landed on this branch with tests; CI gates the merge; the next loop
14	reviews the tree with these changes in.
15	
16	## The findings and what was done
17	
18	| # | From | Sev | Finding | Decision |
19	|---|---|---|---|---|
20	| 1 | dist | H | Stage-advancing store effects (inherited root, stage spec, folded manifest) were fire-and-forget: a handover mid-flight or one S3 error wedged the execution for ever. | **Accepted.** `resumePending()` re-derives the pending effect from the ledger on adopt and again every `STORE_RETRY_MS` (`waitingSince` on the execution); a pruned origin now falls back to the bundle with the `expired-root` warning instead of a silent start. |
21	| 2 | dist | H | `encode()` over 64 KB threw through `dispatch`; params and the queue were unbounded. | **Accepted.** `PARAMS_MAX_BYTES` (4 KB canonical) and `QUEUE_CAP` (32) in `enqueue`; a send that cannot be encoded closes that connection instead of throwing. |
22	| 3 | dist | H | A node reported store or network failures as program faults, failing everybody's execution. | **Accepted.** `hostFailure()` covers fetch, upload, presign timeouts and network errors → `RELEASED`; the store client retries three times with backoff. |
23	| 4 | sec | H | The control plane fetched blobs named by untrusted parties without a size check. | **Accepted.** `StoreDriver.get(hash, maxBytes)` (HeadObject first on S3, `BlobTooLarge` otherwise); caps at every fetch: stage spec 1 MB, inherited root 4 MB, bundle manifest 1 MB, program manifest 64 KB, module 8 MB. |
24	| 5 | sec | H | Presign sizes were unbounded and uncounted. | **Accepted.** `size ≤ maxOutputBytes` and at most 64 items per presign in the schemas; a 1 GiB presigned-byte budget per connection in the core. |
25	| 6 | sec | H | A module could declare a second memory without a maximum and grow past the cap. | **Accepted.** `countMemories()`; more than one memory is refused. |
26	| 7 | fs | H | A node's presign never timed out and an S3 HeadObject error failed the whole presign: a zombie core. | **Accepted.** `SocketPresigner` times out after 30 s (the task is released); `S3Store.presign` treats a HeadObject error as "not there" and signs. |
27	| 8 | tw, devops | H | The hourly rule rotated a *suspended* control plane, so an idle night booted a generation an hour and the README's stays-up paragraph was false. | **Accepted.** A scheduled invocation skips a `SUSPENDED` control plane (`skipped-suspended`); the README says what happens now. |
28	| 9 | devops | H | `deploy` ran build and test in parallel from the working tree with no clean-tree or green-main guard and no commit stamp. | **Accepted.** `deploy` is sequential: build → test → `deploy-guard.ts` (clean tree, HEAD on origin/main; `TABFRAME_DEPLOY_UNGATED=1` to skip) → stacks → up. `stage-image.ts` writes `build.json`; `/health` shows `build` and `imageVersion`. |
29	| 10 | devops | H | The rotate function returned failures as successes; nothing alarmed. | **Accepted.** The Lambda entry throws on `failed`; CloudWatch alarms on rotate/session errors and rotate throttles go to an SNS topic mailed to the budget address when configured; the hourly rule's target retries twice within thirty minutes. |
30	| 11 | sec | M | Destructive controls are unauthenticated; kill-half in a loop terminates and relaunches cloud cores. | **Rejected as a change, accepted as a bound.** A public machine anyone may drive is the design (D4, §6.7); a machine-wide cooldown of three seconds per destructive control (`CONTROL_COOLDOWN_MS`) removes the cost loop. |
31	| 12 | fs | M | The editor tab re-sent `pause` on every live socket, including after its own launch. | **Accepted.** `holdWanted` is cleared by launch and close. |
32	| 13 | fs | M | A failing session fetch during a silent resubscribe kept a green pill; held clicks were dropped silently. | **Accepted.** The page goes back to "connecting"; dropped clicks are said in the status line (`onDropped`). |
33	| 14 | fs | M | An outdated page reloaded itself in a loop. | **Accepted.** One reload per protocol version (`sessionStorage`), then a hard-refresh hint. |
34	| 15 | fs | M | The ledger tab serialised every row per frame; the blob cache and the demo store grew without bound. | **Accepted** (ledger signature; a 64 MB LRU `BlobCache`). The demo store's growth is deferred to loop 2. |
35	| 16 | fs | M | A dead compiler worker left the editor unusable. | **Accepted.** Pending compiles are rejected, a two-minute compile timeout, the next compile gets a fresh worker; failed drops say so. |
36	| 17 | dist | M | Between handover and promote a dead rotation left the machine dark for an hour; a predecessor kept acting after handing over. | **Accepted.** `HANDOVER_LEASE_MS` (90 s) returns a control plane to `active` when no drain follows; ticks run no fleet, loop, or assignment while `handing-over` or `drained`. |
37	| 18 | dist | M | Slow `RunMicrovm` calls were asked twice; surplus cores were never trimmed. | **Accepted.** `meta.coreLaunches` counts launches until acknowledged (or a minute passes); surplus unlinked cores are terminated. |
38	| 19 | dist | M | Any node's report could settle or fail any open task; `computeMs` was unbounded; results per task grew without bound. | **Accepted in part.** A report for an attempt the node never held may still verify or contest a settled task (D7, the tests lean on it) but can no longer *fail* a task; `computeMs` is bounded; sixteen records per task. |
39	| 20 | sec | M | Blobs are served on the page's origin with no `nosniff`, no CSP. | **Accepted.** CloudFront response-header policies: nosniff, DENY framing, HSTS, a CSP for the page; `Content-Disposition: attachment` and a sandbox CSP for `blob/*`. |
40	| 21 | sec | M | Results and presigns were exempt from the rate bucket. | **Accepted.** A second bucket (`SOLICITED_RATE` 256/s) for them. |
… (35 more lines)
```

**Tool `Bash`:**

```
ls -la && echo "---" && ls -R packages/infra packages/fleet/src scripts .github 2>/dev/null | head -150
```

**Result of `Bash`:**

```
total 228
drwxrwxr-x 14 mircea mircea  4096 Sep  4 08:23 .
drwxrwxr-x  5 mircea mircea  4096 Sep  2 07:02 ..
-rw-rw-r--  1 mircea mircea   787 Sep  2 23:35 biome.json
-rw-rw-r--  1 mircea mircea   910 Sep  1 22:51 bunfig.toml
-rw-r--r--  1 mircea mircea 46043 Sep  3 00:13 bun.lock
-rw-rw-r--  1 mircea mircea   543 Sep  1 21:41 cdk.json
drwxrwxr-x 58 mircea mircea 12288 Sep  4 08:10 cdk.out
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:10 .claude
drwxrwxr-x  2 mircea mircea 20480 Sep  4 08:22 coverage
drwxrwxr-x  4 mircea mircea  4096 Sep  3 22:12 docs
drwxrwxr-x  2 mircea mircea  4096 Sep  4 08:08 e2e
-rw-------  1 mircea mircea   189 Sep  2 15:05 .env.local
drwxrwxr-x  8 mircea mircea  4096 Sep  4 08:27 .git
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:27 .github
-rw-rw-r--  1 mircea mircea   364 Sep  2 15:19 .gitignore
-rw-rw-r--  1 mircea mircea 34523 Sep  1 21:08 LICENSE
-rw-rw-r--  1 mircea mircea  5998 Sep  4 08:08 mise.toml
drwxr-xr-x  7 mircea mircea  4096 Sep  1 22:51 node_modules
-rw-rw-r--  1 mircea mircea   436 Sep  1 22:51 package.json
drwxrwxr-x 13 mircea mircea  4096 Sep  1 22:52 packages
-rw-rw-r--  1 mircea mircea  1946 Sep  4 07:54 playwright.config.ts
drwxrwxr-x  5 mircea mircea  4096 Sep  2 23:56 programs
-rw-rw-r--  1 mircea mircea 16114 Sep  4 08:19 README.md
drwxrwxr-x  3 mircea mircea  4096 Sep  2 13:37 scripts
drwxrwxr-x  4 mircea mircea  4096 Sep  4 08:26 test-results
-rw-rw-r--  1 mircea mircea   480 Sep  1 21:08 tsconfig.base.json
-rw-rw-r--  1 mircea mircea   294 Sep  3 20:28 tsconfig.json
---
.github:
workflows

.github/workflows:
ci.yml

packages/fleet/src:
aws.ts
config.ts
cp-client.ts
lambda
… (134 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat -n mise.toml && echo "=====CI=====" && cat -n .github/workflows/ci.yml && echo "=====cdk.json=====" && cat -n cdk.json && echo "=====package.json=====" && cat -n package.json && echo "=====bunfig=====" && cat -n bunfig.toml && echo "=====gitignore=====" && cat -n .gitignore && echo "=====playwright=====" && cat -n playwright.config.ts && echo "=====infra package.json=====" && cat -n packages/infra/package.json && echo "=====env.local keys only=====" && sed -E 's/=.*/=<redacted>/' .env.local
```

**Result of `Bash`:**

```
     1	# Tabframe — tooling, environment, and tasks. Everything installs through mise.
     2	[tools]
     3	# Pinned (WP8.1): the deploy path runs under an admin identity, so the CLIs it runs are exact.
     4	node = "22.23.2"
     5	bun = "1.3.13"
     6	aws-cli = "2.36.37"
     7	gh = "2.97.0"
     8	"npm:aws-cdk" = "2.1139.0"
     9	
    10	[env]
    11	AWS_PROFILE = "tabframe"
    12	AWS_REGION = "us-west-2"
    13	_.file = ".env.local" # gitignored: TABFRAME_ACCOUNT_ID, TABFRAME_BUDGET_EMAIL
    14	
    15	[tasks.install]
    16	description = "Install workspace dependencies and the Playwright browser"
    17	run = ["bun install", "bunx playwright install chromium"]
    18	
    19	[tasks.whoami]
    20	description = "Assert the AWS identity is the Tabframe account; every AWS task depends on it"
    21	run = "node packages/infra/scripts/whoami.ts"
    22	
    23	[tasks.lint]
    24	description = "Biome lint and format check, then a TypeScript type check"
    25	run = [
    26	  "bunx biome check .",
    27	  "bunx tsc -p tsconfig.json --noEmit",
    28	  "bunx tsc -p packages/web/tsconfig.json --noEmit",
    29	  "bunx tsc -p packages/infra/tsconfig.json --noEmit",
    30	]
    31	
    32	[tasks.test]
    33	description = "Unit and integration tests, then browser tests"
    34	depends = ["lint"]
    35	run = ["bun test", "bunx playwright test --pass-with-no-tests"]
    36	
    37	[tasks.dev]
    38	description = "Local topology: control plane, local store, two local cores, web bundles in watch mode"
    39	run = "node packages/dev/src/up.ts"
    40	
… (404 more lines)
```

**Tool `Bash`:**

```
cat -n README.md
```

**Result of `Bash`:**

```
     1	# Tabframe
     2	
     3	A fault-tolerant distributed computer whose cores are browser tabs and Firecracker microVMs,
     4	programmed with WebAssembly. Every worker is a core — a thread in someone's tab, or one of the two
     5	MicroVMs in AWS, the *cloud cores*. Open the page and your tab is a core. Close it and the machine keeps
     6	computing, correctly. The control plane that schedules the work is itself replaced every hour, with
     7	the work in flight.
     8	
     9	Deployed: **https://d2w9z8juw4oo76.cloudfront.net** — the page lends one core when it opens, and
    10	shows the machine rendering a Mandelbrot frame with whoever else is there.
    11	
    12	**Try it, in five clicks.** Spawn 3 (the counters follow), kill half (tiles are taken back and finish
    13	elsewhere), redundancy on (the verified counter moves), editor ↗ (change `CYCLE`, compile, launch:
    14	your program goes ahead of the loop), the ledger tab (hashes, not bytes). `?observe` lends no cores,
    15	`?demo=1` runs a scripted cluster inside the page, and a rotation banner every hour is expected.
    16	
    17	## What it is
    18	
    19	A program is one WebAssembly module with two entry points and a manifest. `plan` turns parameters
    20	and the previous stage's outputs into the next stage's tasks; `run` turns one task's input into
    21	bytes. Both execute on cores — the control plane runs no program code, not even the planner. A
    22	program's only view of the world is a per-execution filesystem of content-addressed blobs: it has
    23	no clock, no randomness, no network, and no failure type anywhere in its API.
    24	
    25	Three programs ship with the machine and go through the same path as anything you write in the
    26	in-page editor: a distributed **Mandelbrot** render (640 tiles of 64×64 per frame, presets that
    27	advance while anyone watches) and a three-stage **word count** over *Moby-Dick* (map by byte range,
    28	reduce by partition, merge to a top-25). You can also compile your own: the editor holds the
    29	Mandelbrot source and the AssemblyScript compiler runs in a browser worker.
    30	
    31	Since M6 a third program ships with the image: **tiny GPT**, an 822 k-parameter character-level
    32	transformer trained on that same corpus, whose whole forward pass runs in WebAssembly on the cores —
    33	one continuation per task, four milliseconds a token, the same bytes on every core
    34	(`docs/feasibility-transformer.md`).
    35	
    36	## Why it looks the way it does
    37	
    38	Three constraints carry the whole fault-tolerance story; everything else follows from them.
    39	
    40	1. **Tasks are idempotent and delivered at least once.** A task may run on two cores, be killed
… (180 more lines)
```

**Tool `Bash`:**

```
cat -n docs/runbook.md
```

**Result of `Bash`:**

```
     1	# Runbook
     2	
     3	How to operate the deployed Tabframe machine. Every command below runs from the repo root with
     4	`mise`, which sets the AWS profile (`tabframe`, the dedicated account) and the region; nothing here
     5	works with any other profile, by design (`mise run whoami` is the guard every task depends on).
     6	
     7	## What is running
     8	
     9	| Thing | Where | Notes |
    10	|---|---|---|
    11	| Control plane | one Lambda MicroVM, 1 GB, generation-stamped | pointed at by the SSM parameter `/tabframe/pointer`; rotates hourly |
    12	| Cloud cores | up to two 0.5 GB MicroVMs while the machine is awake | launched by the control plane; asleep ten minutes after the last observer leaves |
    13	| Session function | Lambda function URL (public) | vends the endpoint and a shared token; heals when nothing runs |
    14	| Rotate function | Lambda, hourly EventBridge rule | launches the successor, hands over, flips the pointer, drains, terminates |
    15	| Store | S3 behind CloudFront (`/blob/*`) | content-addressed, one-year lifecycle |
    16	| Snapshots | S3, `g<generation>/<time>.json.gz` and `latest.json.gz` | one-day lifecycle |
    17	| Page | S3 behind the same CloudFront distribution | `https://d2w9z8juw4oo76.cloudfront.net` |
    18	
    19	## Everyday
    20	
    21	| Task | Command | What it does |
    22	|---|---|---|
    23	| Is the machine up? | `mise run health` (`-- --cores` asks each cloud core too) | `/health` and `/diag` of the active control plane through the proxy on the private port, masked. Counts, phase, fleet, snapshotter status, a store round trip, memory. |
    24	| Bring it up | `mise run up` | Sets the pointer to *on*, enables the hourly rule, invokes rotate once. Idempotent: a running control plane is rotated, not duplicated. |
    25	| Take it down | `mise run down` | Disables the rule, terminates every MicroVM from our image, writes *off*. The page shows an off screen; the session function heals nothing. **Destructive: every MicroVM is terminated and the page goes dark until `up`.** |
    26	| Deploy | `mise run deploy` | Builds programs, page, and image; runs lint and every test; `cdk deploy --all`; then `up`, which rotates the running control plane onto the new image. A deploy is a rotation. |
    27	| Rotate by hand | `mise run rotate` | One rotation now (same code the hourly rule runs). |
    28	| Watch logs | `mise run logs`, `mise run logs:fleet` | The MicroVM log group (one stream per VM) and the two functions' groups. See *Observability* for what actually lands. |
    29	| Verify | `mise run verify:m1` / `verify:m2` / `verify:m3` | The milestone runbooks against the live machine: a frame through a kill-half, the editor and word count, a rotation under load. Each prints one line per check and exits non-zero on a failure. |
    30	| Simulate | `mise run sim -- --seed N` | The churn simulation, locally; `--long` for the nightly shape. |
    31	
    32	## Deploy, step by step
    33	
    34	1. `main` is green in CI (`mise exec -- gh run list --branch main --limit 1`). This is a rule, not a
    35	   preference (plan §0).
    36	2. `mise run deploy`. The image build takes three to five minutes; the first build after a long
    37	   gap has taken fifty. If CloudFormation reports the image "did not stabilize", check the build
    38	   history — the platform has retried and succeeded on its own once while CloudFormation gave up;
    39	   a plain re-run of `cdk deploy` then completes.
    40	3. `up` at the end of the deploy rotates: the old control plane hands its ledger to the new one,
… (77 more lines)
```

**Tool `Bash`:**

```
grep -n "^## \|^# " docs/design.md | head -80
```

**Result of `Bash`:**

```
1:# Tabframe — Design Record
13:## Contents
35:## 1. Decisions register
66:## 2. The system in one page
89:## 3. Runtime picture
140:## 4. Nodes
178:## 5. Programs
250:## 6. The control plane
328:## 7. Data paths and the store
367:## 8. The wire
425:## 9. Hosting and fleet
514:## 10. Security and credentials
542:## 11. Tooling and repo
629:## 12. Tests and dev loop
649:## 13. Deploy, operations, and the reviewer's view
667:## 14. Milestones
682:## 15. Rationale hooks
696:## 16. Glossary
700:## 17. Drift log
```

> **Thinking.** 

**Tool `Bash`:**

```
sed -n '425,667p' docs/design.md
```

**Result of `Bash`:**

```
## 9. Hosting and fleet

### 9.1 Platform facts that shape the design

| Fact (Lambda MicroVMs) | Consequence |
|---|---|
| Image built from a Dockerfile; app started; snapshot taken; every MicroVM boots from it | The control plane is a Node process that boots already listening |
| Dedicated TLS endpoint per MicroVM; HTTP/1.1, HTTP/2, WebSockets, SSE; port 8080 by default | No load balancer, no certificate |
| Every request needs a JWE token (1–60 min), minted with AWS credentials; browsers pass it as a WebSocket subprotocol or a fetch header | A token-vending front door |
| Hard maximum 8 hours, running plus suspended | Hourly handover (D15) |
| Idle policy suspends on no endpoint traffic; auto-resume holds the first request; hooks on run, suspend, resume, terminate | Idle costs nothing; heartbeats and pings keep it awake while anyone is connected |
| Endpoint bandwidth capped by size: 1 MB/s at 0.5 GB, 2 MB/s at 1 GB | D9 mandatory |
| Public egress by default; ARM64; us-west-2 available | Presign from the execution role; build for arm64 |
| CloudFormation/CDK define images; running MicroVMs are API-managed | CDK owns image, roles, buckets, front door; the fleet function starts instances |
| Account quotas read 2026-09-01: 8 GB MicroVM memory, RunMicrovm 1/s, 8 h max | 2–3 GB baseline in use; space launches; back off |

### 9.2 Topology

- **Fleet functions** (two Lambdas sharing code): `session` on a public URL vends sessions — one shared token per control plane, minted every twenty-five minutes and cached — and triggers a heal when no control plane runs, or serves the off state; `rotate` runs hourly on EventBridge with reserved concurrency 1, is idempotent (it checks the pointer and the running MicroVMs before launching anything), and is the sole writer of the pointer.
- **Pointer:** one SSM parameter naming the active control-plane MicroVM and its generation.
- **Control plane:** 1 GB (the API takes only a minimum memory; the vCPU class is the platform's, and for every size we can launch it is the 2-vCPU class, §9.7), `ALL_INGRESS` + `INTERNET_EGRESS`, idle policy suspend after 15 min with auto-resume, max duration 8 h. Public port 8080 carries sockets only; private port 8081 carries hooks and the internal endpoints; browser tokens are scoped to 8080, fleet tokens to all ports.
- **Cloud cores:** two × 0.5 GB, `NO_INGRESS`, no idle policy, max duration 4 h.
- **Store and static:** S3 behind one CloudFront distribution.

### 9.3 One image, two roles

Control plane and core are the same MicroVM image (§11.3). The process boots **neutral**: listening on both ports, answering hooks, holding no role and making no AWS calls. `/ready` returns 200 once listening; `/validate` runs a short in-process hello → assign → result cycle against an in-memory store, which both proves the image and lets Lambda prefetch the parts of the snapshot that matter. `/run` delivers the payload that turns the neutral process into a control plane or a core: role, generation, snapshot key, session URL, store base, fleet secret. Nothing fleet-related is baked into the image, which keeps the CDK stacks acyclic: Core → Image → Fleet.

### 9.4 Authority and handover

Exactly one control plane is **active**, stamped with a generation. Others are booting or draining. "In flight" never means "authoritative."

**Hourly handover — also the deploy path:**

1. `rotate` launches CP(g+1) with a payload naming the generation and the latest snapshot key, and waits for its run hook.
2. `rotate` calls CP(g) `/handover`: it stops assigning, pauses intake, serializes the ledger, and returns it.
3. `rotate` posts the ledger to CP(g+1) `/adopt`: it marks every node gone (releasing their work), becomes active.
4. `rotate` flips the pointer.
5. `rotate` calls CP(g) `/drain`: it closes every client with the *rotating, reconnect now* code and a jittered reconnect delay (§8.4), and is terminated after a grace period.
6. Clients fetch a fresh session and reconnect. Nodes rejoin as new nodes; cores likewise. A few seconds of churn per hour, under load.
… (203 more lines)
```

**Tool `Bash`:**

```
cd packages/infra && for f in bin/tabframe.ts lib/names.ts lib/base-image.ts lib/core-stack.ts lib/image-stack.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
cd packages/infra && for f in lib/fleet-stack.ts lib/web-stack.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== bin/tabframe.ts =====
     1	// CDK app entry: four stacks in dependency order, Core → Image → Fleet → Web (design §11.4).
     2	// Run via the repo-root cdk.json (`node packages/infra/bin/tabframe.ts`).
     3	import { dirname, resolve } from "node:path";
     4	import { fileURLToPath } from "node:url";
     5	import * as cdk from "aws-cdk-lib";
     6	import { CoreStack } from "../lib/core-stack.ts";
     7	import { FleetStack } from "../lib/fleet-stack.ts";
     8	import { ImageStack } from "../lib/image-stack.ts";
     9	import { WebStack } from "../lib/web-stack.ts";
    10	
    11	const here = dirname(fileURLToPath(import.meta.url));
    12	const repoRoot = resolve(here, "../../..");
    13	
    14	const app = new cdk.App();
    15	const account = process.env.CDK_DEFAULT_ACCOUNT;
    16	const env: cdk.Environment = {
    17	  region: process.env.CDK_DEFAULT_REGION ?? "us-west-2",
    18	  ...(account ? { account } : {}),
    19	};
    20	
    21	const budgetEmail = process.env.TABFRAME_BUDGET_EMAIL;
    22	const core = new CoreStack(app, "TabframeCore", {
    23	  env,
    24	  ...(budgetEmail ? { budgetEmail } : {}),
    25	});
    26	
    27	const image = new ImageStack(app, "TabframeImage", {
    28	  env,
    29	  core,
    30	  baseImageVersion: String(app.node.tryGetContext("baseImageVersion") ?? "1"),
    31	  stagingDir: process.env.TABFRAME_IMAGE_DIR ?? resolve(repoRoot, "packages/infra/image"),
    32	});
    33	
    34	const fleet = new FleetStack(app, "TabframeFleet", {
    35	  ...(budgetEmail ? { alarmEmail: budgetEmail } : {}),
    36	  env,
    37	  core,
    38	  image,
    39	  fleetDir: resolve(repoRoot, "packages/fleet"),
… (534 more lines)
```

**Tool `Bash`:**

```
cd packages/infra && for f in scripts/deploy-guard.ts scripts/stage-image.ts scripts/base-image-version.ts scripts/whoami.ts scripts/health.ts scripts/mask.ts image/Dockerfile image/package.json test/base-image.test.ts test/synth.test.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
===== lib/fleet-stack.ts =====
     1	// Fleet stack (design §9.2, §11.4): the session and rotate functions, the hourly rule (created
     2	// disabled), and their least-privilege roles.
     3	import * as cdk from "aws-cdk-lib";
     4	import type { Construct } from "constructs";
     5	import type { CoreStack } from "./core-stack.ts";
     6	import type { ImageStack } from "./image-stack.ts";
     7	import { anyImageArn, functionArn, NAMES, parameterArn, ruleArn } from "./names.ts";
     8	
     9	export interface FleetStackProps extends cdk.StackProps {
    10	  /** Where the alarms mail (WP8.1); the budget address, when configured. */
    11	  alarmEmail?: string;
    12	  core: CoreStack;
    13	  image: ImageStack;
    14	  /** Directory holding packages/fleet (for the Lambda entry files). */
    15	  fleetDir: string;
    16	}
    17	
    18	export class FleetStack extends cdk.Stack {
    19	  readonly session: cdk.aws_lambda_nodejs.NodejsFunction;
    20	  readonly rotate: cdk.aws_lambda_nodejs.NodejsFunction;
    21	  readonly sessionUrl: cdk.aws_lambda.FunctionUrl;
    22	  readonly hourlyRule: cdk.aws_events.Rule;
    23	
    24	  constructor(scope: Construct, id: string, props: FleetStackProps) {
    25	    super(scope, id, props);
    26	    const { core, image } = props;
    27	    const iam = cdk.aws_iam;
    28	    const lambda = cdk.aws_lambda;
    29	    const nodejs = cdk.aws_lambda_nodejs;
    30	
    31	    const bundling: cdk.aws_lambda_nodejs.BundlingOptions = {
    32	      format: nodejs.OutputFormat.ESM,
    33	      target: "node22",
    34	      // The runtime's built-in SDK predates client-lambda-microvms; bundle the SDK we import.
    35	      bundleAwsSDK: true,
    36	      mainFields: ["module", "main"],
    37	      banner:
    38	        "import { createRequire } from 'module'; const require = createRequire(import.meta.url);",
    39	    };
… (212 more lines)
```

**Tool `Bash`:**

```
cd packages/fleet && ls -la scripts src src/lambda test 2>/dev/null; for f in src/config.ts src/types.ts src/pointer.ts src/aws.ts src/microvm-client.ts src/cp-client.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Tool `Bash`:**

```
cd packages/fleet && for f in src/rotate.ts src/ops.ts src/session.ts src/lambda/rotate.ts src/lambda/session.ts; do echo "===== $f ====="; cat -n "$f"; done
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (29.4KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b0tcgipvk.txt

Preview (first 2KB):
===== scripts/deploy-guard.ts =====
     1	// The deploy guard (WP8.1): a deploy builds the image from the working tree, so the tree must be
     2	// committed and its HEAD must be on origin/main — the commit CI made green. Set
     3	// TABFRAME_DEPLOY_UNGATED=1 to skip it for an emergency deploy from a branch.
     4	import { execSync } from "node:child_process";
     5	
     6	const sh = (cmd: string): string => execSync(cmd, { encoding: "utf8" }).trim();
     7	
     8	if (process.env.TABFRAME_DEPLOY_UNGATED === "1") {
     9	  console.log("[deploy-guard] skipped (TABFRAME_DEPLOY_UNGATED=1)");
    10	  process.exit(0);
    11	}
    12	const dirty = sh("git status --porcelain");
    13	if (dirty) {
    14	  console.error("[deploy-guard] the working tree has uncommitted changes:\n" + dirty);
    15	  process.exit(1);
    16	}
    17	try {
    18	  sh("git fetch -q origin main");
    19	} catch {
    20	  console.warn("[deploy-guard] could not fetch origin/main; checking against the local main");
    21	}
    22	const head = sh("git rev-parse HEAD");
    23	let onMain = false;
    24	try {
    25	  sh("git merge-base --is-ancestor HEAD origin/main");
    26	  onMain = true;
    27	} catch {
    28	  try {
    29	    sh("git merge-base --is-ancestor HEAD main");
    30	    onMain = true;
    31	  } catch {
    32	    onMain = false;
    33	  }
    34	}
    35	if (!onMain) {
… (12 more lines)
```

**Tool `Bash`:**

```
cd packages/fleet && for f in scripts/*.ts; do echo "===== $f ====="; cat -n "$f"; done; echo "===== export.ts ====="; cat -n /home/mircea/homework/tabframe/scripts/transcripts/export.ts | head -120
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (30.6KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b5bt3wpou.txt

Preview (first 2KB):
scripts:
total 24
drwxrwxr-x 2 mircea mircea 4096 Sep  1 22:20 .
drwxrwxr-x 6 mircea mircea 4096 Sep  2 01:29 ..
-rw-rw-r-- 1 mircea mircea 1721 Sep  1 22:20 _deps.ts
-rw-rw-r-- 1 mircea mircea  274 Sep  1 22:20 down.ts
-rw-rw-r-- 1 mircea mircea  328 Sep  1 22:20 rotate.ts
-rw-rw-r-- 1 mircea mircea  260 Sep  1 22:20 up.ts

src:
total 76
drwxrwxr-x 4 mircea mircea  4096 Sep  2 02:57 .
drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
-rw-rw-r-- 1 mircea mircea  3499 Sep  1 21:40 aws.ts
-rw-rw-r-- 1 mircea mircea  3117 Sep  1 21:40 config.ts
-rw-rw-r-- 1 mircea mircea  5963 Sep  2 02:57 cp-client.ts
drwxrwxr-x 2 mircea mircea  4096 Sep  2 01:09 lambda
-rw-rw-r-- 1 mircea mircea  4559 Sep  1 21:40 microvm-client.ts
-rw-rw-r-- 1 mircea mircea  1942 Sep  1 21:40 ops.ts
-rw-rw-r-- 1 mircea mircea  3010 Sep  2 01:09 pointer.ts
-rw-rw-r-- 1 mircea mircea 14437 Sep  4 07:51 rotate.ts
-rw-rw-r-- 1 mircea mircea  4351 Sep  1 21:40 session.ts
drwxrwxr-x 2 mircea mircea  4096 Sep  1 21:40 testing
-rw-rw-r-- 1 mircea mircea  4062 Sep  3 00:01 types.ts

src/lambda:
total 16
drwxrwxr-x 2 mircea mircea 4096 Sep  2 01:09 .
drwxrwxr-x 4 mircea mircea 4096 Sep  2 02:57 ..
-rw-rw-r-- 1 mircea mircea 2077 Sep  4 07:54 rotate.ts
-rw-rw-r-- 1 mircea mircea  652 Sep  1 21:40 session.ts

test:
total 68
drwxrwxr-x 2 mircea mircea  4096 Sep  2 02:57 .
drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
… (11 more lines)
```

**Result of `Bash`:**

```
===== src/rotate.ts =====
     1	// The rotate function (design §9.4): the hourly handover, which is also the deploy path and the
     2	// heal path. One run does at most one rotation and leaves the fleet in a state the next run can
     3	// finish or roll back — an EventBridge schedule with no reserved concurrency can overlap runs, and
     4	// a Lambda can die at any line.
     5	//
     6	//   1. launch CP(g+1), naming the latest snapshot so it boots adopted even if step 2 fails
     7	//   2. CP(g) /handover  → its ledger
     8	//   3. CP(g+1) /adopt   ← that ledger
     9	//   4. flip the pointer
    10	//   5. CP(g) /drain, then terminate after a grace period
    11	
    12	import {
    13	  CONTROL_PLANE_IDLE_POLICY,
    14	  CONTROL_PLANE_MAX_DURATION_SECONDS,
    15	  egressConnectorArn,
    16	  ingressConnectorArn,
    17	  type RotateConfig,
    18	} from "./config.ts";
    19	import type { ControlPlaneClient, ControlPlaneTarget } from "./cp-client.ts";
    20	import type { Pointer, PointerStore } from "./pointer.ts";
    21	import {
    22	  type Clock,
    23	  isThrottling,
    24	  type Logger,
    25	  type MicrovmClient,
    26	  type MicrovmInfo,
    27	  SERVING_STATES,
    28	  type SecretReader,
    29	  type Sleeper,
    30	} from "./types.ts";
    31	
    32	export interface RotateDeps {
    33	  pointer: PointerStore;
    34	  microvms: MicrovmClient;
    35	  secrets: SecretReader;
    36	  clock: Clock;
    37	  sleep: Sleeper;
    38	  log: Logger;
    39	  config: RotateConfig;
… (586 more lines)
```

**Result of `Bash`:**

```
===== scripts/_deps.ts =====
     1	// Shared wiring for the operator scripts (run by `mise run rotate|up|down` after the whoami guard).
     2	import { CloudFormationClient, DescribeStacksCommand } from "@aws-sdk/client-cloudformation";
     3	import { EventBridgeRuleControl, LambdaInvoker, SsmPointerStore } from "../src/aws.ts";
     4	import { loadOpsConfig } from "../src/config.ts";
     5	import { SdkMicrovmClient } from "../src/microvm-client.ts";
     6	import type { OpsDeps } from "../src/ops.ts";
     7	import { consoleLogger, realSleeper } from "../src/types.ts";
     8	
     9	/** The image ARN embeds the account id, so it is never in the repo: read it from the deployed stack. */
    10	async function resolveImageArn(): Promise<void> {
    11	  if (process.env.TABFRAME_IMAGE_ARN) return;
    12	  const cfn = new CloudFormationClient({});
    13	  const r = await cfn.send(new DescribeStacksCommand({ StackName: "TabframeImage" }));
    14	  const out = r.Stacks?.[0]?.Outputs?.find((o) => o.OutputKey === "ImageArn")?.OutputValue;
    15	  if (!out) throw new Error("TabframeImage stack has no ImageArn output; deploy first");
    16	  process.env.TABFRAME_IMAGE_ARN = out;
    17	}
    18	
    19	export async function operatorDeps(): Promise<OpsDeps> {
    20	  await resolveImageArn();
    21	  const config = loadOpsConfig(process.env);
    22	  return {
    23	    pointer: new SsmPointerStore(config.pointerParam),
    24	    microvms: new SdkMicrovmClient(),
    25	    invoker: new LambdaInvoker(),
    26	    rules: new EventBridgeRuleControl(),
    27	    sleep: realSleeper,
    28	    log: consoleLogger,
    29	    config,
    30	  };
    31	}
    32	
    33	/** Never let an account id or endpoint token reach the terminal unmasked. */
    34	export function mask(text: string): string {
    35	  return text
    36	    .replace(/\b(\d{8})(\d{4})\b/g, (_m, _h: string, tail: string) => `********${tail}`)
    37	    .replace(/"token":"[^"]+"/g, '"token":"<redacted>"');
    38	}
===== scripts/down.ts =====
… (112 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b0tcgipvk.txt
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (29.4KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/beaf36yan.txt

Preview (first 2KB):
===== scripts/deploy-guard.ts =====
     1	// The deploy guard (WP8.1): a deploy builds the image from the working tree, so the tree must be
     2	// committed and its HEAD must be on origin/main — the commit CI made green. Set
     3	// TABFRAME_DEPLOY_UNGATED=1 to skip it for an emergency deploy from a branch.
     4	import { execSync } from "node:child_process";
     5	
     6	const sh = (cmd: string): string => execSync(cmd, { encoding: "utf8" }).trim();
     7	
     8	if (process.env.TABFRAME_DEPLOY_UNGATED === "1") {
     9	  console.log("[deploy-guard] skipped (TABFRAME_DEPLOY_UNGATED=1)");
    10	  process.exit(0);
    11	}
    12	const dirty = sh("git status --porcelain");
    13	if (dirty) {
    14	  console.error("[deploy-guard] the working tree has uncommitted changes:\n" + dirty);
    15	  process.exit(1);
    16	}
    17	try {
    18	  sh("git fetch -q origin main");
    19	} catch {
    20	  console.warn("[deploy-guard] could not fetch origin/main; checking against the local main");
    21	}
    22	const head = sh("git rev-parse HEAD");
    23	let onMain = false;
    24	try {
    25	  sh("git merge-base --is-ancestor HEAD origin/main");
    26	  onMain = true;
    27	} catch {
    28	  try {
    29	    sh("git merge-base --is-ancestor HEAD main");
    30	    onMain = true;
    31	  } catch {
    32	    onMain = false;
    33	  }
    34	}
    35	if (!onMain) {
… (12 more lines)
```

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b5bt3wpou.txt
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (30.6KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bhw6ox4ml.txt

Preview (first 2KB):
scripts:
total 24
drwxrwxr-x 2 mircea mircea 4096 Sep  1 22:20 .
drwxrwxr-x 6 mircea mircea 4096 Sep  2 01:29 ..
-rw-rw-r-- 1 mircea mircea 1721 Sep  1 22:20 _deps.ts
-rw-rw-r-- 1 mircea mircea  274 Sep  1 22:20 down.ts
-rw-rw-r-- 1 mircea mircea  328 Sep  1 22:20 rotate.ts
-rw-rw-r-- 1 mircea mircea  260 Sep  1 22:20 up.ts

src:
total 76
drwxrwxr-x 4 mircea mircea  4096 Sep  2 02:57 .
drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
-rw-rw-r-- 1 mircea mircea  3499 Sep  1 21:40 aws.ts
-rw-rw-r-- 1 mircea mircea  3117 Sep  1 21:40 config.ts
-rw-rw-r-- 1 mircea mircea  5963 Sep  2 02:57 cp-client.ts
drwxrwxr-x 2 mircea mircea  4096 Sep  2 01:09 lambda
-rw-rw-r-- 1 mircea mircea  4559 Sep  1 21:40 microvm-client.ts
-rw-rw-r-- 1 mircea mircea  1942 Sep  1 21:40 ops.ts
-rw-rw-r-- 1 mircea mircea  3010 Sep  2 01:09 pointer.ts
-rw-rw-r-- 1 mircea mircea 14437 Sep  4 07:51 rotate.ts
-rw-rw-r-- 1 mircea mircea  4351 Sep  1 21:40 session.ts
drwxrwxr-x 2 mircea mircea  4096 Sep  1 21:40 testing
-rw-rw-r-- 1 mircea mircea  4062 Sep  3 00:01 types.ts

src/lambda:
total 16
drwxrwxr-x 2 mircea mircea 4096 Sep  2 01:09 .
drwxrwxr-x 4 mircea mircea 4096 Sep  2 02:57 ..
-rw-rw-r-- 1 mircea mircea 2077 Sep  4 07:54 rotate.ts
-rw-rw-r-- 1 mircea mircea  652 Sep  1 21:40 session.ts

test:
total 68
drwxrwxr-x 2 mircea mircea  4096 Sep  2 02:57 .
drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
… (11 more lines)
```

**Tool `Bash`:**

```
git branch --show-current && git log --oneline -8 && git status --short | head -20 && echo "--- remotes ---" && git remote -v | sed -E 's#(github.com[:/])[^ ]+#\1<redacted>#'
```

**Tool `Bash`:**

```
grep -rn "imageVersion\|build.json\|TABFRAME_BUILD\|imageArn" packages/control-plane/src --include=*.ts | grep -v test | head -60
```

**Result of `Bash`:**

```
wp/8.2-review-loop-2
b12f8e6 Merge WP8.1: review loop 1 — 40 findings from five personas landed
20dd4c8 WP8.1: review loop 1 — 40 findings from five personas landed
8bfc911 Merge M7 close: plan, time log, rationale total, transcripts; the end-to-end test waits for the seeded program by name
183e8cb CI: the end-to-end test waits for programAdded by name (the program field is the bundle hash)
4f7ced7 M7 close: plan 'Where we are', deploy 2 recorded, time log (33.6 h build), rationale total, transcripts re-exported
295a466 Merge WP7.7: the walkthrough, the sentence of state, reasons not disappearances, a core fix for trapped tasks
9a04588 WP7.7: the walkthrough — the page's contract, the sentence of state, reasons not disappearances, ways back, an honest editor; a core fix for trapped tasks
58d39a6 Merge WP7.6: every program in the editor, with its source
--- remotes ---
origin	<email>:<redacted> (fetch)
origin	<email>:<redacted> (push)
```

**Result of `Bash`:**

```
packages/control-plane/src/server.ts:147:        (config.imageArn && config.coreRoleArn && sessionUrl && config.mode === "image"),
packages/control-plane/src/server.ts:165:        imageArn: config.imageArn as string,
packages/control-plane/src/server.ts:166:        imageVersion: config.imageVersion,
packages/control-plane/src/server.ts:645:        build: process.env.TABFRAME_BUILD ?? null,
packages/control-plane/src/server.ts:646:        imageVersion: config.imageVersion ?? null,
packages/control-plane/src/cores.ts:13:  imageArn: string;
packages/control-plane/src/cores.ts:14:  imageVersion: string | null;
packages/control-plane/src/cores.ts:48:        imageArn: config.imageArn,
packages/control-plane/src/cores.ts:49:        imageVersion: config.imageVersion,
packages/control-plane/src/config.ts:34:  imageArn: string | null;
packages/control-plane/src/config.ts:35:  imageVersion: string | null;
packages/control-plane/src/config.ts:64:    imageArn: env.TABFRAME_IMAGE_ARN ?? null,
packages/control-plane/src/config.ts:65:    imageVersion: env.TABFRAME_IMAGE_VERSION ?? null,
packages/control-plane/src/main.ts:27:  if (!process.env.TABFRAME_BUILD) {
packages/control-plane/src/main.ts:28:    const stamp = JSON.parse(readFileSync(new URL("./build.json", import.meta.url), "utf8")) as {
packages/control-plane/src/main.ts:31:    if (stamp.sha) process.env.TABFRAME_BUILD = stamp.sha;
```

**Tool `Bash`:**

```
ls packages/control-plane/src packages/core/src | head -80 && grep -rn "RunMicrovm\|run(" packages/control-plane/src/fleet*.ts packages/control-plane/src/*fleet* 2>/dev/null | head -30
```

**Result of `Bash`:**

```
packages/control-plane/src:
adopt.test.ts
bundles.test.ts
bundles.ts
config.test.ts
config.ts
core-node.test.ts
core-node.ts
core-reaper.test.ts
cores.test.ts
cores.ts
fixtures.ts
handover.integration.test.ts
hooks.test.ts
hooks.ts
log.ts
main.ts
mandelbrot.test.ts
rotation.test.ts
seed.test.ts
seed.ts
server.test.ts
server.ts
snapshotter.ts
static.ts

packages/core/src:
apply.test.ts
apply.ts
bytes.ts
events.ts
executions.ts
filesystem.test.ts
fleet.test.ts
fleet.ts
handover.test.ts
handover.ts
harness.ts
index.ts
interfaces.test.ts
… (15 more lines)
```

> **Thinking.** 

**Tool `Read`:**

```
/home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b0tcgipvk.txt
```

**Result of `Read`:**

```
1	===== scripts/deploy-guard.ts =====
2	     1	// The deploy guard (WP8.1): a deploy builds the image from the working tree, so the tree must be
3	     2	// committed and its HEAD must be on origin/main — the commit CI made green. Set
4	     3	// TABFRAME_DEPLOY_UNGATED=1 to skip it for an emergency deploy from a branch.
5	     4	import { execSync } from "node:child_process";
6	     5	
7	     6	const sh = (cmd: string): string => execSync(cmd, { encoding: "utf8" }).trim();
8	     7	
9	     8	if (process.env.TABFRAME_DEPLOY_UNGATED === "1") {
10	     9	  console.log("[deploy-guard] skipped (TABFRAME_DEPLOY_UNGATED=1)");
11	    10	  process.exit(0);
12	    11	}
13	    12	const dirty = sh("git status --porcelain");
14	    13	if (dirty) {
15	    14	  console.error("[deploy-guard] the working tree has uncommitted changes:\n" + dirty);
16	    15	  process.exit(1);
17	    16	}
18	    17	try {
19	    18	  sh("git fetch -q origin main");
20	    19	} catch {
21	    20	  console.warn("[deploy-guard] could not fetch origin/main; checking against the local main");
22	    21	}
23	    22	const head = sh("git rev-parse HEAD");
24	    23	let onMain = false;
25	    24	try {
26	    25	  sh("git merge-base --is-ancestor HEAD origin/main");
27	    26	  onMain = true;
28	    27	} catch {
29	    28	  try {
30	    29	    sh("git merge-base --is-ancestor HEAD main");
31	    30	    onMain = true;
32	    31	  } catch {
33	    32	    onMain = false;
34	    33	  }
35	    34	}
36	    35	if (!onMain) {
37	    36	  console.error(
38	    37	    `[deploy-guard] HEAD ${head.slice(0, 7)} is not on main; merge first (CI green), then deploy`,
39	    38	  );
40	    39	  process.exit(1);
… (598 more lines)
```

**Tool `Read`:**

```
/home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b5bt3wpou.txt
```

**Result of `Read`:**

```
1	scripts:
2	total 24
3	drwxrwxr-x 2 mircea mircea 4096 Sep  1 22:20 .
4	drwxrwxr-x 6 mircea mircea 4096 Sep  2 01:29 ..
5	-rw-rw-r-- 1 mircea mircea 1721 Sep  1 22:20 _deps.ts
6	-rw-rw-r-- 1 mircea mircea  274 Sep  1 22:20 down.ts
7	-rw-rw-r-- 1 mircea mircea  328 Sep  1 22:20 rotate.ts
8	-rw-rw-r-- 1 mircea mircea  260 Sep  1 22:20 up.ts
9	
10	src:
11	total 76
12	drwxrwxr-x 4 mircea mircea  4096 Sep  2 02:57 .
13	drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
14	-rw-rw-r-- 1 mircea mircea  3499 Sep  1 21:40 aws.ts
15	-rw-rw-r-- 1 mircea mircea  3117 Sep  1 21:40 config.ts
16	-rw-rw-r-- 1 mircea mircea  5963 Sep  2 02:57 cp-client.ts
17	drwxrwxr-x 2 mircea mircea  4096 Sep  2 01:09 lambda
18	-rw-rw-r-- 1 mircea mircea  4559 Sep  1 21:40 microvm-client.ts
19	-rw-rw-r-- 1 mircea mircea  1942 Sep  1 21:40 ops.ts
20	-rw-rw-r-- 1 mircea mircea  3010 Sep  2 01:09 pointer.ts
21	-rw-rw-r-- 1 mircea mircea 14437 Sep  4 07:51 rotate.ts
22	-rw-rw-r-- 1 mircea mircea  4351 Sep  1 21:40 session.ts
23	drwxrwxr-x 2 mircea mircea  4096 Sep  1 21:40 testing
24	-rw-rw-r-- 1 mircea mircea  4062 Sep  3 00:01 types.ts
25	
26	src/lambda:
27	total 16
28	drwxrwxr-x 2 mircea mircea 4096 Sep  2 01:09 .
29	drwxrwxr-x 4 mircea mircea 4096 Sep  2 02:57 ..
30	-rw-rw-r-- 1 mircea mircea 2077 Sep  4 07:54 rotate.ts
31	-rw-rw-r-- 1 mircea mircea  652 Sep  1 21:40 session.ts
32	
33	test:
34	total 68
35	drwxrwxr-x 2 mircea mircea  4096 Sep  2 02:57 .
36	drwxrwxr-x 6 mircea mircea  4096 Sep  2 01:29 ..
37	-rw-rw-r-- 1 mircea mircea  9421 Sep  1 21:40 adapters.test.ts
38	-rw-rw-r-- 1 mircea mircea  2423 Sep  1 21:40 config.test.ts
39	-rw-rw-r-- 1 mircea mircea  4231 Sep  2 02:57 cp-client.test.ts
40	-rw-rw-r-- 1 mircea mircea  2749 Sep  1 21:40 ops.test.ts
… (743 more lines)
```

**Tool `Bash`:**

```
cat -n packages/control-plane/src/cores.ts && echo "===== config.ts =====" && cat -n packages/control-plane/src/config.ts && echo "===== main.ts =====" && cat -n packages/control-plane/src/main.ts
```

**Result of `Bash`:**

```
     1	// Launching and retiring cloud cores (design §6.8). The control plane holds the only role allowed
     2	// to pass the core role, so this lives in the process rather than the fleet functions; the core
     3	// decides how many there should be, this decides what to say to AWS.
     4	import { egressConnectorArn, ingressConnectorArn } from "@tabframe/fleet/config";
     5	import { SdkMicrovmClient } from "@tabframe/fleet/microvm-client";
     6	import type { MicrovmClient, MicrovmInfo } from "@tabframe/fleet/types";
     7	import { SERVING_STATES } from "@tabframe/fleet/types";
     8	
     9	/** A core's ceiling: shorter than the control plane's, as a cost fuse (design §9.2). */
    10	export const CORE_MAX_DURATION_SECONDS = 4 * 60 * 60;
    11	
    12	export interface CoreFleetConfig {
    13	  imageArn: string;
    14	  imageVersion: string | null;
    15	  coreRoleArn: string;
    16	  region: string;
    17	  sessionUrl: string;
    18	  storeBase: string;
    19	  generation: number;
    20	  fleetSecret: string | null;
    21	}
    22	
    23	export interface CoreFleet {
    24	  launch(): Promise<string>;
    25	  terminate(microvmId: string): Promise<void>;
    26	  /** Which of these MicroVMs are no longer serving, so the ledger can forget them. */
    27	  gone(microvmIds: string[]): Promise<string[]>;
    28	}
    29	
    30	/** The payload that turns a neutral image into a core (design §9.3). */
    31	export function corePayload(config: CoreFleetConfig): string {
    32	  return JSON.stringify({
    33	    role: "core",
    34	    generation: config.generation,
    35	    snapshotKey: null,
    36	    sessionUrl: config.sessionUrl,
    37	    storeBase: config.storeBase,
    38	    fleetSecret: config.fleetSecret,
    39	  });
    40	}
… (151 more lines)
```

**Tool `Bash`:**

```
sed -n '130,180p' packages/control-plane/src/server.ts && echo "===== health ~620-680 =====" && sed -n '620,690p' packages/control-plane/src/server.ts
```

**Result of `Bash`:**

```
  const store: StoreDriver =
    deps.store ??
    (config.mode === "image" && config.blobBucket
      ? new S3Store({ bucket: config.blobBucket, base: storeBase })
      : local);
  const snapshots: SnapshotStore =
    deps.snapshots ??
    (config.mode === "image" && config.snapshotBucket
      ? new S3Snapshots({ bucket: config.snapshotBucket })
      : new MemorySnapshots());
  const snapshotter = new Snapshotter(snapshots);

  /** Fresh or adopted, the ledger is ours from here: announce the role and seed the programs. */
  /** Cloud cores need the image, the core role, and a session URL to point the cores at. */
  function canRunCores(): boolean {
    return Boolean(
      deps.cores ??
        (config.imageArn && config.coreRoleArn && sessionUrl && config.mode === "image"),
    );
  }

  function becomeControlPlane(gen: number, base: string, adopted: Ledger | null): void {
    generation = gen;
    if (adopted) {
      execute(adoptLedger(adopted, gen, clock.now()));
      adopted.meta.storeBase = base;
      ledger = adopted;
    } else {
      // The ledger's clocks start now: with the default of zero a fresh control plane believes
      // nobody has watched it for fifty years and is born asleep (found by the reaper test).
      ledger = createLedger(gen, { storeBase: base, cloudCores: canRunCores() }, clock.now());
    }
    ledger.config.cloudCores = canRunCores();
    if (canRunCores() && !cores) {
      cores = createCoreFleet({
        imageArn: config.imageArn as string,
        imageVersion: config.imageVersion,
        coreRoleArn: config.coreRoleArn as string,
        region: config.region,
        sessionUrl: sessionUrl as string,
… (83 more lines)
```

**Tool `Bash`:**

```
grep -n "describe\|test(\|it(" packages/fleet/test/rotate.test.ts | head -60
```

**Result of `Bash`:**

```
1:import { beforeEach, describe, expect, test } from "bun:test";
31:describe("rotate handler", () => {
60:  test("off: does nothing", async () => {
67:  test("a running control plane is rotated, not left alone", async () => {
80:  test("launches a control plane with the design's payload, connectors, policy, and role", async () => {
122:  test("first launch on an empty pointer is generation 1", async () => {
128:  test("polls until RUNNING, sleeping the poll interval each time", async () => {
135:  test("backs off on throttling and then succeeds", async () => {
144:  test("gives up after the backoff schedule is exhausted", async () => {
153:  test("fails when the new control plane never reaches RUNNING before the deadline, and terminates it (WP8.1)", async () => {
165:  test("a suspended control plane is left alone by the scheduled rule and rotated by an operator (WP8.1)", async () => {
184:  test("fails when the new control plane terminates during boot", async () => {
227:describe("rotation", () => {
259:  test("the five steps happen in order, and the successor is told where the snapshot is", async () => {
292:  test("a handover that fails still rotates: the successor booted from the snapshot", async () => {
303:  test("an adopt that fails is the same story, and a drain that fails still terminates", async () => {
314:  test("a successor that never boots leaves the pointer alone", async () => {
326:  test("a rotation that died after launching is finished by the next run", async () => {
348:  test("a pending successor that died is forgotten, and the run carries on", async () => {
362:  test("a pending successor the pointer already passed is terminated", async () => {
376:  test("nothing serving: a heal launches one and never calls the old one", async () => {
385:  test("off is still off, whatever is pending", async () => {
398:  test("a pending successor is terminated while the current control plane still serves; the rotation starts afresh", async () => {
417:  test("a pending successor is still promoted when nothing else serves", async () => {
430:  test("a scheduled rotation minutes after the last pointer change is skipped; an operator's is not", async () => {
```

> **Thinking.** 

**Tool `Bash`:**

```
ls -la packages/infra/image packages/infra/image-dist && echo "===== placeholder main.js (head) =====" && head -40 packages/infra/image/main.js && echo "===== image-dist Dockerfile =====" && cat packages/infra/image-dist/Dockerfile && echo "===== build.json present? =====" && ls -la packages/infra/image-dist/build.json 2>&1; grep -rn "build.json" packages/infra --include=*.ts --include=Dockerfile | grep -v node_modules
```

**Result of `Bash`:**

```
packages/infra/image:
total 28
drwxrwxr-x 3 mircea mircea 4096 Sep  2 02:57 .
drwxrwxr-x 9 mircea mircea 4096 Sep  3 21:46 ..
-rw-rw-r-- 1 mircea mircea  534 Sep  2 02:57 Dockerfile
-rw-rw-r-- 1 mircea mircea 2186 Sep  1 21:41 main.js
-rw-rw-r-- 1 mircea mircea   89 Sep  2 02:57 node-worker.js
-rw-rw-r-- 1 mircea mircea   25 Sep  1 22:20 package.json
drwxrwxr-x 2 mircea mircea 4096 Sep  1 21:41 programs

packages/infra/image-dist:
total 2800
drwxrwxr-x 3 mircea mircea    4096 Sep  3 21:46 .
drwxrwxr-x 9 mircea mircea    4096 Sep  3 21:46 ..
-rw-rw-r-- 1 mircea mircea     534 Sep  3 21:46 Dockerfile
-rw-r--r-- 1 mircea mircea 2163635 Sep  3 21:46 main.js
-rw-r--r-- 1 mircea mircea  679164 Sep  3 21:46 node-worker.js
-rw-rw-r-- 1 mircea mircea      21 Sep  3 21:46 package.json
drwxrwxr-x 5 mircea mircea    4096 Sep  3 21:46 programs
===== placeholder main.js (head) =====
// Placeholder control plane for the image skeleton (WP0.8): answers the lifecycle hooks on the
// private port so the image build completes, and a health route on both ports. Replaced by the
// real bundle from packages/control-plane in WP0.4/WP0.10.
const http = require("node:http");

const PUBLIC_PORT = Number(process.env.TABFRAME_PUBLIC_PORT || 8080);
const PRIVATE_PORT = Number(process.env.TABFRAME_PRIVATE_PORT || 8081);
const HOOK_BASE = "/aws/lambda-microvms/runtime/v1/";

let role = "neutral";
let generation = 0;

function log(message, fields) {
  console.log(JSON.stringify({ level: "info", message, role, ...fields }));
}

function readBody(req) {
  return new Promise((resolve) => {
    let data = "";
    req.on("data", (c) => {
… (36 more lines)
```

**Tool `Bash`:**

```
cat -n packages/control-plane/src/hooks.ts | head -150
```

**Result of `Bash`:**

```
     1	import type { IncomingMessage, ServerResponse } from "node:http";
     2	import type { Role } from "./config.ts";
     3	import { readBody, sendJson } from "./static.ts";
     4	
     5	export const HOOK_PREFIX = "/aws/lambda-microvms/runtime/v1/";
     6	
     7	/** What the fleet function puts in the run-hook payload (design §9.3). */
     8	export interface RunPayload {
     9	  role: Exclude<Role, "neutral">;
    10	  generation: number;
    11	  snapshotKey: string | null;
    12	  sessionUrl: string | null;
    13	  storeBase: string | null;
    14	  fleetSecret: string | null;
    15	}
    16	
    17	export interface HookHost {
    18	  /** Called on /run with the parsed payload; returns false if the payload is unusable. */
    19	  onRun(payload: RunPayload, microvmId: string | null): Promise<boolean>;
    20	  /** Called on /validate; runs the in-process self-test and returns true when it passes. */
    21	  onValidate(): boolean;
    22	  onSuspend(): Promise<void>;
    23	  onResume(): Promise<void>;
    24	  onTerminate(): Promise<void>;
    25	  isListening(): boolean;
    26	}
    27	
    28	/**
    29	 * The MicroVM lifecycle hooks (design §9.3). They live on the private port; the proxy never
    30	 * routes browser tokens here. Locally they are ordinary routes a dev script can POST to.
    31	 */
    32	export async function handleHook(
    33	  host: HookHost,
    34	  name: string,
    35	  req: IncomingMessage,
    36	  res: ServerResponse,
    37	): Promise<void> {
    38	  if (req.method !== "POST") return sendJson(res, 405, { error: "POST only" });
    39	  switch (name) {
    40	    case "ready":
… (65 more lines)
```

**Tool `Bash`:**

```
grep -n "HANDOVER_LEASE_MS\|handing-over\|drained\|leaseUntil\|handoverAt" packages/core/src/handover.ts packages/core/src/apply.ts packages/core/src/ledger.ts packages/control-plane/src/server.ts | head -40 && echo "===== snapshotter latest =====" && grep -n "latest\|phase\|key(" packages/control-plane/src/snapshotter.ts | head -30
```

**Result of `Bash`:**

```
packages/core/src/handover.ts:25:  if (ledger.meta.phase !== "handing-over") ledger.meta.handoverAt = now;
packages/core/src/handover.ts:26:  ledger.meta.phase = "handing-over";
packages/core/src/handover.ts:57:  ledger.meta.phase = "drained";
packages/core/src/apply.ts:147:export const HANDOVER_LEASE_MS = 90_000;
packages/core/src/apply.ts:152:  // A control plane that handed its ledger over acts on nothing until it is drained (WP8.1): no
packages/core/src/apply.ts:155:  if (ledger.meta.phase === "handing-over") {
packages/core/src/apply.ts:156:    if (ledger.meta.handoverAt !== null && now - ledger.meta.handoverAt > HANDOVER_LEASE_MS) {
packages/core/src/apply.ts:158:      ledger.meta.handoverAt = null;
packages/core/src/apply.ts:161:  if (ledger.meta.phase === "drained") return effects;
packages/core/src/ledger.ts:195: * `handing-over` one has given its ledger away and is waiting to be drained.
packages/core/src/ledger.ts:197:export type Phase = "active" | "handing-over" | "drained";
packages/core/src/ledger.ts:232:   * When the handover began (WP8.1): a control plane left `handing-over` without a drain for
packages/core/src/ledger.ts:236:  handoverAt: number | null;
packages/core/src/ledger.ts:329:      handoverAt: null,
packages/control-plane/src/server.ts:76:  readonly phase: "neutral" | "active" | "handing-over" | "drained";
packages/control-plane/src/server.ts:632:      return sendJson(res, 200, { drained: clients, next });
===== snapshotter latest =====
2:// changed, and on the suspend and terminate hooks; keyed by generation and time, with a `latest`
8:export const LATEST_KEY = "latest.json.gz";
```

**Tool `Bash`:**

```
mise --version 2>/dev/null; grep -rn "cdk diff\|cdk synth\|rollback\|canary\|Canary" mise.toml .github/workflows/ci.yml docs/plan.md docs/runbook.md README.md 2>/dev/null | head -20; echo "===== plan WP8.2 mention ====="; grep -n "8\.2\|loop 2\|loop two" docs/plan.md | head -10
```

**Tool `Bash`:**

```
timeout 280 bun test packages/fleet/test/rotate.test.ts packages/infra/test 2>&1 | tail -25
```

**Result of `Bash`:**

```
2026.4.11 linux-x64 (2026-04-13)
docs/runbook.md:50:adopts the current ledger. Clear the pin once the fix is deployed. A rollback of the page is a
docs/plan.md:72:  *Acceptance:* `cdk synth` test passes; `cdk deploy --all` succeeds; the image version reaches ACTIVE (build logs tailed).
docs/plan.md:161:- [x] **WP4.2 Operations.** `/health` and `/diag` complete; log groups named; runbook `docs/runbook.md` (deploy, rotate, rollback, down, verify); budget confirmed firing on a test threshold.
===== plan WP8.2 mention =====
241:- [ ] **WP8.2 Review loop 2.** The same, on the tree after loop 1.
242:- [ ] **WP8.3 Review loop 3.** The same, on the tree after loop 2; then deploy and `mise run demo -- --repeat 3`.
```

**Result of `Bash`:**

```
  This API will be removed in the next major release.
[WARNING] aws-cdk-lib.aws_lambda.FunctionOptions#logRetention is deprecated.
  use `logGroup` instead
  This API will be removed in the next major release.
------------------------------------|---------|---------|-------------------
File                                | % Funcs | % Lines | Uncovered Line #s
------------------------------------|---------|---------|-------------------
All files                           |   76.38 |   81.39 |
 packages/fleet/src/config.ts       |   33.33 |    1.11 | 4-86,90-95
 packages/fleet/src/pointer.ts      |   42.86 |   41.51 | 36,41-62,66-72
 packages/fleet/src/rotate.ts       |  100.00 |   98.29 | 169,327-329
 packages/fleet/src/testing/fake.ts |   64.00 |   75.36 | 61-66,72-76,160-165,171-187
 packages/fleet/src/types.ts        |   12.50 |   80.77 | 108-111
 packages/infra/lib/base-image.ts   |  100.00 |  100.00 | 
 packages/infra/lib/core-stack.ts   |  100.00 |  100.00 | 
 packages/infra/lib/fleet-stack.ts  |  100.00 |  100.00 | 
 packages/infra/lib/image-stack.ts  |  100.00 |  100.00 | 
 packages/infra/lib/names.ts        |   87.50 |   98.21 | 
 packages/infra/lib/web-stack.ts    |  100.00 |  100.00 | 
------------------------------------|---------|---------|-------------------

 40 pass
 0 fail
 128 expect() calls
Ran 40 tests across 3 files. [5.68s]
```

> **Thinking.** 

**Tool `Bash`:**

```
sed -n '140,170p' packages/core/src/apply.ts && echo "===== snapshotter.ts =====" && cat -n packages/control-plane/src/snapshotter.ts | head -90
```

**Result of `Bash`:**

```
          now,
        );
      return [];
  }
}

/** A handover with no drain after this long is a rotation that died: the control plane carries on. */
export const HANDOVER_LEASE_MS = 90_000;

function tick(ledger: Ledger, now: number): Effect[] {
  const effects = sweep(ledger, now);
  pruneExecutions(ledger);
  // A control plane that handed its ledger over acts on nothing until it is drained (WP8.1): no
  // fleet, no loop, no assignment — the successor owns all of that now. Unless the rotation died
  // between the handover and the promote, in which case the lease brings this one back.
  if (ledger.meta.phase === "handing-over") {
    if (ledger.meta.handoverAt !== null && now - ledger.meta.handoverAt > HANDOVER_LEASE_MS) {
      ledger.meta.phase = "active";
      ledger.meta.handoverAt = null;
    } else return effects;
  }
  if (ledger.meta.phase === "drained") return effects;
  effects.push(...fleetTick(ledger, now));
  effects.push(...relabelHealth(ledger));
  effects.push(...ensureDefaultLoop(ledger, now));
  effects.push(...maybeStart(ledger, now)); // a queued continuation whose hold just expired
  effects.push(...fill(ledger, now));
  effects.push(...resumePending(ledger, now));
  return effects;
}

===== snapshotter.ts =====
     1	// The S3 snapshot writer (design §9.4): the ledger as gzipped JSON every few seconds when it
     2	// changed, and on the suspend and terminate hooks; keyed by generation and time, with a `latest`
     3	// pointer the fleet reads when the previous control plane cannot hand over itself.
     4	import { gunzipSync, gzipSync } from "node:zlib";
     5	import { deserializeLedger, type Ledger, serializeLedger } from "@tabframe/core";
     6	import type { SnapshotStore } from "@tabframe/store";
     7	
     8	export const LATEST_KEY = "latest.json.gz";
… (72 more lines)
```

**Tool `Bash`:**

```
grep -n "onRun\|microvmId\|selfId\|ownId" packages/control-plane/src/server.ts | head -30 && echo "===== onRun body =====" && awk '/async onRun\(/,/^    \},$/' packages/control-plane/src/server.ts | head -80
```

**Tool `Bash`:**

```
sed -n '150,185p' packages/fleet/test/rotate.test.ts && echo "===== fake run/clientToken =====" && grep -n "clientToken\|TERMINATED\|run(" packages/fleet/src/testing/fake.ts | head -20
```

**Result of `Bash`:**

```
267:        for (const microvmId of dead) {
268:          log("core-gone", { microvmId });
269:          dispatch({ kind: "coreGone", microvmId });
337:            .then((microvmId) => {
338:              log("core-launched", { microvmId });
339:              dispatch({ kind: "coreLaunched", microvmId });
345:          const { microvmId } = e;
348:            .terminate(microvmId)
349:            .then(() => log("core-terminated", { microvmId }))
350:            .catch((err) => log("core-terminate-failed", { microvmId, error: String(err) }));
483:    async onRun(payload: RunPayload, microvmId: string | null): Promise<boolean> {
491:        microvmId,
522:      startCore({ sessionUrl: payload.sessionUrl, microvmId, log });
523:      log("role", { role, generation, hostId: `core-${microvmId ?? "unknown"}` });
655:          microvmId: c.microvmId,
===== onRun body =====
    async onRun(payload: RunPayload, microvmId: string | null): Promise<boolean> {
      if (role !== "neutral") {
        log("run-refused", { reason: "role already assumed", role });
        return false;
      }
      fleetSecret = payload.fleetSecret;
      sessionUrl = payload.sessionUrl ?? sessionUrl;
      log("run", {
        microvmId,
        role: payload.role,
        generation: payload.generation,
        snapshotKey: payload.snapshotKey,
        hasSecret: fleetSecret !== null,
      });
      if (payload.role === "control-plane") {
        // Adopt from a snapshot when the fleet names one (design §9.4); a missing or unreadable
        // snapshot means a fresh ledger, which idempotency makes safe.
        let adopted: Ledger | null = null;
        if (payload.snapshotKey) {
          try {
            adopted = await snapshotter.read(payload.snapshotKey);
            log(adopted ? "snapshot-adopted" : "snapshot-missing", { key: payload.snapshotKey });
          } catch (err) {
            log("snapshot-unreadable", { key: payload.snapshotKey, error: String(err) });
… (19 more lines)
```

**Result of `Bash`:**

```
    expect(pointer.writes).toHaveLength(0);
  });

  test("fails when the new control plane never reaches RUNNING before the deadline, and terminates it (WP8.1)", async () => {
    microvms.pendingPolls = 1000;
    const pointer = pointerStoreWith({ state: "on" });
    const result = await handler(pointer, { readyTimeoutMs: 5000 })();
    expect(result.action).toBe("failed");
    expect(pointer.writes).toHaveLength(0);
    // The successor that never came up is not left running for hours, blocking every later try.
    expect(microvms.terminated).toEqual([microvms.runs[0]?.microvmId ?? "?"]);
    // And the client token carries the hour, so the next try is not resolved to the same VM.
    expect(microvms.runs[0]?.params.clientToken).toMatch(/^tabframe-cp-g\d+-\d+$/);
  });

  test("a suspended control plane is left alone by the scheduled rule and rotated by an operator (WP8.1)", async () => {
    microvms.add({ microvmId: "mvm-asleep", state: "SUSPENDED" });
    const pointer = pointerStoreWith({
      state: "on",
      microvmId: "mvm-asleep",
      generation: 7,
      updatedAt: new Date(clock.now() - 2 * 60 * 60_000).toISOString(),
    });
    const scheduled = await handler(pointer)({
      source: "aws.events",
      "detail-type": "Scheduled Event",
    });
    expect(scheduled).toEqual({ action: "skipped-suspended" });
    expect(microvms.runs).toHaveLength(0);
    const manual = await handler(pointer)();
    expect(manual.action).toBe("rotated");
    expect(microvms.runs).toHaveLength(1);
  });

  test("fails when the new control plane terminates during boot", async () => {
    microvms.terminateAfterRun = true;
===== fake run/clientToken =====
107:  /** When true, a freshly run VM is TERMINATED on its first `get` (a failed boot). */
131:  async run(params: RunMicrovmParams): Promise<MicrovmInfo> {
156:      if (this.terminateAfterRun) vm.state = "TERMINATED";
… (1 more lines)
```

**Tool `Bash`:**

```
grep -n "2026-09-04\|WP8.1\|deploy 2\|deployed" docs/design.md | tail -15 && echo "===== plan where we are =====" && grep -n -A6 "Where we are" docs/plan.md | head -30
```

**Result of `Bash`:**

```
3:**Status:** written 2026-09-01 before the build; the sections that name a work package were amended in place (§6.7, §8.3 among them) and everything else is read together with the drift log, §17 (last entry 2026-09-04), whose entries are in merge order.
484:Measured on 2026-09-02 against the deployed image (`docs/m0-verification.md`): WebSocket frames count as idle-policy traffic; an open socket survives its token's expiry; the endpoint throttles at about 50 requests per second, so reconnect jitter is 30 ms per client (§8.4); 250 concurrent sockets are sustained when opened at a paced rate; 15 messages per second per connection passes; DNS and S3 resolve inside the image; boot to RUNNING takes about 2 s and resume under 1 s at 1 GB; token minting is not throttled at 20 in a burst; the memory quota is 8 GB and RunMicrovm is limited to 1 per second. No fallback was needed.
792:  free slot; a tie after two contested rounds is not a majority and starts another round, up to Freeze is terminal on the node **record** too: a later `throttleHalf` does not downgrade a frozen node to throttled, since `fill` must keep skipping a worker that computes nothing until the silence window declares it gone (§4, §6.7). - **2026-09-02 (WP3.3).** A cloud core names itself `core-<microvmId>`, which is how the ledger links a node to the core it launched; no registration message (§6.8). Core records travel in the ledger and are inherited at adopt with their node links cleared. Cores launch with **no ingress connector** and no idle policy. The fleet policy is gated by `config.cloudCores`, true only for the MicroVM image with an image ARN, a core role, and a session URL — a laptop wakes and sleeps but has no fleet (§6.8, §12). - **2026-09-02 (WP3.4).** Local mode can boot neutral (`TABFRAME_LOCAL_NEUTRAL`) so `dev:rotate` drives the real run hook and the real rotate handler with control-plane processes standing in for MicroVMs (§12). A run payload with an empty `storeBase` leaves a control plane serving blobs from its own port; locally each generation has its own in-memory store, so a rotation loses earlier blobs and the local test rotates within one stage. On AWS the store is shared and this does not arise (§7.1). - **2026-09-02 (WP3.5).** A bundled image cannot spawn itself as a worker thread: the sandbox worker is bundled as a second entry point, staged beside `main.js`, and named to the process by `TABFRAME_SANDBOX_WORKER` (§4.2, §11.3). The fleet's private-port calls retry on 429 and 5xx (§9.4). **Measured:** one client holds about 16 concurrent sockets through a MicroVM endpoint before it answers 429, and open sockets crowd out the fleet's own requests to the same endpoint — so the ledger's 256-node cap is not the binding constraint (§8.4, §9.1; `docs/m3-verification.md`). A rotation with a render in flight costs about 8.4 s of churn. - **2026-09-02 (WP2.5).** The `taskDone` observer event and the task view carry an optional `log` (inline text or a blob hash), the shape the node already reports to the control plane; the dashboard renders it when present and says plainly when the wire did not carry one (§8.3). The `bars` view draws the single output of the last stage; a last stage with several outputs is listed in the files panel instead (§5.1). The files panel browses any root the execution has had, not only the current one; roots are the stage strip's links (§5.4). The browser suites run on one Playwright worker: they share a local control plane and, since WP2.3, an upload really launches (§12). - **2026-09-02 (WP2.7).** Seeding is idempotent by bundle hash rather than "only when the ledger has no programs": a control plane that adopts a predecessor's ledger still adds programs the image ships that the ledger does not have, which is what lets a deploy-as-rotation deliver a new program (§5.6, §9.4). - **2026-09-02 (WP4.5).** The endpoint ceiling is explained: **16 concurrent connections per MicroVM**, the account's non-adjustable *Concurrent connections per 2 vCPU MicroVM* quota, the same at 512 MiB and 6 GB, per MicroVM rather than per token, process, or source (§9.7). The M0 record's "250 sustained sockets" was a counting error — that script attached its close handlers after the loop and never heartbeated during it, so the sockets it counted as open had already been declared gone; about sixteen were alive. `docs/m0-verification.md` carries the correction. The account's Lambda concurrency increase to 1000 was granted; the design's "default of 10" wording is historical. Scaling to thousands of clients is an architecture decision, not a tuning one; the options are in §9.7 and the plan's WP4.6. - **2026-09-02 (WP4.4).** The demo script runs unattended against the deployed machine (`mise run demo`), and its first runs fixed the fleet policy: a killed or frozen cloud core has its MicroVM terminated with the command, and a core with no node for two minutes — never said hello, or its node left — is retired and replaced (§6.8). Before, a `kill half` that picked a core left a live, unlinked MicroVM that the policy (which counts records) never replaced. Seeding also retires unshipped drops nobody has run in the ledger's memory after an hour, so runbook uploads do not clutter the program list; the snapshot diet clears file maps on their own, not only with the tasks.
881:  their tasks for only the last two (`KEEP_ENDED_TASKS`), which takes the deployed five-second
892:- **2026-09-03 (WP6.8).** Mircea's review of the deployed page. The loop yields to people (§6.7):
927:- **2026-09-04 (WP7.5).** The editor page (§5.6) fills the viewport: the source takes the height,
933:- **2026-09-04 (WP7.6).** A program's source is a blob in the store and its manifest names the
940:- **2026-09-04 (WP7.7).** The walkthrough (§8.3): `docs/walkthrough.md` inventories every screen,
950:- **2026-09-04 (WP8.1).** The first review loop (five personas; `docs/implementation/wp-8.1-review-loop-1.md`
===== plan where we are =====
6:**Where we are (2026-09-04, M7 closed):** M0–M7 done. M6 landed Mircea's six asks of 2026-09-02 and WP6.8 his first review of the deployed page; M7 answered his third review of 2026-09-03 in seven work packages — the header's one slot with a loop pill, a crisp throughput line, a launch form that keeps the caret, files and ledger rows previewed in the page, an editor that fills the viewport with the guide on its own page, every program in the editor with its source kept in the store, and the walkthrough: `docs/walkthrough.md` as the page's contract (every screen × state × control, eight rules), the sentence of state in the status line, reasons in tooltips instead of hidden controls, and `e2e/walkthrough.e2e.ts` asserting each state. Two deploys (generations 99 and 105) each passed three unattended demo passes; the walkthrough found and fixed a core bug (a trapped task's snapshot could not be decoded by a fresh dashboard). Left for Mircea: the README and rationale read-through, developer hours, the narrated video, the Cost Explorer check, the flip. Post-flip candidates: an EC2 host for the control plane (WP4.6), SIMD for the transformer, sampling with a seed.
7-
8-**Shape of the plan:** eight milestones, M0–M7, each ending in a deployable checkpoint, then M8's review loops; M5 is the packaging gate and stays open until the flip. Each milestone
9-is a set of work packages (WP). A WP is done when its code, its tests, its WP document under `docs/implementation/`, and its doc touch
10-are merged together and its acceptance line holds. Checkboxes are updated in the same commit that
11-completes the WP.
12-
```

**Tool `Bash`:**

```
S=/tmp/claude-1000/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/scratchpad/misetest && mkdir -p "$S" && cd "$S" && printf 'PROBE_FROM_FILE=yes\n' > .probe.env && cat > mise.toml <<'EOF'
[tasks.scoped]
env = { _.file = ".probe.env", SCOPED_STATIC = "1" }
run = "echo scoped:PROBE_FROM_FILE=${PROBE_FROM_FILE:-unset} SCOPED_STATIC=${SCOPED_STATIC:-unset}"

[tasks.plain]
run = "echo plain:PROBE_FROM_FILE=${PROBE_FROM_FILE:-unset} SCOPED_STATIC=${SCOPED_STATIC:-unset}"
EOF
MISE_TRUSTED_CONFIG_PATHS="$S" MISE_YES=1 mise run scoped 2>&1 | tail -3; MISE_TRUSTED_CONFIG_PATHS="$S" MISE_YES=1 mise run plain 2>&1 | tail -3
```

**Result of `Bash`:**

```
[scoped] $ echo scoped:PROBE_FROM_FILE=${PROBE_FROM_FILE:-unset} SCOPED_STATIC=…
scoped:PROBE_FROM_FILE=yes SCOPED_STATIC=1
[plain] $ echo plain:PROBE_FROM_FILE=${PROBE_FROM_FILE:-unset} SCOPED_STATIC=${…
plain:PROBE_FROM_FILE=unset SCOPED_STATIC=unset
```

> **Thinking.** 

**Tool `Bash`:**

```
sed -n '950,962p' docs/design.md | cut -c1-900; echo "===== fake run =====" && sed -n '128,160p' packages/fleet/src/testing/fake.ts; echo "===== deploy-guard tests? =====" && grep -rln "deploy-guard" --include=*.ts --include=*.md . 2>/dev/null | grep -v node_modules
```

**Result of `Bash`:**

```
- **2026-09-04 (WP8.1).** The first review loop (five personas; `docs/implementation/wp-8.1-review-loop-1.md`
  has every finding and its verdict). Core: a running execution's pending store effect is
  re-derived after an adopt and after ten silent seconds (§6.6, §9.4); params (4 KB) and the queue
  (32) are bounded (§6.7); a control plane that handed over acts on nothing until drained, and
  comes back after a ninety-second lease when no drain follows (§9.4); launches in flight count
  towards the fleet's size and surplus cores are trimmed (§6.8); a report for an attempt a node
  never held cannot fail a task, compute time is bounded, sixteen records per task (§6.5);
  destructive controls have a two-second machine-wide cooldown (§6.7); results and presigns have
  their own rate bucket and a presigned-byte budget (§8.4). Wire: presign sizes and counts are
  bounded. Sandbox: one memory per module. Store and node: size-capped fetches on the control plane,
  retries and a presign timeout on the node, host and store failures reported as releases (§4.2).
  Fleet and hosting: the scheduled rule leaves a suspended control plane alone (§6.8 — the README's
  stays-up paragraph was wrong); a successor that never boots is terminated and the client token
===== fake run =====
    if (vm) vm.state = state;
  }

  async run(params: RunMicrovmParams): Promise<MicrovmInfo> {
    if (this.throttleRuns > 0) {
      this.throttleRuns--;
      const error = new Error("Rate exceeded");
      error.name = "ThrottlingException";
      throw error;
    }
    this.counter++;
    const microvmId = `mvm-${this.counter}`;
    const info = this.add({
      microvmId,
      state: "PENDING",
      imageArn: params.imageArn,
      imageVersion: params.imageVersion ?? "1",
    });
    this.pendingLeft.set(microvmId, this.pendingPolls);
    this.runs.push({ params, microvmId });
    return { ...info };
  }

  async get(microvmId: string): Promise<MicrovmInfo | null> {
    const vm = this.vms.get(microvmId);
    if (!vm) return null;
… (10 more lines)
```

> **Thinking.** 

## Assistant · 2026-09-04T15:43:53.997Z

## Findings

1. `[H] deploy path — packages/infra/bin/tabframe.ts:31, docs/runbook.md:37-39, mise.toml:98 — A bare `cdk deploy` (no `TABFRAME_IMAGE_DIR`) publishes `packages/infra/image/`, the WP0.8 placeholder (`image/main.js:1-3`), as the newest image version; rotate launches with `imageVersion: null` = latest (`fleet/src/config.ts:80`). The runbook's "did not stabilize" advice is exactly that bare re-run — The next hourly rotation or heal boots a control plane that answers hooks but has no ledger or sockets; `/adopt` on it fails, the real CP is still promoted-over, drained and terminated (`rotate.ts:349-356`): the machine goes dark until another full deploy. The same bare deploy also re-disables the hourly rule whenever the rule resource changes (loop 1 changed its target's retry policy, `fleet-stack.ts:160-166`) with no `up` to re-enable it — Default `stagingDir` to `image-dist`; refuse synth when the staged `main.js` is the placeholder or `build.json` is missing unless `TABFRAME_IMAGE_PLACEHOLDER=1`; add a `deploy:stacks` task (`TABFRAME_IMAGE_DIR=… cdk deploy --all` then `up`) and point the runbook at it.`

2. `[H] rotation — packages/fleet/src/rotate.ts:355-356, packages/core/src/apply.ts:155-159 (loop 1 regression) — `promote()` flips the pointer before `retire()`; if the Lambda dies or times out in between (worst case ≈ 398 s against the 300 s timeout at `fleet-stack.ts:105`: 15 s RunMicrovm backoff + 180 s ready wait + three private-port calls of up to 66 s + 5 s grace), the old CP is never drained, the 90 s lease returns it to `active` with its clients attached, and nothing references it again (rotate reads only `microvmId`/`pending`; only `down` sweeps) — Two active generations: the old one keeps overwriting `latest.json.gz` (`snapshotter.ts:47`), so a later heal can adopt the wrong generation; it sees its cores now linked to the successor as unlinked and terminates them every two minutes, launching its own — churn and double cost until its 8 h cap — Make retire crash-safe like launch: `promote()` writes `retiring: {microvmId, endpoint}`, `retire()` clears it, `repair()` finishes a leftover. At lease expiry the CP reads the pointer (its role has `ssm:GetParameter`, `image-stack.ts:75-80`) and, if it names another MicroVM at a higher generation, drains and terminates itself (its id arrives in the run body, `hooks.ts:94`; the role has `TerminateMicrovm`). Raise the rotate timeout to 10 min or cut `readyTimeoutMs` to 120 s.`

3. `[M] build stamp — packages/infra/image/Dockerfile:7-10 vs scripts/stage-image.ts:42-45, control-plane/src/main.ts:26-35 (loop 1 regression) — stage-image writes `build.json`, the Dockerfile copies only package.json, main.js, node-worker.js, programs/ — `/health.build` (`server.ts:645`) is always `null` in the image (and locally, where no stamp sits beside `src/main.ts`); the loop-1 evidence "/health shows build" was never observable, and `image-dist/` is still the 2026-09-03 staging without the file — `COPY build.json /app/build.json`; a stage-image test that parses the Dockerfile's COPY lines against the staged files; `mise run health` fails when `build` is null on an image-mode CP.`

4. `[M] rollback — docs/runbook.md:47-51, fleet/src/config.ts:80, control-plane/src/cores.ts:48-49, infra/lib/image-stack.ts:123-134 — Rollback is a hand edit of the rotate function's env (the next `cdk deploy` silently reverts it), and cores launch with `TABFRAME_IMAGE_VERSION`, which no image version sets — A rolled-back CP at N-1 runs cores at latest N: mixed protocol/ABI on one machine; the README's "rollback is the same path" has no task behind it — (a) add `imageVersion` to `ControlPlanePayload`/`RunPayload`, set `config.imageVersion` from it in `onRun` so `createCoreFleet` pins cores to the CP's version; (b) `mise run rollback -- --to N` → `fleet/scripts/rollback.ts` invokes rotate with `{reason:"rollback", imageVersion}`; the handler honours it only for non-scheduled events and persists `pointer.pinnedImageVersion`, used by `launch()` until `--clear` or a `deploy`; (c) a test that a pinned pointer launches CP and cores at the pin.`

5. `[M] launch idempotency — packages/fleet/src/rotate.ts:131,138-144, test/rotate.test.ts:162, testing/fake.ts:131-149 (loop 1 regression) — After a successor "never reached RUNNING" it is terminated, but the retry a minute later (Lambda async retry, or the next heal) reuses the identical hour-scoped `clientToken`; if RunMicrovm is idempotent on the token (the reason the token exists) it returns the same, now TERMINATED, MicroVM and every attempt fails for the rest of the hour — the fake ignores `clientToken`, so the test asserts a regex, not behaviour — Treat a `run()` result already TERMINATED/TERMINATING as a replay and retry once with a `-r<n>` suffix (n bounded at 3, kept in the pointer); model token idempotency in the fake (`Map<token, microvmId>`) and test it.`

6. `[M] diff gate and CI synth — mise.toml:98, .github/workflows/ci.yml:74-81, infra/test/synth.test.ts:19 — No `cdk diff`, `--require-approval never`, and CI never synthesises the real app: the synth test disables bundling (`aws:cdk:bundling-stacks: []`) — esbuild bundling errors, `bin`/`lib` drift, and a CLI 2.1139.0 vs lib 2.267.0 schema mismatch surface only at deploy under the admin identity — In `deploy`, before deploying: `cdk diff --all | tee cdk.out/last-diff.txt`, then `cdk diff --all --security-only --fail` (IAM/SG changes stop the run unless `TABFRAME_DEPLOY_IAM=1`), and deploy with `--require-approval broadening` (aborts non-interactively). In CI: `bunx cdk synth --all -q` with `TABFRAME_WEB_DIST=packages/web/public TABFRAME_IMAGE_DIR=packages/infra/image`; no credentials needed, local esbuild 0.28.2 bundles both functions.`

7. `[M] image pinning — packages/infra/image/Dockerfile:4-5, cdk.json:6 — `FROM …:al2023-minimal` by tag and `dnf install -y nodejs22` floating; `baseImageVersion: "1"` is pinned but `base-image-version.ts` is wired to no task and nothing says when "1" is deprecated — The image runs whatever Node al2023 ships on build day while tests ran under 22.23.2 — `FROM public.ecr.aws/lambda/microvms:al2023-minimal@sha256:<digest>` (from `docker manifest inspect` or `aws ecr-public describe-images --region us-east-1 --repository-name lambda/microvms --image-ids imageTag=al2023-minimal`; public, no account); pin the RPM NEVRA from `dnf list --available nodejs22` in an al2023 container; add `RUN node -e 'if (process.versions.node !== "22.23.2") process.exit(1)'` so drift fails the build, not the machine; a `base-image` task that warns when the pinned version is not the latest AVAILABLE.`

8. `[M] synthetic canary — none; fleet-stack.ts:171-201 has the topic and alarms — Alarms fire only when a function throws; a 403 page, a config.json naming a dead session URL, or a heal stuck in `starting` for an hour is invisible — The machine has no external heartbeat during the review period — A third `NodejsFunction` `tabframe-canary` (128 MB, 10 s) on `rate(5 minutes)`: GET `${webOrigin}/config.json`, GET the session URL, validate the body shape (`off` | `starting` | `{endpoint,token,generation}`), `PutMetricData` `Tabframe/Canary` `PageOk`/`SessionOk`/`Starting`; alarms `PageOk<1` or `SessionOk<1` 2 of 3 periods, `Starting>=1` 3 periods → `tabframe-alarms`. It must never touch the MicroVM endpoint: any request there is idle-policy traffic and auto-resumes a suspended CP, so a socket canary would keep the machine awake all night (§9.5, ≈ $3/day); the session function only calls GetMicrovm. ~8,640 invocations/month, free tier. Add a runbook check that the SNS email subscription is confirmed.`

9. `[L] env scoping — mise.toml:10-13 — `_.file` and `AWS_PROFILE` are global, so `bun test`, `sim`, `build`, `dev`, `transcripts` all run with the admin profile and the account id in their env — Any stray SDK call in a non-AWS task runs as admin — Verified with the installed mise 2026.4.11 that task-level `env = { _.file = ".env.local" }` loads the file for that task only: move `AWS_PROFILE`, `AWS_REGION`, `_.file` into per-task `env` on whoami, deploy, verify*, rotate, up, down, logs*, health; `_.file` alone on `transcripts` (scrub words).`

10. `[L] deploy guard — packages/infra/scripts/deploy-guard.ts:25-34, mise.toml:94-100 — `merge-base --is-ancestor HEAD origin/main` passes for any old commit on main; CI green (the runbook's rule, `runbook.md:34`) is not checked; the guard runs after a five-minute build and test; `-dirty` is the only trace of an ungated deploy — A stale checkout deploys an old image with a clean-looking stamp — Require `HEAD == origin/main` (or warn when behind); add `gh run list --commit "$(git rev-parse HEAD)" --workflow CI --status success --limit 1 --json conclusion` and fail when empty (same UNGATED escape); run the guard first; have stage-image record `branch` and `ungated` in `build.json`.`

11. `[L] rule retries and state — packages/infra/lib/fleet-stack.ts:155-168 — `retryAttempts`/`maxEventAge` on the rule target govern EventBridge's delivery to Lambda (throttles, 5xx), not the function's failures; a throwing rotation is retried by Lambda's async default (2 retries, 6 h max age) — the observed behaviour is a default, not the configured knob, and the heal `invokeAsync` shares it; `enabled: false` in the template re-disables the schedule whenever the rule resource changes (see 1) — `this.rotate.configureAsyncInvoke({ retryAttempts: 2, maxEventAge: Duration.minutes(30) })` with a synth assertion on `AWS::Lambda::EventInvokeConfig`; since rotate already no-ops on `off`, set the rule `enabled: true` (D20 holds through the pointer) or fold `up`'s enable into every deploy.`

Verified as real and complete: the sequential deploy with the guard, the suspended-CP skip, the terminated stuck successor, RETAIN buckets (the `autoDeleteObjects` flip is safe: CDK's handler checks the bucket tag), the `g` prefix on the snapshot lifecycle, the response-header policies, pinned tools, CI concurrency/retry/cache, the exporter guard, and the three alarms (Sum ≥ 1 over 5 min; they can fire). `bun test packages/fleet/test/rotate.test.ts packages/infra/test` passes (40 tests).

## Summary

The fleet is now failure-visible (thrown rotations, alarms, retries) and the deploy is gated and sequential, but the deploy path still has two silent traps — a bare `cdk deploy` publishes the placeholder image and can disable the schedule, and a rotation that dies after the pointer flip leaves two active control planes — and the build stamp the first loop added never reaches the image. Fix first: default the image staging to `image-dist` with a placeholder refusal and a `deploy:stacks` task; make the retire step crash-safe (`retiring` in the pointer plus a self-check at lease expiry) and widen the rotate timeout; ship `build.json` in the Dockerfile. Then land the deferred items as specified above — the `rollback` task with the version carried in the run payload so cores follow the pin, digest and NEVRA pinning with a build-time Node assertion, `cdk diff --security-only --fail` and a CI synth, per-task `.env.local`, and a session-level canary that never wakes the machine.
