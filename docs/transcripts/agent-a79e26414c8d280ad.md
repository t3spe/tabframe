# Session agent-a79e26414c8d280ad

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 129 · user messages: 1 · assistant messages: 2 · tool calls: 57 · tool results: 57
- from 2026-09-04T14:16:02.931Z to 2026-09-04T14:24:52.402Z
- scrubbed: nothing

---
## User · 2026-09-04T14:16:02.931Z

You are an expert full-stack software developer reviewing the Tabframe repository at /home/mircea/homework/tabframe (a fault-tolerant distributed computer whose cores are browser tabs and AWS Lambda MicroVMs, programs in WebAssembly; TypeScript, Bun, Playwright). Read docs/design.md (source of truth), README.md, and docs/walkthrough.md first, then review the code with a full-stack eye.

Focus: packages/web/src (host.ts, panels.ts, state.ts, observer.ts, editor.ts, editor-page.ts, editor-core.ts, demo.ts), packages/web/public (index.html, editor.html, guide.html, styles.css), packages/control-plane/src (server.ts, static.ts, the HTTP and WebSocket handling), packages/node/src, packages/store. Look for: correctness bugs in state handling and rendering, error handling gaps (unhandled promise rejections, swallowed errors), memory or listener leaks in long-lived pages, render-loop performance problems, DOM/XSS hazards (innerHTML with untrusted text), accessibility basics (labels, focus, keyboard), dead code, duplicated logic, brittle DOM coupling between tests and markup, and anything a senior reviewer would block a PR on.

Rules: do NOT modify any file, do NOT run git commands that write, do NOT run any AWS, aws, cdk, deploy, up, down, or rotate commands, and never use the `tabframe` AWS profile. Reading, grep, and running `bun test <path>` locally are fine. Do not run Playwright.

Output (at most 1500 words): a section "## Findings" with at most 12 entries, most severe first, each on the form:
`[H|M|L] <area> — <file:line> — <problem> — <why it matters> — <concrete fix>`
Only real defects, risks, or clear improvements; no style nits. Then "## Summary": three sentences on the overall quality and the top three things to fix.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T14:16:05.560Z

I'll start with the source-of-truth docs, then move through the code.

**Tool `Bash`:**

```
ls -la && echo "---" && find . -path ./node_modules -prune -o -type f -print | grep -v node_modules | grep -v '\.git/' | head -200
```

**Result of `Bash`:**

```
total 228
drwxrwxr-x 14 mircea mircea  4096 Sep  3 21:55 .
drwxrwxr-x  5 mircea mircea  4096 Sep  2 07:02 ..
-rw-rw-r--  1 mircea mircea   787 Sep  2 23:35 biome.json
-rw-rw-r--  1 mircea mircea   910 Sep  1 22:51 bunfig.toml
-rw-r--r--  1 mircea mircea 46043 Sep  3 00:13 bun.lock
-rw-rw-r--  1 mircea mircea   543 Sep  1 21:41 cdk.json
drwxrwxr-x 56 mircea mircea 12288 Sep  3 21:55 cdk.out
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:10 .claude
drwxrwxr-x  2 mircea mircea 20480 Sep  3 22:07 coverage
drwxrwxr-x  4 mircea mircea  4096 Sep  3 22:12 docs
drwxrwxr-x  2 mircea mircea  4096 Sep  3 21:40 e2e
-rw-------  1 mircea mircea   189 Sep  2 15:05 .env.local
drwxrwxr-x  8 mircea mircea  4096 Sep  3 22:18 .git
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:27 .github
-rw-rw-r--  1 mircea mircea   364 Sep  2 15:19 .gitignore
-rw-rw-r--  1 mircea mircea 34523 Sep  1 21:08 LICENSE
-rw-rw-r--  1 mircea mircea  5515 Sep  3 00:28 mise.toml
drwxr-xr-x  7 mircea mircea  4096 Sep  1 22:51 node_modules
-rw-rw-r--  1 mircea mircea   436 Sep  1 22:51 package.json
drwxrwxr-x 13 mircea mircea  4096 Sep  1 22:52 packages
-rw-rw-r--  1 mircea mircea  1835 Sep  2 02:57 playwright.config.ts
drwxrwxr-x  5 mircea mircea  4096 Sep  2 23:56 programs
-rw-rw-r--  1 mircea mircea 14793 Sep  3 00:14 README.md
drwxrwxr-x  3 mircea mircea  4096 Sep  2 13:37 scripts
drwxrwxr-x  2 mircea mircea  4096 Sep  3 22:02 test-results
-rw-rw-r--  1 mircea mircea   480 Sep  1 21:08 tsconfig.base.json
-rw-rw-r--  1 mircea mircea   294 Sep  3 20:28 tsconfig.json
---
./README.md
./package.json
./mise.toml
./playwright.config.ts
./cdk.json
./bun.lock
./.env.local
./LICENSE
./.gitignore
./bunfig.toml
./biome.json
… (189 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat /home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (93.5KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bhoedpzfv.txt

Preview (first 2KB):
# Tabframe — Design Record

**Status:** design complete, build not started. Written 2026-09-01 during a full pre-build walkthrough.
This document is the source of truth for the build. It supersedes the earlier handoff and seed
documents wherever they differ.

**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
and which stays correct while cores — and the control plane itself — come and go.

---

## Contents

1. Decisions register
2. The system in one page
3. Runtime picture
4. Nodes
5. Programs
6. The control plane
7. Data paths and the store
8. The wire
9. Hosting and fleet
10. Security and credentials
11. Tooling and repo
12. Tests and dev loop
13. Deploy, operations, and the reviewer's view
14. Milestones
15. Rationale hooks
16. Glossary
17. Drift log

---

## 1. Decisions register

… (7 more lines)
```

**Tool `Bash`:**

```
cat /home/mircea/homework/tabframe/README.md; echo "=================== WALKTHROUGH"; cat /home/mircea/homework/tabframe/docs/walkthrough.md
```

**Result of `Bash`:**

```
# Tabframe

A fault-tolerant distributed computer whose cores are browser tabs and Firecracker microVMs,
programmed with WebAssembly. Open the page and your tab is a core. Close it and the machine keeps
computing, correctly. The control plane that schedules the work is itself replaced every hour, with
the work in flight.

Deployed: **https://d2w9z8juw4oo76.cloudfront.net** — the page lends one core when it opens, and
shows the machine rendering a Mandelbrot frame with whoever else is there.

## What it is

A program is one WebAssembly module with two entry points and a manifest. `plan` turns parameters
and the previous stage's outputs into the next stage's tasks; `run` turns one task's input into
bytes. Both execute on cores — the control plane runs no program code, not even the planner. A
program's only view of the world is a per-execution filesystem of content-addressed blobs: it has
no clock, no randomness, no network, and no failure type anywhere in its API.

Two programs ship with the machine and go through the same path as anything you write in the
in-page editor: a distributed **Mandelbrot** render (640 tiles of 64×64 per frame, presets that
advance while anyone watches) and a three-stage **word count** over *Moby-Dick* (map by byte range,
reduce by partition, merge to a top-25). You can also compile your own: the editor holds the
Mandelbrot source and the AssemblyScript compiler runs in a browser worker.

Since M6 a third program ships with the image: **tiny GPT**, an 822 k-parameter character-level
transformer trained on that same corpus, whose whole forward pass runs in WebAssembly on the cores —
one continuation per task, four milliseconds a token, the same bytes on every core
(`docs/feasibility-transformer.md`).

## Why it looks the way it does

Three constraints carry the whole fault-tolerance story; everything else follows from them.

1. **Tasks are idempotent and delivered at least once.** A task may run on two cores, be killed
   halfway, or be handed to a third after its holder vanishes. The program never knows.
2. **Results are single-assignment, memoized by hash.** The first accepted result for a task is
   final; a duplicate is compared, not repeated. With the redundancy toggle on, two cores must
   agree byte for byte before a tile counts, and a disagreement recomputes from scratch and is
   settled by majority after two contested rounds.
3. **The control plane holds hashes, never bytes.** Results, written files, and logs go from the
… (268 more lines)
```

**Tool `Bash`:**

```
find packages e2e scripts programs docs -path '*/node_modules' -prune -o -type f -print | grep -v node_modules | sort && echo "--- wc ---" && wc -l packages/web/src/*.ts packages/web/public/* packages/control-plane/src/*.ts packages/node/src/*.ts packages/store/src/*.ts 2>/dev/null | sort -n
```

**Result of `Bash`:**

```
docs/design.md
docs/feasibility-transformer.md
docs/implementation/assets/wp-0.10/deployed.png
docs/implementation/assets/wp-0.6/two-tabs.png
docs/implementation/assets/wp-1.8/dashboard.png
docs/implementation/assets/wp-2.5/wordcount.png
docs/implementation/assets/wp-4.1/dashboard.png
docs/implementation/assets/wp-4.1/rotation-banner.png
docs/implementation/README.md
docs/implementation/wp-0.10-first-hello.md
docs/implementation/wp-0.12-ci.md
docs/implementation/wp-0.1-repo-scaffold.md
docs/implementation/wp-0.2-protocol.md
docs/implementation/wp-0.3-core.md
docs/implementation/wp-0.4-control-plane.md
docs/implementation/wp-0.5-node.md
docs/implementation/wp-0.6-web.md
docs/implementation/wp-0.7-dev.md
docs/implementation/wp-0.8-infra-skeleton.md
docs/implementation/wp-0.9-fleet-skeleton.md
docs/implementation/wp-1.10-deploy-m1.md
docs/implementation/wp-1.1-protocol-v1.md
docs/implementation/wp-1.2-core-v1.md
docs/implementation/wp-1.3-store.md
docs/implementation/wp-1.4-sandbox.md
docs/implementation/wp-1.5-sdk-mandelbrot.md
docs/implementation/wp-1.6-node-v1.md
docs/implementation/wp-1.7-control-plane-v1.md
docs/implementation/wp-1.8-dashboard-v1.md
docs/implementation/wp-1.9-churn-sim.md
docs/implementation/wp-2.1-filesystem.md
docs/implementation/wp-2.2-wordcount.md
docs/implementation/wp-2.3-launch-path.md
docs/implementation/wp-2.4-editor.md
docs/implementation/wp-2.5-dashboard-v2.md
docs/implementation/wp-2.6-playwright-v1.md
docs/implementation/wp-2.7-deploy-m2.md
docs/implementation/wp-3.1-cp-lifecycle.md
docs/implementation/wp-3.2-fleet-v1.md
docs/implementation/wp-3.3-cloud-cores.md
… (445 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
wc -l /home/mircea/homework/tabframe/docs/design.md; sed -n 1,330p /home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (34KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bd0o4vci2.txt

Preview (first 2KB):
1002 /home/mircea/homework/tabframe/docs/design.md
# Tabframe — Design Record

**Status:** design complete, build not started. Written 2026-09-01 during a full pre-build walkthrough.
This document is the source of truth for the build. It supersedes the earlier handoff and seed
documents wherever they differ.

**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
and which stays correct while cores — and the control plane itself — come and go.

---

## Contents

1. Decisions register
2. The system in one page
3. Runtime picture
4. Nodes
5. Programs
6. The control plane
7. Data paths and the store
8. The wire
9. Hosting and fleet
10. Security and credentials
11. Tooling and repo
12. Tests and dev loop
13. Deploy, operations, and the reviewer's view
14. Milestones
15. Rationale hooks
16. Glossary
17. Drift log

---

## 1. Decisions register
… (8 more lines)
```

**Tool `Bash`:**

```
sed -n 330,700p /home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

````
<persisted-output>
Output too large (34.9KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/btdqn6p1a.txt

Preview (first 2KB):
### 7.1 Store API

```
GET  /blob/<hash>                 via CloudFront: immutable, cache-forever, Range supported
presign {hash,size}[]             a socket message (node or observer socket) → presigned PUT URLs
                                  for keys equal to the hashes, SHA-256 checksum pinned
PUT  <presigned url>              straight to S3; S3 rejects bytes that don't hash to the key
```

Presign over the socket is the only write path (D18). Because S3 verifies the pinned checksum, a hash the node reports is a hash the store vouches for; the node did not choose it. Locally, the dev server's presign returns URLs to itself and hashes on receipt. One client flow everywhere.

Three infrastructure details make this work from a browser: the blob bucket's CORS allows PUT with the checksum header from the page's origin; the immutable cache-control header is part of the signed request; and the CloudFront blob behavior has an error-caching TTL of zero, so a request that lands a moment before an upload completes does not pin a 404 for five minutes. Observers retry a 404 with backoff.

### 7.2 Read path (during `run`)

Program calls `read` → glue resolves the path to a hash through the stage manifest (or the task's own buffer) → bytes come from the node cache or a synchronous GET by hash over the CDN → copied into module memory. The control plane is not involved.

### 7.3 Write path

1. Program calls `write`; the glue buffers it.
2. `run` returns; the sandbox posts output, writes, and log to the orchestrator.
3. The orchestrator requests presigns for all hashes at once over its socket, PUTs in parallel straight to S3, and collects the confirmations.
4. `result` is sent with hashes only.
5. The control plane accepts under §6.5 and records the write list on the task; the writes are still invisible.
6. The stage completes; the control plane folds outputs and writes into a new manifest, rejects on conflict, uploads it, and sets the root.
...
</persisted-output>
````

**Tool `Bash`:**

```
sed -n 700,1200p /home/mircea/homework/tabframe/docs/design.md
```

**Result of `Bash`:**

```
## 17. Drift log

Dated deviations discovered while building, recorded before the code landed (plan §0).

- **2026-09-02 (WP0.10).** The account's Lambda concurrency default of 10 must stay fully
  unreserved, so the fleet functions run without reserved concurrency; rotate stays single-writer by
  idempotency and a per-generation client token (§9.2). `NodejsFunction` bundles the AWS SDK
  because the runtime's copy predates MicroVMs. `iam:PassRole` grants carry no `PassedToService`
  condition, and `lambda:PassNetworkConnector` is granted on `*` (§10.1). The session function's
  URL has no CORS configuration of its own; the handler answers it (§9.2). The store base handed to
  clients is `https://<distribution>/blob` (§7.1). A fourth stack, **Web**, deploys the page and
  `config.json`; the order is Core → Image → Fleet → Web (§11.4). The image staging directory is
  generated by `build:image` and selected with `TABFRAME_IMAGE_DIR`; the image environment sets
  `TABFRAME_MODE=image` and `TABFRAME_HOST=0.0.0.0` (§9.3, §11.3). `mise run deploy` ends with
  `up`, which is idempotent for a running control plane (§13).
- **2026-09-02 (WP1.6).** A node that reaches its own deadline (the control plane's deadline plus
  a grace second) reports `error: "released"` rather than a fault: the core releases the attempt,
  credits no completed task, and sends an orphaned task back to the front as released work (§4.2,
  §6.4). Throttled nodes idle nine times each task's compute with a fifty-millisecond floor
  (§6.6). Over-capacity assignments are refused, not queued (§4.1).
- **2026-09-02 (WP1.7).** The node message rate limit is 1000 per second, not 20: a node sends
  two messages per task and a tile takes milliseconds (§8.4). Observer snapshots carry the
  program list on page 0 so late observers learn what the machine can run (§8.3). Tiles must be
  RGBA of their placed size or the task fails (§5.2). Snapshot keys are
  `g<generation>/<time>.json.gz` plus a `latest.json.gz` pointer; the suspend and terminate hooks
  write unconditionally (§9.4). Seeded bundles are manifest blobs over `/program.wasm`,
  `/manifest.json`, and `/in/*` (§5.6). The private port gains `GET /snapshot` (§9.3).
- **2026-09-02 (WP1.10).** A presigned PUT must carry the SHA-256 checksum as a **signed header**,
  not as a signed query parameter: with the checksum hoisted into the query, S3 accepts bytes that
  do not match it, and a request carrying it as an unsigned header is refused outright. The store
  passes `signableHeaders`/`unhoistableHeaders` for it and refuses to hand out a URL whose
  signature does not cover the checksum; clients send exactly the signed header set and nothing
  more (§7.1, §7.3). The default loop backs off after a failed execution — five seconds, doubling
  to five minutes, reset by a success — so a broken program cannot spin the machine (§6.8). Ended
  executions beyond the most recent 32 are pruned with their tasks on every tick, keeping the
  ledger and its snapshots bounded (§9.4).
- **2026-09-02 (WP2.1).** A bundle manifest and a filesystem manifest are the same shape, so an
  execution that inherits nothing uses the **bundle hash itself** as its first root (§5.4). The
  bundle's files win over an inherited filesystem on conflicting paths. The expired-root check is
  a fetch of the inherited root before planning, and its absence raises a new `executionWarning`
… (263 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bd0o4vci2.txt
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (34KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bcyke3nzm.txt

Preview (first 2KB):
1002 /home/mircea/homework/tabframe/docs/design.md
# Tabframe — Design Record

**Status:** design complete, build not started. Written 2026-09-01 during a full pre-build walkthrough.
This document is the source of truth for the build. It supersedes the earlier handoff and seed
documents wherever they differ.

**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
and which stays correct while cores — and the control plane itself — come and go.

---

## Contents

1. Decisions register
2. The system in one page
3. Runtime picture
4. Nodes
5. Programs
6. The control plane
7. Data paths and the store
8. The wire
9. Hosting and fleet
10. Security and credentials
11. Tooling and repo
12. Tests and dev loop
13. Deploy, operations, and the reviewer's view
14. Milestones
15. Rationale hooks
16. Glossary
17. Drift log

---

## 1. Decisions register
… (8 more lines)
```

**Tool `Bash`:**

```
cat /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/btdqn6p1a.txt
```

**Result of `Bash`:**

````
<persisted-output>
Output too large (34.9KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bwamxz707.txt

Preview (first 2KB):
### 7.1 Store API

```
GET  /blob/<hash>                 via CloudFront: immutable, cache-forever, Range supported
presign {hash,size}[]             a socket message (node or observer socket) → presigned PUT URLs
                                  for keys equal to the hashes, SHA-256 checksum pinned
PUT  <presigned url>              straight to S3; S3 rejects bytes that don't hash to the key
```

Presign over the socket is the only write path (D18). Because S3 verifies the pinned checksum, a hash the node reports is a hash the store vouches for; the node did not choose it. Locally, the dev server's presign returns URLs to itself and hashes on receipt. One client flow everywhere.

Three infrastructure details make this work from a browser: the blob bucket's CORS allows PUT with the checksum header from the page's origin; the immutable cache-control header is part of the signed request; and the CloudFront blob behavior has an error-caching TTL of zero, so a request that lands a moment before an upload completes does not pin a 404 for five minutes. Observers retry a 404 with backoff.

### 7.2 Read path (during `run`)

Program calls `read` → glue resolves the path to a hash through the stage manifest (or the task's own buffer) → bytes come from the node cache or a synchronous GET by hash over the CDN → copied into module memory. The control plane is not involved.

### 7.3 Write path

1. Program calls `write`; the glue buffers it.
2. `run` returns; the sandbox posts output, writes, and log to the orchestrator.
3. The orchestrator requests presigns for all hashes at once over its socket, PUTs in parallel straight to S3, and collects the confirmations.
4. `result` is sent with hashes only.
5. The control plane accepts under §6.5 and records the write list on the task; the writes are still invisible.
6. The stage completes; the control plane folds outputs and writes into a new manifest, rejects on conflict, uploads it, and sets the root.
...
</persisted-output>
````

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/packages/web/src/host.ts
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (46.6KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bw9ww83ry.txt

Preview (first 2KB):
     1	// The host page (design §3): one observer socket for the dashboard, zero or more node workers.
     2	// The canvas is the hero: finished tiles land on it as they are verified, and the scheduler's
     3	// state is drawn over the rectangles that are still open. Around it, the panels of dashboard v2:
     4	// programs, queue, the stage strip, the result of a bars or text program, files, task detail, the
     5	// ledger; and the polish of WP4.1: flashes with a log of what just moved, a legend, the rotation
     6	// and sleep banners, a throughput chart, and a spawn hint sized to this browser's cores.
     7	import type { HostToWorker, WorkerToHost } from "@tabframe/node/platform/web";
     8	import type { NodeView, PlaceView } from "@tabframe/protocol";
     9	import { connectionCopy, fmtCountdown, machineCopy, ROTATING_DETAIL } from "./banners.ts";
    10	import { DEMO_CYCLE, type DemoHandle, type DemoProgram, startDemo } from "./demo.ts";
    11	import { type ControlRequest, type MachineState, ObserverClient } from "./observer.ts";
    12	import { gridIndexAt, gridLayout, mountPanels, type Panels } from "./panels.ts";
    13	import {
    14	  applyMessage,
    15	  type ClusterState,
    16	  emptyState,
    17	  headerSlot,
    18	  hostCount,
    19	  inFlightByNode,
    20	  isFlashing,
    21	  isRunning,
    22	  loopLabel,
    23	  loopState,
    24	  machineBanner,
    25	  machineSentence,
    26	  type Pulse,
    27	  planTask,
    28	  progress,
    29	  stageTasks,
    30	  stopTitle,
    31	  TASK_COLOR_LABELS,
    32	  type TaskColor,
    33	  taskColor,
    34	  throughput,
    35	  throughputSeries,
    36	  withRedundancy,
… (16 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/packages/web/src/state.ts
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (54.2KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/btmhujyf6.txt

Preview (first 2KB):
     1	import type {
     2	  ControlPlaneToObserver,
     3	  Counters,
     4	  ExecutionView,
     5	  MachineView,
     6	  NodeView,
     7	  ProgramView,
     8	  QueueEntry,
     9	  Snapshot,
    10	  TaskLog,
    11	  TaskView,
    12	} from "@tabframe/protocol";
    13	
    14	/** Throughput is computed on the dashboard from `taskDone` arrival times over this window (design §6.7). */
    15	export const THROUGHPUT_WINDOW_MS = 5_000;
    16	/** How long a reassignment, a retraction, or a control's victims stay highlighted. */
    17	export const FLASH_MS = 1_500;
    18	/** The throughput chart looks back this far, one bucket per second (design §6.7). */
    19	export const CHART_WINDOW_MS = 60_000;
    20	/** Flash-worthy events kept for the pulse list beside the grid. */
    21	export const PULSE_CAP = 12;
    22	/** Notable events kept for the activity list. */
    23	export const ACTIVITY_CAP = 60;
    24	/** Attempt records kept per task; a task that churns more than this keeps the latest. */
    25	export const HISTORY_CAP = 16;
    26	
    27	/** What became of one attempt at a task, as the event stream told it (design §6.4, §6.5). */
    28	export type AttemptOutcome =
    29	  | "running"
    30	  | "done"
    31	  | "verified"
    32	  | "released"
    33	  | "mismatch"
    34	  | "retracted"
    35	  | "cancelled"
    36	  | "failed";
… (17 more lines)
```

> **Thinking.** 

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Result of `Read`:**

```
40	| D2 | **General-purpose programs, minimal but complete.** A program is one WASM module (`plan` + `run`) plus a manifest, content-addressed, sandboxed, uploadable, with in-browser AssemblyScript compilation and an execution queue. | Mandelbrot and word count are real programs shipped through the same path; the node bundle contains no application code. |
41	| D3 | **Node = one orchestrator worker owning its own socket + one disposable sandbox worker.** The host page is an observer and a spawner only. | Heartbeats originate in the orchestrator; program compute never delays them. No iframes. |
42	| D4 | **One global machine.** Everyone joins one cluster. One execution runs at a time, FIFO, user-submitted ahead of automatic continuations. | Frames auto-advance while someone is watching. Zero nodes → tasks wait as pending. |
43	| D5 | **Hosting on AWS, tooling via mise.** | See D13. |
44	| D6 | **The control plane runs no program code.** Planning is a task executed on a core like any other. | Planning is fault-tolerant for free; stage specs are auditable blobs. |
45	| D7 | **Redundancy toggle built in.** A mismatch discards every result for the task and recomputes from scratch; the task is done only when two fresh attempts agree. After two contested rounds, the majority hash across all attempts wins and the tile is flagged red "resolved by vote". | Toggle off: a later disagreeing duplicate un-paints and recomputes. Toggle on: nothing is ever retracted. |
46	| D8 | **Ledger holds metadata and hashes only. Bytes live in a content-addressed store.** The store computes the hash on upload and refuses any other name. | The seed's control-plane / data-plane split, literally. |
47	| D9 | **Results, written files, and logs go node → store. The control plane receives hashes only. Observers fetch by hash over the CDN.** | The control plane never sees a payload byte. Mandatory under D13's bandwidth caps. |
48	| D10 | **Per-execution filesystem**, versioned by stage: reads see the filesystem as of stage start plus own writes; writes commit when the result is accepted and become visible next stage; write conflicts within a stage fail the execution; an execution may inherit a prior execution's final root at launch. | Persistence without breaking idempotency or single-assignment. |
49	| D11 | **Protocol simplifications:** no `ack`; nodes send no hash; a node is a connection (no resume identity; reconnect = new node). | Fewer messages, no stale-identity bugs. |
50	| D12 | **Determinism is a requirement.** Kernels are WASM with no clock, randomness, or network imports; task inputs exclude anything that differs between twins; planner hints are frozen into the plan task input. | Makes verification meaningful across browsers and machines. |
51	| D13 | **Hosting = AWS Lambda MicroVMs** (the stateful Firecracker product with per-VM HTTPS endpoints, not Lambda functions). Control plane = one Node process in a MicroVM; store = S3 behind CloudFront; two small Lambda functions vend tokens and manage the fleet. | Endpoint bandwidth is capped by VM size, which is why D9 is mandatory. |
52	| D14 | **Two cloud cores by default:** two MicroVM workers run whenever the machine is awake, same image as the control plane, role chosen by run-hook payload, no ingress, connecting outward like any node. | Cores are nodes; the control plane cannot tell a tab from a microVM. |
53	| D15 | **The control plane rotates every hour, even under load.** Several control-plane MicroVMs are in flight during handover; exactly one is active, stamped with a generation. | Deploys reuse the same handover path. |
54	| D16 | **AWS identity:** a dedicated account; profile **`tabframe`** (an IAM user with AdministratorAccess) is the operator identity for bootstrap and `cdk deploy` only. Every application component runs under a least-privilege role created by CDK. No IAM users or static keys for the application. | Verified 2026-09-01: profile works, region us-west-2, account empty, MicroVMs API reachable, memory quota 8 GB, RunMicrovm rate 1/s. |
55	| D17 | **Budget:** $100/month notification-only AWS Budget, alerts at 50/80/100 % of actual spend and forecast 100 %. **No automatic kill switch.** The notification address lives in the gitignored `.env.local` and is read at synth time; it is never committed. | The manual kill switch is `mise run down`. |
56	| D18 | **The control plane's browser-facing surface is WebSocket-only.** Presigned upload URLs are requested as socket messages. Lifecycle hooks and the internal fleet endpoints live on a **private port 8081**; browser tokens are scoped to port 8080. | A fetch carrying the proxy auth header cannot pass a CORS preflight, and hook paths must not be reachable with a browser token. |
57	| D19 | **Only the machine's default loop continues automatically.** A human-launched execution's follow-up params are recorded and offered as a button, never auto-enqueued; when a human execution ends the machine returns to the default loop. | Nobody's program runs unattended on the project's bill. |
58	| D20 | **`mise run down` turns the machine off:** disables the rotation schedule, terminates every MicroVM, and writes an *off* state to the pointer that the session function honors with an off page. `mise run up` is the only way back. | Down means down. |
59	
60	**Small defaults, confirmed:** synchronous file reads through the sandbox glue; `persist` programs inherit the latest finished execution; tile results are RGBA written by the program; **blobs are kept one year**; no operator token — every control is public; heartbeat 1 s, gone at 4 s; frame 1024×640 displayed, 64-px computed tiles; **256 nodes and 64 observers, no per-IP limit**; one public-domain book as the word-count corpus (a Project Gutenberg text with the boilerplate stripped, chosen at build time); **license AGPL-3.0**.
61	
62	**Cut on purpose:** channels between running tasks and a mutable key-value store (restartability is the whole point); a consensus control plane (single-active generation with snapshots instead); K-way voting beyond the built-in two-way verify and its tie-break; a RISC-V interpreter; a WebRTC peer mesh; intra-tab `SharedArrayBuffer` multicore; server-side C→WASM compilation (in-browser AssemblyScript and bring-your-own-WASM instead).
63	
64	---
65	
66	## 2. The system in one page
67	
68	**Cores** are browser tabs and MicroVMs. Each core runs the same orchestrator, which owns a WebSocket to the control plane and a disposable sandbox that executes program code. Cores come and go constantly; the design treats every departure as ordinary.
69	
70	**Programs** are WebAssembly modules with two entry points. `plan` turns params and the previous stage's outputs into the next stage's tasks. `run` turns one task's input into bytes. Both execute on cores. A program's only view of the world is a per-execution filesystem of content-addressed blobs. It has no clock, no randomness, no network, and no failure type anywhere in its API.
71	
72	**The control plane** is a pure function over a small ledger: which nodes exist, which execution is running, which task is in which state and who holds it, and the hash of every accepted result. It assigns, detects death, releases, speculates on stragglers, verifies duplicates, folds stage outputs into filesystem roots, and fans events out to observers. It never touches a payload byte. It runs as one Node process in a Lambda MicroVM, snapshots its ledger to S3 every few seconds, and hands over to a fresh MicroVM every hour.
73	
74	**The store** is S3 behind CloudFront. Every blob's key is its SHA-256. Nodes upload results, written files, logs, and program modules; observers and nodes read them back by hash. The store computes hashes on upload, so a hash reported to the ledger is one the store vouches for.
75	
76	**Invariants held everywhere:**
77	
78	- No shared memory across nodes. Coordination is message passing over sockets; data is blobs by hash.
79	- Tasks are idempotent and at-least-once. Re-execution is always safe.
… (290 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/design.md
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/host.ts
```

**Result of `Read`:**

```
370	
371	| Channel | Carries | Transport |
372	|---|---|---|
373	| Node socket | hello, heartbeat, assign, result, cancel, command | WebSocket, JSON text frames |
374	| Observer socket | subscribe, snapshot, events, controls, ping | WebSocket, JSON text frames |
375	| Store | blobs by hash | HTTPS GET via CloudFront; presigned PUT straight to S3; presign requests ride the sockets |
376	| Session | `{endpoint, token, expiresAt, storeBase, generation}` or `{off: true}` | HTTPS GET to the session function |
377	| Internal | handover, adopt, drain, health, diag | HTTPS on the control plane's private port 8081, fleet token plus fleet secret |
378	
379	Every socket message is one JSON text frame with a type field and the generation. Bytes never ride a socket except a task's inline input (base64, ≤ 16 KB); everything else is a hash. Every message is under 64 KB; snapshots are paged. Zod schemas in the protocol package are the single source of truth; both ends validate every inbound message, and an invalid one closes the connection with a reason code.
380	
381	### 8.2 Node socket
382	
383	| Direction | Message | Fields |
384	|---|---|---|
385	| node → cp | hello | protocol version, host id, cores, sandbox version, kind |
386	| cp → node | welcome | node id, heartbeat ms, max in flight, store base, generation |
387	| node → cp | heartbeat | visible, queue length, last task ms, tasks done |
388	| cp → node | assign | task id, attempt, execution id, program hash, kind, stage, index, count, input, fs root, deadline ms, limits |
389	| node → cp | result | task id, attempt, output hash, writes, log hash or inline log, compute ms — or error text and log |
390	| node → cp | presign | list of {hash, size} |
391	| cp → node | presigned | list of {hash, url, headers} |
392	| cp → node | cancel | task id |
393	| cp → node | command | close, freeze, throttle, resume |
394	
395	### 8.3 Observer socket
396	
397	**Subscribe** carries the protocol version and optionally the last sequence number seen. **Snapshot** pages carry nodes with health, the queue, the current execution with stage and root, every task's status, holder, output hash and placement, the counters, and the current sequence number.
398	
399	**Events:** nodes — `nodeJoined`, `nodeLeft`, `nodeHealth`; executions — `executionQueued`, `executionStarted`, `stageStarted`, `stageDone`, `executionDone` (carrying follow-up params when the program offered any), `executionFailed`, `budget`; tasks — `taskAssigned`, `taskDone`, `taskReassigned`, `taskSpeculated`, `taskVerified`, `taskMismatch`, `taskFailed`; system — `controlApplied`, `programAdded`, `programRetired`, `controlPlaneRotating {gen, next}`, `machineSleeping`, `loopYielded {yielded}` (WP6.8), `error`.
400	
401	**Controls:** `killHalf`, `freezeHalf`, `throttleHalf`, `resumeAll`, `restart`, `skip`, `killExecution`, `launch`, `runFollowUp`, `setRedundancy`, `stop`, `start`, `pause`, `resume`, `presign` (for bundle uploads), `ping`. The machine view carries `stopped`, `paused`, and `yielded`.
402	
403	**The header's one slot (WP7.1).** The dashboard's header has one slot for Stop / Start / Resume and it always means "what you can do to the machine right now": Resume while an editor tab holds the machine paused; Stop while anything runs, the loop's frame or a person's launch; Start when nothing runs and the loop is held by Stop or has yielded; Stop again when the loop is free, to hold it before its next frame. A separate loop pill names the loop's state (running · held by Stop · yielded to you · paused by the editor). Every control says what it will do in its tooltip and what it did in the activity line, which the issuing page also shows as a notice for a few seconds.
404	
405	**The status line and the reading tabs (WP7.7).** When no control echo is showing, the status line carries one sentence of state — what the machine is doing and why, in a visitor's words (`machineSentence`); the demo and an observer say what they are first. A control that cannot apply now stays where it is, greyed, with the reason after a dash in its tooltip ("— not connected yet", "— nothing is running", "— an observer lends no cores"). A panel tab drives nothing: the machine's controls leave its header and "← dashboard" leads back with the page's mode kept. The full inventory of screens, states, and controls is `docs/walkthrough.md`, checked by `e2e/walkthrough.e2e.ts`.
406	
407	### 8.4 Sizes, limits, codes
408	
409	| Message | Typical | Cap |
… (290 more lines)
```

**Result of `Read`:**

```
1	// The host page (design §3): one observer socket for the dashboard, zero or more node workers.
2	// The canvas is the hero: finished tiles land on it as they are verified, and the scheduler's
3	// state is drawn over the rectangles that are still open. Around it, the panels of dashboard v2:
4	// programs, queue, the stage strip, the result of a bars or text program, files, task detail, the
5	// ledger; and the polish of WP4.1: flashes with a log of what just moved, a legend, the rotation
6	// and sleep banners, a throughput chart, and a spawn hint sized to this browser's cores.
7	import type { HostToWorker, WorkerToHost } from "@tabframe/node/platform/web";
8	import type { NodeView, PlaceView } from "@tabframe/protocol";
9	import { connectionCopy, fmtCountdown, machineCopy, ROTATING_DETAIL } from "./banners.ts";
10	import { DEMO_CYCLE, type DemoHandle, type DemoProgram, startDemo } from "./demo.ts";
11	import { type ControlRequest, type MachineState, ObserverClient } from "./observer.ts";
12	import { gridIndexAt, gridLayout, mountPanels, type Panels } from "./panels.ts";
13	import {
14	  applyMessage,
15	  type ClusterState,
16	  emptyState,
17	  headerSlot,
18	  hostCount,
19	  inFlightByNode,
20	  isFlashing,
21	  isRunning,
22	  loopLabel,
23	  loopState,
24	  machineBanner,
25	  machineSentence,
26	  type Pulse,
27	  planTask,
28	  progress,
29	  stageTasks,
30	  stopTitle,
31	  TASK_COLOR_LABELS,
32	  type TaskColor,
33	  taskColor,
34	  throughput,
35	  throughputSeries,
36	  withRedundancy,
37	} from "./state.ts";
38	import {
39	  type BlobSource,
40	  storeSource,
… (1046 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/state.ts
```

**Result of `Read`:**

```
1	import type {
2	  ControlPlaneToObserver,
3	  Counters,
4	  ExecutionView,
5	  MachineView,
6	  NodeView,
7	  ProgramView,
8	  QueueEntry,
9	  Snapshot,
10	  TaskLog,
11	  TaskView,
12	} from "@tabframe/protocol";
13	
14	/** Throughput is computed on the dashboard from `taskDone` arrival times over this window (design §6.7). */
15	export const THROUGHPUT_WINDOW_MS = 5_000;
16	/** How long a reassignment, a retraction, or a control's victims stay highlighted. */
17	export const FLASH_MS = 1_500;
18	/** The throughput chart looks back this far, one bucket per second (design §6.7). */
19	export const CHART_WINDOW_MS = 60_000;
20	/** Flash-worthy events kept for the pulse list beside the grid. */
21	export const PULSE_CAP = 12;
22	/** Notable events kept for the activity list. */
23	export const ACTIVITY_CAP = 60;
24	/** Attempt records kept per task; a task that churns more than this keeps the latest. */
25	export const HISTORY_CAP = 16;
26	
27	/** What became of one attempt at a task, as the event stream told it (design §6.4, §6.5). */
28	export type AttemptOutcome =
29	  | "running"
30	  | "done"
31	  | "verified"
32	  | "released"
33	  | "mismatch"
34	  | "retracted"
35	  | "cancelled"
36	  | "failed";
37	
38	export interface AttemptRecord {
39	  attempt: number;
40	  nodeId: string;
… (1257 more lines)
```

> **Thinking.** 

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/panels.ts
```

**Result of `Read`:**

```
1	// Dashboard v2 panels (design §5.1, §6.7, §8.3): programs, queue, the stage strip, the result of
2	// a `bars` or `text` program, the files behind an execution's root, a task's detail, the ledger
3	// of settled tasks (hashes and where their bytes live), and failure surfacing. The state comes from `state.ts`; bytes come from the store by hash; controls go back
4	// through the observer socket. No framework: each panel is a render function over the state that
5	// rebuilds its DOM only when what it shows has changed, so buttons stay put under a finger.
6	import type { ControlRequest } from "./observer.ts";
7	import {
8	  barRows,
9	  type FileEntry,
10	  finalOutput,
11	  fmtBytes,
12	  fmtValue,
13	  groupFiles,
14	  listFiles,
15	  type Preview,
16	  parseManifest,
17	  previewOf,
18	  readBars,
19	  TEXT_PREVIEW_BYTES,
20	} from "./result.ts";
21	import {
22	  type AttemptRecord,
23	  type ClusterState,
24	  type ExecutionState,
25	  ledgerRows,
26	  type ProgramInfo,
27	  programList,
28	  stageStrip,
29	  type TaskState,
30	} from "./state.ts";
31	import type { BlobSource } from "./tiles.ts";
32	
33	export interface PanelDeps {
34	  /** The store as the page reaches it right now (the demo's in-memory store, or the CDN). */
35	  blobs(): BlobSource;
36	  /** Where the store's blobs are addressed from, `<base>/<hash>`; null for the demo's in-page store. */
37	  storeBase(): string | null;
38	  /** Issue a control; false when the machine is not there to take it. */
39	  send(control: ControlRequest): boolean;
40	  /** Ask the page to render again once a fetch has landed or a panel's own state moved. */
… (1037 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/observer.ts
```

**Result of `Read`:**

```
1	import { Backoff } from "@tabframe/node/backoff";
2	import { fetchSession, type Session, socketProtocols } from "@tabframe/node/session";
3	import {
4	  CLOSE,
5	  type Control,
6	  type ControlPlaneToObserver,
7	  controlPlaneToObserver,
8	  decode,
9	  encode,
10	  LIMITS,
11	  type ObserverToControlPlane,
12	  PROTOCOL_VERSION,
13	} from "@tabframe/protocol";
14	import type { PresignedUpload, PresignItem } from "@tabframe/store";
15	import { applyMessage, type ClusterState, emptyState, withRedundancy } from "./state.ts";
16	
17	export type MachineState = "connecting" | "starting" | "off" | "live" | "outdated";
18	
19	/** A control as the page issues it; the client stamps the version and generation. */
20	export type ControlRequest = Control extends infer C
21	  ? C extends { v: number; gen: number }
22	    ? Omit<C, "v" | "gen">
23	    : never
24	  : never;
25	
26	export interface ObserverHandlers {
27	  onState(machine: MachineState, detail?: string): void;
28	  onCluster(state: ClusterState): void;
29	  onSession(session: Session & { kind: "on" }): void;
30	}
31	
32	/** Anything the page sends besides subscribe and ping, before the version and generation are stamped. */
33	type Outgoing = ControlRequest | { t: "presign"; items: PresignItem[] };
34	
35	/** How long a presign may stay unanswered before the upload gives up. */
36	export const PRESIGN_TIMEOUT_MS = 30_000;
37	
38	/** Spacing between outgoing controls: with a ping every two seconds this stays under the observer rate. */
39	export const CONTROL_SPACING_MS = Math.ceil(1000 / (LIMITS.observerMessagesPerSecond - 1));
40	
… (313 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor.ts
```

**Result of `Read`:**

```
1	// The in-page editor (design §5.6): the Mandelbrot source prefilled, the compiler in a lazily
2	// loaded worker, diagnostics with line numbers, a manifest and params form, compile → bundle →
3	// launch through the observer socket, and the drop-a-`.wasm` door. Loaded by host.ts on demand,
4	// so the dashboard never pays for it.
5	import { BUNDLE_PATHS, fsManifest, programManifest } from "@tabframe/protocol";
6	import { type PresignRequester, StoreClient } from "@tabframe/store/client";
7	import { sha256Hex } from "@tabframe/store/hash";
8	import type { WorkerReply, WorkerRequest } from "./compiler-worker.ts";
9	import {
10	  ASC_FLAGS,
11	  assembleSources,
12	  buildBundle,
13	  buildManifest,
14	  type CompileResult,
15	  type Diagnostic,
16	  examples,
17	  fmtBytes,
18	  formatDiagnostic,
19	  type InputRef,
20	  inspectModule,
21	  looksLikeWasm,
22	  MANDELBROT_SOURCE,
23	  MAX_MODULE_BYTES,
24	  type ModuleInfo,
25	  parseParams,
26	  shippedManifest,
27	} from "./editor-core.ts";
28	import { type ClusterState, type ProgramInfo, programList } from "./state.ts";
29	
30	/** What the page gives the editor: the socket's upload and launch paths, and the cluster feed. */
31	export interface EditorHost {
32	  connected(): boolean;
33	  storeBase(): string | null;
34	  presign: PresignRequester["presign"];
35	  launch(bundle: string, params: Record<string, unknown>): boolean;
36	  subscribe(listener: (state: ClusterState) => void): () => void;
37	  /** Where the compiler worker script lives; defaults to `compiler-worker.js` next to this module. */
38	  workerUrl?: string;
39	  /** A demo page: the compile is real, launching needs the live machine (WP7.7). */
40	  demo?: boolean;
… (594 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor-core.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/demo.ts
```

**Result of `Read`:**

```
1	// The editor's pure parts (design §5.6): the virtual filesystem a compile sees, what a compile
2	// reports, the manifest and params the reviewer edits, the bundle a launch uploads, and the
3	// checks on a dropped module. No DOM, no sockets, no compiler: the UI (editor.ts) and the worker
4	// (compiler-worker.ts) are thin around this, and the tests run it under Bun.
5	import {
6	  BUNDLE_PATHS,
7	  canonicalStringify,
8	  type FsManifest,
9	  LIMITS,
10	  type ProgramManifest,
11	  programManifest,
12	} from "@tabframe/protocol";
13	import { validateModuleBytes } from "@tabframe/sandbox/validate";
14	import { sha256Hex } from "@tabframe/store/hash";
15	import {
16	  GUIDE_MARKDOWN,
17	  HELLO_MANIFEST,
18	  HELLO_SOURCE,
19	  MANDELBROT_MANIFEST,
20	  MANDELBROT_SOURCE,
21	  SDK_FILES,
22	  WORDCOUNT_MANIFEST,
23	  WORDCOUNT_SOURCE,
24	} from "./program-sources.generated.ts";
25	
26	/** The compiler flags every program uses; must equal the SDK build's (a test pins it). */
27	export const ASC_FLAGS: readonly string[] = [
28	  "-O3",
29	  "--runtime",
30	  "stub",
31	  "--noAssert",
32	  "--maximumMemory",
33	  "256",
34	];
35	/** The sandbox's reference memory maximum, in 64 KiB pages. */
36	export const MEMORY_PAGES_MAX = 256;
37	
38	/** Where the edited program lives in the virtual filesystem, and where the SDK lives. */
39	export const ENTRY = "program/assembly/index.ts";
40	export const SDK_ROOT = "node_modules/@tabframe/sdk-as";
… (360 more lines)
```

**Result of `Read`:**

```
1	// `?demo=1`: a scripted control plane inside the page. It feeds the reducer the same messages the
2	// wire carries (snapshot, executions, stages, task events, controls, a rotation) and serves real
3	// Mandelbrot tiles from an in-memory store under their true hashes, so the dashboard's fetch and
4	// re-hash path runs unchanged. Nothing here touches the network. Used for screenshots and for
5	// looking at the dashboard without a cluster.
6	//
7	// The machine cycles through three programs: a Mandelbrot frame (the story of dashboard v1: a
8	// straggler, a liar, a scrambled tile, kill half, a rotation), a word count (three stages, a
9	// `bars` result, a filesystem to browse, logs on the tasks), and a broken program whose planner
10	// traps (the failure banner), after which the machine goes to sleep and wakes for the next frame
11	// (the sleep banner). `?program=<name>` starts the cycle there; `?hold=1` pauses after the first
12	// execution ends.
13	import type {
14	  ControlPlaneToObserver,
15	  Counters,
16	  ExecutionView,
17	  FsManifest,
18	  NodeView,
19	  PlaceView,
20	  ProgramView,
21	  Snapshot,
22	  TaskView,
23	} from "@tabframe/protocol";
24	import { canonicalStringify, encodeBars, PROTOCOL_VERSION } from "@tabframe/protocol";
25	import { sha256Hex } from "@tabframe/store/hash";
26	import type { ControlRequest } from "./observer.ts";
27	
28	export const DEMO_CANVAS = { w: 2048, h: 1280 } as const;
29	export const DEMO_TILE = 64;
30	const COLS = DEMO_CANVAS.w / DEMO_TILE;
31	const ROWS = DEMO_CANVAS.h / DEMO_TILE;
32	export const DEMO_TASKS = COLS * ROWS;
33	const SLOTS = 2;
34	const PROGRAM = "9c2f0d6b1e4a7c3f5d8b2a6e0c4f1d7b3a9e5c8f2b6d0a4e8c1f3b7d5a9e2c6f";
35	const WORDCOUNT = "4d3b2a1908f7e6d5c4b3a2918070f6e5d4c3b2a1908f7e6d5c4b3a2918070f6e";
36	const BROKEN = "0f1e2d3c4b5a69788796a5b4c3d2e1f00f1e2d3c4b5a69788796a5b4c3d2e1f0";
37	/** The tile (in completion order) served under its true hash with the wrong bytes. */
38	export const DEMO_SCRAMBLED_AT = 150;
39	/** Why the demo machine sleeps after the broken program: the core's own wording (design §6.8). */
40	export const DEMO_SLEEP_REASON = "an hour without anyone touching the dashboard";
… (1043 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/web && for f in src/editor-page.ts src/tiles.ts src/result.ts src/banners.ts src/compiler.ts src/compiler-worker.ts src/node.ts src/sandbox.ts src/guide-page.ts scripts/build.ts; do echo "=================== $f"; cat -n "$f"; done
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/web/public && for f in index.html editor.html guide.html; do echo "=================== $f"; cat -n "$f"; done
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (39.5KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bwk0oc3c3.txt

Preview (first 2KB):
=================== src/editor-page.ts
     1	// The editor in its own tab (WP6.4): its own observer socket, the same editor as before, and a
     2	// pause on the machine for as long as this tab lives — launching or closing resumes it, and so
     3	// does the control plane by itself when this socket goes away.
     4	import { mountEditor } from "./editor.ts";
     5	import { type MachineState, ObserverClient } from "./observer.ts";
     6	import type { ClusterState } from "./state.ts";
     7	
     8	const $ = <T extends Element>(sel: string): T => {
     9	  const el = document.querySelector<T>(sel);
    10	  if (!el) throw new Error(`missing ${sel}`);
    11	  return el;
    12	};
    13	const machineEl = $<HTMLSpanElement>("#machine");
    14	const pauseEl = $<HTMLSpanElement>("#pauseState");
    15	const listeners = new Set<(state: ClusterState) => void>();
    16	let storeBase: string | null = null;
    17	let client: ObserverClient | null = null;
    18	let paused = false;
    19	
    20	function setMachine(state: MachineState, detail?: string): void {
    21	  machineEl.textContent = detail ? `${state} · ${detail}` : state;
    22	  machineEl.className = `pill ${state === "live" ? "live" : state === "off" || state === "outdated" ? "off" : "wait"}`;
    23	  if (state === "live") {
    24	    // Every live socket asks again: a reconnect (a rotation, a gap) is a new holder.
    25	    client?.send({ t: "pause" });
    26	    paused = true;
    27	    pauseEl.textContent =
    28	      "the machine is paused while this tab is open — in-flight tasks finish, nothing new starts; launch or close to resume";
    29	  } else if (state === "off" || state === "outdated") {
    30	    paused = false;
    31	    pauseEl.textContent = "no machine to pause";
    32	  }
    33	}
    34	
    35	function resume(why: "launch" | "close" = "close"): void {
… (5 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/public/styles.css
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/server.ts
```

**Result of `Bash`:**

```
=================== index.html
     1	<!doctype html>
     2	<html lang="en">
     3	<head>
     4	<meta charset="utf-8">
     5	<meta name="viewport" content="width=device-width, initial-scale=1">
     6	<title>Tabframe</title>
     7	<link rel="stylesheet" href="/styles.css">
     8	</head>
     9	<body>
    10	<header>
    11	  <h1>TABFRAME</h1>
    12	  <span id="machine" class="pill wait">connecting</span>
    13	  <span id="gen" class="pill">gen —</span>
    14	  <span id="nextRotation" class="pill" title="The control plane hands over to a fresh MicroVM every hour; the render continues across it" hidden>rotation in — min</span>
    15	  <span id="counts" class="pill">0 nodes · 0 hosts</span>
    16	  <span id="seq" class="pill">seq 0</span>
    17	  <span id="exec" class="pill">idle</span>
    18	  <span id="loop" class="pill loop" data-loop="running" title="The automatic loop renders frame after frame while someone watches">loop · running</span>
    19	  <span id="rate" class="pill">0.0 tasks/s</span>
    20	  <span class="spacer"></span>
    21	  <button type="button" id="stop" class="danger" title="Ends what is running and holds the automatic loop until Start; a launch of yours still runs at once">stop</button>
    22	  <button type="button" id="start" title="Lets the automatic loop run again" hidden>start</button>
    23	  <button type="button" id="resume" title="Resumes the machine an editor tab holds paused" hidden>resume</button>
    24	  <button type="button" id="freezePanel" class="panel-only" hidden title="Stop this tab from updating so you can read it; updates queue and apply on resume">pause updates</button>
    25	  <a id="backToDashboard" class="panel-only" href="/" hidden title="Back to the dashboard this tab was opened from">← dashboard</a>
    26	  <button type="button" id="openEditor" title="Open the editor in its own tab; the machine pauses while it is open">editor ↗</button>
    27	</header>
    28	<div class="status-line"><div id="machineBanner" class="machine-banner" role="status" hidden></div><div id="notice" class="notice" hidden></div></div>
    29	<main>
    30	  <section class="hero">
    31	    <div id="banner" class="banner" data-state="connecting"><strong id="bannerTitle">Connecting…</strong><span id="bannerBody"></span><span id="bannerHint" class="muted small"></span></div>
    32	    <div id="stage" hidden>
    33	      <div class="execrow">
    34	        <span id="execName">No execution</span>
    35	        <span id="execDetail" class="muted"></span>
    36	        <div class="bar"><div id="progressFill"></div></div>
    37	        <span id="progressText">0/0</span>
    38	        <span class="throughput"><canvas id="throughputChart" width="240" height="36" aria-label="tasks done per second over the last minute"></canvas><span id="throughputFigure">0.0 tasks/s · 0 nodes</span></span>
    39	      </div>
… (186 more lines)
```

**Result of `Read`:**

```
1	:root {
2	  color-scheme: dark;
3	  --bg: #0b0d10;
4	  --panel: #12161b;
5	  --line: #232a33;
6	  --fg: #d7dde5;
7	  --dim: #8a94a3;
8	  --ok: #3ddc84;
9	  --warn: #f5b73d;
10	  --bad: #ff5c5c;
11	  --acc: #6ea8ff;
12	  --mono: ui-monospace, "SF Mono", Menlo, Consolas, monospace;
13	}
14	* {
15	  box-sizing: border-box;
16	}
17	/* biome-ignore lint/complexity/noImportantStyles: the hidden attribute must beat the id and class display rules below */
18	[hidden] {
19	  display: none !important;
20	}
21	body {
22	  margin: 0;
23	  background: var(--bg);
24	  color: var(--fg);
25	  font:
26	    14px / 1.4 system-ui,
27	    sans-serif;
28	}
29	header {
30	  display: flex;
31	  align-items: center;
32	  flex-wrap: wrap;
33	  gap: 0.6rem;
34	  padding: 0.6rem 1rem;
35	  border-bottom: 1px solid var(--line);
36	}
37	header h1 {
38	  font-size: 1rem;
39	  margin: 0 0.4rem 0 0;
40	  letter-spacing: 0.08em;
… (1310 more lines)
```

**Result of `Read`:**

```
1	import { promises as dns } from "node:dns";
2	import { createServer, type IncomingMessage, type Server, type ServerResponse } from "node:http";
3	import type { Socket } from "node:net";
4	import {
5	  adoptLedger,
6	  apply,
7	  beginHandover,
8	  type Clock,
9	  createLedger,
10	  DEFAULT_TASK_LIMITS,
11	  deserializeLedger,
12	  drain,
13	  type Effect,
14	  type Event,
15	  type Ledger,
16	  systemClock,
17	} from "@tabframe/core";
18	import { encode, LIMITS, PROTOCOL_VERSION } from "@tabframe/protocol";
19	import {
20	  HASH_RE,
21	  LocalStore,
22	  MemorySnapshots,
23	  parseRange,
24	  S3Snapshots,
25	  S3Store,
26	  type SnapshotStore,
27	  type StoreDriver,
28	} from "@tabframe/store";
29	import { type WebSocket, WebSocketServer } from "ws";
30	import { resolveBundle } from "./bundles.ts";
31	import type { Config, Role } from "./config.ts";
32	import { type CoreFleet, createCoreFleet } from "./cores.ts";
33	import { HOOK_PREFIX, type HookHost, handleHook, type RunPayload } from "./hooks.ts";
34	import { log } from "./log.ts";
35	import { type DiscoveredProgram, discoverPrograms, seedPrograms } from "./seed.ts";
36	import { type SnapshotStatus, Snapshotter } from "./snapshotter.ts";
37	import { readBody, send, sendJson, serveStatic } from "./static.ts";
38	
39	/** An unshipped program nobody has run in the ledger's memory is retired at seeding after this long. */
40	const STALE_DROP_MS = 60 * 60 * 1000;
… (751 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/control-plane/src && for f in static.ts main.ts hooks.ts snapshotter.ts config.ts seed.ts bundles.ts cores.ts core-node.ts log.ts; do echo "=================== $f"; cat -n "$f"; done
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/node/src && for f in orchestrator.ts tasks.ts session.ts backoff.ts platform/web.ts platform/node.ts; do echo "=================== $f"; cat -n "$f"; done
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe/packages/store/src && for f in index.ts driver.ts hash.ts client.ts local.ts s3.ts snapshots.ts; do echo "=================== $f"; cat -n "$f"; done; echo "=================== package.json"; cat ../package.json
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (33.1KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/by36p6sa9.txt

Preview (first 2KB):
=================== static.ts
     1	import { promises as fs } from "node:fs";
     2	import type { IncomingMessage, ServerResponse } from "node:http";
     3	import path from "node:path";
     4	
     5	const MIME: Record<string, string> = {
     6	  ".html": "text/html; charset=utf-8",
     7	  ".js": "text/javascript; charset=utf-8",
     8	  ".mjs": "text/javascript; charset=utf-8",
     9	  ".css": "text/css; charset=utf-8",
    10	  ".json": "application/json; charset=utf-8",
    11	  ".wasm": "application/wasm",
    12	  ".svg": "image/svg+xml",
    13	  ".png": "image/png",
    14	  ".ico": "image/x-icon",
    15	  ".txt": "text/plain; charset=utf-8",
    16	  ".map": "application/json; charset=utf-8",
    17	};
    18	
    19	const FALLBACK_PAGE = `<!doctype html><meta charset="utf-8"><title>Tabframe</title>
    20	<body style="font:14px system-ui;padding:2rem;background:#111;color:#ddd">
    21	<h1>Tabframe control plane</h1><p>The web bundle is not built. Run <code>mise run build:web</code> or <code>mise run dev</code>.</p>
    22	<p><a href="/health" style="color:#8cf">/health</a></p></body>`;
    23	
    24	/**
    25	 * Serve the web bundle from a directory, refusing anything outside it. Missing directory or
    26	 * file falls back to index.html for the root, or 404.
    27	 */
    28	export async function serveStatic(
    29	  webDir: string | null,
    30	  req: IncomingMessage,
    31	  res: ServerResponse,
    32	): Promise<void> {
    33	  const url = new URL(req.url ?? "/", "http://x");
    34	  let rel = decodeURIComponent(url.pathname);
    35	  if (rel === "/" || rel === "") rel = "/index.html";
… (8 more lines)
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (37.8KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/behm49bxc.txt

Preview (first 2KB):
=================== orchestrator.ts
     1	import {
     2	  type Assign,
     3	  CLOSE,
     4	  type Command,
     5	  type ControlPlaneToNode,
     6	  controlPlaneToNode,
     7	  decode,
     8	  encode,
     9	  type Heartbeat,
    10	  type Hello,
    11	  LIMITS,
    12	  type NodeKind,
    13	  PROTOCOL_VERSION,
    14	  type RotatingReason,
    15	} from "@tabframe/protocol";
    16	import { StoreClient } from "@tabframe/store";
    17	import { Backoff } from "./backoff.ts";
    18	import { type FetchLike, fetchSession, type Session, socketProtocols } from "./session.ts";
    19	import { type SandboxRunner, SocketPresigner, TaskRunner } from "./tasks.ts";
    20	
    21	/** The WHATWG surface the orchestrator needs; browser and Node WebSockets both provide it. */
    22	export interface SocketLike {
    23	  readonly readyState: number;
    24	  send(data: string): void;
    25	  close(code?: number, reason?: string): void;
    26	  onopen: ((ev: unknown) => void) | null;
    27	  onmessage: ((ev: { data: unknown }) => void) | null;
    28	  onclose: ((ev: { code: number; reason: string }) => void) | null;
    29	  onerror: ((ev: unknown) => void) | null;
    30	}
    31	
    32	export interface Timers {
    33	  setTimeout(fn: () => void, ms: number): unknown;
    34	  clearTimeout(handle: unknown): void;
    35	}
… (27 more lines)
```

**Result of `Bash`:**

```
=================== index.ts
     1	export { type FetchLike, type PresignRequester, StoreClient } from "./client.ts";
     2	export type { PresignedUpload, PresignItem, StoreDriver } from "./driver.ts";
     3	export { HASH_RE, hex, hexToBase64, hexToBytes, sha256Hex } from "./hash.ts";
     4	export { LocalStore, parseRange } from "./local.ts";
     5	export { IMMUTABLE, S3Store, signedHeaders } from "./s3.ts";
     6	export { MemorySnapshots, S3Snapshots, type SnapshotStore } from "./snapshots.ts";
=================== driver.ts
     1	/** What the control plane needs from a blob store (design §7.1). */
     2	export interface PresignItem {
     3	  hash: string;
     4	  size: number;
     5	}
     6	
     7	export interface PresignedUpload {
     8	  hash: string;
     9	  /** Absent when the store already holds the bytes. */
    10	  url: string | null;
    11	  /** Headers the client must send exactly as given (the checksum pin is among them). */
    12	  headers: Record<string, string>;
    13	}
    14	
    15	export interface StoreDriver {
    16	  /** Where anyone fetches a blob by hash. */
    17	  urlFor(hash: string): string;
    18	  presign(items: PresignItem[]): Promise<PresignedUpload[]>;
    19	  exists(hash: string): Promise<boolean>;
    20	  get(hash: string): Promise<Uint8Array | null>;
    21	  /** The control plane's own writes: manifests, seeded bundles. Returns the hash. */
    22	  put(bytes: Uint8Array): Promise<string>;
    23	}
=================== hash.ts
     1	/** SHA-256 hex with Web Crypto, which browsers, Node, and Bun all provide. */
     2	export async function sha256Hex(bytes: Uint8Array): Promise<string> {
     3	  const digest = await crypto.subtle.digest("SHA-256", bytes as BufferSource);
     4	  return hex(new Uint8Array(digest));
     5	}
     6	
     7	export function hex(bytes: Uint8Array): string {
     8	  let out = "";
… (417 more lines)
```

> **Thinking.** 

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/editor-page.ts
```

**Result of `Read`:**

```
1	// The editor in its own tab (WP6.4): its own observer socket, the same editor as before, and a
2	// pause on the machine for as long as this tab lives — launching or closing resumes it, and so
3	// does the control plane by itself when this socket goes away.
4	import { mountEditor } from "./editor.ts";
5	import { type MachineState, ObserverClient } from "./observer.ts";
6	import type { ClusterState } from "./state.ts";
7	
8	const $ = <T extends Element>(sel: string): T => {
9	  const el = document.querySelector<T>(sel);
10	  if (!el) throw new Error(`missing ${sel}`);
11	  return el;
12	};
13	const machineEl = $<HTMLSpanElement>("#machine");
14	const pauseEl = $<HTMLSpanElement>("#pauseState");
15	const listeners = new Set<(state: ClusterState) => void>();
16	let storeBase: string | null = null;
17	let client: ObserverClient | null = null;
18	let paused = false;
19	
20	function setMachine(state: MachineState, detail?: string): void {
21	  machineEl.textContent = detail ? `${state} · ${detail}` : state;
22	  machineEl.className = `pill ${state === "live" ? "live" : state === "off" || state === "outdated" ? "off" : "wait"}`;
23	  if (state === "live") {
24	    // Every live socket asks again: a reconnect (a rotation, a gap) is a new holder.
25	    client?.send({ t: "pause" });
26	    paused = true;
27	    pauseEl.textContent =
28	      "the machine is paused while this tab is open — in-flight tasks finish, nothing new starts; launch or close to resume";
29	  } else if (state === "off" || state === "outdated") {
30	    paused = false;
31	    pauseEl.textContent = "no machine to pause";
32	  }
33	}
34	
35	function resume(why: "launch" | "close" = "close"): void {
36	  if (!paused) return;
37	  paused = false;
38	  client?.send({ t: "resume" });
39	  pauseEl.textContent =
40	    why === "launch"
… (70 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/tiles.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/result.ts
```

**Result of `Read`:**

```
1	// The tiles view (design §3 step 3, §5.6): finished tiles are fetched by hash from the store,
2	// re-hashed before they are trusted, and painted into an offscreen canvas at the execution's size.
3	import type { PlaceView } from "@tabframe/protocol";
4	import { sha256Hex } from "@tabframe/store/hash";
5	import { type ClusterState, stageTasks, type TaskState } from "./state.ts";
6	
7	export type TileFlag = "bad-hash" | "bad-size" | "missing";
8	
9	/** Where pixels go. The page backs it with canvases; tests record calls. */
10	export interface TilePainter {
11	  /** A new frame: size the surface and clear it. */
12	  reset(w: number, h: number): void;
13	  put(place: PlaceView, rgba: Uint8ClampedArray): void;
14	  /** Outline a rectangle the dashboard refuses to trust. */
15	  flag(place: PlaceView, flag: TileFlag): void;
16	  /** A retracted or withdrawn tile. */
17	  clear(place: PlaceView): void;
18	}
19	
20	export interface BlobSource {
21	  /** Bytes for a hash, or null when the store has no such blob (yet). */
22	  get(hash: string): Promise<Uint8Array | null>;
23	}
24	
25	export interface TileStats {
26	  fetched: number;
27	  painted: number;
28	  flagged: number;
29	  inFlight: number;
30	}
31	
32	interface Painted {
33	  hash: string;
34	  place: PlaceView;
35	  flag: TileFlag | null;
36	}
37	
38	/** Fetches from `<storeBase>/<hash>` with the cache the CDN allows: blobs are immutable. */
39	export function storeSource(storeBase: string, fetchImpl: typeof fetch = fetch): BlobSource {
40	  const base = storeBase.replace(/\/$/, "");
… (163 more lines)
```

**Result of `Read`:**

```
1	// What an execution produced, read from the store by hash (design §5.1, §5.4): the filesystem
2	// manifest behind `execution.root`, the final output of a `bars` or `text` program, and previews
3	// of the files a person clicks on. Pure functions over bytes; the page fetches.
4	import {
5	  AbiError,
6	  BARS_LIMITS,
7	  type Bar,
8	  BUNDLE_PATHS,
9	  decodeBars,
10	  type FsManifest,
11	  fsManifest,
12	} from "@tabframe/protocol";
13	
14	export interface FileEntry {
15	  path: string;
16	  hash: string;
17	  size: number;
18	}
19	
20	/** Files grouped for the panel: the bundle's own, then each stage's outputs, then everything else. */
21	export interface FileGroup {
22	  /** `/in/`, `/out/<stage>/`, `/` for the bundle's two fixed files, or the first path segment. */
23	  label: string;
24	  files: FileEntry[];
25	  bytes: number;
26	}
27	
28	export function parseManifest(bytes: Uint8Array): FsManifest {
29	  return fsManifest.parse(JSON.parse(new TextDecoder().decode(bytes)));
30	}
31	
32	/** Entries sorted by path, with `/out/<stage>/<index>` sorted numerically inside a stage. */
33	export function listFiles(manifest: FsManifest): FileEntry[] {
34	  return Object.entries(manifest.files)
35	    .map(([path, f]) => ({ path, hash: f.hash, size: f.size }))
36	    .sort((a, b) => comparePaths(a.path, b.path));
37	}
38	
39	export function groupFiles(files: readonly FileEntry[]): FileGroup[] {
40	  const groups = new Map<string, FileGroup>();
… (157 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/banners.ts
```

**Result of `Read`:**

```
1	// Words for the states in which the dashboard is not simply watching a live control plane: what is
2	// happening and what the visitor can do about it (design §6.7, §6.8, §9.4). Pure, so the copy is
3	// unit-tested and the page only places it.
4	import type { MachineState } from "./observer.ts";
5	import type { MachineBanner } from "./state.ts";
6	
7	export interface Copy {
8	  title: string;
9	  body: string;
10	  /** What the visitor can do, or that there is nothing to do. */
11	  hint: string;
12	}
13	
14	/** The detail the observer client attaches to `connecting` after a rotation closed its socket. */
15	export const ROTATING_DETAIL = "control plane rotating";
16	
17	/** The full-page banner while there is no live observer socket. */
18	export function connectionCopy(state: Exclude<MachineState, "live">, detail?: string): Copy {
19	  switch (state) {
20	    case "off":
21	      return {
22	        title: "The machine is off.",
23	        body: "An operator turns it on with mise run up. This page asks the session every few seconds and connects on its own once the machine answers.",
24	        hint: "Meanwhile the demo (?demo=1) shows this dashboard driving a scripted cluster.",
25	      };
26	    case "starting":
27	      return {
28	        title: "Waking the machine…",
29	        body: "A fresh MicroVM is booting the control plane from its snapshot, which takes about a minute; the page connects as soon as it answers.",
30	        hint: "Nothing to do but keep this tab open. Tiles start landing once a node joins — this tab lends one unless it is only observing.",
31	      };
32	    case "outdated":
33	      return {
34	        title: "This page is out of date.",
35	        body: detail
36	          ? `The machine said: ${detail}.`
37	          : "The machine speaks a newer protocol than this page was built for.",
38	        hint: "Reloading…",
39	      };
40	    default:
… (44 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/compiler.ts
```

**Result of `Read`:**

```
1	// The AssemblyScript compiler over an in-memory filesystem (design §5.6). Imported only by the
2	// compiler worker and the tests: asc is 1.6 MB before binaryen, and the dashboard must not pay
3	// for it. The same function runs under Node in the tests that check a page compile is
4	// byte-identical to the build's.
5	import type { DiagnosticMessage } from "assemblyscript";
6	import asc from "assemblyscript/asc";
7	// Types only: the worker bundle must not drag the protocol schemas or the sandbox along.
8	import type { CompileResult, Diagnostic, DiagnosticLevel, VirtualFs } from "./editor-core.ts";
9	
10	const OUT = "program.wasm";
11	
12	/** Normalize what asc asks for: strip `./` segments and join with the base directory. */
13	export function virtualPath(filename: string, baseDir: string): string {
14	  const joined =
15	    baseDir && baseDir !== "." && !filename.startsWith("/") ? `${baseDir}/${filename}` : filename;
16	  const parts: string[] = [];
17	  for (const seg of joined.split("/")) {
18	    if (seg === "" || seg === ".") continue;
19	    if (seg === "..") parts.pop();
20	    else parts.push(seg);
21	  }
22	  return parts.join("/");
23	}
24	
25	const LEVELS: Record<number, DiagnosticLevel> = {
26	  0: "pedantic",
27	  1: "info",
28	  2: "warning",
29	  3: "error",
30	};
31	
32	/**
33	 * asc hands the reporter a plain copy: code, category, message, and a range with byte offsets and
34	 * the source's normalized path — no line numbers. Those come from the virtual file itself.
35	 */
36	function toDiagnostic(d: DiagnosticMessage, fs: VirtualFs): Diagnostic {
37	  let file: string | null = null;
38	  let line: number | null = null;
39	  let column: number | null = null;
40	  if (d.range) {
… (86 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/compiler-worker.ts
```

**Result of `Read`:**

```
1	/// <reference lib="webworker" />
2	// The compiler worker (design §5.6): AssemblyScript's asc, bundled for the browser, loaded only
3	// when the editor opens. Binaryen arrives as its own asset next to this script. One compile at a
4	// time; the page waits.
5	import { COMPILER_VERSION, compileInMemory } from "./compiler.ts";
6	import type { CompileResult, VirtualFs } from "./editor-core.ts";
7	
8	export interface CompileRequest {
9	  type: "compile";
10	  id: number;
11	  fs: VirtualFs;
12	  flags: string[];
13	}
14	export type WorkerRequest = CompileRequest;
15	export type WorkerReply =
16	  | { type: "ready"; version: string }
17	  | ({ type: "compiled"; id: number } & CompileResult);
18	
19	const scope = self as unknown as DedicatedWorkerGlobalScope;
20	
21	scope.onmessage = async (ev: MessageEvent<WorkerRequest>) => {
22	  const msg = ev.data;
23	  if (msg?.type !== "compile") return;
24	  const result = await compileInMemory(msg.fs, msg.flags);
25	  const reply: WorkerReply = { type: "compiled", id: msg.id, ...result };
26	  scope.postMessage(reply);
27	};
28	
29	scope.postMessage({ type: "ready", version: COMPILER_VERSION } satisfies WorkerReply);
30	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/src/guide-page.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/web/scripts/build.ts
```

**Result of `Read`:**

```
1	// The guide in its own page (WP7.5, D4): what a program is and what it may do — the SDK's README
2	// rendered at build time — and the numbers this machine holds a program to. Linked from the top
3	// of the editor as "What is a program?".
4	import { guideMarkdown, limitsSentence, renderGuide } from "./editor-core.ts";
5	
6	const body = document.querySelector<HTMLDivElement>("#guideBody");
7	const limits = document.querySelector<HTMLParagraphElement>("#guideLimits");
8	if (body && body.childElementCount === 0) body.append(renderGuide(guideMarkdown()));
9	if (limits) limits.textContent = limitsSentence();
10	
```

**Result of `Read`:**

```
1	// Bundles the browser entry points that exist and copies public/ into dist/. `--watch` rebuilds on
2	// change. Runs under Bun (the developer toolchain); nothing at runtime depends on it.
3	//
4	// Three bundles come out:
5	//   - the page: host.js, node.js, sandbox.js, editor.js — one file per entry, as before;
6	//   - compiler-worker.js: AssemblyScript's asc for the browser, minified, with its Node-only
7	//     imports left as never-taken dynamic imports and binaryen imported from a sibling file;
8	//   - binaryen.js: binaryen itself, copied as is (13 MB, almost all of it the compiler's own
9	//     WebAssembly, which no minifier shrinks), so the compiler chunk stays small and the browser
10	//     caches the big one separately.
11	import {
12	  copyFileSync,
13	  existsSync,
14	  mkdirSync,
15	  readdirSync,
16	  statSync,
17	  watch,
18	  writeFileSync,
19	} from "node:fs";
20	import path from "node:path";
21	import { generateSources } from "./gen-sources.ts";
22	
23	const root = path.resolve(import.meta.dir, "..");
24	const src = path.join(root, "src");
25	const pub = path.join(root, "public");
26	const dist = path.join(root, "dist");
27	const pageEntries = ["host.ts", "node.ts", "sandbox.ts", "editor-page.ts", "guide-page.ts"]
28	  .map((f) => path.join(src, f))
29	  .filter(existsSync);
30	const compilerEntry = path.join(src, "compiler-worker.ts");
31	const watchMode = process.argv.includes("--watch");
32	
33	/** asc and binaryen touch these only when they detect Node; the browser never runs those imports. */
34	const NODE_ONLY = [
35	  "fs",
36	  "module",
37	  "path",
38	  "url",
39	  "node:fs",
40	  "node:fs/promises",
… (107 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/static.ts
```

**Result of `Read`:**

```
1	import { promises as fs } from "node:fs";
2	import type { IncomingMessage, ServerResponse } from "node:http";
3	import path from "node:path";
4	
5	const MIME: Record<string, string> = {
6	  ".html": "text/html; charset=utf-8",
7	  ".js": "text/javascript; charset=utf-8",
8	  ".mjs": "text/javascript; charset=utf-8",
9	  ".css": "text/css; charset=utf-8",
10	  ".json": "application/json; charset=utf-8",
11	  ".wasm": "application/wasm",
12	  ".svg": "image/svg+xml",
13	  ".png": "image/png",
14	  ".ico": "image/x-icon",
15	  ".txt": "text/plain; charset=utf-8",
16	  ".map": "application/json; charset=utf-8",
17	};
18	
19	const FALLBACK_PAGE = `<!doctype html><meta charset="utf-8"><title>Tabframe</title>
20	<body style="font:14px system-ui;padding:2rem;background:#111;color:#ddd">
21	<h1>Tabframe control plane</h1><p>The web bundle is not built. Run <code>mise run build:web</code> or <code>mise run dev</code>.</p>
22	<p><a href="/health" style="color:#8cf">/health</a></p></body>`;
23	
24	/**
25	 * Serve the web bundle from a directory, refusing anything outside it. Missing directory or
26	 * file falls back to index.html for the root, or 404.
27	 */
28	export async function serveStatic(
29	  webDir: string | null,
30	  req: IncomingMessage,
31	  res: ServerResponse,
32	): Promise<void> {
33	  const url = new URL(req.url ?? "/", "http://x");
34	  let rel = decodeURIComponent(url.pathname);
35	  if (rel === "/" || rel === "") rel = "/index.html";
36	  if (!webDir) {
37	    if (rel === "/index.html") return send(res, 200, "text/html; charset=utf-8", FALLBACK_PAGE);
38	    return send(res, 404, "text/plain", "not found");
39	  }
40	  const root = path.resolve(webDir);
… (59 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/main.ts
```

**Result of `Read`:**

```
1	import { configFromEnv } from "./config.ts";
2	import { log } from "./log.ts";
3	import { createControlPlane } from "./server.ts";
4	
5	const config = configFromEnv();
6	const cp = await createControlPlane(config);
7	log("listening", {
8	  mode: config.mode,
9	  role: cp.role,
10	  generation: cp.generation,
11	  publicPort: cp.publicAddress.port,
12	  privatePort: cp.privateAddress.port,
13	  host: cp.publicAddress.host,
14	});
15	
16	let closing = false;
17	async function shutdown(signal: string): Promise<void> {
18	  if (closing) return;
19	  closing = true;
20	  log("shutdown", { signal });
21	  await cp.close();
22	  process.exit(0);
23	}
24	process.on("SIGTERM", () => void shutdown("SIGTERM"));
25	process.on("SIGINT", () => void shutdown("SIGINT"));
26	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/hooks.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/snapshotter.ts
```

**Result of `Read`:**

```
1	import type { IncomingMessage, ServerResponse } from "node:http";
2	import type { Role } from "./config.ts";
3	import { readBody, sendJson } from "./static.ts";
4	
5	export const HOOK_PREFIX = "/aws/lambda-microvms/runtime/v1/";
6	
7	/** What the fleet function puts in the run-hook payload (design §9.3). */
8	export interface RunPayload {
9	  role: Exclude<Role, "neutral">;
10	  generation: number;
11	  snapshotKey: string | null;
12	  sessionUrl: string | null;
13	  storeBase: string | null;
14	  fleetSecret: string | null;
15	}
16	
17	export interface HookHost {
18	  /** Called on /run with the parsed payload; returns false if the payload is unusable. */
19	  onRun(payload: RunPayload, microvmId: string | null): Promise<boolean>;
20	  /** Called on /validate; runs the in-process self-test and returns true when it passes. */
21	  onValidate(): boolean;
22	  onSuspend(): Promise<void>;
23	  onResume(): Promise<void>;
24	  onTerminate(): Promise<void>;
25	  isListening(): boolean;
26	}
27	
28	/**
29	 * The MicroVM lifecycle hooks (design §9.3). They live on the private port; the proxy never
30	 * routes browser tokens here. Locally they are ordinary routes a dev script can POST to.
31	 */
32	export async function handleHook(
33	  host: HookHost,
34	  name: string,
35	  req: IncomingMessage,
36	  res: ServerResponse,
37	): Promise<void> {
38	  if (req.method !== "POST") return sendJson(res, 405, { error: "POST only" });
39	  switch (name) {
40	    case "ready":
… (66 more lines)
```

**Result of `Read`:**

```
1	// The S3 snapshot writer (design §9.4): the ledger as gzipped JSON every few seconds when it
2	// changed, and on the suspend and terminate hooks; keyed by generation and time, with a `latest`
3	// pointer the fleet reads when the previous control plane cannot hand over itself.
4	import { gunzipSync, gzipSync } from "node:zlib";
5	import { deserializeLedger, type Ledger, serializeLedger } from "@tabframe/core";
6	import type { SnapshotStore } from "@tabframe/store";
7	
8	export const LATEST_KEY = "latest.json.gz";
9	
10	export function snapshotKey(generation: number, at: number): string {
11	  return `g${generation}/${new Date(at).toISOString().replace(/[:.]/g, "-")}.json.gz`;
12	}
13	
14	export interface SnapshotStatus {
15	  writes: number;
16	  lastKey: string | null;
17	  lastAt: number | null;
18	  lastBytes: number;
19	  lastError: string | null;
20	}
21	
22	export class Snapshotter {
23	  private readonly store: SnapshotStore;
24	  private last: string | null = null;
25	  private inFlight: Promise<unknown> | null = null;
26	  readonly status: SnapshotStatus = {
27	    writes: 0,
28	    lastKey: null,
29	    lastAt: null,
30	    lastBytes: 0,
31	    lastError: null,
32	  };
33	
34	  constructor(store: SnapshotStore) {
35	    this.store = store;
36	  }
37	
38	  /** Write when the ledger changed since the last write (or always with `force`). Serialized. */
39	  async write(ledger: Ledger, now: number, force = false): Promise<string | null> {
40	    if (this.inFlight) await this.inFlight.catch(() => undefined);
… (41 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/config.ts
```

**Result of `Read`:**

```
1	/** Process configuration from the environment. Nothing here is secret except the fleet secret. */
2	export type Role = "neutral" | "control-plane" | "core";
3	export type Mode = "local" | "image";
4	
5	export interface Config {
6	  /** `local`: a laptop process that is a control plane at boot, with an in-memory store and an emulated session endpoint. `image`: boots neutral and waits for the /run hook. */
7	  mode: Mode;
8	  publicPort: number;
9	  privatePort: number;
10	  host: string;
11	  generation: number;
12	  /** Where nodes and observers fetch blobs. Locally, the process itself. */
13	  storeBase: string | null;
14	  webDir: string | null;
15	  /** Image mode: the blob bucket the S3 driver writes to (from the image environment). */
16	  blobBucket: string | null;
17	  /** Local mode only: serve `{off: true}` from the emulated session endpoint. */
18	  localOff: boolean;
19	  /** Local mode only: boot neutral and wait for `/run`, the way the image does (`dev:rotate`). */
20	  localNeutral: boolean;
21	  tickMs: number;
22	  /** Where the demo programs live: `/app/programs` in the image, `programs/` in the repo locally. */
23	  programsDir: string | null;
24	  /** The program the machine runs on its own while someone watches (D4). */
25	  defaultProgram: string;
26	  /** Image mode: the bucket the snapshotter writes to. */
27	  snapshotBucket: string | null;
28	  snapshotEveryMs: number;
29	  /** How often the process asks AWS which of the ledger's cores are gone (design §6.8). */
30	  coreCheckMs: number;
31	  /** Image mode: what the control plane needs to launch cloud cores (design §6.8). */
32	  imageArn: string | null;
33	  imageVersion: string | null;
34	  coreRoleArn: string | null;
35	  region: string;
36	  sessionUrl: string | null;
37	}
38	
39	export function configFromEnv(env: NodeJS.ProcessEnv = process.env): Config {
40	  const mode: Mode = env.TABFRAME_MODE === "image" ? "image" : "local";
… (36 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/seed.ts
```

**Result of `Read`:**

```
1	// Seeding (design §5.6, §9.3): the demo programs shipped in the image (or built in the repo) go
2	// into the store on first adopt, each as a bundle — a manifest blob naming the module, the
3	// program manifest, and any inputs — so a seeded program launches exactly like an uploaded one.
4	import { existsSync, readdirSync, readFileSync, statSync } from "node:fs";
5	import path from "node:path";
6	import { DEFAULT_TASK_LIMITS } from "@tabframe/core";
7	import {
8	  BUNDLE_PATHS,
9	  canonicalStringify,
10	  type FsManifest,
11	  type ProgramManifest,
12	  programManifest,
13	} from "@tabframe/protocol";
14	import { validateModuleBytes } from "@tabframe/sandbox";
15	import type { StoreDriver } from "@tabframe/store";
16	
17	export interface DiscoveredProgram {
18	  name: string;
19	  dir: string;
20	  wasm: Uint8Array;
21	  manifestBytes: Uint8Array;
22	  manifest: ProgramManifest;
23	  /** Files under `in/`, as `/in/<file>`. */
24	  inputs: Array<{ path: string; bytes: Uint8Array }>;
25	  /** The program's source (`assembly/index.ts` in the repo, `source.ts` in the image), when present (WP7.6). */
26	  source?: Uint8Array;
27	}
28	
29	export interface SeededProgram {
30	  name: string;
31	  bundle: string;
32	  module: string;
33	  manifest: ProgramManifest;
34	  files: FsManifest["files"];
35	}
36	
37	/**
38	 * Find programs under a directory: `<dir>/<name>/program.wasm` (the image layout) or
39	 * `<dir>/<name>/dist/program.wasm` (the repo layout), with `manifest.json` beside the module.
40	 * Directories without a module are skipped; a bad manifest is an error naming the program.
… (85 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/bundles.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/cores.ts
```

**Result of `Read`:**

```
1	// Resolving an uploaded bundle (design §5.2, §5.5): the ledger only knows hashes, so before an
2	// upload can be launched the process fetches its manifest and module, checks them the way seeding
3	// checks the shipped programs, and reports a program the core can launch — or a refusal a person
4	// can act on.
5	import {
6	  BUNDLE_PATHS,
7	  type FsManifest,
8	  fsManifest,
9	  LIMITS,
10	  type ProgramManifest,
11	  programManifest,
12	} from "@tabframe/protocol";
13	import { validateModuleBytes } from "@tabframe/sandbox";
14	import type { StoreDriver } from "@tabframe/store";
15	
16	export type BundleResolution =
17	  | {
18	      ok: true;
19	      bundle: string;
20	      module: string;
21	      manifest: ProgramManifest;
22	      files: FsManifest["files"];
23	    }
24	  | { ok: false; reason: string };
25	
26	/** Total bytes a bundle's own files may occupy, so an upload cannot fill the store on its own. */
27	export const MAX_BUNDLE_BYTES = 64 * 1024 * 1024;
28	
29	export async function resolveBundle(
30	  store: StoreDriver,
31	  bundle: string,
32	  memoryPagesMax: number,
33	): Promise<BundleResolution> {
34	  const manifestBytes = await store.get(bundle);
35	  if (!manifestBytes) return { ok: false, reason: "the bundle manifest is not in the store" };
36	  let fs: FsManifest;
37	  try {
38	    fs = fsManifest.parse(JSON.parse(new TextDecoder().decode(manifestBytes)));
39	  } catch (err) {
40	    return { ok: false, reason: `the bundle manifest is not a filesystem manifest: ${short(err)}` };
… (46 more lines)
```

**Result of `Read`:**

```
1	// Launching and retiring cloud cores (design §6.8). The control plane holds the only role allowed
2	// to pass the core role, so this lives in the process rather than the fleet functions; the core
3	// decides how many there should be, this decides what to say to AWS.
4	import { egressConnectorArn, ingressConnectorArn } from "@tabframe/fleet/config";
5	import { SdkMicrovmClient } from "@tabframe/fleet/microvm-client";
6	import type { MicrovmClient, MicrovmInfo } from "@tabframe/fleet/types";
7	import { SERVING_STATES } from "@tabframe/fleet/types";
8	
9	/** A core's ceiling: shorter than the control plane's, as a cost fuse (design §9.2). */
10	export const CORE_MAX_DURATION_SECONDS = 4 * 60 * 60;
11	
12	export interface CoreFleetConfig {
13	  imageArn: string;
14	  imageVersion: string | null;
15	  coreRoleArn: string;
16	  region: string;
17	  sessionUrl: string;
18	  storeBase: string;
19	  generation: number;
20	  fleetSecret: string | null;
21	}
22	
23	export interface CoreFleet {
24	  launch(): Promise<string>;
25	  terminate(microvmId: string): Promise<void>;
26	  /** Which of these MicroVMs are no longer serving, so the ledger can forget them. */
27	  gone(microvmIds: string[]): Promise<string[]>;
28	}
29	
30	/** The payload that turns a neutral image into a core (design §9.3). */
31	export function corePayload(config: CoreFleetConfig): string {
32	  return JSON.stringify({
33	    role: "core",
34	    generation: config.generation,
35	    snapshotKey: null,
36	    sessionUrl: config.sessionUrl,
37	    storeBase: config.storeBase,
38	    fleetSecret: config.fleetSecret,
39	  });
40	}
… (36 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/control-plane/src/core-node.ts
```

**Result of `Read`:**

```
1	// A cloud core's node (design §9.3): the same orchestrator a browser tab runs, started inside the
2	// image when the run payload says `role: core`. Its host id names the MicroVM, which is how the
3	// control plane links the node to the core it launched (§6.8).
4	
5	import { Orchestrator, type SocketLike, type Status } from "@tabframe/node";
6	import { createNodeSandboxHost } from "@tabframe/sandbox/adapters/node-host";
7	import { StoreClient } from "@tabframe/store";
8	
9	export interface CoreOptions {
10	  sessionUrl: string;
11	  microvmId: string | null;
12	  log: (event: string, fields?: Record<string, unknown>) => void;
13	  /** Test seam: skip the real sockets. */
14	  start?: boolean;
15	  /** The sandbox worker entry; defaults to `TABFRAME_SANDBOX_WORKER` or the package's source. */
16	  workerFile?: string;
17	}
18	
19	/** Blob reads for the sandbox bridge, straight from the store base the session hands out. */
20	export function blobReaderFor(storeBase: string) {
21	  const reads = new StoreClient(storeBase, {
22	    presign: () => Promise.reject(new Error("a core reads blobs; it uploads through its socket")),
23	  });
24	  return async (hash: string, offset: number, len: number): Promise<Uint8Array | null> => {
25	    if (offset === 0 && !Number.isFinite(len)) return reads.get(hash);
26	    if (Number.isFinite(len)) return reads.get(hash, { offset, length: len });
27	    const whole = await reads.get(hash);
28	    return whole ? whole.slice(offset) : null;
29	  };
30	}
31	
32	export function startCore(opts: CoreOptions): Orchestrator {
33	  const hostId = `core-${opts.microvmId ?? `unknown-${process.pid}`}`;
34	  const workerFile = opts.workerFile ?? process.env.TABFRAME_SANDBOX_WORKER;
35	  const orchestrator = new Orchestrator({
36	    sessionUrl: opts.sessionUrl,
37	    hostId,
38	    kind: "core",
39	    // A 0.5 GB core has a quarter vCPU that bursts to one; one worker is the honest number.
40	    cores: 1,
… (23 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/orchestrator.ts
```

**Result of `Read`:**

```
1	import {
2	  type Assign,
3	  CLOSE,
4	  type Command,
5	  type ControlPlaneToNode,
6	  controlPlaneToNode,
7	  decode,
8	  encode,
9	  type Heartbeat,
10	  type Hello,
11	  LIMITS,
12	  type NodeKind,
13	  PROTOCOL_VERSION,
14	  type RotatingReason,
15	} from "@tabframe/protocol";
16	import { StoreClient } from "@tabframe/store";
17	import { Backoff } from "./backoff.ts";
18	import { type FetchLike, fetchSession, type Session, socketProtocols } from "./session.ts";
19	import { type SandboxRunner, SocketPresigner, TaskRunner } from "./tasks.ts";
20	
21	/** The WHATWG surface the orchestrator needs; browser and Node WebSockets both provide it. */
22	export interface SocketLike {
23	  readonly readyState: number;
24	  send(data: string): void;
25	  close(code?: number, reason?: string): void;
26	  onopen: ((ev: unknown) => void) | null;
27	  onmessage: ((ev: { data: unknown }) => void) | null;
28	  onclose: ((ev: { code: number; reason: string }) => void) | null;
29	  onerror: ((ev: unknown) => void) | null;
30	}
31	
32	export interface Timers {
33	  setTimeout(fn: () => void, ms: number): unknown;
34	  clearTimeout(handle: unknown): void;
35	}
36	
37	export type NodeState =
38	  | "connecting"
39	  | "idle"
40	  | "busy"
… (401 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/tasks.ts
```

**Result of `Read`:**

```
1	import {
2	  type Assign,
3	  encodeRunInput,
4	  type FsManifest,
5	  fsManifest,
6	  LIMITS,
7	  PROTOCOL_VERSION,
8	  RELEASED,
9	  type Result,
10	} from "@tabframe/protocol";
11	import type { HostRequest, TaskResult } from "@tabframe/sandbox";
12	import type { PresignRequester, StoreClient } from "@tabframe/store";
13	
14	/** The sandbox as the orchestrator sees it (design §4.2): one call, one deadline, a kill switch. */
15	export interface SandboxRunner {
16	  run(module: WebAssembly.Module, request: HostRequest, deadlineMs: number): Promise<TaskResult>;
17	  dispose(): void;
18	}
19	
20	export interface TaskRunnerDeps {
21	  store: StoreClient;
22	  createSandbox(): SandboxRunner;
23	  compile(bytes: Uint8Array): Promise<WebAssembly.Module>;
24	  now(): number;
25	  log?: ((event: string, fields?: Record<string, unknown>) => void) | undefined;
26	}
27	
28	const EMPTY_MANIFEST: FsManifest = { version: 1, files: {} };
29	const MAX_CACHED_MODULES = 8;
30	const MAX_CACHED_MANIFESTS = 32;
31	
32	export type Outcome =
33	  | { kind: "result"; msg: Omit<Result, "v" | "gen"> }
34	  | { kind: "dropped"; reason: string };
35	
36	/**
37	 * Runs one task end to end: module and manifest by hash (cached), ABI framing, the sandbox with
38	 * the deadline plus a grace second, then output, writes, and log uploaded through the store so
39	 * the result carries only hashes the store vouches for (design §4.1, §7.3).
40	 */
… (232 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/session.ts
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/backoff.ts
```

**Result of `Read`:**

```
1	/** What the session function (or the local control plane) answers (design §8.1). */
2	export type Session =
3	  | {
4	      kind: "on";
5	      endpoint: string;
6	      token: string;
7	      expiresAt: number;
8	      storeBase: string;
9	      generation: number;
10	    }
11	  | { kind: "off" }
12	  | { kind: "starting"; retryAfterMs: number };
13	
14	export type FetchLike = (
15	  url: string,
16	) => Promise<{ ok: boolean; status: number; json(): Promise<unknown> }>;
17	
18	export async function fetchSession(sessionUrl: string, fetchImpl: FetchLike): Promise<Session> {
19	  const res = await fetchImpl(sessionUrl);
20	  if (!res.ok) throw new Error(`session ${res.status}`);
21	  const body = (await res.json()) as Record<string, unknown>;
22	  if (body.off === true) return { kind: "off" };
23	  if (body.starting === true) {
24	    const retry = typeof body.retryAfterMs === "number" ? body.retryAfterMs : 3_000;
25	    return { kind: "starting", retryAfterMs: retry };
26	  }
27	  const endpoint = body.endpoint;
28	  const token = body.token;
29	  const storeBase = body.storeBase;
30	  const generation = body.generation;
31	  if (
32	    typeof endpoint !== "string" ||
33	    typeof token !== "string" ||
34	    typeof storeBase !== "string" ||
35	    typeof generation !== "number"
36	  ) {
37	    throw new Error("malformed session");
38	  }
39	  const expiresAt =
40	    typeof body.expiresAt === "number"
… (32 more lines)
```

**Result of `Read`:**

```
1	import { LIMITS } from "@tabframe/protocol";
2	
3	/** Exponential backoff with full jitter between the protocol's reconnect bounds. */
4	export class Backoff {
5	  private attempt = 0;
6	  private readonly rng: () => number;
7	
8	  constructor(rng: () => number = Math.random) {
9	    this.rng = rng;
10	  }
11	
12	  next(): number {
13	    const cap = Math.min(LIMITS.reconnectMaxMs, LIMITS.reconnectMinMs * 2 ** this.attempt);
14	    this.attempt = Math.min(this.attempt + 1, 10);
15	    return Math.floor(
16	      LIMITS.reconnectMinMs + this.rng() * Math.max(0, cap - LIMITS.reconnectMinMs),
17	    );
18	  }
19	
20	  reset(): void {
21	    this.attempt = 0;
22	  }
23	}
24	
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/platform/web.ts
```

**Result of `Read`:**

```
1	/// <reference lib="webworker" />
2	/**
3	 * Browser platform: this file is the Web Worker a host page spawns per node. The worker owns the
4	 * socket, the heartbeat, and the sandbox worker; the host only sends init and visibility and
5	 * receives status.
6	 */
7	import { createWebSandboxHost } from "@tabframe/sandbox/adapters/web-host";
8	import { Orchestrator, type SocketLike, type Status } from "../orchestrator.ts";
9	
10	interface InitMessage {
11	  type: "init";
12	  sessionUrl: string;
13	  hostId: string;
14	  visible: boolean;
15	  /** The bundled sandbox worker; defaults to `sandbox.js` next to this worker's script. */
16	  sandboxUrl?: string;
17	}
18	interface VisibilityMessage {
19	  type: "visibility";
20	  visible: boolean;
21	}
22	interface StopMessage {
23	  type: "stop";
24	}
25	export type HostToWorker = InitMessage | VisibilityMessage | StopMessage;
26	export type WorkerToHost = { type: "status" } & Status;
27	
28	const scope = self as unknown as DedicatedWorkerGlobalScope;
29	let orchestrator: Orchestrator | null = null;
30	
31	scope.onmessage = (ev: MessageEvent<HostToWorker>) => {
32	  const msg = ev.data;
33	  switch (msg.type) {
34	    case "init": {
35	      if (orchestrator) return;
36	      const sandboxUrl = msg.sandboxUrl ?? new URL("./sandbox.js", scope.location.href).href;
37	      orchestrator = new Orchestrator({
38	        sessionUrl: msg.sessionUrl,
39	        hostId: msg.hostId,
40	        kind: "tab",
… (29 more lines)
```

**Tool `Read`:**

```
/home/mircea/homework/tabframe/packages/node/src/platform/node.ts
```

**Result of `Read`:**

```
1	/**
2	 * Node platform: one process is one node. Cloud cores run this inside the MicroVM image; the local
3	 * dev topology runs two of them as stand-ins. Status goes to stdout as JSON lines. A close command
4	 * ends the process; whoever supervises it (the fleet policy, the dev runner) brings a fresh one.
5	 */
6	import { hostname } from "node:os";
7	import { createNodeSandboxHost } from "@tabframe/sandbox/adapters/node-host";
8	import { StoreClient } from "@tabframe/store";
9	import { Orchestrator, type SocketLike, type Status } from "../orchestrator.ts";
10	
11	const sessionUrl = process.env.TABFRAME_SESSION_URL;
12	if (!sessionUrl) {
13	  console.error("TABFRAME_SESSION_URL is required");
14	  process.exit(2);
15	}
16	const hostId = process.env.TABFRAME_HOST_ID ?? `core-${hostname()}-${process.pid}`;
17	
18	function emit(event: string, fields: Record<string, unknown> = {}): void {
19	  process.stdout.write(`${JSON.stringify({ at: new Date().toISOString(), event, ...fields })}\n`);
20	}
21	
22	/** The sandbox worker thread reads blobs through the orchestrator's store client. */
23	export function blobReaderFor(storeBase: string) {
24	  const reads = new StoreClient(storeBase, {
25	    presign: () => Promise.reject(new Error("read-only client")),
26	  });
27	  return async (hash: string, offset: number, len: number): Promise<Uint8Array | null> => {
28	    if (offset === 0 && !Number.isFinite(len)) return reads.get(hash);
29	    if (Number.isFinite(len)) return reads.get(hash, { offset, length: len });
30	    const whole = await reads.get(hash);
31	    return whole ? whole.slice(offset) : null;
32	  };
33	}
34	
35	const orchestrator = new Orchestrator({
36	  sessionUrl,
37	  hostId,
38	  kind: "core",
39	  cores: 1,
40	  sandboxVersion: "1",
… (31 more lines)
```

**Tool `Bash`:**

```
cat -n packages/protocol/src/observer.ts | head -250; echo "=========== limits.ts"; cat -n packages/protocol/src/limits.ts; echo "=========== codec.ts"; cat -n packages/protocol/src/codec.ts; echo "=========== codes.ts"; cat -n packages/protocol/src/codes.ts
```

**Result of `Bash`:**

```
     1	import { z } from "zod";
     2	import { LIMITS } from "./limits.ts";
     3	import { envelope, hash, health, millis, nodeId, nodeView, seq } from "./shared.ts";
     4	import { executionView, params, place, queueEntry, taskLog, taskView } from "./task.ts";
     5	
     6	const executionId = z.string().min(1).max(64);
     7	const taskId = z.string().min(1).max(64);
     8	
     9	/** Observer → control plane. */
    10	export const subscribe = z.object({
    11	  t: z.literal("subscribe"),
    12	  ...envelope,
    13	  /** Last sequence number seen, when resubscribing after a gap. */
    14	  since: seq.optional(),
    15	});
    16	
    17	export const ping = z.object({ t: z.literal("ping"), ...envelope });
    18	
    19	/** Bundle uploads presign through the observer socket (design D18). */
    20	export const observerPresign = z.object({
    21	  t: z.literal("presign"),
    22	  ...envelope,
    23	  items: z
    24	    .array(z.object({ hash, size: z.number().int().positive() }))
    25	    .min(1)
    26	    .max(LIMITS.maxPresignItems),
    27	});
    28	
    29	/** Controls (design §6.7, §8.3). Spawn is not a message. */
    30	export const killHalf = z.object({ t: z.literal("killHalf"), ...envelope });
    31	export const freezeHalf = z.object({ t: z.literal("freezeHalf"), ...envelope });
    32	export const throttleHalf = z.object({ t: z.literal("throttleHalf"), ...envelope });
    33	export const resumeAll = z.object({ t: z.literal("resumeAll"), ...envelope });
    34	/** Stop: end the running execution, drop the loop's queued continuations, hold the loop (WP6.1). */
    35	export const stop = z.object({ t: z.literal("stop"), ...envelope });
    36	/** Start: let the loop run again after a stop. */
    37	export const start = z.object({ t: z.literal("start"), ...envelope });
    38	/**
    39	 * Pause (WP6.4): nothing new is assigned or started while the sender's socket lives; in-flight
    40	 * tasks finish. Resume, or the sender going away, lifts it.
… (351 more lines)
```

**Tool `Bash`:**

```
echo "=== selectors used in e2e (id/class/data-attr) ===" && grep -ohE "(#[A-Za-z][A-Za-z0-9_-]+|\[data-[a-z-]+[^]]*\]|\.[a-z][a-z-]+(\.[a-z-]+)*)" e2e/*.ts | sort | uniq -c | sort -rn | head -80; echo; echo "=== textContent / toHaveText assertions on exact strings ==="; grep -nE "toHaveText|toContainText|textContent\(\)" e2e/*.ts | wc -l; grep -nE "toHaveText\(|toContainText\(" e2e/*.ts | head -60
```

**Result of `Bash`:**

```
=== selectors used in e2e (id/class/data-attr) ===
    502 .to
    462 .locator
     93 .click
     42 #machine
     41 .goto
     33 .text
     33 .set
     29 .evaluate
     27 #exec
     25 .wait
     23 #activity
     22 .poll
     22 #launch
     21 #start
     21 #source
     21 .fill
     20 .close
     19 #counts
     18 #programs
     18 .first
     17 #stop
     17 #files
     17 #failure
     16 #redundancy
     16 .query
     16 .get
     15 #example
     14 .then
     14 .stringify
     14 #ledger
     14 #editorStatus
     13 .new
     12 #resume
     12 .not.to
     12 #launchInfo
     12 #killExecution
     12 [data-hash]
     10 .wasm
     10 #spawn1
… (104 more lines)
```

**Tool `Bash`:**

```
grep -rnE "innerHTML|outerHTML|insertAdjacentHTML|document\.write|eval\(|new Function" packages/web/src packages/control-plane/src packages/node/src --include=*.ts | grep -v "\.test\.ts"; echo "--- addEventListener without removal / setInterval in web src ---"; grep -nE "addEventListener|setInterval\(" packages/web/src/*.ts | grep -v test; echo "--- console.* in web/node/cp src ---"; grep -rnE "console\.(log|error|warn)" packages/web/src packages/node/src packages/control-plane/src --include=*.ts | grep -v "\.test\.ts" | head -30
```

**Result of `Bash`:**

```
packages/web/src/editor.ts:209:      els.moduleInfo.innerHTML = "";
--- addEventListener without removal / setInterval in web src ---
packages/web/src/editor-page.ts:59:  $<HTMLElement>("#editor").addEventListener("editor-closed", () => {
packages/web/src/editor-page.ts:95:  $<HTMLElement>("#editor").addEventListener("editor-closed", () => {
packages/web/src/editor-page.ts:99:  window.addEventListener("pagehide", () => resume());
packages/web/src/observer.ts:256:      this.pingTimer = setInterval(() => {
packages/web/src/host.ts:359:els.grid.addEventListener("click", (ev) => {
packages/web/src/host.ts:370:els.tiles.addEventListener("click", (ev) => {
packages/web/src/host.ts:904:document.addEventListener("visibilitychange", () => {
packages/web/src/host.ts:1011:setInterval(() => {
packages/web/src/host.ts:1014:window.addEventListener("resize", scheduleRender);
--- console.* in web/node/cp src ---
packages/web/src/host.ts:876:    worker.onerror = (e) => console.error("node worker error", e.message);
packages/node/src/platform/node.ts:13:  console.error("TABFRAME_SESSION_URL is required");
```

> **Thinking.** 

**Tool `Bash`:**

```
grep -nE "cache|Cache|immutable|no-cache|max-age" packages/infra/lib/web-stack.ts | head -30; echo "--- pausedBy in core ---"; grep -nE "pausedBy|\"pause\"|\"resume\"|loopStopped" packages/core/src/apply.ts | head -40
```

**Result of `Bash`:**

```
--- pausedBy in core ---
365:      ledger.meta.loopStopped = true;
376:    case "pause": {
379:      const held = ledger.meta.pausedBy;
380:      ledger.meta.pausedBy = connId;
382:      return broadcast(ledger, { t: "controlApplied", op: "pause", nodeIds: [] });
384:    case "resume":
387:      ledger.meta.loopStopped = false;
530:    stopped: ledger.meta.loopStopped,
531:    paused: ledger.meta.pausedBy !== null,
676:  if (ledger.meta.pausedBy === connId) effects.push(...resumeMachine(ledger, now));
684:  if (ledger.meta.pausedBy === null) return [];
685:  ledger.meta.pausedBy = null;
686:  const effects = broadcast(ledger, { t: "controlApplied", op: "resume", nodeIds: [] });
```

**Tool `Bash`:**

```
grep -nE "tabframe\.|window\.tabframe" e2e/*.ts | wc -l; grep -nE "toHaveText\(\"" e2e/*.ts | wc -l; grep -nE "since" packages/core/src/observers.ts | head; echo "--- presign effect in core (node) ---"; grep -nE "kind: \"presign\"" packages/core/src/*.ts | head
```

**Result of `Bash`:**

```
5
38
--- presign effect in core (node) ---
packages/core/src/events.ts:58:  | { kind: "presign"; connId: string; items: Array<{ hash: string; size: number }> }
packages/core/src/apply.ts:207:        return [{ kind: "presign", connId, items: d.msg.items }];
packages/core/src/apply.ts:223:      return [{ kind: "presign", connId, items: d.msg.items }];
```

**Tool `Bash`:**

```
timeout 300 bun test packages/web 2>&1 | tail -15
```

**Result of `Bash`:**

```
 packages/sandbox/src/types.ts             |    0.00 |   15.38 | 50-51,55-74
 packages/sandbox/src/validate.ts          |  100.00 |   90.91 | 31,42-45,50,70
 packages/sdk-as/scripts/build-programs.ts |   66.67 |   60.42 | 39-55,60-61
 packages/store/src/client.ts              |    0.00 |    8.93 | 20-26,30-73
 packages/store/src/hash.ts                |   60.00 |   58.82 | 13-15,20-23
 packages/store/src/index.ts               |  100.00 |  100.00 | 
 packages/store/src/local.ts               |   27.27 |   21.67 | 18-34,41-51,55-59,67-80
 packages/store/src/s3.ts                  |    0.00 |    9.02 | 23-32,36,40-111,120-145,149-150
 packages/store/src/snapshots.ts           |    0.00 |   23.26 | 14-20,24,33-57
-------------------------------------------|---------|---------|-------------------

 79 pass
 0 fail
 537 expect() calls
Ran 79 tests across 7 files. [8.82s]
```

> **Thinking.** 

**Tool `Bash`:**

```
sed -n 370,395p packages/core/src/apply.ts; echo "--- web-stack deploy/cache ---"; grep -nE "BucketDeployment|distributionPaths|cachePolicy|CachePolicy|defaultTtl|CACHING|prune|invalidat" packages/infra/lib/web-stack.ts packages/infra/lib/core-stack.ts | head -20; echo "--- subscribe/since in core ---"; grep -nE "since" packages/core/src/apply.ts packages/core/src/observers.ts | head
```

**Result of `Bash`:**

```
          effects.push(...cancelExecution(ledger, queued, "stopped by a person", now));
      }
      effects.push(...broadcast(ledger, { t: "controlApplied", op: "stop", nodeIds: [] }));
      effects.push(...maybeStart(ledger, now), ...fill(ledger, now));
      return effects;
    }
    case "pause": {
      // The editor tab that asked holds the pause; nothing new is assigned or started until it
      // resumes or goes away (WP6.4). Asking twice from the same socket is one pause.
      const held = ledger.meta.pausedBy;
      ledger.meta.pausedBy = connId;
      if (held === connId) return [];
      return broadcast(ledger, { t: "controlApplied", op: "pause", nodeIds: [] });
    }
    case "resume":
      return resumeMachine(ledger, now);
    case "start": {
      ledger.meta.loopStopped = false;
      ledger.meta.loopYielded = false;
      ledger.meta.loopPausedUntil = 0; // a person asked now, not after a hold or a backoff
      ledger.meta.loopBackoffMs = 0;
      const effects = broadcast(ledger, { t: "controlApplied", op: "start", nodeIds: [] });
      effects.push(
        ...ensureDefaultLoop(ledger, now),
        ...maybeStart(ledger, now),
        ...fill(ledger, now),
--- web-stack deploy/cache ---
packages/infra/lib/web-stack.ts:2:// session URL, and the distribution is invalidated. Depends on Core (bucket, distribution) and
packages/infra/lib/web-stack.ts:20:    new deploy.BucketDeployment(this, "Web", {
packages/infra/lib/web-stack.ts:27:      distributionPaths: ["/*"],
packages/infra/lib/web-stack.ts:28:      prune: true,
packages/infra/lib/core-stack.ts:69:        cachePolicy: cf.CachePolicy.CACHING_OPTIMIZED,
packages/infra/lib/core-stack.ts:77:          cachePolicy: cf.CachePolicy.CACHING_OPTIMIZED,
--- subscribe/since in core ---
```

> **Thinking.** 

## Assistant · 2026-09-04T14:24:52.402Z

## Findings

All paths are under `/home/mircea/homework/tabframe/`. Unit tests for `packages/web` pass locally (79/79).

1. `[H] node upload path — packages/node/src/tasks.ts:220-231, packages/control-plane/src/server.ts:297-307, packages/store/src/s3.ts:44-57 — SocketPresigner.presign never times out, and the control plane only logs a failed store.presign (S3Store.presign throws on any non-404 HeadObject error, e.g. SlowDown), so TaskRunner.run awaits putMany forever: running stays set, pump() never runs again, heartbeats continue — the node is a zombie that looks alive until its socket drops; cancel/abort cannot unstick it (they only dispose the sandbox), so one transient S3 error silently removes a core for the life of the connection — add a timeout to SocketPresigner (observer.ts already has PRESIGN_TIMEOUT_MS), have execute() answer a failed presign with an error message so the node reports the task released, and let S3Store.presign fall through to signing when exists() errors.`

2. `[M] editor pause — packages/web/src/editor-page.ts:23-28 — every live transition re-sends pause and rewrites the text to "the machine is paused", including after launch() lifted it — any resubscribe (sequence gap, another observer toggling redundancy → refresh, the hourly rotation) silently re-pauses the machine while the person's launch is running (its next plan task is never assigned) and the tab still says "launched · the pause ended" — keep a holdWanted flag cleared by launch/close; on live send pause only when it is set and keep the launched text.`

3. `[M] silent reconnect — packages/web/src/observer.ts:216-224, 140-152, 115-125 — after a silent resubscribe (resubscribing=true) a failing fetchSession loops through later() without onState("connecting") and resubscribing is cleared only by a full snapshot; send() returns true while disconnected and releaseHeld drops controls older than 10 s without a word — a page whose session function went away after a gap/refresh keeps a green "live" pill, enabled controls and stale numbers, and clicks vanish (walkthrough R2/R8) — clear resubscribing (or emit connecting) on the first failed retry, and report dropped held controls through a callback the host shows as a notice.`

4. `[M] outdated reload loop — packages/web/src/host.ts:427, 871; packages/control-plane/src/static.ts:47-50; packages/web/scripts/build.ts:59 — setMachine("outdated") schedules location.reload() with no once-guard and every node worker reporting outdated schedules another; bundles are not content-hashed and host.js is served immutable, so a stale bundle reloads into itself every 1.5 s and hammers the session function — design §8.4 promises one reload — sessionStorage flag keyed by PROTOCOL_VERSION, after one reload show the banner with a manual hint; hash asset names or serve JS no-cache.`

5. `[M] render-loop cost — packages/web/src/panels.ts:934-940, 823; packages/web/src/state.ts:1131-1142 — render() runs per rAF under load plus every 250 ms; renderLedger rebuilds ledgerRows (scan + sort of all tasks) and JSON.stringifys every row as its change signature — on the ledger tab limit is Infinity, so up to 4096 rows are serialized per frame; renderTask serializes the task history per frame; stageTasks() re-sorts on each of its 3-4 calls per frame — a CPU-bound tab on a busy cluster — derive the signature from (executionId, stage, settled count, last settledAt, cache.version, store) and memoize stageTasks per state object.`

6. `[M] unbounded caches — packages/web/src/demo.ts:297-301, 727; packages/web/src/panels.ts:94-124 — the demo store keeps every tile (640 × 16 KB a frame) and manifest under its hash forever; BlobCache.got never evicts (a manifest per frame on a live dashboard, previews on click) — a demo tab left open grows ~10 MB a frame, the dashboard for hours — drop the previous execution's hashes when a new one starts (keep the pinned/selected ones) or cap by bytes with LRU.`

7. `[M] compile worker hang — packages/web/src/editor.ts:128-174, 231-267, 410-420 — pending compile resolvers are settled only by a compiled reply; onerror after ready rejects an already-resolved promise and leaves pending intact, so a worker crash (binaryen OOM, a thrown import) leaves busy=true and the compile button disabled for the tab's life; void takeFile() also swallows arrayBuffer() rejections — the editor stops working without saying why — reject every pending entry on error/messageerror, add a compile timeout, recreate the worker; catch in the drop handlers.`

8. `[L] demo controls — packages/web/src/host.ts:186-194, 899-900, 918-923; packages/web/src/demo.ts:956-1049 — in ?demo=1 the spawn buttons are live: spawn() posts sessionUrl "", the worker's fetch("") resolves to /node.js, res.json() throws and it retries with backoff forever ("… · connecting"); demo.control() has no case for stop/start/pause/resume, so the header slot does nothing while issue() reports success — contradicts "nothing is sent anywhere" and R2 — grey spawn in the demo with a reason; add the four cases (emit controlApplied and flip the machine flags) or answer with the demo error.`

9. `[L] off-state copy — packages/web/src/observer.ts:226-230 vs packages/web/src/banners.ts:22-24 — connect() returns on off without scheduling later(), but the banner says the page asks the session every few seconds and connects on its own — a visitor told to wait waits forever, and the walkthrough's Off row forbids exactly that — fix the copy ("reload after mise run up"), or poll every 15 s.`

10. `[L] keyboard and a11y — packages/web/src/panels.ts:646-666, 984-987, 323-344; packages/web/src/editor.ts:183-187; packages/web/public/index.html:24-25, 28, 50; packages/web/public/editor.html:62 — file rows, ledger rows, diagnostics and both canvases are onclick on li/tr/canvas with no tabindex, role or key handler; #notice (control echoes, "not sent") has no aria-live; renderQueue's signature includes "waiting N s", so the list and its drop buttons are rebuilt every second and focus is lost; #freezePanel/#backToDashboard keep the hidden attribute while CSS shows them; the file input is hidden inside its label so "choose one" is unreachable — buttons for row actions, aria-live="polite" on #notice, update the age text in place, clear hidden in JS, visually-hide the input.`

11. `[L] test-markup coupling — e2e/dashboard.e2e.ts:86, 107, 109, 112 (38 exact toHaveText strings across e2e/) — assertions on composed copy ("mandelbrot · render · 300/640", "5 nodes · 4 hosts", "299 painted · 1 refused · 0 fetching", "killHalf: n2 core-2 n6") break on any wording change, which the WP7.x reviews make routinely — expose data-* values as #throughputFigure already does (#exec data-phase/done/total, #counts data-nodes/hosts, #tileStats) and assert on those or on window.tabframe.state (used 5×).`

12. `[L] duplication and stale bits — packages/web/src/editor-core.ts:254 vs packages/web/src/result.ts:187 (two fmtBytes: the editor says KB, the files panel KiB for the same blob); host.ts:444 vs panels.ts:79 (fmtTime); packages/control-plane/src/core-node.ts:20 vs packages/node/src/platform/node.ts:23 (blobReaderFor verbatim); editor.ts:209 innerHTML="" (use replaceChildren); editor.ts:611 root.hidden=true then window.close() blanks a tab that was not script-opened; packages/control-plane/src/hooks.ts:43 answers {validated:true} on 503; s3.ts:142-146 comment contradicts the code above it; server.ts:42 MAX_BLOB_BYTES 8 MB vs LIMITS.maxOutputBytes 16 MB (a 10 MB output uploads on AWS, 413s locally); host.ts:967-975 file=/root= query values reach fetch URLs without HASH_RE — one shared fmtBytes/fmtTime/blobReaderFor, replaceChildren, close only when window.opener exists, align the caps, validate the hashes.`

## Summary

The codebase is unusually disciplined for its size: a pure, unit-tested reducer behind the dashboard, no unsafe HTML anywhere (every string is a text node), in-place DOM updates where the caret matters, capped histories and activity logs, and a control plane whose effects are cleanly separated from I/O; the defects are at the seams where a promise can fail to settle or a reconnect replays state. Fix first the node presign hang (#1), which turns one S3 hiccup into a permanently wedged core and undercuts the fault-tolerance story the README leads with; second the editor's re-pause on reconnect (#2), which stalls a person's own launch after any rotation or refresh; third the silent-failure cluster (#3, #4, #9), where the page stays green, loops, or promises polling it does not do. After those, the ledger-tab render cost and the unbounded demo store are the changes that matter for a page left open all day.
