# Session agent-a6e6478b3f0eb1897

Exported from a Claude Code transcript with account ids, endpoints, tokens, presigned URLs,
and addresses scrubbed; tool results are cut to their first lines.

- events: 95 · user messages: 1 · assistant messages: 2 · tool calls: 39 · tool results: 39
- from 2026-09-04T15:33:38.180Z to 2026-09-04T15:43:21.574Z
- scrubbed: nothing

---
## User · 2026-09-04T15:33:38.180Z

You are an expert technical writer reviewing the documentation of the Tabframe repository at /home/mircea/homework/tabframe (a take-home project: a fault-tolerant distributed computer whose cores are browser tabs and AWS Lambda MicroVMs, programs in WebAssembly). This is the SECOND review loop. Read docs/implementation/wp-8.1-review-loop-1.md first: it lists the first loop's findings and what was done, including twelve documentation corrections (the README's stays-up paragraph, counts, a "try it" list, reading rows, "core" defined once; the design record's status line and stale facts, §9 order, the drift log's orphan paragraph; the rationale; runbook rows; the plan's shape; the editor's copy; the guide cut at "Compiling"). Verify those were done well (a rewrite that now contradicts the code is a finding), do not repeat them, and go deeper: the reader is an Anthropic reviewer who reads README.md, then docs/design.md, then docs/rationale.md, docs/runbook.md, docs/walkthrough.md, docs/plan.md, docs/implementation/README.md and a few notes (read wp-8.1-review-loop-1.md and two others), packages/sdk-as/README.md (rendered at /guide.html), and the user-facing strings in packages/web/public/*.html and packages/web/src (host.ts, panels.ts, state.ts, editor.ts). Check concrete claims against the code (numbers, names, commands, paths, limits), vocabulary (walkthrough rule R6: program, execution, frame, stage, task, tile, node, core, host, tab), missing pieces a first-time reader needs, overlong sections, author-only sentences, broken relative links, and anything that would make a careful reviewer doubt the project's rigor. The new loop-one note itself is a document to review: is its verdict table readable and honest?

Rules: do NOT modify any file, do NOT run git commands that write, do NOT run any AWS, aws, cdk, deploy, up, down, or rotate commands, and never use the `tabframe` AWS profile. Reading and grep are fine.

Output (at most 1500 words): a section "## Findings" with at most 12 entries, most important first, each on the form:
`[H|M|L] <document or screen> — <location> — <problem> — <why it matters to the reader> — <concrete rewrite or fix>`
Then "## Summary": three sentences on the documentation's state now and the top three things to fix.

*Attachment: (file)*

*Attachment: (file)*

> **Thinking.** 

## Assistant · 2026-09-04T15:33:43.665Z

I'll start with the loop-one note, then read the docs in the reviewer's order and check claims against the code.

**Tool `Read`:**

```
/home/mircea/homework/tabframe/docs/implementation/wp-8.1-review-loop-1.md
```

**Result of `Read`:**

```
1	# WP8.1 — Review loop 1
2	
3	**Branch** `wp/8.1-review-loop-1` · **Milestone** M8 · **Date** 2026-09-04 · **Ask** Mircea:
4	review the code and critique it from five perspectives — full-stack developer, distributed-systems
5	developer, security engineer, technical writer, DevOps engineer — get the individual feedback,
6	aggregate, critique, incorporate; three loops. This is loop one.
7	
8	## How the loop ran
9	
10	Five independent reviewers (fresh agents, read-only, no AWS access) each returned at most twelve
11	findings with severity, location, cause, and fix. The 52 findings were de-duplicated into the
12	table below; each was accepted, deferred to a later loop, or rejected with the reason (the
13	critique). The accepted ones landed on this branch with tests; CI gates the merge; the next loop
14	reviews the tree with these changes in.
15	
16	## The findings and what was done
17	
18	| # | From | Sev | Finding | Decision |
19	|---|---|---|---|---|
20	| 1 | dist | H | Stage-advancing store effects (inherited root, stage spec, folded manifest) were fire-and-forget: a handover mid-flight or one S3 error wedged the execution for ever. | **Accepted.** `resumePending()` re-derives the pending effect from the ledger on adopt and again every `STORE_RETRY_MS` (`waitingSince` on the execution); a pruned origin now falls back to the bundle with the `expired-root` warning instead of a silent start. |
21	| 2 | dist | H | `encode()` over 64 KB threw through `dispatch`; params and the queue were unbounded. | **Accepted.** `PARAMS_MAX_BYTES` (4 KB canonical) and `QUEUE_CAP` (32) in `enqueue`; a send that cannot be encoded closes that connection instead of throwing. |
22	| 3 | dist | H | A node reported store or network failures as program faults, failing everybody's execution. | **Accepted.** `hostFailure()` covers fetch, upload, presign timeouts and network errors → `RELEASED`; the store client retries three times with backoff. |
23	| 4 | sec | H | The control plane fetched blobs named by untrusted parties without a size check. | **Accepted.** `StoreDriver.get(hash, maxBytes)` (HeadObject first on S3, `BlobTooLarge` otherwise); caps at every fetch: stage spec 1 MB, inherited root 4 MB, bundle manifest 1 MB, program manifest 64 KB, module 8 MB. |
24	| 5 | sec | H | Presign sizes were unbounded and uncounted. | **Accepted.** `size ≤ maxOutputBytes` and at most 64 items per presign in the schemas; a 1 GiB presigned-byte budget per connection in the core. |
25	| 6 | sec | H | A module could declare a second memory without a maximum and grow past the cap. | **Accepted.** `countMemories()`; more than one memory is refused. |
26	| 7 | fs | H | A node's presign never timed out and an S3 HeadObject error failed the whole presign: a zombie core. | **Accepted.** `SocketPresigner` times out after 30 s (the task is released); `S3Store.presign` treats a HeadObject error as "not there" and signs. |
27	| 8 | tw, devops | H | The hourly rule rotated a *suspended* control plane, so an idle night booted a generation an hour and the README's stays-up paragraph was false. | **Accepted.** A scheduled invocation skips a `SUSPENDED` control plane (`skipped-suspended`); the README says what happens now. |
28	| 9 | devops | H | `deploy` ran build and test in parallel from the working tree with no clean-tree or green-main guard and no commit stamp. | **Accepted.** `deploy` is sequential: build → test → `deploy-guard.ts` (clean tree, HEAD on origin/main; `TABFRAME_DEPLOY_UNGATED=1` to skip) → stacks → up. `stage-image.ts` writes `build.json`; `/health` shows `build` and `imageVersion`. |
29	| 10 | devops | H | The rotate function returned failures as successes; nothing alarmed. | **Accepted.** The Lambda entry throws on `failed`; CloudWatch alarms on rotate/session errors and rotate throttles go to an SNS topic mailed to the budget address when configured; the hourly rule's target retries twice within thirty minutes. |
30	| 11 | sec | M | Destructive controls are unauthenticated; kill-half in a loop terminates and relaunches cloud cores. | **Rejected as a change, accepted as a bound.** A public machine anyone may drive is the design (D4, §6.7); a machine-wide cooldown of three seconds per destructive control (`CONTROL_COOLDOWN_MS`) removes the cost loop. |
31	| 12 | fs | M | The editor tab re-sent `pause` on every live socket, including after its own launch. | **Accepted.** `holdWanted` is cleared by launch and close. |
32	| 13 | fs | M | A failing session fetch during a silent resubscribe kept a green pill; held clicks were dropped silently. | **Accepted.** The page goes back to "connecting"; dropped clicks are said in the status line (`onDropped`). |
33	| 14 | fs | M | An outdated page reloaded itself in a loop. | **Accepted.** One reload per protocol version (`sessionStorage`), then a hard-refresh hint. |
34	| 15 | fs | M | The ledger tab serialised every row per frame; the blob cache and the demo store grew without bound. | **Accepted** (ledger signature; a 64 MB LRU `BlobCache`). The demo store's growth is deferred to loop 2. |
35	| 16 | fs | M | A dead compiler worker left the editor unusable. | **Accepted.** Pending compiles are rejected, a two-minute compile timeout, the next compile gets a fresh worker; failed drops say so. |
36	| 17 | dist | M | Between handover and promote a dead rotation left the machine dark for an hour; a predecessor kept acting after handing over. | **Accepted.** `HANDOVER_LEASE_MS` (90 s) returns a control plane to `active` when no drain follows; ticks run no fleet, loop, or assignment while `handing-over` or `drained`. |
37	| 18 | dist | M | Slow `RunMicrovm` calls were asked twice; surplus cores were never trimmed. | **Accepted.** `meta.coreLaunches` counts launches until acknowledged (or a minute passes); surplus unlinked cores are terminated. |
38	| 19 | dist | M | Any node's report could settle or fail any open task; `computeMs` was unbounded; results per task grew without bound. | **Accepted in part.** A report for an attempt the node never held may still verify or contest a settled task (D7, the tests lean on it) but can no longer *fail* a task; `computeMs` is bounded; sixteen records per task. |
39	| 20 | sec | M | Blobs are served on the page's origin with no `nosniff`, no CSP. | **Accepted.** CloudFront response-header policies: nosniff, DENY framing, HSTS, a CSP for the page; `Content-Disposition: attachment` and a sandbox CSP for `blob/*`. |
40	| 21 | sec | M | Results and presigns were exempt from the rate bucket. | **Accepted.** A second bucket (`SOLICITED_RATE` 256/s) for them. |
… (35 more lines)
```

**Tool `Bash`:**

```
ls -la && ls docs docs/implementation packages packages/web/public packages/web/src packages/sdk-as 2>/dev/null && wc -l README.md docs/*.md docs/implementation/*.md packages/sdk-as/README.md packages/web/public/*.html packages/web/src/*.ts 2>/dev/null
```

**Result of `Bash`:**

```
total 228
drwxrwxr-x 14 mircea mircea  4096 Sep  4 08:23 .
drwxrwxr-x  5 mircea mircea  4096 Sep  2 07:02 ..
-rw-rw-r--  1 mircea mircea   787 Sep  2 23:35 biome.json
-rw-rw-r--  1 mircea mircea   910 Sep  1 22:51 bunfig.toml
-rw-r--r--  1 mircea mircea 46043 Sep  3 00:13 bun.lock
-rw-rw-r--  1 mircea mircea   543 Sep  1 21:41 cdk.json
drwxrwxr-x 58 mircea mircea 12288 Sep  4 08:10 cdk.out
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:10 .claude
drwxrwxr-x  2 mircea mircea 20480 Sep  4 08:22 coverage
drwxrwxr-x  4 mircea mircea  4096 Sep  3 22:12 docs
drwxrwxr-x  2 mircea mircea  4096 Sep  4 08:08 e2e
-rw-------  1 mircea mircea   189 Sep  2 15:05 .env.local
drwxrwxr-x  8 mircea mircea  4096 Sep  4 08:27 .git
drwxrwxr-x  3 mircea mircea  4096 Sep  1 21:27 .github
-rw-rw-r--  1 mircea mircea   364 Sep  2 15:19 .gitignore
-rw-rw-r--  1 mircea mircea 34523 Sep  1 21:08 LICENSE
-rw-rw-r--  1 mircea mircea  5998 Sep  4 08:08 mise.toml
drwxr-xr-x  7 mircea mircea  4096 Sep  1 22:51 node_modules
-rw-rw-r--  1 mircea mircea   436 Sep  1 22:51 package.json
drwxrwxr-x 13 mircea mircea  4096 Sep  1 22:52 packages
-rw-rw-r--  1 mircea mircea  1946 Sep  4 07:54 playwright.config.ts
drwxrwxr-x  5 mircea mircea  4096 Sep  2 23:56 programs
-rw-rw-r--  1 mircea mircea 16114 Sep  4 08:19 README.md
drwxrwxr-x  3 mircea mircea  4096 Sep  2 13:37 scripts
drwxrwxr-x  4 mircea mircea  4096 Sep  4 08:26 test-results
-rw-rw-r--  1 mircea mircea   480 Sep  1 21:08 tsconfig.base.json
-rw-rw-r--  1 mircea mircea   294 Sep  3 20:28 tsconfig.json
docs:
design.md
feasibility-transformer.md
implementation
m0-verification.md
m1-verification.md
m2-verification.md
m3-verification.md
plan.md
rationale.md
runbook.md
submission-checklist.md
… (217 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/README.md
```

**Result of `Bash`:**

```
     1	# Tabframe
     2	
     3	A fault-tolerant distributed computer whose cores are browser tabs and Firecracker microVMs,
     4	programmed with WebAssembly. Every worker is a core — a thread in someone's tab, or one of the two
     5	MicroVMs in AWS, the *cloud cores*. Open the page and your tab is a core. Close it and the machine keeps
     6	computing, correctly. The control plane that schedules the work is itself replaced every hour, with
     7	the work in flight.
     8	
     9	Deployed: **https://d2w9z8juw4oo76.cloudfront.net** — the page lends one core when it opens, and
    10	shows the machine rendering a Mandelbrot frame with whoever else is there.
    11	
    12	**Try it, in five clicks.** Spawn 3 (the counters follow), kill half (tiles are taken back and finish
    13	elsewhere), redundancy on (the verified counter moves), editor ↗ (change `CYCLE`, compile, launch:
    14	your program goes ahead of the loop), the ledger tab (hashes, not bytes). `?observe` lends no cores,
    15	`?demo=1` runs a scripted cluster inside the page, and a rotation banner every hour is expected.
    16	
    17	## What it is
    18	
    19	A program is one WebAssembly module with two entry points and a manifest. `plan` turns parameters
    20	and the previous stage's outputs into the next stage's tasks; `run` turns one task's input into
    21	bytes. Both execute on cores — the control plane runs no program code, not even the planner. A
    22	program's only view of the world is a per-execution filesystem of content-addressed blobs: it has
    23	no clock, no randomness, no network, and no failure type anywhere in its API.
    24	
    25	Three programs ship with the machine and go through the same path as anything you write in the
    26	in-page editor: a distributed **Mandelbrot** render (640 tiles of 64×64 per frame, presets that
    27	advance while anyone watches) and a three-stage **word count** over *Moby-Dick* (map by byte range,
    28	reduce by partition, merge to a top-25). You can also compile your own: the editor holds the
    29	Mandelbrot source and the AssemblyScript compiler runs in a browser worker.
    30	
    31	Since M6 a third program ships with the image: **tiny GPT**, an 822 k-parameter character-level
    32	transformer trained on that same corpus, whose whole forward pass runs in WebAssembly on the cores —
    33	one continuation per task, four milliseconds a token, the same bytes on every core
    34	(`docs/feasibility-transformer.md`).
    35	
    36	## Why it looks the way it does
    37	
    38	Three constraints carry the whole fault-tolerance story; everything else follows from them.
    39	
    40	1. **Tasks are idempotent and delivered at least once.** A task may run on two cores, be killed
… (180 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/walkthrough.md; echo ======; cat -n /home/mircea/homework/tabframe/docs/rationale.md
```

**Result of `Bash`:**

```
     1	# The walkthrough: every screen, every state, everything clickable
     2	
     3	A visitor should be able to land anywhere, in any state, and answer three questions without help:
     4	what is the machine doing, what can I do here, and what just happened because of what I did. This
     5	document is the inventory that makes that testable — for each screen, every state it can be in, what
     6	the page must say, what can be clicked and what happens, and what must not be on the screen. It is
     7	the page's contract; `e2e/walkthrough.e2e.ts` drives the machine through the states below and
     8	asserts the sentence, the header's slot, and the exact set of enabled controls, with a screenshot
     9	per state in the test's output. Written for WP7.7 (2026-09-04), from Mircea's third review.
    10	
    11	## The rules the inventory is checked against
    12	
    13	- **R1 · One sentence of state, always.** The status line says what the machine is doing and why,
    14	  in a visitor's words (`machineSentence` in `packages/web/src/state.ts`): "rendering mandelbrot
    15	  e41 · render · 9 nodes", "running your wordcount e42 · map · 4 nodes · the loop waits behind it",
    16	  "stopped by you · nothing runs until Start · a launch of yours still runs at once", "your
    17	  wordcount e42 is done · the result stays · the loop waits for Start or ten quiet minutes",
    18	  "paused · the editor tab is open · in-flight tasks finish, nothing new starts · closing it or
    19	  launching resumes", "idle · the loop starts a frame when someone watches". The demo and an
    20	  observer say what they are first. While the page is not connected the connection banner speaks.
    21	- **R2 · Every control says what it will do, then what it did.** A label and a tooltip before the
    22	  click; after it, an activity line and, for the page that clicked, the same line in the status
    23	  line for five seconds. A click that could not be sent says so instead of being swallowed.
    24	- **R3 · Disabled with a reason, not hidden.** A control that cannot apply now stays where it is,
    25	  greyed, with the reason after a dash in its tooltip ("— not connected yet", "— nothing is
    26	  running", "— an observer lends no cores; open the plain address to lend some"). The only slot that
    27	  swaps its label is Stop / Start / Resume, and it always means "what you can do to the machine
    28	  right now"; the one other label that changes is a program's "launch…", which reads "cancel" while
    29	  its form is open.
    30	- **R4 · Same kind, same look.** Controls that end things (Stop, kill half, kill execution, close
    31	  all mine) share the red outline and sit together; controls that make things share the plain look.
    32	  Links that leave the page carry ↗, and nothing else opens a tab.
    33	- **R5 · A click never moves the page.** Results land in boxes that were already there; lists
    34	  change a row's class instead of being rebuilt; the only scrolling is the visitor's.
    35	- **R6 · One name per thing.** program · execution (e42) · frame (one execution of the loop's
    36	  program) · stage · task · tile · node (a worker: a browser tab's thread or a cloud core) · core (a
    37	  cloud node) · host (a browser) · tab. The pills, the activity log, the tooltips and the docs use
    38	  these words.
    39	- **R7 · Every screen says where you are and how to get back.** Panel tabs and the editor say they
    40	  were opened from the dashboard and link back; the demo says it is a demo in the status line;
… (203 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/runbook.md; echo ======; cat -n /home/mircea/homework/tabframe/docs/implementation/README.md
```

**Result of `Bash`:**

```
     1	# Runbook
     2	
     3	How to operate the deployed Tabframe machine. Every command below runs from the repo root with
     4	`mise`, which sets the AWS profile (`tabframe`, the dedicated account) and the region; nothing here
     5	works with any other profile, by design (`mise run whoami` is the guard every task depends on).
     6	
     7	## What is running
     8	
     9	| Thing | Where | Notes |
    10	|---|---|---|
    11	| Control plane | one Lambda MicroVM, 1 GB, generation-stamped | pointed at by the SSM parameter `/tabframe/pointer`; rotates hourly |
    12	| Cloud cores | up to two 0.5 GB MicroVMs while the machine is awake | launched by the control plane; asleep ten minutes after the last observer leaves |
    13	| Session function | Lambda function URL (public) | vends the endpoint and a shared token; heals when nothing runs |
    14	| Rotate function | Lambda, hourly EventBridge rule | launches the successor, hands over, flips the pointer, drains, terminates |
    15	| Store | S3 behind CloudFront (`/blob/*`) | content-addressed, one-year lifecycle |
    16	| Snapshots | S3, `g<generation>/<time>.json.gz` and `latest.json.gz` | one-day lifecycle |
    17	| Page | S3 behind the same CloudFront distribution | `https://d2w9z8juw4oo76.cloudfront.net` |
    18	
    19	## Everyday
    20	
    21	| Task | Command | What it does |
    22	|---|---|---|
    23	| Is the machine up? | `mise run health` (`-- --cores` asks each cloud core too) | `/health` and `/diag` of the active control plane through the proxy on the private port, masked. Counts, phase, fleet, snapshotter status, a store round trip, memory. |
    24	| Bring it up | `mise run up` | Sets the pointer to *on*, enables the hourly rule, invokes rotate once. Idempotent: a running control plane is rotated, not duplicated. |
    25	| Take it down | `mise run down` | Disables the rule, terminates every MicroVM from our image, writes *off*. The page shows an off screen; the session function heals nothing. **Destructive: every MicroVM is terminated and the page goes dark until `up`.** |
    26	| Deploy | `mise run deploy` | Builds programs, page, and image; runs lint and every test; `cdk deploy --all`; then `up`, which rotates the running control plane onto the new image. A deploy is a rotation. |
    27	| Rotate by hand | `mise run rotate` | One rotation now (same code the hourly rule runs). |
    28	| Watch logs | `mise run logs`, `mise run logs:fleet` | The MicroVM log group (one stream per VM) and the two functions' groups. See *Observability* for what actually lands. |
    29	| Verify | `mise run verify:m1` / `verify:m2` / `verify:m3` | The milestone runbooks against the live machine: a frame through a kill-half, the editor and word count, a rotation under load. Each prints one line per check and exits non-zero on a failure. |
    30	| Simulate | `mise run sim -- --seed N` | The churn simulation, locally; `--long` for the nightly shape. |
    31	
    32	## Deploy, step by step
    33	
    34	1. `main` is green in CI (`mise exec -- gh run list --branch main --limit 1`). This is a rule, not a
    35	   preference (plan §0).
    36	2. `mise run deploy`. The image build takes three to five minutes; the first build after a long
    37	   gap has taken fifty. If CloudFormation reports the image "did not stabilize", check the build
    38	   history — the platform has retried and succeeded on its own once while CloudFormation gave up;
    39	   a plain re-run of `cdk deploy` then completes.
    40	3. `up` at the end of the deploy rotates: the old control plane hands its ledger to the new one,
… (143 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/plan.md
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (49.2KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/b0k3dwcgc.txt

Preview (first 2KB):
     1	# Tabframe — Execution Plan
     2	
     3	**Status:** written 2026-09-01 from [`design.md`](design.md). The design record is the contract; this
     4	plan is the order of work. When the two disagree, fix the design record first, then the plan.
     5	
     6	**Where we are (2026-09-04, M7 closed):** M0–M7 done. M6 landed Mircea's six asks of 2026-09-02 and WP6.8 his first review of the deployed page; M7 answered his third review of 2026-09-03 in seven work packages — the header's one slot with a loop pill, a crisp throughput line, a launch form that keeps the caret, files and ledger rows previewed in the page, an editor that fills the viewport with the guide on its own page, every program in the editor with its source kept in the store, and the walkthrough: `docs/walkthrough.md` as the page's contract (every screen × state × control, eight rules), the sentence of state in the status line, reasons in tooltips instead of hidden controls, and `e2e/walkthrough.e2e.ts` asserting each state. Two deploys (generations 99 and 105) each passed three unattended demo passes; the walkthrough found and fixed a core bug (a trapped task's snapshot could not be decoded by a fresh dashboard). Left for Mircea: the README and rationale read-through, developer hours, the narrated video, the Cost Explorer check, the flip. Post-flip candidates: an EC2 host for the control plane (WP4.6), SIMD for the transformer, sampling with a seed.
     7	
     8	**Shape of the plan:** eight milestones, M0–M7, each ending in a deployable checkpoint, then M8's review loops; M5 is the packaging gate and stays open until the flip. Each milestone
     9	is a set of work packages (WP). A WP is done when its code, its tests, its WP document under `docs/implementation/`, and its doc touch
    10	are merged together and its acceptance line holds. Checkboxes are updated in the same commit that
    11	completes the WP.
    12	
    13	---
    14	
    15	## 0. Ground rules for the build
    16	
...
</persisted-output>
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/packages/sdk-as/README.md; echo ======; cat -n /home/mircea/homework/tabframe/packages/web/public/index.html; echo ======; cat -n /home/mircea/homework/tabframe/packages/web/public/editor.html; echo ======; cat -n /home/mircea/homework/tabframe/packages/web/public/guide.html
```

**Result of `Bash`:**

````
     1	# @tabframe/sdk-as
     2	
     3	The AssemblyScript SDK for Tabframe programs, and the tooling around programs: the build script,
     4	the goldens script, and a minimal Node host.
     5	
     6	A program is one WebAssembly module with two entry points and a manifest (design §5). It sees the
     7	world only through a per-execution filesystem, has no clock, no randomness, and no network, and
     8	its API has no failure type: tasks complete, or the execution fails with a program fault.
     9	
    10	## Writing a program
    11	
    12	```ts
    13	import { readRunInput, readPlanInput, stage, done, emit, ByteWriter, ByteReader, Params, fs, log }
    14	  from "@tabframe/sdk-as/assembly/index";
    15	export { alloc } from "@tabframe/sdk-as/assembly/index";
    16	
    17	export function plan(ptr: usize, len: i32): usize {
    18	  const input = readPlanInput(ptr, len);          // stage, params, hints
    19	  if (input.stage == 0) {
    20	    const s = stage("map").canvas(2048, 1280);
    21	    for (let i = 0; i < 640; i++) s.taskAt(inputBytes(i), x(i), y(i), 64, 64);   // or s.task(bytes)
    22	    return emit(s.toBytes());
    23	  }
    24	  const next = new Params();                      // optional follow-up params
    25	  next.setI32("preset", input.params.getI32("preset", 0) + 1);
    26	  return emit(done(next));                        // or done(null)
    27	}
    28	
    29	export function run(ptr: usize, len: i32): usize {
    30	  const t = readRunInput(ptr, len);               // stage, taskIndex, taskCount, input bytes
    31	  const r = new ByteReader(t.input);
    32	  // ... compute ...
    33	  return emit(outputBytes);                       // a Uint8Array
    34	}
    35	```
    36	
    37	- **Params** are a flat table of key → JSON text. `getString`, `getI32`, `getF64`, `getBool` take a
    38	  fallback; `raw(key)` gives the JSON text (arrays and objects come through untouched). Setters
    39	  write JSON. Keys are sorted when written, so equal tables are equal bytes.
    40	- **Stage** builds what `plan` returns: a name, an optional canvas (for the `tiles` view), and tasks
… (287 more lines)
````

> **Thinking.** 

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/plan.md | sed -n 1,130p
```

**Result of `Bash`:**

````
     1	# Tabframe — Execution Plan
     2	
     3	**Status:** written 2026-09-01 from [`design.md`](design.md). The design record is the contract; this
     4	plan is the order of work. When the two disagree, fix the design record first, then the plan.
     5	
     6	**Where we are (2026-09-04, M7 closed):** M0–M7 done. M6 landed Mircea's six asks of 2026-09-02 and WP6.8 his first review of the deployed page; M7 answered his third review of 2026-09-03 in seven work packages — the header's one slot with a loop pill, a crisp throughput line, a launch form that keeps the caret, files and ledger rows previewed in the page, an editor that fills the viewport with the guide on its own page, every program in the editor with its source kept in the store, and the walkthrough: `docs/walkthrough.md` as the page's contract (every screen × state × control, eight rules), the sentence of state in the status line, reasons in tooltips instead of hidden controls, and `e2e/walkthrough.e2e.ts` asserting each state. Two deploys (generations 99 and 105) each passed three unattended demo passes; the walkthrough found and fixed a core bug (a trapped task's snapshot could not be decoded by a fresh dashboard). Left for Mircea: the README and rationale read-through, developer hours, the narrated video, the Cost Explorer check, the flip. Post-flip candidates: an EC2 host for the control plane (WP4.6), SIMD for the transformer, sampling with a seed.
     7	
     8	**Shape of the plan:** eight milestones, M0–M7, each ending in a deployable checkpoint, then M8's review loops; M5 is the packaging gate and stays open until the flip. Each milestone
     9	is a set of work packages (WP). A WP is done when its code, its tests, its WP document under `docs/implementation/`, and its doc touch
    10	are merged together and its acceptance line holds. Checkboxes are updated in the same commit that
    11	completes the WP.
    12	
    13	---
    14	
    15	## 0. Ground rules for the build
    16	
    17	- **Every session starts the same way:** read `design.md` and this plan, run `mise run whoami`. Nothing touches AWS before the guard passes.
    18	- **AWS identity:** profile `tabframe` only, set by mise and passed explicitly on ad-hoc calls. No fallback to any other profile — stop and ask.
    19	- **Independence first.** The build is optimized for uninterrupted work: I stop and ask only when I genuinely cannot proceed. Everything already agreed in the design record and this plan proceeds without asking — `cdk bootstrap`, the first deploy with the budget, enabling the hourly schedule at M3 included. Non-blocking questions go into the WP report's Open line and work continues.
    20	- **Hard stops** (the only ones): destructive or irreversible actions — deleting stacks, buckets, or data, rewriting pushed history, `mise run down`; spending outside the agreed design or budget; contradicting a decision Mircea explicitly owns; and gates only he can pass, such as flipping the repo public.
    21	- **Transcript hygiene:** commands whose output can contain an account id, token, presigned URL, or address write raw output to the scratchpad; only a masked summary reaches the conversation.
    22	- **Git flow:** a branch per WP, `wp/<m>.<n>-<slug>`, pushed as work progresses. Never commit directly to `main`. When lint and tests are green, I merge the branch into `main` myself with a `--no-ff` merge commit, push, and delete the branch. Commit messages name the WP (`M1/WP1.4 sandbox: deadline kill`). Goldens change in the same commit as the kernel that changed them. History on `main` is never rewritten.
    23	- **WP document:** every WP lands `docs/implementation/wp-<m>.<n>-<slug>.md` on its branch, and no WP is done without it. Sections: **What** was delivered, **How** it works (key modules, interfaces, the shape of the code), **Why** it is built that way (alternatives considered, links to the design sections it implements), **Evidence** (tests, commands, screenshots under `docs/implementation/assets/`), **Drift** recorded in the design record, **Open** items. The chat report is a condensation of this file.
    24	- **Time log:** `docs/timelog.md` gets one line per session with two columns — **developer time** (Mircea's hours) and **total time** (wall-clock of the session, agent included) — starting with the design day (2026-09-01). The rationale states the developer total honestly and shows the total alongside it.
    25	- **Design drift:** any deviation discovered while building is recorded in `design.md` as a dated entry before the code lands, and work continues. Only a drift that contradicts an explicit decision waits for Mircea.
    26	- **CI green before the next phase — hard rule (added 2026-09-02 after a day of red builds nobody looked at).** After every merge to `main` the CI run for that commit is checked (`gh run watch` or `gh run list`) and must pass before the next work package starts; a red `main` is the first thing fixed, and no milestone is tagged or reported as done while CI is red. Local green is not a substitute: CI runs on a fresh checkout with no built artifacts and no AWS, which is exactly what caught the simulation's dependency on `programs/*/dist`.
    27	- **Review mode:** merge-when-green. Each WP merges into `main` when lint and tests pass and is reviewed after the fact, on GitHub or in the WP document. Independent WPs may run in parallel on their own branches; each still merges separately with its own report (Built / Verified / Drift / Open / Next).
    28	
    29	---
    30	
    31	## 1. Dependency map
    32	
    33	```
    34	 protocol ──► core ──► control-plane (local mode) ──► dev topology
    35	     │          │              │
    36	     │          │              └──► store (local driver) ──► S3 driver (deploy)
    37	     │          │
    38	     ▼          ▼
    39	   node ◄── sandbox ◄── sdk-as ◄── programs (mandelbrot, wordcount)
    40	     │
… (90 more lines)
````

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/plan.md | sed -n 130,244p
```

**Result of `Bash`:**

```
<persisted-output>
Output too large (29.7KB). Full output saved to: /home/mircea/.claude/projects/-home-mircea-homework/2f9f4ebc-d551-4417-ad7d-e749c7e0ea1a/tool-results/bt35r27rx.txt

Preview (first 2KB):
   130	- [x] **WP2.2 Word count program.** Map (byte ranges extended to whitespace, eight partitions by fixed hash), reduce (read `/out/0/*`, slice partition, merge), merge (global top-K); corpus chosen here (Moby-Dick from Project Gutenberg via the cache URL or the pglaf mirror, both reachable in preflight; boilerplate stripped), attribution file; goldens against a JavaScript reference count; `bars` view.
   131	- [x] **WP2.3 Launch path.** Bundle upload from the page via presign; `launch` with validation; queue priority (human ahead of automatic); `skip`, `killExecution`; follow-ups of human executions offered via `executionDone` and `runFollowUp`, never auto-enqueued, then back to the default loop; per-execution budgets (task cap, compute-seconds), per-observer launch rate, caps of 256 nodes and 64 observers; program records and `programAdded`; execution failures for trap, over budget, conflict, invalid spec.
   132	- [x] **WP2.4 Editor.** AssemblyScript compiler bundled for the browser with its Node-only imports stubbed (verified in Chromium in preflight: ~7 s load unminified, ~0.4 s compile), minified and split from binaryen, loaded lazily in a worker; prefilled Mandelbrot source with the SDK; diagnostics panel; params editor seeded from manifest defaults; compile → bundle → launch; the drop-a-`.wasm` door.
   133	- [x] **WP2.5 Dashboard v2.** Programs panel, queue view, stage strip, `bars` and `text` views, files panel (browse an execution's filesystem by root), task detail with log and attempts, execution failure surfacing.
   134	- [x] **WP2.6 Playwright v1.** Money shot; editor compile and launch; web sandbox adapter tests. Chromium is installed; three optional font packages missing on the dev machine only affect non-Latin glyphs.
   135	- [x] **WP2.7 Deploy M2.**
   136	
   137	**M2 done when:** an edited program runs on AWS from the page; word count completes with the right top-K; a program fault fails its execution visibly and the machine moves on.
...
</persisted-output>
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/design.md | sed -n 1,250p
```

**Result of `Bash`:**

```
     1	# Tabframe — Design Record
     2	
     3	**Status:** written 2026-09-01 before the build; the sections that name a work package were amended in place (§6.7, §8.3 among them) and everything else is read together with the drift log, §17 (last entry 2026-09-04), whose entries are in merge order.
     4	This document is the source of truth for the build. It supersedes the earlier handoff and seed
     5	documents wherever they differ.
     6	
     7	**One line:** a fault-tolerant distributed computer whose cores are browser tabs and Firecracker
     8	microVMs, programmed with WebAssembly modules, whose control plane holds only metadata and hashes,
     9	and which stays correct while cores — and the control plane itself — come and go.
    10	
    11	---
    12	
    13	## Contents
    14	
    15	1. Decisions register
    16	2. The system in one page
    17	3. Runtime picture
    18	4. Nodes
    19	5. Programs
    20	6. The control plane
    21	7. Data paths and the store
    22	8. The wire
    23	9. Hosting and fleet
    24	10. Security and credentials
    25	11. Tooling and repo
    26	12. Tests and dev loop
    27	13. Deploy, operations, and the reviewer's view
    28	14. Milestones
    29	15. Rationale hooks
    30	16. Glossary
    31	17. Drift log
    32	
    33	---
    34	
    35	## 1. Decisions register
    36	
    37	| # | Decision | Notes |
    38	|---|----------|-------|
    39	| D1 | **Scope:** everything in the original handoff, plus straggler speculation, duplicate-result verification with a built-in redundancy toggle, a program ABI, and MapReduce word count. Time is not a design constraint. | The handoff's 8-hour line did not drive any choice here; §15 says how the rationale owns that. |
    40	| D2 | **General-purpose programs, minimal but complete.** A program is one WASM module (`plan` + `run`) plus a manifest, content-addressed, sandboxed, uploadable, with in-browser AssemblyScript compilation and an execution queue. | Mandelbrot and word count are real programs shipped through the same path; the node bundle contains no application code. |
… (210 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/design.md | sed -n 250,520p
```

**Result of `Bash`:**

```
   250	## 6. The control plane
   251	
   252	### 6.1 Shape
   253	
   254	A pure function `(ledger, event, now) → (ledger', effects)`. Events: node hello, heartbeat, result, disconnect; observer subscribe, control, ping; clock tick; fleet events. Effects: send, close, launch or terminate a core, upload a manifest. The same core runs in the MicroVM, in a local process, and inside a deterministic test with a fake clock.
   255	
   256	### 6.2 Ledger
   257	
   258	| Record | Fields |
   259	|---|---|
   260	| Node | id, connection, host id, lastSeen, health, visible, in-flight attempts, rolling compute stats, kind (tab or core) |
   261	| Observer | connection, lastSeen, last acked sequence |
   262	| Execution | id, bundle hash, params, status, queue position, current stage, **filesystem root hash**, budget counters, inheritedFrom, human-launched flag |
   263	| Task | execution, stage, index, kind (`run` or `plan`), input, status (pending, assigned, done, failed), attempts [{node, assignedAt, deadline, outcome}], output hash, **writes [{path, hash, size}]**, log hash, result identity |
   264	| Program | bundle hash, manifest, uploadedAt |
   265	| Meta | generation, epoch, node cap, redundancy toggle, last snapshot time |
   266	
   267	Result bytes are never in the ledger. A frame is 640 task records; the whole ledger is a few hundred kilobytes.
   268	
   269	### 6.3 Filling a node
   270	
   271	Whenever a node has a free slot the control plane draws from three sources in strict priority:
   272	
   273	1. **Released tasks** — work that belonged to a node now gone, oldest first, so churn never starves anything.
   274	2. **Pending tasks** in stage order, centre outward for tiles.
   275	3. **Overdue tasks** with one open attempt past its deadline: assigning one here creates a speculative twin.
   276	
   277	Speculation is just the lowest-priority source of work; a free slot only reaches it when nothing else is left, which is exactly the end of a stage where stragglers hurt. Health never gates assignment; the deadline is the safety net.
   278	
   279	### 6.4 Liveness, deadlines, health
   280	
   281	Any inbound node message refreshes lastSeen. Gone after four seconds of silence or immediately on socket close. Going gone releases every open attempt the node had (a task with a live twin stays with the twin), closes the connection server-side, and emits `nodeLeft` plus one `taskReassigned` per released task. Observers ping every two seconds; the pong carries the current sequence number.
   282	
   283	Each attempt's deadline is assignedAt plus three times the rolling median compute time, floored at about two seconds. A task never has more than two open attempts.
   284	
   285	Health labels are computed by the control plane: fast or slow against the cluster median; throttled when the host tab reports itself hidden; gone on silence. A change emits `nodeHealth`.
   286	
   287	### 6.5 Results and verification
   288	
   289	A result names hashes the store vouches for. If the task is not done: it becomes done with that result identity, the node is refilled, `taskDone` goes to observers, and a running twin gets `cancel`. If the task is already done: identities are compared; a match counts as a verified duplicate (the live proof that recompute yields identical bytes); a mismatch follows D7. A result is valid for its task whatever attempt produced it, so a thawed node's late result is accepted if the task is still open and verified if not.
… (231 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/design.md | sed -n 520,760p
```

**Result of `Bash`:**

````
   520	- **Application roles**, all created by CDK with least privilege:
   521	
   522	| Role | Trusted by | Can |
   523	|---|---|---|
   524	| Image build | MicroVM image builder | read the artifact bucket, write build logs |
   525	| Control-plane execution | MicroVM runtime | put to the blob bucket (presigning), read/write the snapshot bucket, read the pointer, run/get/list/terminate MicroVMs tagged as cores, pass the core role, write logs |
   526	| Core execution | MicroVM runtime | write logs — nothing else |
   527	| Session function | Lambda | mint endpoint tokens, get one MicroVM, read the pointer, invoke `rotate` |
   528	| Rotate function | Lambda | run/get/list/terminate control planes, mint tokens for handover calls, read/write the pointer, pass the control-plane role, read snapshots |
   529	| CDK roles | CDK bootstrap | conventional; the CloudFormation execution role stays administrator so deploys can create the roles above |
   530	
   531	Build and execution roles trust `lambda.amazonaws.com` for `sts:AssumeRole` and `sts:TagSession`, per the MicroVMs security guide; the control-plane and fleet roles get the `lambda:*Microvm*` actions plus `iam:PassRole` for the roles they launch with. No IAM users or static keys for the application. S3 buckets are private behind CloudFront. The browser never holds AWS credentials: it gets a JWE token scoped to one MicroVM and port 8080 only, shared by every visitor and refreshed every twenty-five minutes, and presigned S3 URLs scoped to one key with a pinned checksum and a short expiry. Privileged paths — hooks, handover, adopt, drain, health, diag — live on port 8081, which browser tokens cannot reach, and additionally require the fleet secret from the run payload.
   532	
   533	### 10.2 Hygiene rules
   534	
   535	- **Repo:** no keys, no account id, no email addresses, no CDK context that embeds the account. `.env.local` is gitignored and holds the profile override, the expected account id, and the budget address.
   536	- **Transcripts** are a deliverable, so no secret, token, presigned URL, or account id may appear in them. Commands whose output could include one write raw output to a scratch file; only a masked summary reaches the conversation.
   537	- **Hard stops only:** destructive or irreversible actions (deleting stacks, buckets, or data; rewriting pushed history; `mise run down`), spending outside the agreed design or budget, and contradicting a decision Mircea explicitly owns. Everything agreed in this record and the plan — bootstrap, the budget deploy, enabling the schedule — proceeds without asking.
   538	- **Budget:** $100/month, notification-only, alerts at 50/80/100 % actual and forecast 100 %, no automatic kill switch; address from `.env.local` (`TABFRAME_BUDGET_EMAIL`), never committed. Manual kill switch: `mise run down`, which turns the machine off until `mise run up` (D20).
   539	
   540	---
   541	
   542	## 11. Tooling and repo
   543	
   544	### 11.1 Runtimes
   545	
   546	Node 22 everywhere in production: the MicroVM image, the fleet Lambdas, CDK. Node 22 runs `.ts` files directly, so the control plane needs no build step locally. Bun is the developer toolchain: workspaces, `bun test`, browser bundles. Nothing at runtime depends on Bun. One coding rule follows: Node strips only erasable TypeScript syntax, so no enums, namespaces, or parameter properties, enforced with `erasableSyntaxOnly` in the base tsconfig. Unit tests run under Bun; process-level tests spawn Node so the runtime under test is the production one. Verified 2026-09-01: Node 22.23 imports `.ts` across workspace packages through `exports` entries that point at sources.
   547	
   548	### 11.2 mise.toml
   549	
   550	```toml
   551	[tools]
   552	node = "22"
   553	bun = "1.3"
   554	aws-cli = "latest"
   555	gh = "latest"
   556	"npm:aws-cdk" = "latest"
   557	
   558	[env]
   559	AWS_PROFILE = "tabframe"
… (201 more lines)
````

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/docs/design.md | sed -n 760,967p
```

**Result of `Bash`:**

```
   760	  `"TFBR" u32 version | u32 count | count × (str label | f64 value)`, values finite (§5.1, §5.3).
   761	  Word count's map tasks own the words that *start* inside their byte range — they skip a word
   762	  straddling the start and read past the end to finish one straddling the end — which is the
   763	  precise form of "extended to whitespace on both ends" (§5.6). The corpus is normalized at build
   764	  time beyond stripping the boilerplate: typographic apostrophes, quotation marks, dashes, and a
   765	  few accented letters become ASCII, and the edition's transcriber's notes go; the word rule is a
   766	  byte rule (ASCII letters and apostrophes) and non-ASCII bytes separate words. The attribution
   767	  file ships inside the bundle as `/in/ATTRIBUTION.txt`.
   768	
   769	- **2026-09-02 (WP2.4).** The in-page compiler is asc bundled with Bun for the browser, minified
   770	  (1.6 MB), its Node-only imports left as dynamic imports that a worker never takes; binaryen is
   771	  served **as is** as a sibling asset (13.6 MB, almost all of it the compiler's own WebAssembly —
   772	  a minifying pass made it larger), not minified as §5.6 planned. Both load in a module worker
   773	  only when the editor opens. A page compile is byte-identical to the build's (pinned under Bun
   774	  and in Chromium). A page-built bundle is the manifest blob seeding produces, except its
   775	  manifest is compact JSON, so the same program uploaded from the page and shipped in the image
   776	  are two bundles and two program records (§5.6, §7.3). `@tabframe/sandbox` exports a
   777	  `./validate` subpath for the page.
   778	- **2026-09-03 (WP1.9).** Found by the churn simulation. Results and presigns do not count
   779	  against the per-node message bucket at all: they answer assignments, which `maxInFlight` already
   780	  paces (§8.4; WP1.7 had raised the bucket to 1000 a second for the same reason). Agreement means
   781	  two nodes (§6.5, D7): a repeat report from the same node in a round adds no evidence, the second
   782	  attempt of a round never goes to a node that already reported, and the vote after two contested
   783	  rounds counts distinct nodes; with the toggle on, a task on a cluster of one waits for a second
   784	  node. Once a stage is folded, or a plan task's spec consumed, its results are sealed: a later
   785	  mismatch is announced and counted but withdraws nothing, since the manifest that holds the
   786	  output has already advanced the stage (§6.5, §6.6). Two more invariants (§6.10): a node holds
   787	  only work of the running execution, and an ended execution leaves nothing pending or assigned.
   788	  Snapshot pages are packed by bytes as well as by row count, since 256 rows of done tiles exceed
   789	  the message cap (§8.3). A result closes the attempt it names; a report for another attempt of
   790	  the same task is evidence only, so a stale report never closes a newer attempt (§6.5). D7's
   791	  "recompute from scratch" goes to nodes that have not reported on the task whenever one has a
   792	  free slot; a tie after two contested rounds is not a majority and starts another round, up to Freeze is terminal on the node **record** too: a later `throttleHalf` does not downgrade a frozen node to throttled, since `fill` must keep skipping a worker that computes nothing until the silence window declares it gone (§4, §6.7). - **2026-09-02 (WP3.3).** A cloud core names itself `core-<microvmId>`, which is how the ledger links a node to the core it launched; no registration message (§6.8). Core records travel in the ledger and are inherited at adopt with their node links cleared. Cores launch with **no ingress connector** and no idle policy. The fleet policy is gated by `config.cloudCores`, true only for the MicroVM image with an image ARN, a core role, and a session URL — a laptop wakes and sleeps but has no fleet (§6.8, §12). - **2026-09-02 (WP3.4).** Local mode can boot neutral (`TABFRAME_LOCAL_NEUTRAL`) so `dev:rotate` drives the real run hook and the real rotate handler with control-plane processes standing in for MicroVMs (§12). A run payload with an empty `storeBase` leaves a control plane serving blobs from its own port; locally each generation has its own in-memory store, so a rotation loses earlier blobs and the local test rotates within one stage. On AWS the store is shared and this does not arise (§7.1). - **2026-09-02 (WP3.5).** A bundled image cannot spawn itself as a worker thread: the sandbox worker is bundled as a second entry point, staged beside `main.js`, and named to the process by `TABFRAME_SANDBOX_WORKER` (§4.2, §11.3). The fleet's private-port calls retry on 429 and 5xx (§9.4). **Measured:** one client holds about 16 concurrent sockets through a MicroVM endpoint before it answers 429, and open sockets crowd out the fleet's own requests to the same endpoint — so the ledger's 256-node cap is not the binding constraint (§8.4, §9.1; `docs/m3-verification.md`). A rotation with a render in flight costs about 8.4 s of churn. - **2026-09-02 (WP2.5).** The `taskDone` observer event and the task view carry an optional `log` (inline text or a blob hash), the shape the node already reports to the control plane; the dashboard renders it when present and says plainly when the wire did not carry one (§8.3). The `bars` view draws the single output of the last stage; a last stage with several outputs is listed in the files panel instead (§5.1). The files panel browses any root the execution has had, not only the current one; roots are the stage strip's links (§5.4). The browser suites run on one Playwright worker: they share a local control plane and, since WP2.3, an upload really launches (§12). - **2026-09-02 (WP2.7).** Seeding is idempotent by bundle hash rather than "only when the ledger has no programs": a control plane that adopts a predecessor's ledger still adds programs the image ships that the ledger does not have, which is what lets a deploy-as-rotation deliver a new program (§5.6, §9.4). - **2026-09-02 (WP4.5).** The endpoint ceiling is explained: **16 concurrent connections per MicroVM**, the account's non-adjustable *Concurrent connections per 2 vCPU MicroVM* quota, the same at 512 MiB and 6 GB, per MicroVM rather than per token, process, or source (§9.7). The M0 record's "250 sustained sockets" was a counting error — that script attached its close handlers after the loop and never heartbeated during it, so the sockets it counted as open had already been declared gone; about sixteen were alive. `docs/m0-verification.md` carries the correction. The account's Lambda concurrency increase to 1000 was granted; the design's "default of 10" wording is historical. Scaling to thousands of clients is an architecture decision, not a tuning one; the options are in §9.7 and the plan's WP4.6. - **2026-09-02 (WP4.4).** The demo script runs unattended against the deployed machine (`mise run demo`), and its first runs fixed the fleet policy: a killed or frozen cloud core has its MicroVM terminated with the command, and a core with no node for two minutes — never said hello, or its node left — is retired and replaced (§6.8). Before, a `kill half` that picked a core left a live, unlinked MicroVM that the policy (which counts records) never replaced. Seeding also retires unshipped drops nobody has run in the ledger's memory after an hour, so runbook uploads do not clutter the program list; the snapshot diet clears file maps on their own, not only with the tasks.
   793	- **2026-09-03 (WP6.5).** A third shipped program, `tinygpt` (§5.6): a 822 k-parameter
   794	  character-level GPT trained offline on the corpus, int8 weights as a bundle input, the forward
   795	  pass in AssemblyScript, one continuation per task and a collect stage for the text view; 4 ms per
   796	  token, token-for-token equal to the PyTorch reference, deterministic bytes. The feasibility
   797	  numbers are in `docs/feasibility-transformer.md`.
   798	
   799	- **2026-09-03 (WP6.7).** The "asleep" banner at the start of a rotation and the half-hour-old
… (168 more lines)
```

**Tool `Bash`:**

```
cat -n /home/mircea/homework/tabframe/mise.toml; echo =====; ls /home/mircea/homework/tabframe/docs/transcripts | head; ls /home/mircea/homework/tabframe/programs /home/mircea/homework/tabframe/programs/*/ ; ls /home/mircea/homework/tabframe/e2e /home/mircea/homework/tabframe/packages/infra/scripts /home/mircea/homework/tabframe/scripts
```

**Result of `Bash`:**

```
     1	# Tabframe — tooling, environment, and tasks. Everything installs through mise.
     2	[tools]
     3	# Pinned (WP8.1): the deploy path runs under an admin identity, so the CLIs it runs are exact.
     4	node = "22.23.2"
     5	bun = "1.3.13"
     6	aws-cli = "2.36.37"
     7	gh = "2.97.0"
     8	"npm:aws-cdk" = "2.1139.0"
     9	
    10	[env]
    11	AWS_PROFILE = "tabframe"
    12	AWS_REGION = "us-west-2"
    13	_.file = ".env.local" # gitignored: TABFRAME_ACCOUNT_ID, TABFRAME_BUDGET_EMAIL
    14	
    15	[tasks.install]
    16	description = "Install workspace dependencies and the Playwright browser"
    17	run = ["bun install", "bunx playwright install chromium"]
    18	
    19	[tasks.whoami]
    20	description = "Assert the AWS identity is the Tabframe account; every AWS task depends on it"
    21	run = "node packages/infra/scripts/whoami.ts"
    22	
    23	[tasks.lint]
    24	description = "Biome lint and format check, then a TypeScript type check"
    25	run = [
    26	  "bunx biome check .",
    27	  "bunx tsc -p tsconfig.json --noEmit",
    28	  "bunx tsc -p packages/web/tsconfig.json --noEmit",
    29	  "bunx tsc -p packages/infra/tsconfig.json --noEmit",
    30	]
    31	
    32	[tasks.test]
    33	description = "Unit and integration tests, then browser tests"
    34	depends = ["lint"]
    35	run = ["bun test", "bunx playwright test --pass-with-no-tests"]
    36	
    37	[tasks.dev]
    38	description = "Local topology: control plane, local store, two local cores, web bundles in watch mode"
    39	run = "node packages/dev/src/up.ts"
    40	
… (186 more lines)
```

> **Thinking.** 

**Tool `Bash`:**

```
git log --oneline -12 && echo ---- && git log --oneline -6 -- docs/design.md && echo ---- && git status --short | head -20
```

**Result of `Bash`:**

```
b12f8e6 Merge WP8.1: review loop 1 — 40 findings from five personas landed
20dd4c8 WP8.1: review loop 1 — 40 findings from five personas landed
8bfc911 Merge M7 close: plan, time log, rationale total, transcripts; the end-to-end test waits for the seeded program by name
183e8cb CI: the end-to-end test waits for programAdded by name (the program field is the bundle hash)
4f7ced7 M7 close: plan 'Where we are', deploy 2 recorded, time log (33.6 h build), rationale total, transcripts re-exported
295a466 Merge WP7.7: the walkthrough, the sentence of state, reasons not disappearances, a core fix for trapped tasks
9a04588 WP7.7: the walkthrough — the page's contract, the sentence of state, reasons not disappearances, ways back, an honest editor; a core fix for trapped tasks
58d39a6 Merge WP7.6: every program in the editor, with its source
958784c WP7.6: every program in the editor, with its source (manifest.source in the store)
0bd3f33 Merge WP7.5: the editor fills the viewport; the guide is its own page
060604e CI: the end-to-end Mandelbrot test waits for the seeded program when the first snapshot beats the seeder
abb2aa9 WP7.5: the editor fills the viewport; the guide is its own page behind 'What is a program?' at the top
----
20dd4c8 WP8.1: review loop 1 — 40 findings from five personas landed
9a04588 WP7.7: the walkthrough — the page's contract, the sentence of state, reasons not disappearances, ways back, an honest editor; a core fix for trapped tasks
958784c WP7.6: every program in the editor, with its source (manifest.source in the store)
abb2aa9 WP7.5: the editor fills the viewport; the guide is its own page behind 'What is a program?' at the top
80bdde9 WP7.4: files and ledger rows show their bytes in the page; a click changes a class, not the list
e92236f WP7.3: the programs panel keeps its rows and launch forms in place; typing params keeps the caret
----
```

**Tool `Bash`:**

```
awk 'NR==792 {print length($0)}' docs/design.md && grep -n "up to Freeze" docs/design.md | cut -c1-80 && echo ---- && git log --format='%h %ad %s' --date=short -S'up to Freeze is terminal' -- docs/design.md && echo ---- && for c in $(git log --format=%h -3 -- docs/design.md); do echo "== $c"; git show $c:docs/design.md | grep -n "starts another round, up to" | cut -c1-120; git show $c:docs/design.md | grep -c "^- \*\*2026"; done
```

**Result of `Bash`:**

```
4446
792:  free slot; a tie after two contested rounds is not a majority and starts a
----
20dd4c8 2026-09-04 WP8.1: review loop 1 — 40 findings from five personas landed
----
== 20dd4c8
792:  free slot; a tie after two contested rounds is not a majority and starts another round, up to Freeze is terminal o
35
== 9a04588
792:  free slot; a tie after two contested rounds is not a majority and starts another round, up to
41
== 958784c
790:  free slot; a tie after two contested rounds is not a majority and starts another round, up to
40
```

**Tool `Bash`:**

```
grep -rn "CONTROL_COOLDOWN_MS\s*=\|STORE_RETRY_MS\s*=\|HANDOVER_LEASE_MS\s*=\|PARAMS_MAX_BYTES\s*=\|QUEUE_CAP\s*=\|SOLICITED_RATE\s*=\|YIELD_IDLE_MS\s*=\|KEEP_ENDED_TASKS\s*=\|DEADLINE_FLOOR\|DEADLINE_MULT\|GONE_MS\s*=\|NODE_CAP\s*=\|OBSERVER_CAP\s*=\|SNAPSHOT_INTERVAL\|SLEEP_MS\s*=\|IDLE_MS\s*=\|NO_OBSERVER\|maxNodes\s*[:=]\|maxObservers\s*[:=]\|PRESIGN_BUDGET\|MAX_RESULTS_PER_TASK\|RECORDS_PER_TASK" packages/core/src packages/protocol/src packages/control-plane/src packages/node/src 2>/dev/null | grep -v test | head -40
```

**Result of `Bash`:**

```
packages/core/src/fleet.ts:23:export const SLEEP_AFTER_NO_OBSERVER_MS = 10 * 60 * 1_000;
packages/core/src/fleet.ts:35:  if (now - ledger.meta.lastObserverAt >= SLEEP_AFTER_NO_OBSERVER_MS) {
packages/core/src/executions.ts:502:export const PARAMS_MAX_BYTES = 4096;
packages/core/src/executions.ts:503:export const QUEUE_CAP = 32;
packages/core/src/executions.ts:505:export const STORE_RETRY_MS = 10_000;
packages/core/src/executions.ts:698:export const YIELD_IDLE_MS = 10 * 60 * 1000;
packages/core/src/executions.ts:804:export const KEEP_ENDED_TASKS = 2;
packages/core/src/apply.ts:147:export const HANDOVER_LEASE_MS = 90_000;
packages/core/src/apply.ts:259:export const SOLICITED_RATE = 256;
packages/core/src/apply.ts:263:export const CONTROL_COOLDOWN_MS = 2_000;
packages/core/src/index.ts:29:  SLEEP_AFTER_NO_OBSERVER_MS,
```

**Tool `Bash`:**

```
grep -rn "median\|floor" packages/core/src/deadline*.ts packages/core/src/*.ts 2>/dev/null | grep -in "deadline\|floor" | grep -v test | head -12; echo ----; grep -rn "TABFRAME_IMAGE_VERSION\|skipped-suspended\|SUSPENDED\|FIVE_MIN\|MIN_ROTATION\|youngerThan\|tooYoung" packages/fleet/src/*.ts | grep -v test | head -20
```

**Result of `Bash`:**

```
1:packages/core/src/harness.ts:179:      place: { x: (i % 8) * 8, y: Math.floor(i / 8) * 8, w: 8, h: 8 },
2:packages/core/src/executions.ts:753:    const j = Math.floor(rng() * (i + 1));
3:packages/core/src/bytes.ts:23:  const out = new Uint8Array(Math.floor((clean.length * 3) / 4));
4:packages/core/src/ledger.ts:264:  /** Deadline floor and multiplier (design §6.4). */
5:packages/core/src/scheduler.ts:7:/** Median of the execution's recent compute samples times the factor, floored (design §6.4). */
6:packages/core/src/scheduler.ts:12:  const median = sorted[Math.floor(sorted.length / 2)] ?? 0;
7:packages/core/src/scheduler.ts:13:  return Math.max(ledger.config.deadlineFloorMs, Math.round(median * ledger.config.deadlineFactor));
9:packages/core/src/scheduler.ts:277:  const median = sorted[Math.floor(sorted.length / 2)] ?? 0;
10:packages/core/src/scheduler.ts:281:      node.ewmaMs > 2 * median && node.ewmaMs > ledger.config.deadlineFloorMs / 4 ? "slow" : "fast";
----
packages/fleet/src/microvm-client.ts:29:  "SUSPENDED",
packages/fleet/src/config.ts:80:    imageVersion: env.TABFRAME_IMAGE_VERSION ?? null,
packages/fleet/src/rotate.ts:60:  | { action: "skipped-suspended" }
packages/fleet/src/rotate.ts:281:    if (scheduled && running?.state === "SUSPENDED") {
packages/fleet/src/rotate.ts:288:      return { action: "skipped-suspended" };
packages/fleet/src/types.ts:7:  | "SUSPENDED"
packages/fleet/src/types.ts:15:  "SUSPENDED",
```

**Tool `Bash`:**

```
grep -rn "memoryMiB\|memory:\s*[0-9]\|1024\|512\|expiration\|Duration.days\|lifecycleRules\|prefix:\|RETAIN\|DESTROY\|BudgetsBudget\|budgetName\|alarm-test\|tabframe-alarms\|logRetention\|RetentionDays" packages/infra/lib/*.ts packages/infra/src/*.ts 2>/dev/null | grep -v test | head -40; ls packages/infra packages/infra/lib 2>/dev/null
```

**Result of `Bash`:**

```
packages/infra/lib/web-stack.ts:29:      memoryLimit: 512,
packages/infra/lib/fleet-stack.ts:59:    const logRetention = cdk.aws_logs.RetentionDays.TWO_WEEKS;
packages/infra/lib/fleet-stack.ts:61:      logRetention,
packages/infra/lib/fleet-stack.ts:98:      logRetention,
packages/infra/lib/core-stack.ts:31:        removalPolicy: cdk.RemovalPolicy.DESTROY,
packages/infra/lib/core-stack.ts:41:      removalPolicy: cdk.RemovalPolicy.RETAIN,
packages/infra/lib/core-stack.ts:43:      lifecycleRules: [{ id: "expire-one-year", expiration: cdk.Duration.days(365) }],
packages/infra/lib/core-stack.ts:58:      removalPolicy: cdk.RemovalPolicy.RETAIN,
packages/infra/lib/core-stack.ts:63:      lifecycleRules: [{ id: "expire-one-day", prefix: "g", expiration: cdk.Duration.days(1) }],
packages/infra/lib/core-stack.ts:82:          accessControlMaxAge: cdk.Duration.days(365),
packages/infra/lib/core-stack.ts:158:          budgetName: "tabframe-monthly",
packages/infra/lib/names.ts:6:  alarmTopic: "tabframe-alarms",
packages/infra/lib/image-stack.ts:44:      retention: cdk.aws_logs.RetentionDays.ONE_WEEK,
packages/infra/lib/image-stack.ts:45:      removalPolicy: cdk.RemovalPolicy.DESTROY,
packages/infra/lib/image-stack.ts:120:      resources: [{ minimumMemoryInMiB: 1024 }],
packages/infra:
bin
image
image-dist
lib
node_modules
package.json
scripts
test
tsconfig.json

packages/infra/lib:
base-image.ts
core-stack.ts
fleet-stack.ts
image-stack.ts
names.ts
web-stack.ts
```

**Tool `Bash`:**

```
echo "bun tests (test( occurrences in *.test.ts):"; grep -rhoE "^\s*(test|it)\(" --include=*.test.ts packages scripts e2e 2>/dev/null | wc -l; echo "test.each / describe counts:"; grep -rhoE "test\.each|it\.each" --include=*.test.ts packages 2>/dev/null | wc -l; echo "playwright tests:"; grep -hoE "^\s*test\(" e2e/*.ts | wc -l; grep -hoE "^\s*test\.(skip|only|fixme)\(" e2e/*.ts | wc -l; ls e2e/*.ts | wc -l; echo ---; cat bunfig.toml; echo ---; cat playwright.config.ts | head -60
```

**Result of `Bash`:**

```
bun tests (test( occurrences in *.test.ts):
522
test.each / describe counts:
0
playwright tests:
31
1
9
---
[test]
root = "packages"
coverage = true
coverageSkipTestFiles = true
coverageReporter = ["text", "lcov"]
coverageDir = "coverage"
coverageThreshold = { line = 0.85 }
coveragePathIgnorePatterns = [
  "packages/web/**",
  "**/dist/**",
  "**/sim/**",
  # Process-level code is exercised by tests that spawn Node (the production runtime); Bun cannot measure it.
  "packages/control-plane/src/server.ts",
  "packages/control-plane/src/main.ts",
  "packages/control-plane/src/static.ts",
  "packages/control-plane/src/log.ts",
  # Sandbox worker entries run inside a worker thread (exercised by a spawned Node driver) or a
  # browser worker (exercised from Playwright in WP2.6); the host and glue are measured in-process.
  "packages/sandbox/src/adapters/node-worker.ts",
  "packages/sandbox/src/adapters/node-host.ts",
  "packages/sandbox/src/adapters/web-worker.ts",
  "packages/sandbox/test/driver-node.ts",
]
---
import { defineConfig, devices } from "@playwright/test";

// Browser tests live under e2e/ and are named *.e2e.ts, so `bun test` (which picks up *.test.ts)
// and Playwright never see each other's files. The web server is the real control plane in local
// mode serving the built web bundle; TABFRAME_URL points the tests at a deployed machine instead.
const local = !process.env.TABFRAME_URL;
const port = 4090;
… (38 more lines)
```

**Tool `Bash`:**

```
grep -n "CYCLE\|canvas(\|640\|64, 64\|taskAt" programs/mandelbrot/assembly/index.ts | head -20; echo ----; cat programs/mandelbrot/manifest.json; echo; cat programs/tinygpt/manifest.json; echo; grep -rn "822\|params\|parameters" docs/feasibility-transformer.md | head -8; echo ----; grep -n "4 ms\|ms per token\|ms/token" docs/feasibility-transformer.md | head -5
```

**Result of `Bash`:**

```
1:// Mandelbrot: the machine's default program (design §5.6). One stage of 640 tiles per frame, RGBA
24:const CYCLE: f64 = 48.0; // iterations per palette cycle
102:  const s = stage("render").canvas(CANVAS_W, CANVAS_H);
110:    s.taskAt(w.toBytes(), x, y, <i32>TILE, <i32>TILE);
172:  let t = mu / CYCLE;
----
{
  "name": "mandelbrot",
  "view": "tiles",
  "persist": false,
  "defaultParams": { "preset": 0, "palette": "ocean" },
  "description": "Distributed Mandelbrot render: 640 tiles of 64×64 per 2048×1280 frame, smooth coloring, presets that advance every frame."
}

{
  "name": "tinygpt",
  "view": "text",
  "persist": false,
  "defaultParams": { "prompts": "Call me Ishmael|The whale|It was|Ahab", "tokens": 96 },
  "description": "A 0.8 M-parameter character-level GPT trained on Moby-Dick, running its forward pass in WebAssembly: one continuation per prompt per task, greedy decoding, identical bytes on every core."
}

docs/feasibility-transformer.md:8:Yes, for a *small* one: a character-level GPT of one to two million parameters fits the sandbox,
docs/feasibility-transformer.md:21:| Memory maximum a module may declare | 256 pages = **16 MB** | weights, activations, and the KV cache all live here; f32 weights of *N* parameters need 4 *N* bytes |
docs/feasibility-transformer.md:27:| Determinism | no clock, no randomness, no NaN; plain f32/f64 arithmetic | inference is arithmetic only — sampling needs a seed *from the params* (a counter-based PRNG in the program), which keeps it deterministic |
docs/feasibility-transformer.md:31:- **Parameters.** With f32 weights and room for activations and a cache, about **2 M parameters**
docs/feasibility-transformer.md:45:  A token costs about 2 × parameters multiply-adds, so a 1 M-parameter model runs a token in
docs/feasibility-transformer.md:79:| Model | character-level GPT, 97-symbol byte vocabulary, context 128, 4 layers × 4 heads × width 128, **822 144 parameters** |
docs/feasibility-transformer.md:81:| Weights | `programs/tinygpt/in/weights.bin`, **822 685 bytes**: int8 with one f32 scale per tensor; dequantised to f32 in the module (3.3 MB), well inside the 16 MB cap with the KV cache (0.5 MB) |
docs/feasibility-transformer.md:90:seed from the params is the obvious next step and stays deterministic.
----
83:| Speed | **4.0 ms per token** under Bun's WebAssembly (4 prompts × 24 tokens in 387 ms, weights loaded once per task) — a 96-token continuation is under half a second here, a few seconds on a core |
```

**Tool `Bash`:**

```
grep -n "filesNote\|panel-explain\|panelExplain\|spawnCount\|hardwareConcurrency\|out of date\|Reloading\|hard-refresh\|hard refresh\|reloaded once\|Control plane rotating\|held for\|HOLD_MS\|DROP_MS\|onDropped\|every visitor\|lends no cores" packages/web/src/host.ts packages/web/src/panels.ts packages/web/src/state.ts packages/web/src/banners.ts packages/web/src/observer.ts packages/web/src/editor.ts packages/web/src/editor-core.ts 2>/dev/null | cut -c1-200 | head -50
```

**Result of `Bash`:**

```
packages/web/src/banners.ts:34:        title: "This page is out of date.",
packages/web/src/banners.ts:38:        hint: "Reloading…",
packages/web/src/banners.ts:43:            title: "Control plane rotating…",
packages/web/src/banners.ts:64:        title: `Control plane rotating to generation ${banner.next}.`,
packages/web/src/observer.ts:28:  onDropped?: (count: number) => void;
packages/web/src/observer.ts:45: * sequence gap or a refresh, or the seconds of a rotation — is held for the next live socket this
packages/web/src/observer.ts:49:export const CONTROL_HOLD_MS = 10_000;
packages/web/src/observer.ts:115:   * Queue a control for the control plane. Between subscribes it is held for the next live socket
packages/web/src/observer.ts:116:   * (`CONTROL_HOLD_MS`); returns false only when the client is stopped or the machine is off, in
packages/web/src/observer.ts:146:    const fresh = this.held.filter((h) => now - h.at <= CONTROL_HOLD_MS);
packages/web/src/observer.ts:150:    if (dropped > 0) this.handlers.onDropped?.(dropped);
packages/web/src/state.ts:1119:      ? "observing · this tab lends no cores · "
packages/web/src/host.ts:122:const cores = navigator.hardwareConcurrency || 1;
packages/web/src/host.ts:164:  spawnCount: $<HTMLSpanElement>("#spawnCount"),
packages/web/src/host.ts:184:els.spawnCount.textContent = String(spawnDefault);
packages/web/src/host.ts:190:  // An observer lends no cores (WP7.7, rule R3): the spawn controls stay, greyed, and say why.
packages/web/src/host.ts:192:    "Observing: this tab lends no cores. Open the plain address (without ?observe) to lend some.";
packages/web/src/host.ts:195:    b.title = `${b.title} — an observer lends no cores; open the plain address to lend some`;
packages/web/src/host.ts:432:      // itself every second and a half; after one try the banner asks for a hard refresh.
packages/web/src/host.ts:443:          "Reloaded once already: hard-refresh this page (Shift+reload) to fetch the current bundle.";
packages/web/src/host.ts:674:    title.append("Control plane rotating to generation ", gen, ".");
packages/web/src/host.ts:1077:    onDropped: (count) => {
```

> **Thinking.** 

**Tool `Bash`:**

```
echo "=== before WP8.1 (9a04588) lines 786-802 ==="; git show 9a04588:docs/design.md | sed -n 786,802p | cut -c1-160; echo; echo "=== now, line 792 split on ' - **' ==="; sed -n 792p docs/design.md | sed 's/ - \*\*/\n- **/g' | cut -c1-110
```

**Result of `Bash`:**

```
=== before WP8.1 (9a04588) lines 786-802 ===
  output has already advanced the stage (§6.5, §6.6). Two more invariants (§6.10): a node holds
  only work of the running execution, and an ended execution leaves nothing pending or assigned.
  Snapshot pages are packed by bytes as well as by row count, since 256 rows of done tiles exceed
  the message cap (§8.3). A result closes the attempt it names; a report for another attempt of
  the same task is evidence only, so a stale report never closes a newer attempt (§6.5). D7's
  "recompute from scratch" goes to nodes that have not reported on the task whenever one has a
  free slot; a tie after two contested rounds is not a majority and starts another round, up to
  four, after which report order breaks it.
- **2026-09-02 (WP3.3).** A cloud core names itself `core-<microvmId>`, which is how the ledger
  links a node to the core it launched; no registration message (§6.8). Core records travel in the
  ledger and are inherited at adopt with their node links cleared. Cores launch with **no ingress
  connector** and no idle policy. The fleet policy is gated by `config.cloudCores`, true only for
  the MicroVM image with an image ARN, a core role, and a session URL — a laptop wakes and sleeps
  but has no fleet (§6.8, §12).
- **2026-09-02 (WP3.4).** Local mode can boot neutral (`TABFRAME_LOCAL_NEUTRAL`) so `dev:rotate`
  drives the real run hook and the real rotate handler with control-plane processes standing in for
  MicroVMs (§12). A run payload with an empty `storeBase` leaves a control plane serving blobs from

=== now, line 792 split on ' - **' ===
  free slot; a tie after two contested rounds is not a majority and starts another round, up to Freeze is term
- **2026-09-02 (WP3.3).** A cloud core names itself `core-<microvmId>`, which is how the ledger links a node t
- **2026-09-02 (WP3.4).** Local mode can boot neutral (`TABFRAME_LOCAL_NEUTRAL`) so `dev:rotate` drives the re
- **2026-09-02 (WP3.5).** A bundled image cannot spawn itself as a worker thread: the sandbox worker is bundle
- **2026-09-02 (WP2.5).** The `taskDone` observer event and the task view carry an optional `log` (inline text
- **2026-09-02 (WP2.7).** Seeding is idempotent by bundle hash rather than "only when the ledger has no progra
- **2026-09-02 (WP4.5).** The endpoint ceiling is explained: **16 concurrent connections per MicroVM**, the ac
- **2026-09-02 (WP4.4).** The demo script runs unattended against the deployed machine (`mise run demo`), and
```

**Tool `Bash`:**

```
sed -n 255,300p packages/core/src/ledger.ts | grep -n "deadline\|maxNodes\|maxObservers\|snapshot\|Ms\|cap" ; echo ----; grep -rn "minimumMemoryInMiB\|memoryMiB:" packages/control-plane/src packages/fleet/src packages/infra/lib | grep -v test | head; echo ----; grep -rn "filesNote\|panel-explain\|panelExplain" packages/web/src packages/web/public/styles.css | cut -c1-160; echo ----; grep -n "test\.\(skip\|only\|fixme\)" e2e/*.ts | head -3; echo ----; for d in packages/*/; do n=$(grep -rhoE "^\s*(test|it)\(" --include=*.test.ts $d 2>/dev/null | wc -l); [ "$n" != 0 ] && echo "$d $n"; done; grep -rhoE "^\s*(test|it)\(" --include=*.test.ts scripts 2>/dev/null | wc -l; find packages scripts -name "*.spec.ts" -not -path "*/node_modules/*" | wc -l; grep -rn "for (const .* of .*) {\s*$" --include=*.test.ts packages -A1 2>/dev/null | grep -c "test(\|it("
```

**Tool `Bash`:**

```
sed -n 138,244p docs/plan.md
```

**Tool `Bash`:**

```
cat -n docs/implementation/wp-7.7-walkthrough.md; echo =====; cat -n docs/implementation/wp-4.5-endpoint-ceiling.md
```

**Result of `Bash`:**

```
11:  deadlineFloorMs?: number;
12:  deadlineFactor?: number;
20:      | "computeMsCap"
22:      | "deadlineFloorMs"
23:      | "deadlineFactor"
----
packages/control-plane/src/server.ts:712:        memoryMiB: {
packages/infra/lib/image-stack.ts:120:      resources: [{ minimumMemoryInMiB: 1024 }],
----
packages/web/public/styles.css:858:.panel-explain {
packages/web/public/styles.css:870:body:not([data-panel]) #filesNote,
packages/web/public/styles.css:884:body[data-panel] .panel-explain {
----
e2e/demo.e2e.ts:12:test.skip(!url, "needs a deployed machine (TABFRAME_URL)");
----
packages/control-plane/ 51
packages/core/ 129
packages/dev/ 12
packages/fleet/ 64
packages/infra/ 21
packages/node/ 42
packages/protocol/ 37
packages/sandbox/ 34
packages/sdk-as/ 40
packages/store/ 13
packages/web/ 79
0
0
6
```

**Result of `Bash`:**

```
---

## 5. M3 — Fleet

**Goal:** the control plane rotates hourly under load on AWS with the render continuing; two cloud cores come and go with the machine's sleep policy; deploys are rotations.

- [x] **WP3.1 Control-plane lifecycle for real.** `/run` reads role, generation, snapshot key; `/suspend` and `/terminate` flush snapshots; `/resume` revalidates; `/handover`, `/adopt`, `/drain` on port 8081, gated by a fleet token and the fleet secret from the payload; drain closes clients with the rotating code plus a jittered reconnect delay sized to the client count (30 ms per client, ≥ 2 s); generation stamping and rejection; `controlPlaneRotating`; rotating-reconnect close code.
- [x] **WP3.2 Fleet v1.** Full handover protocol with failure paths and repair-on-next-run; session heals through rotate unless the pointer says off; hourly schedule **enabled** (gate); reserved concurrency 1; idempotency under concurrent heal requests; `rotate.ts`, `up.ts`, `down.ts` finished.
  *Acceptance:* fake-client tests for every branch; a real rotation on AWS with a render in flight completes the frame with matching hashes.
- [x] **WP3.3 Cloud cores.** Node platform hardened (session fetch, reconnect, Atomics read path under load); core role in the image; fleet policy in the control plane (desired 2 while awake, one launch per second, replace on death or age, core ids kept in the ledger and inherited at adopt); sleep policy (10 min without observers; 60 min without interaction); `machineSleeping`; wake on first visitor; core killed by a demo control is replaced.
- [x] **WP3.4 Handover tests.** `dev:rotate` local driver; two-process handover integration test; Playwright rotation-mid-render test.
- [x] **WP3.5 Deploy M3.** Measure churn seconds per rotation under load and record them; confirm from CloudWatch that the session function's concurrent executions stay under 10 with no throttles during a rotation with a few hundred simulated clients.

**M3 done when:** the dashboard shows the generation change at the top of the hour while tiles keep landing; cores appear on wake and vanish on sleep; `mise run deploy` is a rotation.

---

## 6. M4 — Polish and observability

**Goal:** the five-minute demo script runs unattended in Playwright; the dashboard explains itself.

- [x] **WP4.1 Dashboard polish.** Reassignment flashes, state colors and legend, rotation banner with countdown and generation, throughput chart, core-count hint on spawn, waking states, files and ledger panels finished.
- [x] **WP4.2 Operations.** `/health` and `/diag` complete; log groups named; runbook `docs/runbook.md` (deploy, rotate, rollback, down, verify); budget confirmed firing on a test threshold.
- [x] **WP4.3 Performance and pacing.** Frame pacing about a minute per frame single-node; tile timing; reduce read batching if word count is slow; snapshot size and resume latency. _Parent, 2026-09-02: deployed snapshot 917–967 KB gzipped at 1 925 tasks (the ended executions' file maps remain — WP4.9); resume 4.1 s to the first 200 via auto-resume (M0 measured 0.7 s; one run took over 20 s), covered by the session function's retry loop._
- [x] **WP4.4 Unattended demo.** `e2e/demo.e2e.ts` runs the design's demo script against the deployed machine (`mise run demo -- --repeat 3 [--video]`); three passes in a row on 2026-09-02 at generation 42 (`docs/implementation/wp-4.4-unattended-demo.md`). Twelve failing runs before that each found something real: killed or frozen cores left alive and unlinked (now terminated and replaced; unlinked cores retired after two minutes), stale runbook drops in the program list (retired at seeding), dashboard controls lost between subscribes (held ten seconds), the redundancy toggle's counter never moving (agreement now counted as verified), a person's result replaced by the loop at once (a twenty-second hold, continuations included, and the snapshot shows the last ended execution), every rotation terminating every core (adoption grace), a CI host failing to instantiate a module taking a frame down (released, not failed). The nightly long simulation stays as it was.
- [x] **WP4.5 Endpoint capacity.** Why one client stops at 16 sockets: measured against fresh MicroVMs with one and three tokens, one and three processes, 512 MiB to 6 GB; explained by the non-adjustable *Concurrent connections per 2 vCPU MicroVM* quota; the M0 "250 sockets" record corrected; design §9.7 written. Script: `packages/infra/scripts/socket-ceiling.ts`.
- [ ] **WP4.6 Scaling the client edge — decided 2026-09-02: document for now, revisit after M5.** Mircea's call: the ceiling is recorded (design §9.7) and the demo is scoped to what one endpoint holds; after M5, evaluate **swapping the MicroVM for an EC2 instance** as the control plane's host — an EC2 instance has no per-VM connection quota, terminates thousands of WebSockets, and keeps the same process and protocol; what it costs is the MicroVM story (snapshot boot, hooks, suspend/resume) and the hourly rotation would need an AMI or container path instead of an image version. Earlier options kept for the record: (a) API Gateway WebSocket API as the edge, (b) IoT Core, (c) a relay tier of MicroVMs (capped near 240), (d) accept and state. **Original framing:** Thousands of concurrent clients cannot reach a MicroVM endpoint at any size (§9.7). Options: (a) API Gateway WebSocket API as the edge — `PostToConnection` for pushes, a Lambda integration to the control plane's private port for inbound; about $1 per million messages plus connection-minutes; (b) IoT Core over WebSockets — managed connections and lifecycle events, so heartbeats could go, but per-message pricing punishes a chatty protocol; (c) a relay tier of MicroVMs — same image, a `relay` role, but each relay is itself capped at 16 and the control plane's budget caps the tier near 240 clients, so it does not reach thousands; (d) accept the ceiling and state it. Recommended: (a), scoped as a milestone of its own after M5 unless the deadline allows it sooner.
- [x] **WP4.7 Alarm test and cost watch.** A one-cent test budget (`tabframe-alarm-test`) with the same subscriber was created 2026-09-02; AWS Budgets evaluates a few times a day and the account had no billing data yet, so confirmation is pending. Mircea will say when the email arrives; **if it has not by 2026-09-03, ask him.** Delete the test budget once confirmed. Cost Explorer likewise had no data on day two; check it daily and record the first real number here. _2026-09-02: Cost Explorer still answers DataUnavailableException (ingestion lags a day or more after enabling); the budget reads $0; retry daily._ _2026-09-02, evening: Cost Explorer still `DataUnavailableException`; the demo runs and deploys of the day (image builds, ~15 rotations, cores) are the first real load — check again on 2026-09-03._ _Done 2026-09-03: the one-cent test budget's email arrived ("actual cost … exceeds $0.01 … $2.55"), so the notification path to the budget address works; the test budget is deleted and the real $100/month budget reads $2.55 month-to-date after three days of building — about twenty-five deploys and rotations, cores while anyone watched, S3 and CloudFront. Cost Explorer answers now but its daily rows still read zero (it lags the budget); the per-service breakdown is a later look._
- [x] **WP4.8 CI green.** CI had been red since the WP1.9 merge (23 runs): the churn simulation loads `programs/mandelbrot/dist/program.wasm`, which is not committed and was never built in CI. The loader now builds the programs on demand, CI builds them explicitly before the tests, and the ground rules gained the hard rule above. A second, intermittent red followed: the panels browser suite dropped a module into the editor before `editor.js` had finished loading on the slower CI runner (`#editor` is shown *before* the lazy import resolves, so visibility is not readiness); the suite now waits for the compiler's "ready in" first, as the money shot already did. The gate is read from the run's `conclusion` (`gh run view --json conclusion`), never from a piped exit code — the first two watches in this work package were misread that way.
- [x] **WP4.9 Seeding follow-through and snapshot diet.** Found by the first verify-m1 on the paced image: a deploy that changes a program seeded a second `mandelbrot` but left the default loop on the old bundle, so the machine kept rendering the old frame and listed two programs with one name. Seeding now retires a record under a shipped name whose bundle is not the shipped one (hidden, refuses launches so its follow-up chain ends, dropped once unreferenced) and moves the default loop to the shipped program when the loop's bundle is gone or retired. The ended executions' file maps — 21 000 entries at 32 frames, what kept the deployed snapshot at 917–967 KB gzipped — go with the tasks beyond the two most recent frames; inheritance reads the map from the inherited root's manifest blob instead of the ledger's copy. Runbook: the symptom and the memory samples (RSS 312 → 372 MiB over 33 min, flat). Parent: deploy, rotate, verify-m1 green on the new image, record the snapshot size.

**M4 done when:** the demo script passes unattended three times in a row against AWS, and WP4.6 has a decision recorded.

---

## 7. M5 — Package

**Goal:** submitted.

- [x] **WP5.1 README** final: what, why, architecture, in and out of scope, run, deploy. Written 2026-09-02 (`docs/implementation/wp-5.1-packaging.md`), with the measured numbers and the limits stated plainly.
… (66 more lines)
```

**Result of `Bash`:**

```
     1	# WP7.7 — The walkthrough
     2	
     3	**Branch** `wp/7.7-walkthrough` · **Milestone** M7 · **Date** 2026-09-04 · **Ask** Mircea, with the
     4	third review: "a comprehensive walkthrough of the UI considering all the things that can be
     5	done/clicked at any point in time in a certain screen, and ensure that the experience is consistent
     6	for a visitor and they don't get confused as to what is going on".
     7	
     8	## What landed
     9	
    10	- **The contract: `docs/walkthrough.md`.** Every screen (the dashboard, the panel tabs, the
    11	  editor, the guide), every state each can be in, what the page must say, what can be clicked and
    12	  what happens, and what must not be on the screen — checked against eight rules: one sentence of
    13	  state always (R1); every control says what it will do and then what it did (R2); disabled with a
    14	  reason, not hidden (R3); same kind, same look (R4); a click never moves the page (R5); one name
    15	  per thing (R6); every screen says where you are and how to get back (R7); nothing happens
    16	  silently (R8).
    17	- **The sentence of state (R1).** `machineSentence()` in `packages/web/src/state.ts` puts one
    18	  sentence in the status line whenever no control echo is showing: the loop's frame, a person's
    19	  launch, held by Stop, yielded, paused by the editor, failed, queued, idle — with "demo · a
    20	  scripted cluster inside this page, nothing is sent anywhere" or "observing · this tab lends no
    21	  cores" first when that is what the page is. While the page is not connected the connection banner
    22	  speaks instead.
    23	- **Reasons, not disappearances (R3).** Every control's tooltip gains the reason after a dash
    24	  when it cannot apply: "— not connected yet" while connecting or asleep; "— nothing is running"
    25	  on restart, skip, and kill execution while the machine idles (kill execution is no longer hidden);
    26	  "— an observer lends no cores; open the plain address to lend some" on the spawn controls of an
    27	  observe-only page, whose spawn hint says the same instead of counting this browser's cores.
    28	- **Where you are and the way back (R7).** Panel tabs show "← dashboard" in the header (the
    29	  dashboard with the page's own mode kept) and no longer show Stop / Start / Resume or editor ↗: a
    30	  reading tab drives nothing. The editor's head links "← dashboard" beside "What is a program? ↗".
    31	- **The editor's honesty.** Editing after a compile greys launch until the next compile and says so
    32	  in the module line (the uploaded source is always the compiled text). A dropped module empties
    33	  the source box into a note — a dropped module has no source here — and greys compile until an
    34	  example or a machine program is picked. After a launch the pause line reads "launched · the pause
    35	  ended and the machine runs your program · keep editing, or close this tab". The demo's dashboard
    36	  opens the editor as `/editor.html?demo=1`: the compile is real, launch is greyed with "demo" in
    37	  its tooltip, and the head says nothing is sent anywhere.
    38	- **Small words (R1, R8).** A launch queued while the editor holds the machine says it waits for
    39	  the pause to end; a failed execution's stage says "no result · the execution failed; the box above
    40	  says why" in grey instead of repeating the reason in red.
… (58 more lines)
```

**Tool `Bash`:**

```
for f in README.md docs/design.md docs/rationale.md docs/runbook.md docs/walkthrough.md docs/plan.md docs/implementation/README.md docs/implementation/wp-8.1-review-loop-1.md docs/implementation/wp-7.7-walkthrough.md docs/feasibility-transformer.md packages/sdk-as/README.md e2e/README.md; do dir=$(dirname $f); grep -oE '\]\(([^)#]+)(#[^)]*)?\)' "$f" | sed -E 's/^\]\(//; s/\)$//' | sed -E 's/#.*$//' | grep -v '^https\?://' | grep -v '^$' | sort -u | while read l; do [ -e "$dir/$l" ] || echo "BROKEN in $f: $l"; done; done; echo "link check done"; echo ----; grep -rn "§9\.[0-9]\|§1[0-9]\.[0-9]\|§[0-9]\.[0-9]*" README.md docs/rationale.md docs/runbook.md | grep -o "§[0-9.]*" | sort | uniq -c | sort -rn | head -20
```

**Result of `Bash`:**

```
link check done
----
      2 §9.7
      1 §9.5
      1 §9.3
```

**Tool `Bash`:**

```
grep -n "spawnDefault\s*=\|const spawnDefault\|Math.max(1, cores\|cores - 1\|cores-1" packages/web/src/host.ts | head -5; echo ----; grep -n "the loop starts a frame when someone watches\|the loop waits behind it\|nothing runs until Start\|the result stays\|in-flight tasks finish\|closing it or launching resumes\|ten quiet minutes\|demo · a scripted cluster\|nothing is sent anywhere" packages/web/src/state.ts | cut -c1-170; echo ----; grep -n "the machine is paused while this tab is open\|the pause ended\|source changed since the compile\|from the machine · source loaded\|queued as\|dropped module\|no source (a dropped\|compile again to launch\|launching needs the live machine\|not connected" packages/web/src/editor.ts packages/web/src/editor-core.ts | cut -c1-170 | head -20
```

**Result of `Bash`:**

```
183:const spawnDefault = Math.max(1, cores - 1);
----
605:              ? "pause: an editor tab holds the machine; in-flight tasks finish, nothing new starts"
642:      // The loop yielded to a person's launch, or took the stage back after ten quiet minutes
651:          ? "the loop yielded to you: the result stays until Start or ten quiet minutes"
652:          : "ten quiet minutes: the loop is back",
1093:      "paused · the editor tab is open · in-flight tasks finish, nothing new starts · closing it or launching resumes";
1097:      ? `running your ${exec.programName} ${exec.executionId} · ${where} · ${nodes} · the loop waits behind it`
1100:    core = "stopped by you · nothing runs until Start · a launch of yours still runs at once";
1105:      ? `your ${exec.programName} ${exec.executionId} ${ended} · the result stays · the loop waits for Start or ten quiet minutes`
1106:      : "the loop yielded to you · Start hands it the stage back, or ten quiet minutes do";
1115:  else core = "idle · the loop starts a frame when someone watches";
1117:    ? "demo · a scripted cluster inside this page, nothing is sent anywhere · "
----
packages/web/src/editor-core.ts:3:// checks on a dropped module. No DOM, no sockets, no compiler: the UI (editor.ts) and the worker
packages/web/src/editor.ts:39:  /** A demo page: the compile is real, launching needs the live machine (WP7.7). */
packages/web/src/editor.ts:239:    if (host.demo) els.launch.title = "demo: the compile is real; launching needs the live machine";
packages/web/src/editor.ts:306:    // so this program can be reopened in the editor from any browser. A dropped module has none.
packages/web/src/editor.ts:325:      info(els.launchInfo, "not connected to the machine; nothing was sent", true);
packages/web/src/editor.ts:388:        `queued as ${mine.executionId}; a person's launch goes ahead of the machine's own loop`,
packages/web/src/editor.ts:478:    if (!base) throw new Error("not connected to the machine");
packages/web/src/editor.ts:524:          els.exampleNote.textContent = `from the machine · source loaded (${fmtBytes(source.length)})${inputsNote} · edit, compile, laun
packages/web/src/editor.ts:533:          els.exampleNote.textContent = `from the machine · no source (a dropped .wasm)${inputsNote} · launch runs the module as it is`;
packages/web/src/editor.ts:616:      info(els.moduleInfo, "the source changed since the compile; compile again to launch it");
```

**Tool `Bash`:**

```
grep -n "settled tasks\|hashes, not bytes\|newest first\|resume updates\|pause updates\|follow the execution\|raw ↗\|held)\|Tabframe · \|files ·\|in the store" packages/web/src/panels.ts | cut -c1-170 | head -24; echo ----; grep -rn "\bcores\?\b" packages/web/src/host.ts packages/web/src/panels.ts packages/web/src/state.ts packages/web/src/editor.ts packages/web/src/tiles.ts packages/web/src/banners.ts | grep -v "^\s*//" | grep -i "\"\|'\|\`" | grep -iv "cloud core\|core-\|kind ===\|\.core\b\|@tabframe/core" | cut -c1-190 | head -30
```

**Result of `Bash`:**

```
3:// of settled tasks (hashes and where their bytes live), and failure surfacing. The state comes from `state.ts`; bytes come from the store by hash; controls go back
646:    els.filesSummary.textContent = `root ${short(root, 12)} · ${files.length} files · ${fmtBytes(total)} · every byte fetched from the store by hash`;
651:        `${files.length} files · ${fmtBytes(total)}${browsingRoot ? " · browsing a chosen root" : ""}`,
670:      const follow = el("button", "small", "follow the execution");
779:   * One renderer for both previews: a head with the name, size, hash, and a small "raw ↗" link to
801:      const raw = el("a", "mono raw", " raw ↗");
996:      ? `${settled} settled ${settled === 1 ? "task" : "tasks"} · ${fmtBytes(bytes)} in the store · hashes, not bytes`
997:      : "hashes, not bytes — nothing settled yet";
999:      ? `The control plane holds hashes, not bytes: for each of the ${settled} settled ${settled === 1 ? "task" : "tasks"} of stage ${exec.stage} it keeps a 64-hex ou
1000:      : "The control plane holds hashes, not bytes. No execution is running, so there is nothing settled to list.";
1016:          const a = el("a", "mono raw", "raw ↗");
----
packages/web/src/panels.ts:927:            ? "log: not carried on the wire yet (the control plane keeps it; forwarding is a one-line core change)"
packages/web/src/state.ts:77:  /** The accepted result's log when the wire carried one; null until the core forwards it. */
packages/web/src/state.ts:465:      // The winner's attempt is done; any twin still running is cancelled by the core (§6.5). A
packages/web/src/state.ts:1100:    core = "stopped by you · nothing runs until Start · a launch of yours still runs at once";
packages/web/src/state.ts:1112:    core = `${exec.programName} ${exec.executionId} was stopped · the loop is free again`;
packages/web/src/state.ts:1114:    core = `${state.queue.length} queued · the next one starts in a moment`;
packages/web/src/state.ts:1115:  else core = "idle · the loop starts a frame when someone watches";
packages/web/src/state.ts:1119:      ? "observing · this tab lends no cores · "
packages/web/src/host.ts:6:// and sleep banners, a throughput chart, and a spawn hint sized to this browser's cores.
packages/web/src/host.ts:187:els.spawnHint.textContent = `This browser reports ${cores} CPU ${cores === 1 ? "thread" : "threads"}; spawn ${spawnDefault} keeps one for the page. Nodes in this
packages/web/src/host.ts:192:    "Observing: this tab lends no cores. Open the plain address (without ?observe) to lend some.";
packages/web/src/host.ts:195:    b.title = `${b.title} — an observer lends no cores; open the plain address to lend some`;
packages/web/src/host.ts:922:    "Demo: the nodes are scripted inside this page; open the live address to lend real cores.";
packages/web/src/host.ts:925:    b.title = `${b.title} — the demo's nodes are scripted; open the live address to lend cores`;
packages/web/src/editor.ts:27:} from "./editor-core.ts";
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; git show 9a04588:docs/design.md | grep -n "Freeze is terminal" | cut -c1-120; git show 9a04588:docs/design.md | sed -n '/Freeze is terminal/,+3p' | cut -c1-140 | head -5; echo ----; grep -n "TABFRAME_DEPLOY_UNGATED\|origin/main\|clean\|refuse\|console.error" packages/infra/scripts/deploy-guard.ts | head -8; echo ----; grep -n "build\b\|imageVersion\|build:" packages/control-plane/src/server.ts | grep -i "health\|build\|imageVersion" | head -8; echo ----; grep -rn "1024\|512\|idle\|Idle\|15 \*\|fifteen" packages/fleet/src/rotate.ts packages/fleet/src/launch*.ts packages/control-plane/src/core-fleet.ts packages/control-plane/src/fleet*.ts 2>/dev/null | grep -iv "test\|//" | head -12; ls packages/control-plane/src | head -40
```

**Result of `Bash`:**

```
822:  four, after which report order breaks it. Freeze is terminal on the node **record** too: a later
  four, after which report order breaks it. Freeze is terminal on the node **record** too: a later
  `throttleHalf` does not downgrade a frozen node to throttled, since `fill` must keep skipping a
  worker that computes nothing until the silence window declares it gone (§4, §6.7).
- **2026-09-02 (WP2.7).** Seeding is idempotent by bundle hash rather than "only when the ledger
----
2:// committed and its HEAD must be on origin/main — the commit CI made green. Set
3:// TABFRAME_DEPLOY_UNGATED=1 to skip it for an emergency deploy from a branch.
8:if (process.env.TABFRAME_DEPLOY_UNGATED === "1") {
9:  console.log("[deploy-guard] skipped (TABFRAME_DEPLOY_UNGATED=1)");
14:  console.error("[deploy-guard] the working tree has uncommitted changes:\n" + dirty);
20:  console.warn("[deploy-guard] could not fetch origin/main; checking against the local main");
25:  sh("git merge-base --is-ancestor HEAD origin/main");
36:  console.error(
----
166:        imageVersion: config.imageVersion,
645:        build: process.env.TABFRAME_BUILD ?? null,
646:        imageVersion: config.imageVersion ?? null,
----
packages/fleet/src/rotate.ts:127:      idlePolicy: CONTROL_PLANE_IDLE_POLICY,
adopt.test.ts
bundles.test.ts
bundles.ts
config.test.ts
config.ts
core-node.test.ts
core-node.ts
core-reaper.test.ts
cores.test.ts
cores.ts
fixtures.ts
handover.integration.test.ts
hooks.test.ts
hooks.ts
log.ts
main.ts
mandelbrot.test.ts
rotation.test.ts
seed.test.ts
seed.ts
… (4 more lines)
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; grep -rn "PRESIGN_TIMEOUT\|PRESIGN_BUDGET\|presignBudget\|GiB\|1_073_741_824\|MAX_PRESIGN_ITEMS\|max(64)\|\.max(64" packages/node/src packages/core/src packages/protocol/src | grep -v test | cut -c1-150 | head -10; echo ----; grep -rn "RESULTS_PER_TASK\|MAX_RECORDS\|records.length >= 16\|> 16\|16)" packages/core/src/apply.ts packages/core/src/verify*.ts packages/core/src/results*.ts 2>/dev/null | grep -v test | cut -c1-150 | head -6; echo ----; grep -rn "maxBytes\|MAX_.*BYTES\s*=" packages/control-plane/src/*.ts | grep -v test | grep -o "[A-Z_]*BYTES[A-Z_]* = [0-9_ *]*\|get([^)]*)" | sort -u | head -14
```

**Result of `Bash`:**

```
packages/node/src/tasks.ts:206:export const PRESIGN_TIMEOUT_MS = 30_000;
packages/node/src/tasks.ts:222:  constructor(send: (text: string) => void, timeoutMs = PRESIGN_TIMEOUT_MS) {
packages/protocol/src/shared.ts:10:export const nodeId = z.string().min(1).max(64);
packages/protocol/src/shared.ts:11:export const hostId = z.string().min(1).max(64);
packages/protocol/src/observer.ts:6:const executionId = z.string().min(1).max(64);
packages/protocol/src/observer.ts:7:const taskId = z.string().min(1).max(64);
packages/protocol/src/observer.ts:25:    .max(64)
packages/protocol/src/observer.ts:104:  name: z.string().min(1).max(64),
packages/protocol/src/observer.ts:136:  code: z.string().min(1).max(64),
packages/protocol/src/observer.ts:178:  name: z.string().max(64),
----
packages/core/src/results.ts:110:  if (task.results.length >= RESULTS_PER_TASK_CAP) return { effects, settlement: { kind: "none" } };
packages/core/src/results.ts:163:export const RESULTS_PER_TASK_CAP = 16;
----
MAX_BLOB_BYTES = 
MAX_BUNDLE_BYTES = 64 * 1024 * 1024
MAX_LEDGER_BYTES = 64 * 1024 * 1024
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; grep -n "idlePolicy\|idle_policy\|maxDurationSeconds\|maxDuration\|suspendAfter\|minimumMemory\|memoryMiB\|MemoryInMiB" packages/fleet/src/*.ts packages/control-plane/src/*.ts | grep -v test | cut -c1-160 | head -14; echo ----; sed -n 425,445p packages/web/src/host.ts
```

**Result of `Bash`:**

```
packages/fleet/src/types.ts:48:  idlePolicy: IdlePolicy | null;
packages/fleet/src/microvm-client.ts:87:        idlePolicy: params.idlePolicy ?? undefined,
packages/fleet/src/rotate.ts:127:      idlePolicy: CONTROL_PLANE_IDLE_POLICY,
packages/control-plane/src/cores.ts:55:        idlePolicy: null,
packages/control-plane/src/server.ts:712:        memoryMiB: {
----
    els.banner.hidden = false;
    const copy = connectionCopy(state, detail);
    els.bannerTitle.textContent = copy.title;
    els.bannerBody.textContent = copy.body;
    els.bannerHint.textContent = copy.hint;
    if (state === "outdated") {
      // One reload, not a loop (WP8.1): a stale bundle the cache keeps serving would reload into
      // itself every second and a half; after one try the banner asks for a hard refresh.
      let reloaded = false;
      try {
        reloaded = sessionStorage.getItem("tabframe-reloaded") === PROTOCOL_VERSION.toString();
        if (!reloaded) sessionStorage.setItem("tabframe-reloaded", PROTOCOL_VERSION.toString());
      } catch {
        // storage may be off; reload once anyway
      }
      if (!reloaded) setTimeout(() => location.reload(), 1_500);
      else
        els.bannerHint.textContent =
          "Reloaded once already: hard-refresh this page (Shift+reload) to fetch the current bundle.";
    }
  }
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; grep -n "walkthrough\|Reloaded once\|hard-refresh\|out of date" e2e/walkthrough.e2e.ts e2e/host.e2e.ts | head -6; echo ----; grep -rn "core\b\|cores\b" docs/walkthrough.md | grep -v "cloud core" | cut -c1-140; echo ----; grep -n "every visitor\|is a core\|are cores\|Every worker" README.md docs/design.md docs/rationale.md packages/web/public/index.html | cut -c1-140
```

**Result of `Bash`:**

```
e2e/walkthrough.e2e.ts:1:// The walkthrough (WP7.7): every state a visitor can land in, and for each the sentence of state,
e2e/walkthrough.e2e.ts:89:  await page.screenshot({ path: test.info().outputPath(`walkthrough-${name}.png`) });
----
docs/walkthrough.md:26:  running", "— an observer lends no cores; open the plain address to lend some"). The only slot that
docs/walkthrough.md:41:  observe-only says it lends no cores.
docs/walkthrough.md:68:| Observe only | "observing · this tab lends no cores · …"; the spawn hint says how to lend some. | Everything bu
----
packages/web/public/index.html:122:<footer>Tabframe · every visitor is a core · <a href="?observe">observe only</a> · <a href="?demo=1">d
README.md:4:programmed with WebAssembly. Every worker is a core — a thread in someone's tab, or one of the two
README.md:5:MicroVMs in AWS, the *cloud cores*. Open the page and your tab is a core. Close it and the machine keeps
docs/design.md:134:9. **Tab close.** Every worker in the tab dies at once, sockets drop, the control plane marks them gone, and their unfini
docs/design.md:531:Build and execution roles trust `lambda.amazonaws.com` for `sts:AssumeRole` and `sts:TagSession`, per the MicroVMs securi
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; grep -rn "Compiling\|## Byte formats\|## Tooling\|sdk-readme\|README" packages/web/scripts/build.ts packages/web/src/guide-page.ts packages/web/src/editor-page.ts packages/web/src/program-sources.generated.ts 2>/dev/null | cut -c1-160 | head -8; echo ----; grep -rn "presigned\|budget" packages/core/src/apply.ts | grep -i "budget\|GiB\|1 <<" | head -4; echo ----; grep -c "" docs/m3-verification.md; grep -n "fifteen\|15 tabs\|dozen" docs/design.md docs/rationale.md README.md docs/m3-verification.md | cut -c1-140
```

**Result of `Bash`:**

```
packages/web/src/guide-page.ts:1:// The guide in its own page (WP7.5, D4): what a program is and what it may do — the SDK's README
packages/web/src/program-sources.generated.ts:28:/** The SDK's README, the editor's guide: what a program is and what it may do. */
packages/web/src/program-sources.generated.ts:29:export const GUIDE_MARKDOWN = "# @tabframe/sdk-as\n\nThe AssemblyScript SDK for Tabframe programs, and the tool
----
packages/core/src/apply.ts:229:          return refuse(ledger, connId, CLOSE.rateLimited, "presign budget exhausted", now);
packages/core/src/apply.ts:247:        return refuse(ledger, connId, CLOSE.rateLimited, "presign budget exhausted", now);
packages/core/src/apply.ts:266:/** Count the bytes a presign asks for against the connection's budget; false once it is spent. */
----
81
docs/m3-verification.md:63:What follows for the design: one control plane holds about fifteen browser tabs; the ledger's
README.md:122:  One control plane therefore serves about fifteen browser tabs, and the ledger's 256-node cap is
README.md:188:watches, the cores are terminated after ten minutes and the control plane suspends after fifteen.
docs/rationale.md:103:  about fifteen tabs, and the demo is scoped to that. The decision on record is to document it
docs/design.md:311:After **ten minutes without an observer**, or **sixty minutes without any human interaction even with a dashboard open** 
docs/design.md:499:Consequences: one control plane serves at most about fifteen browser tabs plus the fleet's own
```

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; node -e 'const s=require("fs").readFileSync("packages/web/src/program-sources.generated.ts","utf8");const m=s.match(/GUIDE_MARKDOWN = "((?:[^"\\]|\\.)*)"/);const g=JSON.parse("\""+m[1]+"\"");console.log("guide length",g.length,"| has Compiling:",g.includes("## Compiling"),"| has Byte formats:",g.includes("## Byte formats"),"| has Tooling:",g.includes("## Tooling"),"| tail:",JSON.stringify(g.slice(-120)))'
```

**Result of `Bash`:**

```
guide length 5642 | has Compiling: true | has Byte formats: true | has Tooling: true | tail: "malizes the word-count corpus into\n  `programs/wordcount/in/corpus.txt` (committed; the script is for reproducibility).\n"
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; cat packages/web/src/guide-page.ts; echo ----; grep -n "GUIDE\|Compiling\|README\|slice\|indexOf\|cut" packages/web/scripts/build.ts packages/web/src/editor-page.ts packages/web/src/editor.ts | cut -c1-170 | head -12; echo ----; git ls-files packages/web/src/program-sources.generated.ts; git log -1 --format='%h %ad' --date=short -- packages/web/src/program-sources.generated.ts; grep -n "generated" .gitignore
```

**Result of `Bash`:**

```
// The guide in its own page (WP7.5, D4): what a program is and what it may do — the SDK's README
// rendered at build time — and the numbers this machine holds a program to. Linked from the top
// of the editor as "What is a program?".
import { guideMarkdown, limitsSentence, renderGuide } from "./editor-core.ts";

const body = document.querySelector<HTMLDivElement>("#guideBody");
const limits = document.querySelector<HTMLParagraphElement>("#guideLimits");
if (body && body.childElementCount === 0) body.append(renderGuide(guideMarkdown()));
if (limits) limits.textContent = limitsSentence();
----
packages/web/src/editor.ts:378:      state.execution?.human && state.execution.programName === launchedName
packages/web/src/editor.ts:379:        ? state.execution
packages/web/src/editor.ts:383:      info(els.launchInfo, `running as ${running.executionId} · ${running.phase}`);
packages/web/src/editor.ts:388:        `queued as ${mine.executionId}; a person's launch goes ahead of the machine's own loop`,
packages/web/src/editor.ts:413:        .slice(0, 64);
packages/web/src/editor.ts:480:    if (!r.ok) throw new Error(`${hash.slice(0, 8)}…: HTTP ${r.status}`);
packages/web/src/editor.ts:506:      els.name.value = `${manifest.name}-edit`.slice(0, 64);
packages/web/src/editor.ts:592:      if (v.startsWith("machine:")) void loadMachineProgram(v.slice("machine:".length));
packages/web/src/editor.ts:624:      els.source.value = `${value.slice(0, s)}  ${value.slice(t)}`;
----
packages/web/src/program-sources.generated.ts
05f53ad 2026-09-02
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; grep -n "guideMarkdown\|Compiling\|GUIDE_MARKDOWN" -A6 packages/web/src/editor-core.ts | head -30; echo ----; grep -rln "program-sources.generated" packages scripts --include=*.ts --include=*.toml --include=*.json 2>/dev/null | grep -v node_modules; echo ----; grep -rn "GUIDE_MARKDOWN\|sdk-as/README" packages/web/scripts/*.ts packages/sdk-as/scripts/*.ts 2>/dev/null | cut -c1-160 | head -6; echo ----; git diff --stat 20dd4c8~1 20dd4c8 -- packages/sdk-as/README.md packages/web/src/program-sources.generated.ts
```

**Result of `Bash`:**

```
16:  GUIDE_MARKDOWN,
17-  HELLO_MANIFEST,
18-  HELLO_SOURCE,
19-  MANDELBROT_MANIFEST,
20-  MANDELBROT_SOURCE,
21-  SDK_FILES,
22-  WORDCOUNT_MANIFEST,
--
304:export function guideMarkdown(): string {
305:  // The README's "Compiling" and "Tooling" sections are about the repository, not the page (WP8.1):
306-  // the guide stops where they start.
307:  const cut = GUIDE_MARKDOWN.indexOf("\n## Compiling");
308:  return cut === -1 ? GUIDE_MARKDOWN : GUIDE_MARKDOWN.slice(0, cut).trimEnd();
309-}
310-
311-/**
312- * Enough Markdown for the guide: headings, paragraphs, fenced code, bullet lists, inline code,
313- * and bold. Everything is text nodes — nothing from the README is interpreted as HTML.
314- */
----
packages/web/scripts/gen-sources.ts
packages/web/src/editor-core.ts
----
packages/web/scripts/gen-sources.ts:13:const readmeFile = path.join(root, "packages/sdk-as/README.md");
packages/web/scripts/gen-sources.ts:58:    `export const GUIDE_MARKDOWN = ${JSON.stringify(readme)};`,
----
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; git show --stat --format='%h %s' 20dd4c8 | grep -iE "README|generated|docs/|editor.html|walkthrough" ; echo ----; git log -3 --format='%h %ad %s' --date=short -- packages/sdk-as/README.md; echo ----; grep -n "export function limitsSentence" -A14 packages/web/src/editor-core.ts | cut -c1-200
```

**Result of `Bash`:**

```
 README.md                                   |  30 +++--
 docs/design.md                              | 113 ++++++----------
 docs/implementation/README.md               |   3 +-
 docs/implementation/wp-8.1-review-loop-1.md |  74 ++++++++++
 docs/plan.md                                |  14 +-
 docs/rationale.md                           |   4 +-
 docs/runbook.md                             |   9 +-
 docs/walkthrough.md                         |   3 +-
 packages/web/public/editor.html             |   6 +-
----
106428c 2026-09-02 WP2.2: word count program — three-stage map/reduce/merge over Moby-Dick, bars payload, staged host and goldens
381098b 2026-09-01 M1/WP1.5 sdk-as: AssemblyScript SDK, Mandelbrot program, build/goldens/host scripts
----
300:export function limitsSentence(): string {
301-  return `Limits on this machine: a module declares a memory maximum of at most ${MEMORY_PAGES_MAX} pages (${(MEMORY_PAGES_MAX * 64) / 1024} MB) and is at most ${MAX_MODULE_BYTES / (1024 * 1024)} 
302-}
303-
304-export function guideMarkdown(): string {
305-  // The README's "Compiling" and "Tooling" sections are about the repository, not the page (WP8.1):
306-  // the guide stops where they start.
307-  const cut = GUIDE_MARKDOWN.indexOf("\n## Compiling");
308-  return cut === -1 ? GUIDE_MARKDOWN : GUIDE_MARKDOWN.slice(0, cut).trimEnd();
309-}
310-
311-/**
312- * Enough Markdown for the guide: headings, paragraphs, fenced code, bullet lists, inline code,
313- * and bold. Everything is text nodes — nothing from the README is interpreted as HTML.
314- */
```

> **Thinking.** 

**Tool `Bash`:**

```
cd /home/mircea/homework/tabframe; sed -n 301p packages/web/src/editor-core.ts | fold -w 200; echo ----; sed -n 60,82p packages/sdk-as/README.md | grep -n "Allowed imports\|NaN\|maximumMemory" | cut -c1-120
```

**Result of `Bash`:**

```
  return `Limits on this machine: a module declares a memory maximum of at most ${MEMORY_PAGES_MAX} pages (${(MEMORY_PAGES_MAX * 64) / 1024} MB) and is at most ${MAX_MODULE_BYTES / (1024 * 1024)} MB; 
a task's inline input is at most 16 KB; an output at most 16 MB, a task's writes at most 256 files and 16 MB, its log 64 KB; a task that runs past its deadline is killed and given to another core. The
 views: tiles (RGBA bytes placed on a canvas), bars (the bars() payload), text (UTF-8).`;
----
8:    --path programs/<name>/node_modules --baseDir . -O3 --runtime stub --noAssert --maximumMemory 256
15:- `--maximumMemory <pages>`: the sandbox requires a declared memory maximum (64 KiB pages; 256 is
19:**Allowed imports:** `tf.stat`, `tf.read`, `tf.write`, `tf.list`, `tf.log`, and `env.abort`.
22:machines; never write NaN into an output, because NaN payload bits are not.
```

> **Thinking.** 

## Assistant · 2026-09-04T15:43:21.574Z

## Findings

1. `[H] docs/design.md — §17 drift log, line 792 — Loop 1's "orphan paragraph" repair went wrong: the clause "up to four, after which report order breaks it." was deleted (the WP1.9 entry now reads "starts another round, up to Freeze is terminal on the node record too"), and seven dated entries (WP3.3, 3.4, 3.5, 2.5, 2.7, 4.5, 4.4) were joined into one 4,446-character line — 41 bullets before WP8.1, 35 after. — The README sells the drift log as "the honest history"; the reviewer meets a wall of text and a sentence that is nonsense. — Restore the clause, keep "Freeze is terminal … (§4, §6.7)" as the WP1.9 entry's last sentence, and put each of the seven entries back on its own "- **2026-09-02 (WPx.y).**" line (source text: commit 9a04588, lines 793–822).`

2. `[M] README.md — "Limits, stated plainly", bullet 2 — "a consensus control plane is the an extension, not a feature" — a typo introduced by the loop-1 rewrite, in the paragraph meant to demonstrate care. — "a consensus control plane is an extension, not a feature — the second in the rationale's list, after the client edge."`

3. `[M] README l.4–5, index.html footer, host.ts spawn hints, editor-core.ts limits sentence, design §16, walkthrough R6 — "core" still means three things: README "Every worker is a core … your tab is a core", footer "every visitor is a core", hints "lends no cores / lend real cores", limits sentence "given to another core", glossary "Node / core — a tab worker or a MicroVM", while R6 says node = any worker and core = a cloud node — and R6's own example ("this tab lends no cores") breaks R6. — The first sentence a reviewer reads teaches a word the page then uses differently ("9 nodes · 3 hosts", "cloud cores"). — Decide once: either adopt R6 everywhere (README "Every worker is a *node* — a thread in someone's tab, or one of the two MicroVMs, the *cloud cores*"; footer "every visitor is a node"; hints "lends no nodes"; glossary split into Node and Cloud core) or amend R6 to "core = any worker; cloud core = one in a MicroVM; node = the ledger's word for a connected core" and align the pills and hints.`

4. `[M] README l.122, design §9.7 l.499, rationale l.103, m3-verification l.63 — "one control plane serves about fifteen browser tabs" — a plain tab costs two of the 16 connections (its dashboard socket and its node's socket, D3/§3) and the two cloud cores cost two more; wp-4.5 says it right ("a dashboard costs one connection and each node one more … about a dozen nodes"), and the demo is scoped to ten nodes across two dashboards for that reason. — A reviewer who opens eight tabs and sees 429s finds the number wrong in four documents. — "about seven tabs each lending a node (a tab costs two connections, its dashboard and its node; the cloud cores cost two more), or fifteen observe-only tabs".`

5. `[M] docs/implementation/wp-8.1-review-loop-1.md — row 11, row 44–52, the totals — row 11 says "a cooldown of three seconds" but packages/core/src/apply.ts:263 has CONTROL_COOLDOWN_MS = 2_000 and design §17 says two; row 44–52 points at "the diffs to … packages/sdk-as/README.md", but commit 20dd4c8 did not touch that file (last change 2026-09-02; the cut was made in editor-core.ts); the table reconciles to 36 accepted, 9 accepted in part, 6 deferred, 1 rejected-as-a-change, while plan.md says "40 accepted, 11 deferred, 1 rejected" and the index and merge commit say "40 landed". — The note is the evidence the loop was done carefully; a number that contradicts the code, a diff that does not exist, and totals its own table cannot produce undo that. — Write "two seconds"; say where each documentation fix actually landed; add one line under the table with counts derived from it and reuse them in plan.md and docs/implementation/README.md; add a legend for dist / sec / fs / tw / devops.`

6. `[M] wp-8.1-review-loop-1.md — row 44–52 and the end of the table — nine technical-writer findings share one row, one severity "H–L", one verdict; nothing lists what loop 2 owes. — Every other finding has a verdict a reader can check; here the reviewer cannot tell what was found or where it changed, and must scan 43 rows for "Deferred". — Give the nine their own rows (finding → file → change) and close with a "Deferred to loops 2–3" list of the six (22, 23, 27, 28, 36, 42).`

7. `[M] /guide.html (editor-core.ts guideMarkdown) and packages/sdk-as/README.md — the loop-1 cut at "## Compiling" also drops the "Allowed imports" paragraph (tf.stat … env.abort; Date.now, Math.random, WASI rejected at upload; never write NaN) and the memory-maximum note; limitsSentence() does not mention imports. — The guide page promises "what a program may do"; a page author now learns the allowlist from a rejected upload. — Move "Allowed imports" and the NaN caveat above "## Compiling" (a "What a program may do" section); keep the asc command and Tooling below the cut.`

8. `[M] docs/runbook.md — "Everyday › Deploy", "Deploy, step by step", "Observability" — no word of the WP8.1 guard: deploy refuses a dirty tree or a HEAD not on origin/main (packages/infra/scripts/deploy-guard.ts; TABFRAME_DEPLOY_UNGATED=1 skips it), and /health now carries build and imageVersion. — The operator document describes a deploy that no longer works that way; the first deploy from a branch fails with a message the runbook does not explain. — Deploy row: "build → test → guard (clean tree, HEAD on origin/main; TABFRAME_DEPLOY_UNGATED=1 for an emergency deploy from a branch) → cdk deploy --all → up"; add build and imageVersion to step 4 and the /health bullet.`

9. `[M] docs/walkthrough.md — Screen A, "Outdated page" — "reloads itself after 1.5 s … Nothing needed", but since WP8.1 the page reloads once per protocol version and then shows "Reloaded once already: hard-refresh this page (Shift+reload) …" (host.ts 432–443). — The walkthrough is the page's contract and misses a state where the visitor must act. — Add a row: "Outdated, reloaded once | the banner asks for a hard refresh | Shift+reload | a reload loop".`

10. `[L] docs/runbook.md — "What is running", "Cost and budget", "Observability" — "Snapshots … one-day lifecycle" (since WP8.1 scoped to the g prefix; latest.json.gz is kept); "tabframe-alarm-test … delete it once the email has arrived" while plan WP4.7 records it deleted on 2026-09-03; the tabframe-alarms topic appears only in an incident row; "Accepted; see design §9.3 (…)" repeats the sentence before it. — Stale facts and a one-off chore in an operator document. — Fix the lifecycle wording, drop the alarm-test sentence, add a row for the SNS topic and its two alarms, cut the duplicate parenthetical.`

11. `[L] README.md — "Try it, in five clicks", "What it is" — "Spawn 3" is index.html's placeholder; the button reads "spawn N" with N = the browser's threads − 1 (host.ts 183), so most reviewers see "spawn 7"; and "Three programs ship … Mandelbrot … word count … compile your own" is followed by a separate "Since M6 a third program ships" paragraph that reads like a patched count. — First-minute copy that matches neither the screen nor itself. — "spawn N (the larger spawn button, sized to your browser)"; one list of three programs, "Since M6" dropped.`

12. `[L] docs/plan.md "Where we are", docs/rationale.md l.129, docs/design.md §11.2 and §6.7 — the plan still says "M7 closed … Left for Mircea" with WP8.1 checked further down; the rationale says "M0 to M7"; the design's verbatim mise.toml block (tools at latest, a parallel deploy) differs from the file on every line (pinned tools, a sequential guarded deploy) and the WP8.1 drift entry omits the pins; §6.7 says "§6.7 below" from inside §6.7. — Three "where we are" statements are a milestone behind, and a quoted file that does not match invites the question of what else is stale. — Plan: "(2026-09-04, M8 loop 1 merged): loops 2–3 next, then a deploy with three demo passes"; rationale "M0 to M8"; replace the toml block with the task names and "see mise.toml" (or refresh it and note the pins in the drift entry); "(WP6.4, in this section)".`

## Summary

Most of loop one's documentation fixes hold up against the code — the stays-up paragraph matches `skipped-suspended` and the ten/fifteen-minute constants, the 31 Playwright tests and the R1 sentences are exact, every relative link resolves, the §9 order, the rationale's "second extension", the plan's shape and the editor label are right — but two of the twelve rewrites did damage: the drift log now has a garbled sentence and seven entries fused into one 4.4 KB line, and the "first extension" rewrite left a typo in the README's most careful paragraph. The remaining problems are consistency rather than substance: "core" is still defined three ways across README, page, glossary and R6; "fifteen tabs" overstates the connection ceiling by two in four documents; and the loop-one note contradicts the code (three vs two seconds), cites a diff that does not exist, and carries totals its own table cannot produce. Fix first: restore the drift log from commit 9a04588; settle the node/core vocabulary in one place and apply it to README, footer, hints and glossary; make the loop-one note honest (two seconds, real file paths, counts derived from the table, nine doc rows, a deferred list) and carry its numbers into the plan and the index.
